# Version pins for external binaries without a native manifest.
#
# Rust toolchain: rust-toolchain.toml
# Cargo CLIs:     Cargo.toml [workspace.metadata.tools]
# Standalone CLIs: tools.toml

[prek]
version = "0.4.0"

[osv-scanner]
version = "2.3.8"
