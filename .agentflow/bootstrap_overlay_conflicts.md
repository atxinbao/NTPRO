# Bootstrap Overlay Conflict Report

Existing target files with the same path were detected. Default bootstrap does not overwrite them.

- `.github/PULL_REQUEST_TEMPLATE.md`

To intentionally overwrite after review, rerun with `OVERWRITE_EXISTING=1`.
