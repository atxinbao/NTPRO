# Legacy Upstream Python API Appendix

:::warning
This page is a legacy upstream Python API appendix. It is retained for migration, lineage, and source-reading context only. It is not an NTPRO Rust-only product API or implementation path.
:::

This section is retained as a legacy upstream Python API appendix. It is not an
NTPRO Rust-only product API, install path, runtime surface, or implementation
guide.

The pages under `docs/api_reference/**` still contain historical
`nautilus_trader.*` `automodule` references. Keep them only for migration,
lineage, or source-reading context. Current NTPRO product work should use:

- Rust crates and crate-level docs;
- Rust CLI contracts and help output;
- Rust examples and product guides;
- release and migration notes under `docs/rust-cutover/`.

Do not treat this appendix as evidence that Python, PyO3, Cython, wheels, PyPI,
or mixed Rust/Python packaging are supported NTPRO product surfaces.
