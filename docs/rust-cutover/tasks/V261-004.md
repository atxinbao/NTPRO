# V261-004 v26 final scope release gate hardening

Date: 2026-07-07
Executor: Codex
GitHub issue: `#850`
Milestone: `v0.26.1`
Status: LOCAL VALIDATION PASSED

## Goal

Harden the v26 release gates so the final `v0.26.0` release scope cannot drift
back to 9 tasks after corrective tasks `V260-009..V260-013` are included.

Plain Chinese summary: 本任务加固 `v0.26.0` release gate，确保最终 scope 只能是
14，不能在 release notes 已包含 `V260-009..V260-013` 时让 manifest/gates 仍停留在
scope=9。它只收紧发布校验，不改变 runtime，不新增 submit，不开放 Dashboard 交易控件。

## Scope

- Require final scope issue/evidence count `14` across release notes,
  readiness, closeout evidence, manifest, release gates, and strict provenance.
- Require corrective scope `V260-009..V260-013`, issues
  `#837/#839/#841/#843/#845`, and PRs `#838/#840/#842/#844/#846` in the gate.
- Require milestone #18 closeout to stay aligned with 14 closed issues in the
  post-publication gate path.
- Add negative self-tests for final scope remaining `9`, readiness final scope
  drift, and corrective tasks present in release notes but absent from
  manifest/gates.

## Non-Goals

- No v0.26.0 tag rewrite.
- No GitHub Release republish.
- No runtime behavior change.
- No production order submission or mutation.
- No execution adapter send or live exchange request.
- No retry scheduler.
- No automatic remediation runtime.
- No Dashboard order, approval, cancel, retry, submit, replace, amend,
  flatten, remediation, order-ticket, or live operation controls.
- No product-grade live trading terminal claim.

## Required Evidence

- `bash -n scripts/ai/verify_v26_release_gates.sh scripts/ai/verify_v26_strict_provenance.sh`
- `python3 -m json.tool docs/rust-cutover/release/v0_26_0_release_manifest.json >/dev/null`
- `scripts/ai/verify_release.sh v26-release-gates`
- `scripts/ai/verify_release.sh v26-strict-provenance`
- `scripts/ai/verify_fast.sh`
- `git diff --check`

## Done Criteria

- `scripts/ai/verify_v26_release_gates.sh` fails when final scope remains `9`
  after corrective tasks are present in release notes/readiness/closeout.
- `scripts/ai/verify_v26_release_gates.sh` fails when corrective tasks are
  present in release notes but absent from manifest/gates.
- The gate passes only when manifest, readiness, closeout evidence, release
  notes, and strict provenance agree on final scope `14` and corrective scope
  `5`.
- Boundary remains release-governance-only with no submit, mutation, adapter
  send, live exchange request, retry, remediation, Dashboard trading controls,
  or product-grade trading terminal claim.
