# V320-008 fail-closed negative tests for unscoped production execution and controls

Date: 2026-07-15
Executor: Codex
GitHub issue: #1050
Milestone: v0.32.0
Status: READY FOR PR

## Goal

Add explicit fail-closed negative coverage for every unscoped production
execution and control path during backend closeout.

Plain Chinese summary: 本任务把 v0.32.0 后端收尾的负例收口为一个明确矩阵。缺少
approval、risk/audit/go-no-go、rollback/DR、telemetry/SLO/incident、config/venue
或 release evidence，或者发现未授权 submit/mutation/adapter send/live exchange/
retry/control flag，都必须 fail-closed；负例本身不授权任何正向生产执行。

## Dependency

- Depends on `V320-007` backend enablement state read model and admin bridge
  closeout.
- Requires GitHub issue `#1049` to be closed before this gate can pass its live
  dependency check.

## Scope

Included:

- Cover missing approval, missing risk/audit/go-no-go, missing rollback/DR,
  missing telemetry/SLO/incident, stale config, wrong venue, unresolved
  incident, and stale release evidence.
- Cover forbidden submit, mutation, adapter send, live exchange request, retry,
  remediation, Dashboard/Admin/Trader Terminal controls, and order-ticket flags.
- Require control boundaries to be explicit `false`; missing required boundary
  evidence fails closed.
- Include the verifier in the v32 release gate path.

Not included:

- positive production execution authorization;
- product-grade live trading terminal readiness;
- frontend completion;
- backend go-live execution.

## Acceptance Criteria

- Negative test suite passes and fails closed when any forbidden flag is true or
  missing-required proof is absent.
- No broad production execution path can pass without the full v32 closeout
  proof set.
- Verifier output is included in v32 release evidence.

## Validation

Required:

```bash
bash -n scripts/ai/verify_v32_fail_closed_negative_tests.sh scripts/ai/verify_release.sh
python3 -m json.tool docs/rust-cutover/release/v0_32_0_fail_closed_negative_tests.json >/dev/null
scripts/ai/verify_release.sh v32-backend-enable-read-model-admin-bridge-closeout
scripts/ai/verify_release.sh v32-fail-closed-negative-tests
scripts/ai/verify_fast.sh
git diff --check
```

## Result

Pending PR validation.
