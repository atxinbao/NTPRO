# V151-001 - Add formal v0.15.0 tag-triggered release gate evidence

Milestone: v0.15.1 mutation dry-run semantics / approval / evidence hardening
Risk: Medium
Auto-merge: Allowed after required local and hosted checks pass

## Goal

Record the formal `ntpro-rust-only-v0.15.0` tag-triggered hosted release gate
run in v0.15.0 release/readiness accounting.

Plain Chinese summary: 这个任务只补正式发版后的 hosted release gate 证据。它证明
v0.15.0 tag 触发的 GitHub Release Gate 已经跑完并通过，但不新增任何交易能力。

## Scope

Included:

- record GitHub Actions run `27956715055`;
- record `conclusion=success`;
- record `47/47` jobs success;
- record tag `ntpro-rust-only-v0.15.0`;
- record head SHA `6bae005c6cf9f2ab4e2cd610abbb579f9cbe7a58`;
- update v0.15.0 release/readiness accounting;
- add task evidence.

Not included:

- runtime behavior changes;
- production request sending;
- production order submission;
- production order-test submission;
- production order mutation;
- production execution adapter calls;
- Dashboard order controls.

## Acceptance Criteria

- `docs/rust-cutover/release/v0_15_0_readiness_report.md` records the formal
  tag-triggered release gate run.
- `docs/rust-cutover/release/v0_15_0_release_notes.md` references the formal
  tag-triggered release gate evidence.
- `docs/rust-cutover/evidence/V151-001.md` exists.
- `scripts/ai/verify_release.sh release-publication-guard` passes.
- `scripts/ai/verify_fast.sh` passes.
- `git diff --check` passes.

## Validation

Required:

```bash
scripts/ai/verify_release.sh release-publication-guard
scripts/ai/verify_fast.sh
git diff --check
```

