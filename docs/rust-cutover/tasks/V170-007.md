# V170-007 - Add Dashboard reconciliation and orphan panel

Date: 2026-06-24
Executor: Codex
Milestone: v0.17.0 production reconciliation and orphan recovery
Risk: medium
Auto-merge: allowed after local validation and hosted smoke PASS

## Goal

Expose v0.17 local/exchange reconciliation and orphan-risk evidence in the local
Dashboard as a read-only panel.

Plain Chinese summary: 这个任务让 Dashboard 看得见 v0.17 对账结果。大白话：页面只展示本地
账本、交易所回查、对账分类、孤儿单风险、人工复核和新单阻断状态；不提供下单、撤单、
重试、修正或凭据输入。

## Scope

Included:

- add a Dashboard snapshot read model for v0.17 reconciliation/orphan artifacts;
- render a Chinese read-only Dashboard panel;
- show local order state, exchange order state, lineage id, orphan risk,
  `manual_review_required`, `new_orders_blocked`, and artifact paths;
- add a local/offline Dashboard verification script;
- add unit coverage proving the renderer has no order/cancel controls.

Not included:

- cancel, retry, replace, amend, correction, flatten, or remediation controls;
- credential entry or persisted secrets;
- new production mutation behavior;
- new exchange/network readback execution;
- release tag creation.

## Acceptance Criteria

- Dashboard snapshot includes `production_reconciliation_orphan`.
- Panel title is `v0.17 对账与孤儿单风险`.
- Panel shows:
  - `order_lineage_id`;
  - local ledger status and local order state;
  - exchange readback status and exchange order state;
  - reconciliation outcome;
  - orphan detection outcome and orphan risk;
  - `manual_review_required`;
  - `new_orders_blocked`;
  - artifact paths.
- Renderer contains no button, fetch, credential, API key, retry, cancel, or
  correction action entry.
- Dashboard forbidden-key scan still passes.

## Validation

Required:

```text
scripts/ai/verify_v17_dashboard_reconciliation_panel.sh
cargo fmt --all --check
cargo clippy -p nautilus-cli --all-targets -- -D warnings
scripts/ai/verify_fast.sh
git diff --check
```
