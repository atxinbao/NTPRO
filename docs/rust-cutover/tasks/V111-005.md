# V111-005 Wire Public Read Probe Into v11 Offline Release Gates

Date: 2026-06-20
Executor: Codex
Owner role: Verification & Release Gatekeeper
Review role: Rust Product Surface Agent
Risk level: medium
Status: BACKLOG
Depends on: V111-004

## Goal

Wire the existing v0.11 production public read-only probe contract into the
v11 offline release gate bundle and PR smoke release-verification surface.

Plain Chinese summary: 这次不是新增线上读取能力，而是把已经存在的 public read
probe 验证脚本接进 v11 发布门禁。以后 release gate 会一起确认：public read
probe 仍然是离线合同、不会联网、不会用凭证、不会下单。

## Scope

- Run `scripts/ai/verify_v11_public_read_probe.sh` from the full
  `scripts/ai/verify_v11_offline_release_gates.sh` path.
- Run `production_public_read_probe` unit tests from the lightweight v11
  offline gate path used by PR smoke.
- Include `verify_v11_public_read_probe.sh` in PR smoke v11 path selection,
  script allowlist, release-verify selection, and shell syntax checks.

## Non-Goals

- No production network connection.
- No online public read execution.
- No credential read.
- No production account access.
- No production order submission.
- No production cancel, replace, amend, retry, or correction.
- No Dashboard order controls.
- No new release tag or GitHub Release publication.

## Acceptance

- `scripts/ai/verify_v11_public_read_probe.sh` passes.
- `NTPRO_V11_LIGHTWEIGHT=1 scripts/ai/verify_v11_offline_release_gates.sh`
  passes and includes `production_public_read_probe` tests.
- `scripts/ai/verify_release.sh v11-offline-release-gates` passes and includes
  the full public read probe script.
- Workflow YAML parses.
- `scripts/ai/verify_fast.sh` passes.
- `git diff --check` passes.

## Automation Approval

This task is marked `autoMergeAllowed=true` in Shrimp. It is release-gate
wiring only and does not add production network access, credential use, order
mutation, Dashboard order controls, or trading-semantic behavior.
