# V231-003 split v23 pre-release and post-release closeout gates

Date: 2026-07-04
Executor: Codex
GitHub issue: `#739`
Milestone: `v0.23.1`
Status: LOCAL VALIDATION PASSED

## Goal

Split the v23 pre-release gate and post-release closeout gate so the published
v0.23.0 release cannot pass by satisfying only pre-release wording.

Plain Chinese summary: 本任务把 `v0.23.0` 发布前和发布后的 gate 明确拆开。发布前可以
允许 `#718` open，表示还在等 tag、hosted gate、public release 和 publication evidence；
发布后必须要求 GitHub Release 存在、hosted release gate 成功、`#718` closed，并且
`v0.23.0` milestone closed。它只改发布治理和验证，不改交易 runtime。

## Scope

- Add an explicit phase-split contract for v23 pre-release and post-release
  closeout gates.
- Add a v0.23.1 gate that verifies the pre-release contract and live
  post-release GitHub state.
- Make v23 release gates call the post-release live closeout gate.
- Record V231-003 task evidence and validation commands.

## Non-Goals

- Do not republish v0.23.0.
- Do not move or recreate the v0.23.0 tag.
- Do not add submit, cancel, retry, replace, amend, or flatten capability.
- Do not enable production order mutation.
- Do not add Dashboard operation controls.

## Required Evidence

- `bash -n scripts/ai/verify_release.sh scripts/ai/verify_v23_release_gates.sh scripts/ai/verify_v23_1_gate_phase_split.sh`
- `python3 -m json.tool docs/rust-cutover/release/v0_23_0_release_manifest.json >/dev/null`
- `scripts/ai/verify_release.sh v23.1-gate-phase-split`
- `scripts/ai/verify_v23_1_gate_phase_split.sh pre-release-contract`
- `scripts/ai/verify_v23_1_gate_phase_split.sh post-release-live`
- `scripts/ai/verify_release.sh v23-release-gates`
- `scripts/ai/verify_fast.sh`
- `git diff --check`

## Done Criteria

- Pre-release and post-release contracts are separate and readable.
- Post-release closeout requires GitHub Release, hosted gate success, closed
  issue closeout, and closed milestone proof.
- Negative self-tests prove missing release, failed hosted run, open #718, or
  open milestone fail closed.
- GitHub issue `#739` is closed only after PR merge evidence is recorded.
