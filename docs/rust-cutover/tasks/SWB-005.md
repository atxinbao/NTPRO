# SWB-005 - 默认基线 Run 产物一致性与详情降级

Date: 2026-08-15
Executor: Codex
GitHub issue: #1317
Risk: high
Owner role: Rust Product Surface Agent
Review role: Verification & Release Gatekeeper
Status: LOCAL_VALIDATION_PASSED / REVIEW_REQUIRED

## 目标

让默认基线 Run 的配置状态、不可变结果产物和策略工作台展示保持一致。全新 MVP 工作区
不得发布一个无法读取结果的 `available` Run；结果子资源异常时仍应保留 Run 身份和生命周期。

## 范围

- 节点配置显式绑定默认基线 Backtest 配置；
- `mvp serve` 在发布身份和状态合同前生成缺失基线，并核对冻结 SHA-256；
- 已存在产物只验证、不覆盖，缺失或漂移 fail closed；
- Backtest 指标错误改为结果区域局部错误，Run 基本信息继续显示；
- 增加全新工作区、重启、篡改、前端和真实浏览器回归。

## 非目标

- 不修改 S3-LV-011 / Issue #1312；
- 不开放真实订单、订单 mutation、自动重试、自动补救或交易控件；
- 不修改 v0.32.0 冻结发布文件。

## 验收

```bash
cargo fmt --all -- --check
cargo test -p nautilus-cli --lib mvp::tests -- --test-threads=1
cargo test -p nautilus-cli --lib dashboard::product_api::tests
cargo clippy -p nautilus-cli --lib -- -D warnings

cd apps/strategy-workbench
npm run format:check
npm run lint
npm run typecheck
npm run test
npm run build

cd ../..
NTPRO_PLAYWRIGHT_CORE_PATH=/absolute/path/to/playwright-core \
NTPRO_BROWSER_EVIDENCE_DIR=/tmp/ntpro-swb005-browser-evidence \
  node scripts/ai/test_strategy_workbench_browser.mjs
scripts/ai/check_backend_runtime_risk_inventory.sh
scripts/ai/check_mvp_freeze_baseline.sh
git diff --check
```

## 完成条件

本任务为 high risk。必须取得独立审查批准并通过 hosted checks，随后手动合并；不得启用
auto-merge。PR 合并并关闭 Issue #1317 后才是 DONE。
