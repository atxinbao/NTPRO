# V06-003 - dry-run and optional testnet runtime

Milestone: v0.6.0 Binance testnet dry-run foundation
Priority: P0
Owner role: Rust Product Surface Agent
Review role: Verification & Release Gatekeeper
Risk: medium

## Goal

Provide a dry-run runtime mode and an explicit optional testnet-network intent
flag.

## Scope

- Add workflow mode parsing.
- Keep dry-run as default.
- Add `--allow-testnet-network` as an acknowledgement flag.
- Keep v0.6 artifacts offline even when network intent is acknowledged.

## Non-goals

- Do not open sockets in v0.6 smoke.
- Do not submit orders.
- Do not claim the optional network path is production-ready.

## Required evidence

- CLI parser tests cover testnet options.
- Evidence file exists at `docs/rust-cutover/evidence/V06-003.md`.
