# P0-002 - Release Gate Strengthening

Milestone: M0 - Public messaging and release gate convergence
Priority: P0
Owner role: Verification Release Gatekeeper
Review role: Control Scope Agent
Risk: medium

## Goal

Upgrade the v0.2 release gate from fast smoke to a strong release validation path.

## Dependencies

- `P0-001`

## Scope

- Keep v0.2.0 scoped to Local Multi-Node Runtime Foundation.
- Preserve sandbox/local-only boundaries unless this task explicitly says otherwise.
- Create task evidence under `docs/rust-cutover/evidence/`.

## Non-goals

- Do not create release tags or publish GitHub Releases from this task.
- Do not claim production live trading readiness.
- Do not connect to real exchanges or submit real orders.
- Do not add manual order entry, Dashboard UI scope, or distributed multi-server orchestration unless the task explicitly asks for it.
- Do not introduce Python, PyO3, or Cython product surfaces.

## Related files

- `scripts/ai/verify_release.sh` (TO_MODIFY): Release validation gate
- `scripts/ai/v02_two_node_supervisor_smoke.sh` (TO_MODIFY): Two-node smoke must not skip build in release mode
- `.github/workflows/release-tag.yml` (TO_MODIFY): Tag workflow release gate
- `docs/rust-cutover/evidence/P0-002.md` (CREATE): Task evidence

## Implementation guidance

verify_release.sh must cover verify_full, release build, CLI help, rust-only checks, Cython removal check, and the v0.2 two-node supervisor smoke. The tag workflow must run verify_release.sh before any release action.

## Verification criteria

- `scripts/ai/verify_release.sh`
- `cargo run -q -p nautilus-cli --bin nautilus -- supervisor --help | rg "register|list|start|stop|status|logs|metrics"`
- `cargo run -q -p nautilus-cli --bin ntpro-node -- --help | rg "config|run-id|output|stop-file"`
- `scripts/ai/v02_two_node_supervisor_smoke.sh`

Evidence must record changed files, commands run, command output summary, behavior impact, public API impact, migration note status, rollback plan, and a plain Chinese non-expert summary.

## Evidence required

Create `docs/rust-cutover/evidence/P0-002.md`.
