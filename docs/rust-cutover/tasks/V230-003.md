# V230-003 - multi-strategy supervisor identity and isolation

Date: 2026-07-03
Executor: Codex
GitHub issue: `#714`
Milestone: `v0.23.0`
Risk: medium
Owner role: Rust Core Runtime Agent
Review role: Verification & Release Gatekeeper
Auto-merge: allowed after local validation and hosted checks pass

## Goal

Implement multi-strategy identity and supervisor isolation under the v0.23.0
contract.

Plain Chinese summary: 本任务把多策略隔离先落到 Rust 可执行 replay 层。新增
`strategy_key`、`account_key`、`venue_node_key` 和 `isolation_scope_key` 的
strategy supervisor fixture，证明两个策略的 supervisor/runtime/risk/events/audit
状态不会混读；跨策略 component mismatch 会 fail closed，unknown strategy identity
也会 fail closed。本任务不新增实盘 submit，不新增 strategy-driven production
execution，不做 venue node lifecycle，也不新增 Dashboard 操作控件。

## Contract Traceability

This task satisfies the V230-001 contract sections required for #714:

```text
Identity Model
Strategy Boundary
Future Owner-Approved Control Paths
Logs And Evidence Boundary
```

## Scope

Included:

- add V230 strategy supervisor golden trace fixtures;
- add executable Rust projection replay for strategy supervisor identity;
- validate `strategy_key`, `account_key`, `venue_node_key`, and
  `isolation_scope_key` presence;
- fail closed on cross-strategy component mismatch;
- fail closed on unknown strategy identity;
- prove owner approval remains per-scope and production execution remains
  disabled.

Not included:

- multi-venue node lifecycle implementation;
- live order submission expansion;
- dashboard redesign;
- production order mutation;
- strategy-driven production execution.

## Acceptance Criteria

- `tests/golden/read_model_strategy_supervisor_schema.jsonl` includes isolated,
  cross-strategy mismatch, and unknown strategy identity cases.
- `crates/cli/tests/golden_trace_read_model_projection.rs` replays the V230
  strategy supervisor cases through Rust projection logic.
- `docs/rust-cutover/golden_trace/RELEASE_REPLAY_SCOPE.json` records the V230
  strategy supervisor cases as executable replay.
- Evidence maps #714 to the V230-001 contract and records local validation.
- No submit, order mutation, venue lifecycle, Dashboard operation-control
  behavior, or strategy-driven production execution is added.

## Validation

Required:

```text
cargo test -p nautilus-cli --test golden_trace_read_model_projection -- --nocapture
python3 -c 'import json,pathlib; [json.loads(line) for line in pathlib.Path("tests/golden/read_model_strategy_supervisor_schema.jsonl").read_text().splitlines() if line.strip()]'
jq . docs/rust-cutover/golden_trace/RELEASE_REPLAY_SCOPE.json
python3 scripts/ai/validate_golden_trace_release_scope.py
rg -n "read_model.strategy_supervisor|strategy_key|isolation_scope_key|cross_strategy_component_mismatch|missing_strategy_key|rust_cli_read_model_projection_replays_v230_strategy_supervisor_paths" tests/golden/read_model_strategy_supervisor_schema.jsonl crates/cli/tests/golden_trace_read_model_projection.rs docs/rust-cutover/golden_trace/RELEASE_REPLAY_SCOPE.json docs/rust-cutover/evidence/V230-003.md
scripts/ai/verify_fast.sh
git diff --check
```
