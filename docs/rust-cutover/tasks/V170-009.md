# V170-009 - Add v0.17 release gates and readiness notes

Date: 2026-06-24
Executor: Codex
Milestone: v0.17.0 production reconciliation and orphan recovery
Risk: medium
Auto-merge: allowed after local validation and hosted smoke PASS

## Goal

Wire v0.17 production reconciliation and orphan recovery evidence into release
verification, then produce readiness and release notes for the owner release
decision.

Plain Chinese summary: 这个任务是 v0.17 的发版收口。大白话：把本地账本、交易所只读回查、
对账分类、孤儿单检测、重启恢复、Dashboard 只读展示、失败 incident 语义串成一个离线
发版门禁，并写清楚本版本能说什么、不能说什么。

## Scope

Included:

- add `scripts/ai/verify_v17_release_gates.sh`;
- add `v17-release-gates` to `scripts/ai/verify_release.sh`;
- add PR smoke workflow routing for v0.17 release gates;
- add tag release workflow routing for v0.17 release gates;
- add `v0_17_0_readiness_report.md`;
- add `v0_17_0_release_notes.md`;
- record V170-000 through V170-008 merged-PR accounting;
- keep all default gates local/offline and fail-closed.

Not included:

- release tag creation;
- GitHub Release publication;
- production network readback execution;
- production order submission or mutation;
- cancel execution;
- retry, replace, amend, correction, flatten, or remediation;
- Dashboard order/cancel controls;
- strategy-driven production execution;
- multi-account or multi-venue production execution.

## Acceptance Criteria

- `scripts/ai/verify_v17_release_gates.sh` runs the v0.17 local/offline evidence
  chain and rejects forbidden production mutation markers.
- `scripts/ai/verify_release.sh v17-release-gates` works.
- PR smoke can run v17 release gates for v0.17-relevant files.
- Tag release workflow includes `release-v17-release-gates`.
- Readiness and release notes state:
  - `Production Reconciliation And Orphan Recovery Evidence`;
  - `capability_expansion_from_v16 = reconciliation_evidence_only`;
  - `lineage_scope = single_v16_mutation_candidate`;
  - `default execution posture = offline fail-closed`;
  - production order submission and mutation are not included;
  - actual cancel send is deferred;
  - Dashboard order/cancel controls remain disabled.

## Validation

Required:

```text
bash -n scripts/ai/verify_v17_release_gates.sh scripts/ai/verify_release.sh
scripts/ai/verify_v17_release_gates.sh
scripts/ai/verify_release.sh v17-release-gates
ruby -e 'require "yaml"; YAML.load_file(".github/workflows/release-tag.yml"); YAML.load_file(".github/workflows/rust-cutover-smoke.yml")'
scripts/ai/verify_fast.sh
git diff --check
```
