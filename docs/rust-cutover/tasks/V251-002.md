# V251-002 V250-009 corrective release scope integration

Date: 2026-07-06
Executor: Codex
GitHub issue: `#807`
Milestone: `v0.25.1`
Status: LOCAL VALIDATION PASSED

## Goal

Integrate `V250-009` / GitHub issue `#804` / PR `#805` into the final
`v0.25.0` release scope evidence, manifest, readiness report, release notes,
and strict provenance inputs.

Plain Chinese summary: 本任务把 v0.25.0 最终 tag 中必需的 `V250-009` corrective
fix 纳入 release scope。它解释最终 release scope 是 `#777-#785` 这 9 个
milestone issue，加上 `#804` corrective issue；这个 corrective fix 只修发布门禁，
不扩展 v25 capability，不改变 runtime 或 trading behavior。

## Scope

- Add `V250-009` task/evidence to the v25 release manifest scope.
- Record the corrective failed run `28762387835` and final success run
  `28764231552` in release notes/readiness/manifest.
- Update v25 release gates so missing `V250-009` task/evidence, open `#804`, or
  unmerged PR `#805` fails closed.
- Update v25 strict provenance inputs and generated strict manifest to include
  `V250-009` task/evidence and corrective release scope metadata.
- Add `v25.1-corrective-release-scope` as the V251-002 validation entrypoint.

## Required Facts

```text
V250 milestone issue scope = #777-#785
V250 milestone issue count = 9
V250 corrective issue scope = #804 / V250-009
V250 final release scope issue count = 10
V250 final release scope evidence count = 10
failed release gate run = https://github.com/atxinbao/NTPRO/actions/runs/28762387835
final success release gate run = https://github.com/atxinbao/NTPRO/actions/runs/28764231552
PR #805 merge commit = eedcdab1d3ca85d6f51b368b5f36208a7b591026
capability expansion = false
runtime behavior change = false
trading behavior change = false
```

## Non-Goals

- No v0.25.0 tag rewrite.
- No v0.25.0 milestone issue-count change.
- No runtime behavior change.
- No production order submission or mutation.
- No execution adapter send or live exchange request.
- No automatic recovery, retry scheduler, or Dashboard trading controls.

## Required Evidence

- `bash -n scripts/ai/verify_release.sh scripts/ai/verify_v25_release_gates.sh scripts/ai/verify_v25_strict_provenance.sh scripts/ai/verify_v25_1_corrective_release_scope.sh`
- `python3 -m json.tool docs/rust-cutover/release/v0_25_0_release_manifest.json >/dev/null`
- `scripts/ai/verify_release.sh v25.1-corrective-release-scope`
- `scripts/ai/verify_release.sh v25-release-gates`
- `scripts/ai/verify_release.sh v25-strict-provenance`
- `scripts/ai/verify_fast.sh`
- `git diff --check`

## Done Criteria

- v25 release docs no longer claim the final release scope is only
  `V250-000..V250-008`.
- The v25 release manifest records the 9 milestone issues plus the corrective
  `V250-009` issue as final release scope.
- The strict provenance generated manifest contains both
  `docs/rust-cutover/tasks/V250-009.md` and
  `docs/rust-cutover/evidence/V250-009.md`.
- Missing corrective task/evidence, open `#804`, or unmerged PR `#805` fails
  the release scope gate.
