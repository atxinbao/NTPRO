# V031-007 - reconnect control contract cleanup

Milestone: v0.3.1 Local Supervisor Control Console Hardening
Priority: P1
Owner role: Rust Product Surface Agent
Review role: Verification & Release Gatekeeper
Risk: medium

## Goal

Make reconnect control wording and results consistent across CLI, Dashboard,
API, artifacts, and docs.

Plain language: users should immediately understand that reconnect in the local
sandbox path is an explicit `not_supported`, not a real venue reconnect.

## Dependencies

- `V031-006`

## Scope

- Review reconnect wording across CLI output, Dashboard labels, API responses,
  and release docs.
- Keep the result `status=not_supported` and
  `sandbox_reconnect_not_supported` aligned everywhere relevant.
- Clarify user-facing wording so the sandbox limitation is harder to misread.
- Add or update tests where wording or response contracts change.

## Non-goals

- Do not implement production reconnect behavior.
- Do not connect to real exchanges.
- Do not broaden the control surface beyond the existing local sandbox scope.

## Related files

- `crates/cli/src/supervisor.rs` (TO_MODIFY): CLI reconnect output if needed.
- `crates/cli/src/dashboard.rs` (TO_MODIFY): Dashboard wording and API result.
- `docs/rust-cutover/release/v0_3_0_supervisor_control_readiness_report.md`
  (REFERENCE): current wording.
- `docs/rust-cutover/evidence/V031-007.md` (CREATE): task evidence.

## Implementation guidance

Prefer explicit user-facing wording over hidden implementation notes. Keep the
behavior honest rather than making the buttons look more capable than they are.

## Verification criteria

- Reconnect wording is consistent across CLI, Dashboard, API, and docs.
- Tests still prove `not_supported` behavior.
- No path implies real venue reconnect capability.

## Required commands

```bash
cargo test -p nautilus-cli dashboard --lib
cargo test -p nautilus-cli supervisor --lib
scripts/ai/v03_supervisor_control_smoke.sh
scripts/ai/v03_dashboard_smoke.sh
git diff --check
```

## Evidence required

Create `docs/rust-cutover/evidence/V031-007.md` with:

- task ID;
- plain Chinese summary;
- files changed;
- commands run;
- reconnect contract cleanup result;
- behavior impact;
- public API impact;
- migration note status;
- rollback plan.
