# V240-008 Dashboard Workbench read-only order-control preview

Date: 2026-07-04
Executor: Codex
Task: `V240-008` / GitHub issue `#751`
Milestone: `v0.24.0`
Status: LOCAL RENDER VALIDATION PASSED

## Goal

Render the v0.24 order-control preview line in Dashboard / Workbench as
read-only evidence. The panel must show order intent, execution policy,
rate-limit, slicing, cancel/replace/amend preview, retry/no-retry,
readback/audit, blocked reasons, scope keys, source provenance, and redaction
state without exposing real operation controls.

Plain Chinese summary: 本任务只把 v0.24 order-control preview 的证据展示到
Dashboard / Workbench。页面能看到 intent、policy、rate-limit、slicing、
cancel/replace/amend、retry/no-retry、readback/audit、blocked reason、scope、
provenance 和 redaction；但不会出现真实 submit/cancel/replace/amend/flatten
控件，也不会新增 live control API。

## Scope

- Add a Workbench read-only `v24 Order-control preview` panel.
- Surface preview status, blocked reasons, scope key, source provenance,
  redaction state, evidence refs, and missing preview evidence.
- Keep submit/cancel/replace/amend/flatten/order-ticket and manual operation
  entry boundaries explicitly disabled.
- Add a render smoke fixture covering normal, blocked, missing provenance, and
  forbidden-control states.

## Non-Goals

- Do not implement a trader operation terminal.
- Do not add a live control API.
- Do not add submit, cancel, replace, amend, flatten, retry, or order-ticket
  UI controls.
- Do not call network, exchange, or execution adapter code.

## Required Evidence

- `bash -n scripts/ai/verify_release.sh scripts/ai/verify_v24_dashboard_workbench_order_control_preview.sh`
- `python3 -m json.tool tests/golden/v240_dashboard_workbench_order_control_preview.json`
- `scripts/ai/verify_release.sh v24-dashboard-workbench-preview`
- `cargo test -p nautilus-cli dashboard_v24_order_control_preview --lib`
  attempted locally; interrupted after `rustc` made no progress.
- `scripts/ai/verify_fast.sh` not run locally because it would reuse the same
  Rust compile path that stalled.
- `git diff --check`

## Done Criteria

- Normal preview evidence renders as read-only `ready_preview`.
- Blocked preview evidence renders blocked reasons and stays read-only.
- Missing provenance / preview evidence renders `degraded_unavailable`, not
  ready.
- Forbidden control markers render as fail-closed evidence and do not expose
  controls.
- Rendered Workbench HTML and the relevant JS renderer bodies contain no
  button, form, input, action endpoint, or order-control action marker.
