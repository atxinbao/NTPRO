# V261-002 V260-009..013 final release scope integration

Date: 2026-07-07
Executor: Codex
GitHub issue: `#848`
Milestone: `v0.26.1`
Status: LOCAL VALIDATION PASSED

## Goal

Promote the post-publication corrective tasks `V260-009..V260-013` into the
formal final `v0.26.0` release scope so release notes, manifest, readiness,
release gates, and strict provenance all agree that final scope is 14 tasks.

Plain Chinese summary: 本任务把 `V260-009..V260-013` 从“后续 corrective 修复”
提升为 `v0.26.0` 最终 release scope 的正式组成部分。最终范围必须记录 14 个
issue/evidence，以及 `#838/#840/#842/#844/#846` 这 5 个 corrective PR。它只收紧
发布治理和 provenance，不改变 runtime，不新增 submit，不开放 Dashboard 交易控件。

## Scope

- Add `V260-009..V260-013` to `v0.26.0` release manifest scope and evidence.
- Record corrective issue and PR mapping: `#837/#838`, `#839/#840`,
  `#841/#842`, `#843/#844`, `#845/#846`.
- Update release notes, readiness, and closeout evidence to state final
  release scope count `14`.
- Add `v26.1-final-scope-integration` verifier and route it through
  `scripts/ai/verify_release.sh`.
- Update v26 release gates and strict provenance inputs to fail closed on
  missing corrective scope.

## Non-Goals

- No v0.26.0 tag rewrite.
- No GitHub Release republish.
- No runtime behavior change.
- No production order submission or mutation.
- No execution adapter send or live exchange request.
- No retry scheduler.
- No automatic remediation runtime.
- No Dashboard order, approval, cancel, retry, submit, replace, amend,
  flatten, remediation, order-ticket, or live operation controls.
- No product-grade live trading terminal claim.

## Required Evidence

- `bash -n scripts/ai/verify_release.sh scripts/ai/verify_v26_release_gates.sh scripts/ai/verify_v26_strict_provenance.sh scripts/ai/verify_v26_1_final_scope_integration.sh`
- `python3 -m json.tool docs/rust-cutover/release/v0_26_0_release_manifest.json >/dev/null`
- `python3 -c 'import json,pathlib; [json.loads(line) for line in pathlib.Path("tests/golden/v260/release_gates_strict_provenance.jsonl").read_text().splitlines() if line.strip()]'`
- `scripts/ai/verify_release.sh v26.1-final-scope-integration`
- `scripts/ai/verify_release.sh v26-release-gates`
- `scripts/ai/verify_release.sh v26-strict-provenance`
- `scripts/ai/verify_fast.sh`
- `git diff --check`

## Done Criteria

- Release notes, manifest, readiness, release gates, and strict provenance all
  agree on final scope `14`.
- `V260-009..V260-013` task/evidence files are strict provenance inputs.
- A missing corrective evidence entry, corrective scope entry, or corrective
  release notes mapping fails closed.
- Corrective tasks remain release-governance-only and do not expand submit,
  mutation, adapter send, live exchange, retry, remediation, Dashboard trading,
  or product-grade trading-terminal capability.
