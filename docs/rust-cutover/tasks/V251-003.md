# V251-003 V250-008 stale pre-tag evidence cleanup

Date: 2026-07-06
Executor: Codex
GitHub issue: `#808`
Milestone: `v0.25.1`
Status: LOCAL VALIDATION PASSED

## Goal

Clean stale pre-tag, missing-tag, offline, and issue-open language from v25
post-release evidence so source-tree closeout facts match the published
`v0.25.0` release state.

Plain Chinese summary: 本任务清理 V250-008 里残留的 pre-tag / missing-tag /
offline / issue-open 描述。必要的历史 PR validation 可以保留，但必须明确标成
historical pre-release snapshot，不能再被读成发布后的当前状态。

## Scope

- Update `docs/rust-cutover/evidence/V250-008.md` with current post-release
  facts for `#785`, `#804`, PR `#805`, tag, release, and milestone closeout.
- Update the V250-008 section in `verification.md` so pre-release validation is
  explicitly historical and no longer uses stale raw markers.
- Add manifest metadata for the V251-003 cleanup.
- Add a `v25.1-stale-pretag-cleanup` gate that rejects stale markers in v25
  post-release evidence and the V250-008 verification section.

## Non-Goals

- No v0.25.0 tag rewrite.
- No runtime behavior change.
- No production order submission or mutation.
- No execution adapter send or live exchange request.
- No automatic recovery, retry scheduler, or Dashboard trading controls.
- No deletion of necessary historical PR validation records.

## Required Evidence

- `bash -n scripts/ai/verify_release.sh scripts/ai/verify_v25_1_stale_pretag_cleanup.sh`
- `python3 -m json.tool docs/rust-cutover/release/v0_25_0_release_manifest.json >/dev/null`
- `scripts/ai/verify_release.sh v25.1-stale-pretag-cleanup`
- `scripts/ai/verify_release.sh v25.1-release-closeout-evidence`
- `scripts/ai/verify_fast.sh`
- `git diff --check`

## Done Criteria

- v25 post-release evidence does not contain unexplained
  `tag_exists=false`, `current_issue_state=OPEN`, missing-tag, offline-only
  publication, pending release publication, or pending tag-gate markers.
- V250-008 historical validation is explicitly marked as pre-release and not
  current post-release state.
- `#785`, `#804`, PR `#805`, and v0.25.0 milestone closeout remain traceable in
  source-tree evidence.
