# BFH-003 Stale Tooling Configuration Cleanup

Date: 2026-07-19
Executor: Codex
GitHub issue: #1114
Milestone: post-freeze-backend-hygiene
Risk: medium
Classification: baseline-preserving-governance
Status: READY FOR PR

## Goal

Remove unreachable repository configuration left after the Python/wheel,
legacy control-plane, and helper-script retirement.

Plain Chinese summary: 本任务删除 `.gitignore` 中 28 个不存在脚本的例外和已退役
control 脚本例外，删除无人使用的 `ci-pr-wheel` Cargo profile，并修正一条指向已删除
脚本的注释。当前 Rust Make、CI、pre-commit 和脚本入口继续保留。

## Dependency

BFH-001 (`#1112`) is closed through merged PR `#1127`.

## Scope

- audit every explicit non-glob `.gitignore` exception against the current
  worktree;
- remove absent-file exceptions and the retired `scripts/control/*.sh`
  exception;
- prove `ci-pr-wheel` has no caller and remove its two profile blocks;
- audit script references from Make, workflows/actions, pre-commit, and
  supported scripts;
- correct the active comment that names deleted `scripts/ci/plan.sh`;
- record the result in the hygiene authority map and evidence.

## Not In Scope

- dependency or lockfile updates;
- release, bench, dev, test, nextest, or current `ci-pr` profile changes;
- BFH-004 contributor documentation repair;
- removal of historical names from `docs/rust-cutover/`;
- runtime behavior under `crates/**`.

## Acceptance Criteria

- every retained explicit `.gitignore` file exception exists;
- active Make, workflow/action, pre-commit, and script references resolve;
- `ci-pr-wheel` has zero current references and is absent after cleanup;
- `cargo metadata` still reports the complete 42-package workspace;
- zero-Python and backend-freeze gates pass;
- no dependency, runtime, public API, or frozen release change occurs.
