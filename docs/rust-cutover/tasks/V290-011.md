# V290-011 v29 hosted release gate JSON payload fix

Date: 2026-07-10
Executor: Codex
GitHub issue: `#961`
Milestone: `v0.29.0`
Risk: medium
Owner role: Verification & Release Gatekeeper
Review role: Control & Scope Agent
Auto-merge: allowed after local validation and hosted checks pass
Status: READY_FOR_REVIEW

## Goal

Fix the hosted v0.29.0 release gate failure caused by passing oversized GitHub
run JSON through environment variables, then register this corrective task as
part of the final v0.29.0 release scope.

Plain Chinese summary: 本任务修复 v0.29.0 hosted release gate 在读取 v0.28.1 run
jobs JSON 时触发的 `Argument list too long`。修复方式是把大型 GitHub JSON 写入临时文件
再由 Python 读取，并把 #961/V290-011 作为正式 corrective release-gate scope 纳入
manifest、release notes、readiness report、release gates、strict provenance 和发布检查。
它不改变 runtime，不开放 backend go-live，也不开放任何下单、mutation、adapter send、
live exchange request、retry、remediation 或交易控件。

## Dependency

- Depends on `V290-000` through `V290-010`.
- Depends on the failed hosted release gate run for tag
  `ntpro-rust-only-v0.29.0` proving the oversized environment payload failure.
- Blocks the final v0.29.0 tag gate and GitHub Release publication until #961 is
  merged and closed.

## Scope

Included:

- Change `verify_v29_intake_gate.sh` to pass large GitHub JSON payloads through
  temporary files instead of environment variables.
- Register #961 / V290-011 as the only corrective-scope exception in the final
  v0.29.0 release manifest, notes, readiness report, intake gate, release
  gates, strict provenance, and release publication guard.
- Require the exact final V290 issue set `#926-#936, #961`, 12 evidence files,
  and one registered corrective-scope exception before the tag gate passes.

Not included:

- backend go-live;
- product-grade live trading terminal readiness;
- submit/order mutation/cancel/replace/amend/flatten;
- adapter send or live exchange request;
- retry scheduler or automatic remediation;
- Dashboard/Admin/Trader Terminal operation or trading controls.

## Validation

Required:

```text
bash -n scripts/ai/verify_v29_intake_gate.sh scripts/ai/verify_v29_release_gates.sh scripts/ai/verify_v29_strict_provenance.sh scripts/ai/check_github_release_published.sh scripts/ai/verify_release.sh
python3 -m json.tool docs/rust-cutover/release/v0_29_0_release_manifest.json >/dev/null
scripts/ai/verify_release.sh v29-intake-gate
scripts/ai/verify_release.sh v29-release-gates
scripts/ai/verify_release.sh v29-strict-provenance
scripts/ai/verify_fast.sh
git diff --check
```
