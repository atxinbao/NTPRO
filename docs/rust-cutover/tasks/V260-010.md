# V260-010 - v26 nested historical release gate correction

Date: 2026-07-07
Executor: Codex
GitHub issue: #839
Milestone: v0.26.0

## Goal

Fix the v0.26.0 hosted release gate failure where `v26-release-gates` invokes
the v25.1 gate as historical prerequisite evidence under the v0.26.0 tag.

## Context

The direct v25.1 tag-stage gate must prove that `HEAD` matches
`ntpro-rust-only-v0.25.1`. That same assertion is wrong when v25.1 is checked
from a later v26 tag as immutable historical prerequisite evidence.

## Scope

- Keep the direct v25.1 tag-stage `HEAD == ntpro-rust-only-v0.25.1` check.
- Add an explicit historical-prerequisite mode for v26 nested calls.
- Keep v25.1 release publication, issue closeout, milestone closeout, and
  evidence validation intact.
- Do not change runtime, adapter, Dashboard, order, or public API behavior.

## Acceptance

- `scripts/ai/verify_release.sh v26-release-gates` passes locally.
- `scripts/ai/verify_release.sh v26-strict-provenance` passes locally.
- Hosted `Rust Cutover Release Gate` for `ntpro-rust-only-v0.26.0` succeeds
  after the corrective PR merges and the tag is updated.
