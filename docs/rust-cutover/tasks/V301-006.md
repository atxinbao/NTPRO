# V301-006 v31 start gate hardening and dependency proof

Date: 2026-07-12
Executor: Codex
GitHub issue: `#1004`
Milestone: `v0.30.1`
Risk: medium
Owner role: Verification & Release Gatekeeper
Review role: Control & Scope Agent
Auto-merge: allowed after local validation and hosted checks pass
Status: READY_FOR_REVIEW

## Goal

Harden the v0.31.0 start gate so production enablement cannot start before
v0.30.1 closeout is published.

Plain Chinese summary: 本任务把 v0.31.0 的启动条件收紧到必须等待完整 v0.30.1
patch closeout。v31 不能只依赖 v0.30.0 发布事实，也不能绕过 V301 issue closeout；
必须等 `ntpro-rust-only-v0.30.1` 发布证据和 source-controlled closeout 可重建后，
再通过独立 scoped approval、risk gate、audit gate、rollback readiness 和 telemetry/SLO
gate 进入后续能力实现。

## Dependency

- Depends on V301-001 through V301-005.
- Blocks all v0.31.0 capability work until V301-001..V301-007 are closed and
  the v0.30.1 GitHub Release evidence is published and reconstructable.

## Scope

Included:

- Add v0.30.1 -> v0.31.0 start-gate contract and JSON.
- Update v30 v31 handoff to require v0.30.1 release closeout.
- Add verifier proving v31 remains blocked while v0.30.1 release evidence is
  missing.
- Preserve v31 non-inheritance of submit, mutation, adapter send, live exchange,
  automatic remediation, retry scheduler, backend go-live, and trading controls.

Not included:

- backend go-live;
- production submit or production order mutation;
- execution adapter send or live exchange request;
- retry scheduler or automatic remediation;
- Dashboard/Admin/Trader Terminal trading controls;
- product-grade live trading claim.

## Validation

```text
bash -n scripts/ai/verify_v30_1_v31_start_gate.sh
python3 -m json.tool docs/rust-cutover/release/v0_30_1_v31_start_gate.json >/dev/null
scripts/ai/verify_v30_1_v31_start_gate.sh source
scripts/ai/verify_v30_1_v31_start_gate.sh live
scripts/ai/verify_v30_release_gates.sh
scripts/ai/verify_fast.sh
git diff --check
```
