# V110-002 Add Production Public Read-Only Probe Contract

Date: 2026-06-19
Executor: Codex
Owner role: Rust Product Surface Agent
Review role: Verification & Release Gatekeeper
Risk level: medium
Status: BACKLOG
Depends on: V110-001

## Goal

Add the v0.11 production public read-only probe contract for Binance Spot
production public endpoints without credentials, account access, or order
mutation.

Plain Chinese summary: 本任务只给 v0.11 增加“生产公开只读探针”的本地契约。
它可以描述 `GET /api/v3/time` 和 `GET /api/v3/exchangeInfo` 这类公开只读
endpoint，但默认不联网、不读取凭证、不访问账户、不下单、不撤单、不开放
Dashboard 下单控制。

## Scope

- Add a Rust CLI command for production public read-only probe artifacts.
- Support only public unauthenticated production read endpoints.
- Keep default execution offline and fail-closed.
- Add a local verification script that proves no credentials, no orders, no
  account mutation, and no Dashboard order controls are involved.
- Record release design and evidence.

## Non-Goals

- No production order submission.
- No production cancel, replace, amend, retry, or correction orders.
- No authenticated account snapshot.
- No production WebSocket stream.
- No actual online production HTTP probe in this task.
- No Dashboard order controls.
- No production trading readiness claim.

## Acceptance

- `nautilus live production-public-read-probe` writes a JSON artifact.
- Missing CLI/env gates produce `blocked_missing_gate`.
- Fully gated offline mode produces `ready_offline_contract`.
- `--manual-online` remains blocked in V110-002 and records
  `network_attempted=false`.
- Artifacts record:
  - `endpoint_class=production_public_read_only`;
  - `requires_api_key=false`;
  - `requires_signature=false`;
  - `mutation_allowed=false`;
  - `credentials_used=false`;
  - `production_order_submission_attempted=false`;
  - `production_order_mutation_attempted=false`;
  - `dashboard_order_controls_enabled=false`.
- `scripts/ai/verify_v11_public_read_probe.sh` passes.
*** Add File: docs/rust-cutover/release/v0_11_0_public_read_probe.md
# NTPRO v0.11.0 Production Public Read-Only Probe Contract

Date: 2026-06-19
Executor: Codex
Status: PLANNED CONTRACT

## Summary

`v0.11.0` introduces a local production public read-only probe contract. The
contract classifies Binance Spot production public endpoints and writes
auditable artifacts, but V110-002 does not open a network connection.

Plain Chinese summary: 这一步只是把“哪些生产公开 endpoint 可以只读”写成 CLI
和 JSON 证据。它默认离线，不用 API key，不读账户，不提交订单，不撤单，也不能说明
NTPRO 已经可以做真实生产交易。

## Supported Public Endpoints

| Endpoint | Method | Class | Credentials | Mutation |
| --- | --- | --- | --- | --- |
| `https://api.binance.com/api/v3/time` | `GET` | `production_public_read_only` | No | No |
| `https://api.binance.com/api/v3/exchangeInfo` | `GET` | `production_public_read_only` | No | No |

Any signed, account, order, cancel, replace, amend, retry, correction, or
unknown production endpoint remains out of scope for this task.

## CLI Contract

```bash
nautilus live production-public-read-probe \
  --endpoint server-time \
  --output /tmp/public-read-probe.json
```

Default behavior is closed:

```text
status=blocked_missing_gate
network_attempted=false
credentials_used=false
production_order_submission_attempted=false
production_order_mutation_attempted=false
dashboard_order_controls_enabled=false
```

Offline ready-contract mode requires both CLI gates and environment gates:

```bash
NTPRO_ALLOW_PRODUCTION_PUBLIC_READ=1 \
NTPRO_CONFIRM_PRODUCTION_PUBLIC_READ_ONLY=1 \
NTPRO_CONFIRM_NO_PRODUCTION_ORDER_MUTATION=1 \
nautilus live production-public-read-probe \
  --endpoint exchange-info \
  --allow-production-public-read \
  --confirm-read-only \
  --confirm-no-order-mutation \
  --output /tmp/public-read-probe.json
```

This still records `network_attempted=false`. `--manual-online` is deliberately
blocked in V110-002 because actual online production reads require a later task
and separate evidence.

## Artifact Contract

The artifact schema is `ntpro.v110_production_public_read_probe.v1`.

Required boundary fields:

```text
endpoint_class = production_public_read_only
requires_api_key = false
requires_signature = false
read_allowed = false unless all gates are present and manual_online=false
mutation_allowed = false
online_execution_supported = false
network_attempted = false
credentials_used = false
account_mutation_attempted = false
production_order_submission_attempted = false
production_order_mutation_attempted = false
dashboard_order_controls_enabled = false
```

## Release Boundary

V110-002 may be used as evidence that the production public read-only path is
classified and fail-closed. It must not be used as evidence of:

- successful online production reads;
- authenticated account reads;
- production order lifecycle readiness;
- real-funds trading readiness.
*** Add File: docs/rust-cutover/evidence/V110-002.md
# V110-002 Evidence

Date: 2026-06-19
Executor: Codex
Task: V110-002 Add production public read-only probe contract
Risk level: medium

## Goal

Add a v0.11 production public read-only probe contract for non-authenticated
Binance Spot production endpoints while keeping the default path offline and
fail-closed.

Plain Chinese summary: 这次只是加“生产公开只读探针”的本地契约和验证脚本。
它不会联网，不会使用 API key，不会读取账户，不会下单/撤单/改单，也不会给 Dashboard
增加下单按钮。

## Files Changed

- `crates/cli/src/opt.rs`
  - Adds `live production-public-read-probe` CLI options and endpoint enum.
- `crates/cli/src/live.rs`
  - Adds the offline probe artifact builder and unit tests.
- `scripts/ai/verify_v11_public_read_probe.sh`
  - Adds local verification for blocked and ready offline artifacts.
- `docs/rust-cutover/tasks/V110-002.md`
  - Records the task contract.
- `docs/rust-cutover/release/v0_11_0_public_read_probe.md`
  - Records the release-surface contract.
- `docs/rust-cutover/evidence/V110-002.md`
  - Records this evidence.

## Behavior Impact

Adds a CLI contract command:

```bash
nautilus live production-public-read-probe
```

The command only writes/read-reports local artifacts. It does not open network
connections and does not touch exchange state.

## Public API Impact

CLI surface added under `live`. No Rust library API or trading semantics changed.

## Migration Note

No migration note is required because this is a new v0.11 CLI contract.

## Boundary Confirmed

```text
supported_paths = /api/v3/time, /api/v3/exchangeInfo
endpoint_class = production_public_read_only
requires_api_key = false
requires_signature = false
network_attempted = false
credentials_used = false
account_mutation_attempted = false
production_order_submission_attempted = false
production_order_mutation_attempted = false
dashboard_order_controls_enabled = false
```

## Validation

Pending local validation before PR:

```bash
cargo test -p nautilus-cli production_public_read_probe --lib
scripts/ai/verify_v11_public_read_probe.sh
scripts/ai/verify_fast.sh
git diff --check
```

## Rollback Plan

Revert this PR to remove the CLI command, verification script, task/release
docs, and evidence file. No exchange state, credentials, or runtime trading
behavior require rollback.
