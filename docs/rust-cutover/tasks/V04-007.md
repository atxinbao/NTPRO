# V04-007 - RSI smoke

Milestone: v0.4.0 Binance Sandbox Product Foundation
Priority: P0
Owner role: Trading Runtime Agent
Review role: Verification & Release Gatekeeper
Risk: medium

## Goal

Produce the second deterministic strategy smoke for v0.4 using RSI.

Plain language: after EMA works, prove RSI can run through the same Binance
sandbox path without inventing a separate product model.

## Dependencies

- `V04-006`

## Scope

- Run RSI on the v0.4 Binance sandbox path.
- Reuse fixture replay, mock order lifecycle, and scoped risk behavior.
- Keep output shape compatible with the EMA smoke evidence model.

## Non-goals

- Do not introduce a new generic strategy platform.
- Do not claim cross-exchange portability.
- Do not require real funds or real orders.

## Related files

- `crates/trading/` (TO_MODIFY): RSI smoke harness or strategy wiring.
- `crates/backtest/` (TO_MODIFY): deterministic entrypoint if needed.
- `docs/rust-cutover/evidence/V04-007.md` (CREATE): task evidence.

## Implementation guidance

Keep the RSI smoke parallel to EMA. The goal is comparability and a stable
product path, not a second unrelated demo stack.

## Verification criteria

- RSI smoke runs locally on the same sandbox foundation.
- Output shape is compatible with the EMA evidence model.
- Smoke remains deterministic and non-production.

## Required commands

```bash
cargo test -p nautilus-backtest --lib
cargo test -p nautilus-live --lib
git diff --check
```

## Evidence required

Create `docs/rust-cutover/evidence/V04-007.md` with:

- task ID;
- plain Chinese summary;
- files changed;
- commands run;
- smoke result summary;
- behavior impact;
- public API impact;
- migration note status;
- rollback plan.
