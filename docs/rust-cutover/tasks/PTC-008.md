# PTC-008 Zero-Python Repository Tooling Closeout

Date: 2026-07-17
Executor: Codex
GitHub issue: #1103
Milestone: python-tooling-closeout
Risk: medium
Status: READY FOR PR

## Goal

Fail closed on future repository Python tooling drift and establish the
source-controlled half of the Python Tooling Closeout proof.

## Scope

- add a Rust governance command that scans the current Git tracked tree;
- reject Python source, notebooks, bytecode, manifests, environments, and
  caches;
- reject executable Python, uv, pytest, Ruff, and pip commands in supported
  scripts, Make, workflows, and actions;
- reject Python setup and wheel workflow/action surfaces;
- retain historical `docs/rust-cutover/` evidence and verify required PTC
  records remain tracked;
- bind the guard into current release governance and hosted smoke;
- record the post-merge `source_tree_plus_github_remote` closeout contract.

## Prohibited

- modifying `docs/rust-cutover/release/v0_32_0_*`;
- text-cleaning historical Python, Cython, PyO3, or wheel evidence;
- adding backend capability, submit, mutation, adapter send, live exchange,
  retry, remediation, or trading controls;
- closing milestone #33 before every PTC issue and PR is closed/merged and
  hosted gates are successful.

## Acceptance

- tracked `.py/.pyi/.pyx/.pxd/.ipynb/.pyc` files are zero;
- `pyproject.toml`, `uv.lock`, `.venv`, `venv`, `__pycache__`, and local `.pyc`
  artifacts are absent;
- active Python execution in scripts, Make, workflows, and actions is zero;
- Python package/wheel workflow and action paths are zero;
- deterministic positive and fail-closed negative guard tests pass;
- current Rust smoke, docs, security, and backend-freeze gates pass;
- issue #1103 and milestone #33 close only after the PR merges.
