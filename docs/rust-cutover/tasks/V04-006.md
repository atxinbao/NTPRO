# V04-006 - EMA smoke

Milestone: v0.4.0 Binance Sandbox Product Foundation
Priority: P0
Owner role: Trading Runtime Agent
Review role: Verification & Release Gatekeeper
Risk: medium

## Goal

Produce the first deterministic EMA sandbox smoke for v0.4.

Plain language: prove one built-in EMA strategy can run on Binance fixture
replay with mock execution and scoped risk behavior.

## Dependencies

- `V04-009`

## Scope

- Run EMA on the v0.4 Binance sandbox path.
- Use fixture replay, mock order lifecycle, and scoped risk evidence.
- Keep outputs deterministic enough for release evidence and regression checks.

## Non-goals

- Do not claim strategy profitability.
- Do not claim production readiness.
- Do not require real venue connectivity.

## Related files

- `crates/trading/` (TO_MODIFY): EMA smoke harness or strategy wiring.
- `crates/backtest/` (TO_MODIFY): deterministic entrypoint if needed.
- `docs/rust-cutover/evidence/V04-006.md` (CREATE): task evidence.

## Implementation guidance

The smoke should prove the product path works, not that the strategy is
optimal. Keep the run short, repeatable, and easy to diagnose.

## Verification criteria

- EMA smoke runs locally on Binance fixture replay.
- Order and risk outputs are deterministic enough for evidence.
- Smoke stays inside sandbox-only boundaries.

## Required commands

```bash
cargo test -p nautilus-backtest --lib
cargo test -p nautilus-live --lib
git diff --check
```

## Evidence required

Create `docs/rust-cutover/evidence/V04-006.md` with:

- task ID;
- plain Chinese summary;
- files changed;
- commands run;
- smoke result summary;
- behavior impact;
- public API impact;
- migration note status;
- rollback plan.
