# V091-006 Split Kill Switch Enabled And Active Semantics

Date: 2026-06-18
Executor: Codex
Owner role: Rust Core Runtime Agent
Review role: Verification & Release Gatekeeper
Risk level: medium
Status: BACKLOG
Depends on: V091-005

Owner override: on 2026-06-19, atxinbao approved automatic execution and
auto-merge for the remaining V091 queue items. The no-Binance-testnet-order,
no-real-funds, no-production-trading, and no-Dashboard-order-controls boundary
still applies.

## Goal

Separate risk kill-switch configuration from active kill-switch state.

## Scope

- Replace ambiguous `risk.kill_switch` semantics with explicit enabled and
  active fields.
- Write risk artifacts with `kill_switch_enabled` and `kill_switch_active`.
- Add `kill_switch_active` rejection reason only when active.

## Acceptance

- Config can express kill switch enabled but initially inactive.
- Risk artifacts no longer contradict config.
- Active kill switch state rejects intents with an explicit reason.
- v0.9.1 still submits no orders.
