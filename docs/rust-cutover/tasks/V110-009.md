# V110-009 Wire v0.11 Offline Release Gates

Date: 2026-06-20
Executor: Codex
Owner role: Verification & Release Gatekeeper
Review role: Control & Scope Agent
Risk level: medium
Status: BACKLOG
Depends on: V110-008

## Goal

Wire the v0.11 offline release gates into the local release verification script,
pull request smoke workflow, and tag-triggered release gate workflow.

Plain Chinese summary: 本任务只把 v0.11 的离线检查接进自动验证。它会检查账户快照
契约、Dashboard 只读 production shadow 状态、release 文档边界和禁用生产变更的计数。
它不需要生产凭证，不联网，不提交订单，也不发布 tag 或 GitHub Release。

## Scope

- Add a dedicated `scripts/ai/verify_v11_offline_release_gates.sh` script.
- Add a `v11-offline-release-gates` stage to `scripts/ai/verify_release.sh`.
- Run the v0.11 offline gate from pull request smoke in lightweight mode when
  v0.11 release gate files change.
- Add the v0.11 offline gate to the tag-triggered release workflow matrix.
- Record task evidence under `docs/rust-cutover/evidence/V110-009.md`.

## Non-Goals

- No production credentials.
- No production network connection.
- No production order submission.
- No production cancel, replace, amend, retry, or correction.
- No Dashboard order controls.
- No trading semantic change.
- No tag creation.
- No GitHub Release publication.

## Acceptance

- `scripts/ai/verify_v11_offline_release_gates.sh` passes locally.
- `scripts/ai/verify_release.sh v11-offline-release-gates` passes locally.
- `NTPRO_V11_LIGHTWEIGHT=1 scripts/ai/verify_v11_offline_release_gates.sh`
  passes locally for the PR-smoke mode.
- Workflow YAML parses for changed workflow files.
- `bash -n` passes for changed release verification scripts.
- `scripts/ai/verify_fast.sh` passes.
- `git diff --check` passes.
- Evidence records that the gate is offline-only and does not submit or mutate
  production orders.

## Automation Approval

The owner approved downgrading pending Shrimp tasks to automation-mergeable
medium risk on 2026-06-20 in the Codex thread. This task remains offline-only
and no-production-mutation.
