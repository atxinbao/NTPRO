# S3-CLS-001 - S3 Live 产品退出条件审计与治理收口

Date: 2026-08-14
Executor: Codex
GitHub issue: #1310
Risk: critical
Owner role: Control & Scope Agent
Review role: Verification & Release Gatekeeper
Status: AUDIT_IN_PROGRESS

## 目标

把 S3-LV-001 至 S3-LV-010 的合并事实、真实 Live 产品路径、异常恢复测试和禁止能力边界
收敛为可执行的 S3 退出合同。只有 source guard、GitHub exact scope 和独立审查全部通过，才
允许在本 PR 合并后关闭 milestone #39。

## 审计问题

1. 真实账户、生产 Venue、三角色审批、单笔订单与交易所回报是否形成受控端到端产品路径？
2. 断连、重复或倒退回报、部分成交、状态漂移、中断恢复和完整工作区回滚是否 fail closed？
3. Backtest/Demo 是否不能继承 Live 权限，连续下单、追加订单、改单、自动重试和自动恢复是否
   继续关闭？
4. 十个交付 Issue、对应 PR、task/evidence、Roadmap 与项目页面状态是否一致？

## 范围

- 新增 S3 closeout manifest、source guard 与负向自测；
- 注册 `verify_release.sh s3-live-closeout`；
- 收口 S3-LV-001、S3-LV-010、Roadmap 和 `project.html` 的陈旧状态；
- 新增 S3 closeout evidence；
- PR 合并并关闭 Issue #1310 后，执行 live-postmerge guard，再关闭 milestone #39。

## 强制边界

- 不修改 Rust、前端、OpenAPI、节点配置或交易行为；
- 不访问真实 Binance endpoint，不发送真实订单；
- 不新增连续下单、追加订单、改单、批量操作、自动重试、自动修复或自动恢复；
- 不修改 `docs/rust-cutover/release/v0_32_0_*`；
- 任何退出条件缺少源码或测试证据时保持 S3 milestone 开放并登记后续阻断任务。

## 完成条件

- exact issue/PR、能力证据、文档状态和禁止边界由 guard 可执行验证；
- guard 负向自测覆盖缺失/额外 scope、证据缺失、边界开启和 milestone 漂移；
- current-governance、backend-freeze-baseline、Rust-only 和风险 inventory 通过；
- 独立 Gatekeeper 对 final head 输出 `REVIEW_APPROVED`，hosted checks 全绿；
- 手动合并，Issue #1310 关闭，live-postmerge guard 通过，milestone #39 关闭。

## 产品结论边界

S3 收口证明产品代码具备独立授权的生产 Venue 单笔交易路径及 fail-closed 异常处理，不声明
本任务在真实资金账户上执行过订单。真实账户上线、额度和变更窗口仍由部署方独立授权。
