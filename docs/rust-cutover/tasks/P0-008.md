# P0-008 - LiveNode Startup Cancellation

Milestone: M2 - Node runtime and LiveNode cancellation
Priority: P0
Owner role: Rust Core Runtime Agent
Review role: Verification Release Gatekeeper
Risk: high

## Goal

Make LiveNode execution client connection startup cancellable and timeout-bound like data client startup.

## Dependencies

- `P0-007`

## Scope

- Keep v0.2.0 scoped to Local Multi-Node Runtime Foundation.
- Preserve sandbox/local-only boundaries unless this task explicitly says otherwise.
- Create task evidence under `docs/rust-cutover/evidence/`.

## Non-goals

- Do not create release tags or publish GitHub Releases from this task.
- Do not claim production live trading readiness.
- Do not connect to real exchanges or submit real orders.
- Do not add manual order entry, Dashboard UI scope, or distributed multi-server orchestration unless the task explicitly asks for it.
- Do not introduce Python, PyO3, or Cython product surfaces.

## Related files

- `crates/live/src/node.rs` (TO_MODIFY): Execution client startup cancellation
- `docs/rust-cutover/evidence/P0-008.md` (CREATE): Task evidence

## Implementation guidance

Wrap execution client connection with the same startup cancellation/timeout mechanism used for data clients. Stop/shutdown/timeout must prevent entering Running. High-risk task: stop at REVIEW_REQUIRED.

High-risk task: stop at `REVIEW_REQUIRED` before merge and do not enable auto-merge.

## Verification criteria

- `cargo test -p nautilus-live`
- `cargo check -p nautilus-live`
- `scripts/ai/verify_fast.sh`

Evidence must record changed files, commands run, command output summary, behavior impact, public API impact, migration note status, rollback plan, and a plain Chinese non-expert summary.

## Evidence required

Create `docs/rust-cutover/evidence/P0-008.md`.
