# FEF-001 - 策略工作台 React/TypeScript/Vite 工程底座

Date: 2026-08-05
Executor: Codex
GitHub issue: #1253
Risk: medium
Owner role: Rust Product Surface Agent
Review role: Verification & Release Gatekeeper
Status: REVIEW_REQUIRED

## 依赖

FEA-001 / Issue #1251 已通过 PR #1252 合并。

## 目标

在 `apps/strategy-workbench/` 建立正式前端工程，将 SWB-001 页面框架组件化，并建立
类型检查、单测、生产构建、浏览器 smoke 和按路径执行的前端 CI。

## 范围

- React、TypeScript、Vite、TanStack Router、TanStack Query、Lucide 和 CSS Modules；
- 左侧导航、顶部上下文、主画布、右侧详情栏、底部 Dock 和状态栏组件；
- 总览与系统状态基础路由；
- `GET /api/mvp/v1/status` 类型化、运行时验证和 fail-closed 请求；
- Vitest、Testing Library、MSW 与 Playwright；
- npm audit、format、lint、typecheck、unit、build 与 e2e gate；
- CI `frontend_app` 路径分类；
- PR 临时 merge checkout 的 v0.33.0 后发布基线绑定和被跳过证据上传治理；
- 安全审计分类使用 depth-2 PR merge 父提交，避免全历史 checkout 超时；
- 更新前端架构，将 Axum 集成拆为后续 FEI-001。

## 非目标

- 不修改 Rust/Axum runtime 或现有 `/strategy-workbench`；
- 不提交 `dist/`、`node_modules/`、coverage 或浏览器缓存；
- 不新增 Strategy、StrategyVersion 或 Run 产品 API；
- 不建设 Backtest、Demo 或 Live 写操作；
- 不开放 submit、mutation、adapter send、live exchange、retry、remediation 或交易控件；
- 不修改 `docs/rust-cutover/release/v0_32_0_*`。

## 验收

```bash
cd apps/strategy-workbench
npm ci
npm run audit
npm run format:check
npm run lint
npm run typecheck
npm run test
npm run build
npm run test:e2e

cd ../..
scripts/ci/test-ci-change-classifier.sh
scripts/ai/check_docs_examples_governance.sh
scripts/ai/check_rust_only_runtime.sh
scripts/ai/check_mvp_freeze_baseline.sh
git diff --check
```

1440x1000 与 390x844 必须无页面级横向溢出、空白主体或重叠。合同异常必须清空旧身份；
Live 与未交付栏目必须禁用。

## 完成条件

风险为 medium，PR 必须完成 Verification & Release Gatekeeper 审查并通过 hosted checks
后合并。合并后创建 FEI-001 与 S0-API-001，二者可并行。
