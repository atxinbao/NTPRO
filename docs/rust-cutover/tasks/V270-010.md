# V270-010 release governance golden trace category support

Date: 2026-07-08
Executor: Codex
GitHub issue: `#885`
Milestone: `v0.27.0`
Risk: medium
Owner role: Verification & Release Gatekeeper
Review role: Rust Product Surface Agent
Auto-merge: allowed after local validation and hosted checks pass

## Goal

Allow the full golden trace file validator to accept V270 release governance
trace rows by adding `release_governance` to the golden trace category enum.

Plain Chinese summary: 本任务修复 v0.27.0 tag-gate 暴露的
`full-golden-traces-files` 失败。V270 release gate trace 使用
`category=release_governance` 是发布治理语义，但通用 golden trace schema 仍只允许
market/order/risk/read_model 等旧类别。本任务同步更新 Python runner 和 Rust schema
test 的 category 枚举，并把 V270 scope 纳入 #885；不改变运行时行为，也不新增任何交易
能力。

## Dependency

- Depends on V270-008 and V270-009.
- Blocks repushing the `ntpro-rust-only-v0.27.0` tag and publishing the public
  GitHub Release.
- Based on cancelled release gate run `28919612892`, where
  `verify-release (full-golden-traces-files)` failed.

## Scope

Included:

- Add `release_governance` to `scripts/ai/golden_trace_runner.py`.
- Add `release_governance` to `crates/testkit/tests/golden_trace_schema.rs`.
- Add V270-010 to v0.27.0 release notes, readiness, manifest, issue/evidence
  counts, and gate scripts.
- Update V270 release governance trace counts from 10 to 11.

Not included:

- No v0.27.0 tag push in this PR.
- No GitHub Release publication in this PR.
- No product runtime change.
- No production order submission or mutation.
- No execution adapter send or live exchange request.
- No retry scheduler.
- No automatic remediation runtime.
- No Dashboard/Admin trading controls.
- No product-grade live trading terminal claim.

## Acceptance Criteria

- `scripts/ai/verify_full.sh golden-traces-files` passes.
- `python3 scripts/ai/validate_golden_trace_release_scope.py` passes.
- `scripts/ai/verify_release.sh v27-release-gates` passes in PR mode with
  `#885` open.
- `scripts/ai/verify_release.sh v27-strict-provenance` passes in PR mode.
- Hosted PR smoke passes before merge.

## Validation

Required:

```text
python3 -m py_compile scripts/ai/golden_trace_runner.py
bash -n scripts/ai/check_github_release_published.sh scripts/ai/verify_v27_release_gates.sh scripts/ai/verify_v27_strict_provenance.sh
python3 -m json.tool docs/rust-cutover/release/v0_27_0_release_manifest.json >/dev/null
python3 -c 'import json,pathlib; [json.loads(line) for line in pathlib.Path("tests/golden/v270_release_gates_strict_provenance.jsonl").read_text().splitlines() if line.strip()]'
scripts/ai/verify_full.sh golden-traces-files
cargo test -p nautilus-testkit --test golden_trace_schema
scripts/ai/verify_release.sh v27-release-gates
scripts/ai/verify_release.sh v27-strict-provenance
python3 scripts/ai/validate_golden_trace_release_scope.py
scripts/ai/verify_fast.sh
git diff --check
```
