# V281-002 stale V280-009 evidence cleanup

Date: 2026-07-09
Executor: Codex
GitHub issue: `#920`
Milestone: `v0.28.1`
Risk: low
Owner role: Verification & Release Gatekeeper
Review role: Control & Scope Agent
Auto-merge: allowed after local validation and hosted checks pass

## Goal

Clean stale pre-publication semantics from V280-009 evidence after the formal
`v0.28.0` publication.

Plain Chinese summary: 本任务清理 `V280-009` 里还停留在发布前状态的证据词，例如
missing local tag、offline publication guard、`current_issue_state=OPEN`、
`tag_exists=false` 和 `source_dirty=true`。清理后，V280-009 证据必须反映
`ntpro-rust-only-v0.28.0` 已公开发布、#902 已关闭、v0.28.0 milestone 已关闭、release
body 与源码 release notes 一致，并且 strict provenance 会拒绝这些 stale marker。

## Scope

- Update `docs/rust-cutover/evidence/V280-009.md` from local validation state
  to published closeout state.
- Add `V281-002` task and evidence documents.
- Add stale marker checks to `scripts/ai/verify_v28_strict_provenance.sh`.
- Keep the task limited to release evidence wording and provenance guardrails.

## Non-Goals

- No v0.28.0 tag rewrite.
- No GitHub Release republish.
- No runtime behavior change.
- No production order submission or mutation.
- No execution adapter send or live exchange request.
- No retry scheduler.
- No automatic remediation runtime.
- No Dashboard/Admin/Trader Terminal trading controls.
- No product-grade live trading terminal claim.

## Required Evidence

- `bash -n scripts/ai/verify_v28_strict_provenance.sh` = PASS
- `scripts/ai/verify_release.sh v28-strict-provenance` = PASS
- `scripts/ai/verify_release.sh v28-release-gates` = PASS
- `NTPRO_CURRENT_RELEASE_VERSION=v0.28.0 NTPRO_CURRENT_RELEASE_TAG=ntpro-rust-only-v0.28.0 NTPRO_CURRENT_RELEASE_NAME="NTPRO Rust-only v0.28.0" NTPRO_RELEASE_PUBLICATION_STRICT_BODY=1 scripts/ai/verify_release.sh release-publication-guard` = PASS
- `scripts/ai/verify_fast.sh` = PASS
- `git diff --check` = PASS

## Done Criteria

- `V280-009.md` records published closeout facts rather than pending or
  pre-publication proof.
- `V280-009.md` no longer contains stale final-proof markers:
  `missing_local_git_tag`, `offline_skip`, `current_issue_state=OPEN`,
  `tag_exists=false`, or `source_dirty=true`.
- Strict provenance fails if those stale markers return to V280-009 evidence.
- The boundary remains Backend Closure / Product Operations Runtime
  Finalization: no new submit, no production mutation, no adapter send, no live
  exchange request, no retry scheduler, no automatic remediation, no
  Dashboard/Admin/Trader Terminal trading controls, and no product-grade live
  trading terminal claim.
