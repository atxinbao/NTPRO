# V230-002 - multi-account runtime identity and read-model partitioning

Date: 2026-07-03
Executor: Codex
GitHub issue: `#713`
Milestone: `v0.23.0`
Risk: medium
Owner role: Rust Core Runtime Agent
Review role: Verification & Release Gatekeeper
Auto-merge: allowed after local validation and hosted checks pass

## Goal

Implement multi-account identity and read-model partitioning under the v0.23.0
isolation contract.

Plain Chinese summary: 本任务把多账户隔离先落到 Rust read-model executable replay
层。新增 `account_key` 和 `isolation_scope_key` 的可执行 fixture，证明两个账户不会
混读，跨账户 component mismatch 会 fail closed，unknown account identity 也会 fail
closed。本任务不新增实盘 submit，不做策略编排，不做 venue node lifecycle，也不新增
Dashboard 操作控件。

## Contract Traceability

This task satisfies the V230-001 contract sections required for #713:

```text
Identity Model
Account Boundary
Allowed Read Paths
Logs And Evidence Boundary
```

## Scope

Included:

- add V230 account partition golden trace fixtures;
- add executable Rust projection replay for account partition identity;
- validate `account_key` and `isolation_scope_key` presence;
- fail closed on cross-account component mismatch;
- fail closed on unknown account identity;
- keep Dashboard operation controls and production submit disabled.

Not included:

- strategy-level orchestration;
- venue node lifecycle implementation;
- live order submission expansion;
- dashboard implementation;
- production order mutation or operation controls.

## Acceptance Criteria

- `tests/golden/read_model_account_partition_schema.jsonl` includes isolated,
  cross-account mismatch, and unknown account identity cases.
- `crates/cli/tests/golden_trace_read_model_projection.rs` replays the V230
  account partition cases through Rust projection logic.
- `docs/rust-cutover/golden_trace/RELEASE_REPLAY_SCOPE.json` records the V230
  cases as executable replay.
- Evidence maps #713 to the V230-001 contract and records local validation.
- No submit, order mutation, strategy orchestration, venue lifecycle, or
  Dashboard operation-control behavior is added.

## Validation

Required:

```text
cargo test -p nautilus-cli --test golden_trace_read_model_projection -- --nocapture
jq . docs/rust-cutover/golden_trace/RELEASE_REPLAY_SCOPE.json
rg -n "read_model.account_partition|account_key|isolation_scope_key|cross_account_component_mismatch|missing_account_key|rust_cli_read_model_projection_replays_v230_account_partition_paths" tests/golden/read_model_account_partition_schema.jsonl crates/cli/tests/golden_trace_read_model_projection.rs docs/rust-cutover/golden_trace/RELEASE_REPLAY_SCOPE.json docs/rust-cutover/evidence/V230-002.md
scripts/ai/verify_fast.sh
git diff --check
```
