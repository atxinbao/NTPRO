# V150-010 v0.15 Readiness and Release Notes

Date: 2026-06-22
Executor: Codex
Risk: medium
Auto-merge: allowed

## Goal

Prepare the v0.15 readiness report and release notes for the Guarded Live Alpha
Mutation Scope + Execution Dry-Run Harness release line.

Plain Chinese summary: 这个任务只做 v0.15 的发布材料收口，不新增交易能力。文档必须把
“能说什么”和“不能说什么”讲清楚：v0.15 只能做生产 mutation 请求预览、人工审批、
dry-run 执行隔离、kill switch、事故证据和 Dashboard 只读查看，不能真实下单或改单。

## Scope

Allowed:

- create `docs/rust-cutover/release/v0_15_0_readiness_report.md`;
- create `docs/rust-cutover/release/v0_15_0_release_notes.md`;
- update `docs/rust-cutover/release/README.md`;
- record V150-010 evidence.

Not allowed:

- production order submission;
- production cancel, replace, amend, retry, correction, flatten, or remediation;
- production request sending;
- production execution adapter calls;
- network access by default automation;
- listenKey lifecycle;
- real funds;
- production trading;
- Dashboard order controls;
- runtime trading semantic changes.

## Acceptance

The task is done when:

- readiness and release notes describe v0.15 as Guarded Live Alpha Mutation
  Scope + Execution Dry-Run Harness only;
- V150-000 through V150-010 are accounted for;
- v0.15 non-goals are explicit in release-facing material;
- release index links the new documents;
- `scripts/ai/verify_release.sh v15-release-gates` passes locally;
- `scripts/ai/verify_release.sh release-surface-current-guard` passes locally;
- `scripts/ai/verify_fast.sh` and `git diff --check` pass locally;
- release evidence is recorded under `docs/rust-cutover/evidence/V150-010.md`.
