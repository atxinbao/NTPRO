# BFH-007 Repository Hygiene Guard And Phase-1 Closeout

Date: 2026-07-19
Executor: Codex
GitHub issue: #1118
Milestone: post-freeze-backend-hygiene
Risk: medium
Classification: baseline-preserving-governance
Status: READY FOR PR

## Goal

Fail closed on drift from BFH-002 through BFH-006 and establish a
source-controlled plus live-GitHub phase-1 closeout contract.

Plain Chinese summary: 新增 Rust hygiene guard，阻止生成物进入 Git、失效配置或贡献
入口回流、Make 清理范围扩大，以及大 fixture owner/hash/重建策略漂移。该任务只完成
仓库治理收口，不发布 v0.32.1，不修改后端运行能力。

## Dependencies

BFH-002 through BFH-006 (`#1113-#1117`) are closed through merged PRs
`#1128-#1132`.

## Scope

- add the `ntpro-governance backend-hygiene` command and shell entry;
- validate tracked generated-output and Finder cache hygiene;
- validate retired configuration and current contributor routes;
- validate guarded Make cleanup allowlists and protected local state;
- validate the large fixture inventory, hashes, Git state, metadata, checksum,
  and reconstruction authority;
- add fail-closed negative mutations;
- wire the guard into current governance and pull-request smoke;
- record the exact source plus live GitHub closeout contract.

## Not In Scope

- runtime, adapter, fixture-content, dependency, schema, or public API change;
- deleting retained audit history or quarantined fixtures;
- editing `docs/rust-cutover/release/v0_32_0_*`;
- publishing a backend patch or inheriting any forbidden capability.

## Acceptance Criteria

- the hygiene guard and ten negative cases pass;
- all 17 fixture entries remain authoritative and all 14 tracked hashes pass;
- current governance and backend freeze pass;
- hosted smoke passes;
- the BFH issue/PR/run mapping is reconstructable;
- after merge, issue #1118 and milestone #34 are closed with zero open issues.
