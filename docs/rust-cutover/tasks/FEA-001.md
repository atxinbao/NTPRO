# FEA-001 - 策略工作台前端产品架构与实施规范

Date: 2026-08-05
Executor: Codex
GitHub issue: #1251
Risk: low
Owner role: Rust Product Surface Agent
Review role: Verification & Release Gatekeeper
Status: DONE_ON_MERGE

## 目标

将 SWB-001 页面框架之后的前端产品架构写入仓库，固定技术栈、产品资源、页面路由、
Rust/Axum 集成、测试策略和后续任务依赖。

## 范围

- 新增 `docs/architecture/strategy_workbench_frontend_architecture.md`；
- 明确 React、TypeScript、Vite、React Router 和 TanStack Query 的职责；
- 明确 Node.js 仅参与前端构建，生产运行时继续为 Rust/Axum；
- 固定 Strategy、不可变 StrategyVersion 与 Run 三层产品资源；
- 固定首个纵向切片和 FEA-001 至 S4 的依赖顺序；
- 更新产品 Roadmap；
- 新增任务和验证证据。

## 非目标

- 不新增前端源码、`package.json` 或 Node.js workflow；
- 不修改 Rust runtime、Axum 路由或 API 行为；
- 不建设任何栏目内容或写操作；
- 不开放 submit、mutation、adapter send、live exchange、retry、remediation 或交易控件；
- 不修改 `docs/rust-cutover/release/v0_32_0_*`。

## 验收

- 文档全部使用中文并区分当前事实与未来目标；
- 技术栈、目录、构建、部署、路由、API、状态和测试策略完整；
- 首个页面切片及前置依赖明确；
- `scripts/ai/check_docs_examples_governance.sh` 通过；
- `scripts/ai/check_rust_only_runtime.sh` 通过；
- `scripts/ai/check_mvp_freeze_baseline.sh` 通过；
- `git diff --check` 通过。

## 完成条件

PR 合并并关闭 Issue #1251 后，创建 `FEF-001` 前端工程底座任务。旧 Rust 内嵌页面在
新应用达到同等验收前继续保留。
