# V02-002 - Node and Supervisor product contract

Milestone: v0.2 Local Multi-Node Runtime Foundation
Priority: P0
Owner role: Control & Scope Agent
Review role: Rust Product Surface Agent
Risk: medium

## Goal

Define the product contract for ntpro-node, ntpro-supervisor, node IDs, local file layout, lifecycle commands, config ownership, and supported sandbox-only first release behavior.

## Dependencies

- `V02-001`

## Scope

- Keep v0.2 scoped to Local Multi-Node Runtime Foundation.
- Use sandbox, mock, or fixture evidence by default.
- Preserve DRG-010 boundaries and do not claim release readiness from this task alone.

## Non-goals

- Do not start Dashboard UI implementation from this task.
- Do not add production real-exchange connectivity from this task.
- Do not implement manual order entry from this task.
- Do not create release tags or publish GitHub Releases.
- Do not claim v0.2 release readiness unless this is V02-010 and all required evidence passes.

## Related files

- `docs/architecture/node_lifecycle_state_machine.md` (REFERENCE): Existing node lifecycle contract
- `docs/architecture/control_api_contract.md` (REFERENCE): Existing future control action contract
- `docs/architecture/local_multi_node_runtime_contract.md` (CREATE): Node/supervisor product contract
- `docs/rust-cutover/evidence/V02-002.md` (CREATE): Task evidence

## Implementation guidance

Follow AGENTS.md, docs/rust-cutover/CONTRACT.md, docs/rust-cutover/DEFINITION_OF_DONE.md, docs/rust-cutover/TASK_EXECUTION.md, and docs/rust-cutover/AGENT_ROLES.md. Do not start Dashboard UI work, real exchange connectivity, VWAP/POV productization, Nexus-like communication, manual order entry, or distributed multi-server deployment in this v0.2 queue. Use sandbox/mock/fixture evidence by default. Tasks above medium risk must stop for review according to repo rules.

Define node as an independent trading runtime instance and supervisor as the local manager. Specify one node = one independent process target for product architecture, even if early tests use local-only sandbox paths. Define file layout for configs, pid/status, logs, metrics, and run artifacts. Keep order entry and real venue connectivity out of scope.

## Verification criteria

Contract explains ntpro-node, ntpro-supervisor, node_id, config path, process model, lifecycle command boundary, artifact layout, and explicit non-goals. Run scripts/ai/verify_fast.sh and git diff --check on changed docs.

Evidence must record changed files, commands run, command output summary, behavior impact, public API impact, migration note status, rollback plan, and a plain Chinese non-expert summary.

## Evidence required

Create `docs/rust-cutover/evidence/V02-002.md` with changed files, commands, validation results, behavior impact, public API impact, migration note status, rollback plan, and a plain Chinese summary.
