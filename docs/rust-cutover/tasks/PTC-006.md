# PTC-006 Historical Release Executable Gate Retirement

Date: 2026-07-16
Executor: Codex
GitHub issue: #1101
Risk: high
Required terminal state: `REVIEW_REQUIRED`

## Goal

Retire historical release verification implementations from the current source
tree while preserving deterministic recovery, immutable release history, and a
small current-only Rust verification authority.

## Scope

- inventory and remove all 262 `scripts/ai/verify_v*.sh` files and
  `scripts/ai/verify_release_strict.sh`;
- record source commit, Git blob, SHA-256, size, last change, and restore command
  for every retired executable;
- replace historical workflow matrices with current Rust, golden trace,
  publication, security, and backend-freeze gates;
- add a Rust guard that proves every retired blob is recoverable and absent;
- keep release tags, GitHub Releases, hosted runs, Rust tests, golden traces,
  and historical documentation intact.

## Prohibited

- modifying `docs/rust-cutover/release/v0_32_0_*`;
- deleting or rewriting historical `docs/rust-cutover/` evidence;
- changing runtime, public API, trading semantics, adapters, or backend
  capability boundaries;
- deleting Python packaging or the local `.venv` owned by PTC-007;
- auto-merging this high-risk PR.

## Acceptance

- exactly 263 retired executables are absent from the source tree;
- all 263 Git blobs are reconstructable from the recorded source commit and
  match their recorded SHA-256;
- all 49 immutable backend release tags remain available;
- current Make, workflow, action, and supported release authority has no caller
  for a retired executable;
- current smoke and release gates remain executable and fail closed;
- PR status is `REVIEW_REQUIRED` after local and hosted validation.
