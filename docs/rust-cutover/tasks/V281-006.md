# V281-006 release-publish-after-gate current-release binding

Date: 2026-07-09
Executor: Codex
GitHub issue: `#924`
Milestone: `v0.28.1`
Risk: low
Owner role: Verification & Release Gatekeeper
Review role: Control & Scope Agent
Auto-merge: allowed after local validation and hosted checks pass

## Goal

Bind `release-publish-after-gate` checks to the current release instead of only
proving historical fixture and dry-run cases.

Plain Chinese summary: 本任务让 `release-publish-after-gate` 先证明当前 v0.28.0
release 的 tag、tag SHA、hosted gate run 和 published_at 顺序，再运行历史 fixture/dry-run
自测。历史 fixture 只能作为负向覆盖，不能替代当前 release proof。

## Scope

- Validate current-release publication ordering from source-controlled manifest
  and release closeout evidence.
- Run a live dry-run cross-check when local tag and authenticated GitHub access
  are available.
- Keep historical v0.22.1 fixture cases as negative coverage.
- Add a v0.28.1 release stage alias for current-release binding.
- Preserve `source_tree_plus_github_remote` publication evidence strategy.

## Non-Goals

- No GitHub Release republish.
- No release tag rewrite.
- No runtime behavior change.
- No production order submission or mutation.
- No execution adapter send or live exchange request.
- No retry scheduler.
- No automatic remediation runtime.
- No Dashboard/Admin/Trader Terminal trading controls.
- No product-grade live trading terminal claim.

## Required Evidence

- `bash -n scripts/ai/verify_release_publish_after_gate.sh scripts/ai/verify_release.sh` = PASS
- `python3 -m json.tool docs/rust-cutover/release/v0_28_0_release_manifest.json` = PASS
- `scripts/ai/verify_release.sh release-publish-after-gate` = PASS
- `scripts/ai/verify_release.sh v28.1-release-publish-after-gate-current-binding` = PASS
- `scripts/ai/verify_release.sh v28-release-gates` = PASS
- `scripts/ai/verify_release.sh v28-strict-provenance` = PASS
- `scripts/ai/verify_fast.sh` = PASS
- `git diff --check` = PASS

## Done Criteria

- `release-publish-after-gate` fails closed when current release closeout proof
  is missing, stale, fixture-only, or tag-sha mismatched.
- v0.28.0 current release proof binds tag SHA
  `41ef23417a4f21226cbc069de8cc31d0fa5e696e`, hosted gate run `28969059200`,
  and public release publication timestamp `2026-07-08T20:39:19Z`.
- Historical fixture/dry-run cases remain present but cannot substitute for
  current-release proof.
- v0.29.0 can inherit current-release publication proof instead of a historical
  fixture-only gate.
