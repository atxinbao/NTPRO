# V290-010 v29 release gates and v30 go-live candidate handoff

Date: 2026-07-10
Executor: Codex
GitHub issue: `#936`
Milestone: `v0.29.0`
Risk: medium
Owner role: Verification & Release Gatekeeper
Review role: Control & Scope Agent
Auto-merge: allowed after local validation and hosted checks pass
Status: READY_FOR_REVIEW

## Goal

Create v0.29.0 release gates, strict provenance, release notes, readiness
report, manifest, and the v0.30.0 backend production go-live candidate handoff.

Plain Chinese summary: 本任务把 v0.29.0 Backend Production Readiness Foundation
收口为可发布版本。它要求 V290-000 到 V290-009 的证据都已存在，并把 #926-#936
作为原始 milestone issue scope。后续 V290-011 注册为 hosted release gate corrective
scope，不改变本任务的 release/handoff 边界。v0.29.0 只声明 backend production readiness
foundation 完成，不声明 backend go-live，也不开放 submit、order mutation、adapter
send、live exchange request、retry scheduler、automatic remediation、
Dashboard/Admin/Trader Terminal trading controls 或产品级实盘交易终端。

## Dependency

- Depends on `V290-000` through `V290-009`.
- Depends on the published `ntpro-rust-only-v0.28.1` release evidence.
- Builds the handoff for the later `v0.30.0` backend production go-live
  candidate track.

## Scope

Included:

- Add v0.29.0 release notes, readiness report, release manifest, and release
  surface updates.
- Add `v29-release-gates` and `v29-strict-provenance` release stages.
- Require exact V290 milestone/manifest/readiness/provenance issue scope
  matching.
- Promote `v29_release_gates_v30_handoff` from deferred to production-ready
  readiness evidence in the v29 matrix.
- Define v0.30.0 as the next backend production go-live candidate track without
  inherited default trading controls.

Not included:

- backend go-live;
- product-grade live trading terminal readiness;
- submit/order mutation/cancel/replace/amend/flatten;
- adapter send or live exchange request;
- retry scheduler or automatic remediation;
- Dashboard/Admin/Trader Terminal operation or trading controls.

## Validation

Required:

```text
bash -n scripts/ai/verify_v29_release_gates.sh scripts/ai/verify_v29_strict_provenance.sh scripts/ai/verify_v29_backend_production_readiness_boundary_contract.sh scripts/ai/verify_release.sh
python3 -m json.tool docs/rust-cutover/release/v0_29_0_release_manifest.json >/dev/null
python3 -m json.tool docs/rust-cutover/release/v0_29_0_backend_production_readiness_matrix.json >/dev/null
scripts/ai/verify_release.sh v29-release-gates
scripts/ai/verify_release.sh v29-strict-provenance
scripts/ai/verify_release.sh v29-backend-production-readiness-boundary-contract
scripts/ai/verify_release.sh v29-intake-gate
scripts/ai/verify_fast.sh
git diff --check
```
