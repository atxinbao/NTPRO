# BFG-004 generated artifact and local audit output hygiene

Date: 2026-07-15
Executor: Codex
GitHub issue: #1071
Milestone: backend-freeze-governance
Status: READY FOR PR

## Goal

Keep post-baseline verification and analysis outputs out of the Git worktree
while preserving reconstructable release evidence.

Plain Chinese summary: 本任务把 release publication snapshot、Graphify 输出和
strict-provenance target 文件明确归类为可重建本地生成物。它们被忽略、不提交，也不
作为唯一发布证明；正式证据仍是 source tree 加 GitHub remote。

## Dependency

Depends on BFG-001 / #1068, closed through merged PR #1074.

## Scope

- ignore root `release-publication-evidence/` and `graphify-out/` outputs;
- document existing `target/ntpro-v*` ignore coverage;
- define tracked, remote-reconstructable, and ephemeral-local classifications;
- prove no generated artifact is tracked or left as undeclared worktree noise.

## Non-Goals

- deleting local user-generated files;
- deleting tracked historical evidence;
- changing release, runtime, trading, or public API behavior.

## Acceptance Criteria

- all declared ephemeral paths resolve to ignore rules;
- none of the declared paths contain tracked files;
- related verification leaves no undeclared untracked output;
- `source_tree_plus_github_remote` remains the audit authority.

## Result

Local validation passed. Declared generated paths are ignored, none are
tracked, and publication/freeze/strict-provenance/fast verification leaves the
worktree status unchanged.
