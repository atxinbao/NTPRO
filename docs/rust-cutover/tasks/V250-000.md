# V250-000 v0.25.0 intake gate and v0.24.1 dependency proof

Date: 2026-07-05
Executor: Codex
Task: `V250-000` / GitHub issue `#777`
Milestone: `v0.25.0`
Status: LOCAL VALIDATION PASSED

## Goal

Establish the v0.25.0 intake gate by proving that the v0.24.1 patch closeout
dependency is complete before any monitoring, incident, or disaster-recovery
foundation implementation starts.

Plain Chinese summary: 本任务只做 v0.25.0 入口门禁。它确认 V241 全部 issue 已关闭，
v0.24.1 tag、hosted release gate、GitHub Release、publication evidence 和 strict
provenance 都可重建；之后 v0.25.0 才能以 gated implementation 方式进入监控、
incident 和灾备基础能力任务。

## Scope

- Verify GitHub issues `#770` through `#776` are closed.
- Verify milestone `v0.24.1` is closed with zero open issues.
- Verify `ntpro-rust-only-v0.24.1` points at `origin/main`.
- Verify the v0.24.1 hosted `Rust Cutover Release Gate` completed successfully.
- Verify the v0.24.1 GitHub Release is public, non-draft, non-prerelease, and
  was published after the hosted release gate.
- Verify v24 replay classification remains explicit: validator executable
  replay is recorded, schema-only scoped v240 rows are zero, and runtime adapter
  integration is still false.
- Record the v0.25.0 start gate as satisfied for future gated tasks only.

## Non-Goals

- Do not implement submit, cancel, retry, replace, amend, flatten, or
  order-ticket controls.
- Do not add production order mutation.
- Do not add adapter send or live exchange request behavior.
- Do not add a retry scheduler.
- Do not open Dashboard trading operation controls.
- Do not claim product-grade live trading terminal readiness.

## Required Evidence

- `bash -n scripts/ai/verify_release.sh scripts/ai/verify_v25_intake_gate.sh`
- `scripts/ai/verify_release.sh v25-intake-gate`
- `NTPRO_V241_RELEASE_REQUIRE_PUBLICATION=1 scripts/ai/verify_release.sh v24.1-release-gates`
- `NTPRO_V241_STRICT_REQUIRE_PUBLICATION=1 scripts/ai/verify_release.sh v24.1-strict-provenance`
- `scripts/ai/verify_fast.sh`
- `git diff --check`

## Done Criteria

- Missing any closed V241 issue fails closed.
- Missing v0.24.1 Release, hosted release gate, tag/main match, strict
  provenance, or publication evidence fails closed.
- Missing explicit false boundary for submit, production order mutation,
  adapter send, live exchange request, retry scheduler, Dashboard operation
  controls, or product-grade terminal claim fails closed.
- `docs/rust-cutover/release/v0_25_0_intake_gate.md` records
  `start_gate_status = satisfied`.
- v0.25.0 remains a new gated monitoring / incident / disaster-recovery track
  and does not inherit order operation controls from v0.24.1.
