# SWB-006 - 左侧策略导航与独立策略页面

Date: 2026-08-16
Executor: Codex
GitHub issue: #1319
Risk: high
Owner role: Rust Product Surface Agent
Review role: Verification & Release Gatekeeper
Status: LOCAL_VALIDATION_PASSED / REVIEW_REQUIRED

## 目标

把策略工作台左侧“策略”从禁用占位改为正式导航，并新增独立只读策略页面。用户可以
从任意工作台页面进入策略产品信息，不再依赖总览页间接选择。

## 范围

- 新增 `/strategies` 路由和策略页面；
- 复用 `useOverviewProductContext` 读取策略、版本和 Run 摘要；
- 展示策略身份、不可变版本、固定参数、数据要求和三种运行模式状态；
- 增加导航点击、直接路由、空列表和运行源降级回归；
- 增加 production bundle 浏览器导航证据。

## 非目标

- 不新增或修改 Product API；
- 不开放策略创建、编辑、删除或其他 mutation；
- 不修改 v0.32.0 冻结发布文件；
- 不改变真实交易、订单、重试或自动补救边界。

## 验收

```bash
cd apps/strategy-workbench
npm run format:check
npm run lint
npm run typecheck
npm run test
npm run build

cd ../..
NTPRO_PLAYWRIGHT_CORE_PATH=apps/strategy-workbench/node_modules/playwright-core \
NTPRO_BROWSER_EVIDENCE_DIR=/tmp/ntpro-swb006-browser-evidence \
  node scripts/ai/test_strategy_workbench_browser.mjs
scripts/ai/check_mvp_freeze_baseline.sh
scripts/ai/check_rust_only_runtime.sh
git diff --check
```

## 完成条件

本任务为 high risk。必须取得独立审查批准并通过 hosted checks，随后手动合并；不得启用
auto-merge。PR 合并并关闭 Issue #1319 后才是 DONE。
