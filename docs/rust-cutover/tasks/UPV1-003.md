# UPV1-003 - Backtest 产品验收

Date: 2026-08-26
Executor: Codex
GitHub issue: #1324
Risk: high
Owner role: Strategy Workbench Agent
Review role: Verification & Release Gatekeeper
Status: REVIEW_REQUIRED

## 目标

让不了解 Cargo、Rust 和内部工件目录的用户，从全新本地工作区只通过 production 页面完成
Backtest 创建、结果查看、比较、确定性复现和服务重启后的历史恢复。

## 范围

- 统一 Backtest 创建、Run 深链和比较页的加载、失败、取消与恢复语言；
- 查询失败继续 fail closed，只允许用户显式重新加载；
- Backtest 创建与确定性复现不自动重试，不把客户端操作伪装成服务端可中途取消；
- 使用真实 Rust Product API 与 production frontend bundle 验证创建、结果、明细和分析；
- 验证页面刷新、直接 URL、比较、复现以及同一 workspace 服务重启后的历史读取；
- 更新 Usable Product v1 Roadmap 和项目说明页状态。

## 非目标

- 不新增策略算法、参数优化或收益声明；
- 不扩展 Demo 或 Live 产品能力；
- 不修改 Product API schema 或 HTTP 路由；
- 不开放真实订单、自动重试、自动补救或交易控件；
- 不修改 v0.32.0 冻结发布文件。

## 验收

```bash
npm --prefix apps/strategy-workbench run format:check
npm --prefix apps/strategy-workbench run lint
npm --prefix apps/strategy-workbench run typecheck
npm --prefix apps/strategy-workbench test -- --run
npm --prefix apps/strategy-workbench run build

NTPRO_PLAYWRIGHT_CORE_PATH=<playwright-core> \
NTPRO_BROWSER_EVIDENCE_DIR=/tmp/ntpro-upv1-003-browser-evidence-final-3 \
  node scripts/ai/test_strategy_workbench_browser.mjs

scripts/ai/check_mvp_freeze_baseline.sh
scripts/ai/check_rust_only_runtime.sh
scripts/ai/verify_fast.sh
git diff --check
```

## 完成条件

本任务为 high risk。必须取得独立审查批准并通过 hosted checks，随后手动合并；不得启用
auto-merge。PR 合并并关闭 Issue #1324 后才是 DONE，并且只有此时才能解锁 UPV1-004。
