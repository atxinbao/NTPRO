# V261-003 stale V260 evidence cleanup

Date: 2026-07-07
Executor: Codex
GitHub issue: `#849`
Milestone: `v0.26.1`
Status: LOCAL VALIDATION PASSED

## Goal

Clean stale pre-final wording from V260 release evidence so the source tree
reflects the actual published `v0.26.0` release state after hosted gate run
`28853960135` and publish run `28858791493`.

Plain Chinese summary: 本任务清理 `v0.26.0` 证据里残留的预发布措辞，例如 open
issue、scope=9、hosted gate pending/offline 等旧状态。清理后的证据必须指向最终
发布事实：gate run `28853960135` 成功、publish run `28858791493` 成功、最终 scope
为 14，corrective scope 为 5。

## Scope

- Replace `V260-008` stale local/open/scope-9 validation wording with final
  published release facts.
- Replace `V260-012` hosted-gate-required/pending validation wording with final
  hosted gate and publish proof.
- Replace `V260-013` pending final validation wording with final publish proof.
- Search V260 evidence, readiness, closeout, release notes, and relevant
  verification slices for pre-tag, pending, offline, open-issue, or scope-9
  wording that conflicts with the published release.
- Add `scripts/ai/verify_v26_1_stale_v260_evidence_cleanup.sh` and route it
  through v26 release gates, manifest, and strict provenance inputs.

## Non-Goals

- No v0.26.0 tag rewrite.
- No GitHub Release republish.
- No runtime behavior change.
- No production order submission or mutation.
- No execution adapter send or live exchange request.
- No retry scheduler.
- No automatic remediation runtime.
- No Dashboard order, approval, cancel, retry, submit, replace, amend,
  flatten, remediation, order-ticket, or live operation controls.
- No product-grade live trading terminal claim.

## Required Evidence

- `bash -n scripts/ai/verify_v26_1_stale_v260_evidence_cleanup.sh scripts/ai/verify_v26_release_gates.sh scripts/ai/verify_v26_strict_provenance.sh`
- `python3 -m json.tool docs/rust-cutover/release/v0_26_0_release_manifest.json >/dev/null`
- `scripts/ai/verify_v26_1_stale_v260_evidence_cleanup.sh`
- `scripts/ai/verify_release.sh v26-release-gates`
- `scripts/ai/verify_release.sh v26-strict-provenance`
- `scripts/ai/verify_fast.sh`
- `git diff --check`

## Done Criteria

- V260 release evidence no longer claims `current_issue_state=OPEN`,
  `final_scope_issues=9`, hosted gate pending, offline publication allowance, or
  pending final validation for the final published release state.
- Final facts record hosted gate run `28853960135`, publish run `28858791493`,
  final scope `14`, and corrective scope `5`.
- Post-release gates fail closed if stale pre-publication wording returns.
- Boundary remains release-governance-only with no submit, mutation, adapter
  send, live exchange request, retry, remediation, Dashboard trading controls,
  or product-grade trading terminal claim.
