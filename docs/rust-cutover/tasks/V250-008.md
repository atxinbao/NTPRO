# V250-008 v25 release gates and strict provenance

Date: 2026-07-06
Executor: Codex
GitHub issue: `#785`
Milestone: `v0.25.0`
Risk: critical
Owner role: Verification & Release Gatekeeper
Review role: Control & Scope Agent
Auto-merge: allowed after local validation and hosted checks pass

## Goal

Add the v0.25.0 release gates and strict provenance package for the monitoring,
incident, runbook/audit, Dashboard monitoring, SLO/freshness, and DR preview
foundation.

Plain Chinese summary: 本任务只做 v0.25.0 发布治理。它把 V250-000 到
V250-007 的证据、golden trace、Dashboard smoke、artifact ingestion、v0.24.1 已发布
证据和 no-trading-controls 约束聚合到 release gate 与 strict provenance 中；它不新增
submit、cancel、replace、amend、flatten、adapter send、live exchange request、retry
scheduler 或产品级实盘终端声明。

## Dependencies

- Depends on V250-000 through V250-007.
- Hard-blocked by v0.24.1 issue closeout and release publication evidence
  through `scripts/ai/verify_release.sh v25-intake-gate`.

## Scope

Included:

- Add `scripts/ai/verify_release.sh v25-release-gates`.
- Add `scripts/ai/verify_release.sh v25-strict-provenance`.
- Add v0.25.0 release notes, readiness report, and release manifest.
- Update release surface current guard to v0.25.0 with next patch `v0.25.1`
  and next capability `v0.26.0`.
- Add hosted tag gate matrix entries scoped to `ntpro-rust-only-v0.25.0`.
- Fail closed if any V250 evidence is missing.
- Fail closed if submit/mutation/adapter send/live exchange/retry scheduler,
  Dashboard trading controls, remediation action, or product-grade trading
  terminal flags open.

Not included:

- Automated remediation runtime.
- Real trading operations.
- Product-grade live trading system or terminal claim.
- New runtime, adapter, exchange, or strategy behavior.

## Acceptance Criteria

- `scripts/ai/verify_release.sh v25-release-gates` passes locally.
- `scripts/ai/verify_release.sh v25-strict-provenance` passes locally.
- Missing any V250 evidence fails closed in the v25 release gate.
- Any forbidden trading capability opened in the manifest fails closed.
- Hosted release gate stage for v0.25.0 exists and is tag-scoped.
- Public GitHub Release publication remains routed through the gate-before-publish
  workflow/script.

## Validation

Required:

```text
bash -n scripts/ai/verify_release.sh scripts/ai/verify_v25_release_gates.sh scripts/ai/verify_v25_strict_provenance.sh scripts/ai/check_release_surface_current.sh scripts/ai/check_github_release_published.sh
python3 -m json.tool docs/rust-cutover/release/v0_25_0_release_manifest.json >/dev/null
scripts/ai/verify_release.sh v25-release-gates
scripts/ai/verify_release.sh v25-strict-provenance
NTPRO_RELEASE_SURFACE_ALLOW_MISSING_TAG=1 scripts/ai/verify_release.sh release-surface-current-guard
scripts/ai/verify_release.sh v25-slo-freshness-diagnostics-gate
python3 scripts/ai/validate_golden_trace_release_scope.py
scripts/ai/verify_fast.sh
git diff --check
```
