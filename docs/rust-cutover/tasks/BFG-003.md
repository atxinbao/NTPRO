# BFG-003 backend freeze baseline drift guard

Date: 2026-07-15
Executor: Codex
GitHub issue: #1070
Milestone: backend-freeze-governance
Status: READY FOR PR

## Goal

Add a deterministic, fail-closed guard for the v0.32.0 frozen backend identity,
source evidence, forbidden capability boundaries, and v0.33+ non-inheritance.

Plain Chinese summary: 本任务增加后端冻结防漂移检查。tag、commit、release 文件哈希、
禁止能力字段和 v0.33+ 非继承规则只要缺失、变成 true 或与注册表不一致，检查就失败。
它不会增加任何交易能力。

## Dependency

Depends on BFG-001 / #1068, closed through merged PR #1074.

## Scope

- add `scripts/ai/check_backend_freeze_baseline.sh`;
- add `backend-freeze-baseline` to `scripts/ai/verify_release.sh`;
- cover missing fields, forbidden true values, wrong tag/SHA/hash, scheduled
  patch drift, and inheritance drift with negative selftests;
- add task, governance index, validation, and evidence updates.

## Acceptance Criteria

- the positive guard passes on the current frozen baseline;
- every critical drift category fails closed;
- all 27 manifest boundary fields are present and explicit false;
- v0.33+ remains separately scoped with no forbidden inheritance;
- v32 release, strict-provenance, publication, and fast checks still pass.

## Result

Local validation passed. The guard validates 27 explicit-false boundaries and
four registered source hashes, while 20 mutation cases fail closed for their
expected reasons. Existing v32 release and publication checks remain green.
