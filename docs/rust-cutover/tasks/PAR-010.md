# PAR-010 Executable Release Replay Closeout

Date: 2026-07-29
Executor: Codex
GitHub issue: #1178
Risk: high
Classification: separately-scoped-verification-governance
Status: REVIEW_APPROVED / HOSTED_CHECKS_PENDING

## Goal

Promote the final five `schema_only_scoped` release cases to executable Rust
replay and prevent schema-only scope from returning through the release runner.

中文摘要：本任务把统一 read-model contract、账户 provenance/redaction
负向路径和市场报价 envelope 从“只校验 JSON 结构”提升为“执行 Rust
投影或模型构造”，同时在 release gate 中禁止 schema-only case 回归。

## Scope

- execute two read-model contract cases by deriving health and blocking reasons
  from component lineage, source provenance, freshness, and component status;
- execute two account negative cases by validating source provenance and
  redaction evidence;
- execute the market-data smoke row through `QuoteTick`;
- register all five cases as executable replay;
- run both Rust harnesses from the golden trace release runner;
- fail release mode whenever any schema-only case exists.

## Non-Goals

- no runtime adapter, exchange, submit, mutation, send, retry, remediation, or
  trading-control capability;
- no change to golden input/expected rows or trading semantics;
- no edit to frozen `docs/rust-cutover/release/v0_32_0_*` files;
- no claim that validator executable rows are product runtime execution.

## Acceptance

- release scope is exactly 289 total, 103 executable Rust replay, 186 executable
  validator replay, and 0 schema-only;
- direct Rust replay matches all five existing expected events;
- release runner executes both new/expanded harnesses;
- negative self-test proves schema-only regression is rejected;
- standard/high precision, Clippy, golden release replay, governance, freeze,
  and risk inventory checks pass;
- independent review and final-head hosted checks approve before manual merge.
