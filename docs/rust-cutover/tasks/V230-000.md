# V230-000 - v0.23.0 intake gate after v0.22.1 release

Date: 2026-07-03
Executor: Codex
GitHub issue: `#711`
Milestone: `v0.23.0`
Risk: low
Owner role: Control & Scope Agent
Review role: Verification & Release Gatekeeper
Auto-merge: allowed after local validation and hosted checks pass

## Goal

Move `v0.23.0` from hard-blocked intake to scoped implementation intake after
the `v0.22.1` release evidence is published.

Plain Chinese summary: 本任务只解除 v0.23.0 的 intake 硬阻塞，并把入口边界写清楚。
v0.22.1 已发布、V221 issue 已关闭、hosted release gate 已成功、strict provenance
已记录，所以 v0.23.0 可以按 #712-#718 的顺序进入具体任务；本任务不实现多账户、
多策略或多 venue runtime。

## Scope

Included:

- record the live `v0.22.1` release, gate, issue, and milestone facts that
  satisfy the v0.23.0 start gate;
- update the current release surface from `v0.22.0` to `v0.22.1`;
- add a v0.23.0 intake/scope gate document;
- keep the v0.23.0 implementation split across #712-#718;
- preserve the no-submit/no-operation-control boundary inherited from
  `v0.22.1`.

Not included:

- multi-account runtime implementation;
- multi-strategy supervisor implementation;
- multi-venue node lifecycle implementation;
- dashboard implementation for v0.23.0;
- release tag or GitHub Release publication;
- production submit, cancel, retry, replace, amend, flatten, or automatic
  remediation.

## Acceptance Criteria

- `docs/rust-cutover/scope/v0_23_0_multi_node_isolation_scope.md` records the
  v0.23.0 intake status, dependency proof, issue order, and non-goals.
- `README.md`, `ROADMAP.md`, and `docs/versioning.md` identify
  `ntpro-rust-only-v0.22.1` as the current release surface.
- `scripts/ai/check_release_surface_current.sh` and
  `scripts/ai/check_github_release_published.sh` default to v0.22.1 and pass.
- `docs/rust-cutover/evidence/V230-000.md` records live GitHub evidence.
- No runtime, adapter, or dashboard behavior changes.

## Validation

Required:

```text
gh issue view 711 --repo atxinbao/NTPRO --json number,title,state,milestone,url
gh issue list --repo atxinbao/NTPRO --state open --milestone v0.23.0
gh issue view 710 --repo atxinbao/NTPRO --json number,state,closedAt,url
gh release view ntpro-rust-only-v0.22.1 --repo atxinbao/NTPRO
gh run view 28647486521 --repo atxinbao/NTPRO
gh api 'repos/atxinbao/NTPRO/milestones?state=all'
scripts/ai/check_release_surface_current.sh
scripts/ai/check_github_release_published.sh
scripts/ai/verify_fast.sh
git diff --check
```
