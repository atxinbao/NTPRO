# V04-008 - mock order lifecycle

Milestone: v0.4.0 Binance Sandbox Product Foundation
Priority: P0
Owner role: Adapter & Integration Agent
Review role: Trading Runtime Agent
Risk: medium

## Goal

Add a mock order lifecycle for the Binance sandbox path.

Plain language: v0.4 needs to show how an order moves through submit, accept,
fill, cancel, and reject states without talking to the real venue.

## Dependencies

- `V04-005`

## Scope

- Add or harden a mock order lifecycle for Binance sandbox flows.
- Cover submit, accept, fill, cancel, and scoped reject behavior.
- Keep outputs readable by later strategy and dashboard surfaces.

## Non-goals

- Do not submit real orders.
- Do not claim venue-matching parity.
- Do not implement production exchange recovery logic.

## Related files

- `crates/execution/` (TO_MODIFY): mock lifecycle behavior.
- `crates/adapters/binance/` (TO_MODIFY): sandbox execution path.
- `docs/rust-cutover/evidence/V04-008.md` (CREATE): task evidence.

## Implementation guidance

Think in terms of product evidence, not deep simulation fidelity. The lifecycle
must be deterministic enough for tests and dashboard consumption.

## Verification criteria

- Mock lifecycle covers core order state transitions.
- Lifecycle runs locally with no real venue access.
- Later smokes can consume the lifecycle output.

## Required commands

```bash
cargo test -p nautilus-live --lib
cargo test -p nautilus-cli --lib
git diff --check
```

## Evidence required

Create `docs/rust-cutover/evidence/V04-008.md` with:

- task ID;
- plain Chinese summary;
- files changed;
- commands run;
- lifecycle coverage summary;
- behavior impact;
- public API impact;
- migration note status;
- rollback plan.
