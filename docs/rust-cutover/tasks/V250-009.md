# V250-009 v25 tag gate base provenance scoping

Date: 2026-07-06
Executor: Codex
GitHub issue: `#804`
Milestone: none
Risk: high
Owner role: Verification & Release Gatekeeper
Review role: Control & Scope Agent
Auto-merge: allowed after local validation and hosted checks pass

## Goal

Fix the v0.25.0 hosted tag release gate after the first
`ntpro-rust-only-v0.25.0` run proved that the v25 release gate was inheriting
the v0.24.1 strict provenance HEAD/tag constraint while checking the already
published v0.24.1 baseline.

Plain Chinese summary: 本任务只修复 v0.25.0 tag 发布门禁的继承约束。v25 自己仍然必须
校验 HEAD 等于 `ntpro-rust-only-v0.25.0`，但 v25 intake 在验证 v0.24.1 已发布基线时，
不能要求当前 HEAD 回到旧的 `ntpro-rust-only-v0.24.1`。

## Failure Evidence

- Release gate run: `28762387835`
- Failed job: `verify-release (release-v25-release-gates)`
- Failure:

```text
v24.1 strict release provenance drift: HEAD 4fcacec2cbb05efe5594e2781e87edcdb741179b does not match ntpro-rust-only-v0.24.1 commit fa5bb537a3002655efb3e5abc7e47fdf957bb298
```

## Scope

Included:

- Keep the v25 tag gate same-tag HEAD check.
- Keep the v24.1 publication proof required by v25 intake.
- Prevent v25 intake from passing `NTPRO_RELEASE_GATE=1` into the historical
  v24.1 strict provenance check.

Not included:

- No v0.25.0 milestone issue-count change.
- No runtime, adapter, Dashboard, trading, or order-control behavior change.

Post-release scope integration:

- V251-002 records this task as a final release-scope corrective fix in the
  v0.25.0 manifest, readiness, release notes, and strict provenance inputs.
- This does not expand the v0.25.0 capability boundary.

## Acceptance Criteria

- `NTPRO_RELEASE_GATE=1 scripts/ai/verify_release.sh v25-release-gates` passes
  on the v0.25.0 tag commit.
- `scripts/ai/verify_release.sh v24.1-strict-provenance` still passes normally.
- v25 release gate still fails if current HEAD does not match
  `ntpro-rust-only-v0.25.0` in release-gate mode.
- GitHub issue `#804` is closed by the PR.

## Validation

Required:

```text
bash -n scripts/ai/verify_v25_intake_gate.sh
scripts/ai/verify_release.sh v24.1-strict-provenance
NTPRO_RELEASE_GATE=1 scripts/ai/verify_release.sh v25-release-gates
git diff --check
```
