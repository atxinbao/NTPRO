# V111-006 Fix openOrders Wording Conflict

Date: 2026-06-20
Executor: Codex
Owner role: Verification & Release Gatekeeper
Review role: Rust Product Surface Agent
Risk level: medium
Status: BACKLOG
Depends on: V111-005

## Goal

Align v0.11 release-facing wording so `/api/v3/openOrders` is not presented as
included in the v0.11 authenticated account snapshot support.

Plain Chinese summary: 这次只改文档口径。v0.11 支持的是离线的
`/api/v3/account` 账户快照合同，不代表已经支持生产 open orders 或订单状态读取。

## Scope

- Clarify the authenticated account snapshot contract is limited to
  `/api/v3/account`.
- State that `/api/v3/openOrders`, `/api/v3/allOrders`, and `/api/v3/order`
  order-state reads are out of scope for v0.11.
- Align README, versioning, and v0.11 release boundary wording.

## Non-Goals

- No Rust runtime code changes.
- No endpoint classifier behavior changes.
- No production network connection.
- No production online read runtime.
- No production order submission.
- No production cancel, replace, amend, retry, or correction.
- No Dashboard order controls.
- No new release tag or GitHub Release publication.

## Acceptance

- `rg /api/v3/openOrders README.md docs/versioning.md docs/rust-cutover/release`
  shows only explicit out-of-scope wording.
- `scripts/ai/check_release_surface_current.sh` passes.
- `scripts/ai/verify_fast.sh` passes.
- `git diff --check` passes.

## Automation Approval

This task is marked `autoMergeAllowed=true` in Shrimp. It is release wording
only and does not add production network access, credential use, order mutation,
Dashboard order controls, or trading-semantic behavior.
