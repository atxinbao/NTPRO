# V02-004 - ntpro-node sandbox-only real LiveNode start/stop

Milestone: v0.2 Local Multi-Node Runtime Foundation
Priority: P0
Owner role: Rust Core Runtime Agent
Review role: Verification & Release Gatekeeper
Risk: high

## Goal

Create a real ntpro-node sandbox-only runtime entry that builds, starts, reports status for, and stops a Rust LiveNode without simulated_demo output.

## Dependencies

- `V02-003`

## Scope

- Keep v0.2 scoped to Local Multi-Node Runtime Foundation.
- Use sandbox, mock, or fixture evidence by default.
- Preserve DRG-010 boundaries and do not claim release readiness from this task alone.

## Non-goals

- Do not start Dashboard UI implementation from this task.
- Do not add production real-exchange connectivity from this task.
- Do not implement manual order entry from this task.
- Do not create release tags or publish GitHub Releases.
- Do not claim v0.2 release readiness unless this is V02-010 and all required evidence passes.

## Related files

- `crates/live/src/node.rs` (REFERENCE): LiveNode lifecycle source
- `crates/cli/src/**` (TO_MODIFY): Potential CLI/bin wiring for ntpro-node
- `crates/live/examples/**` (TO_MODIFY): Existing or new sandbox node example
- `docs/rust-cutover/evidence/V02-004.md` (CREATE): Task evidence

## Implementation guidance

Follow AGENTS.md, docs/rust-cutover/CONTRACT.md, docs/rust-cutover/DEFINITION_OF_DONE.md, docs/rust-cutover/TASK_EXECUTION.md, and docs/rust-cutover/AGENT_ROLES.md. Do not start Dashboard UI work, real exchange connectivity, VWAP/POV productization, Nexus-like communication, manual order entry, or distributed multi-server deployment in this v0.2 queue. Use sandbox/mock/fixture evidence by default. Tasks above medium risk must stop for review according to repo rules.

Implement the smallest real sandbox-only node process path. It must use real LiveNode start/stop and sandbox/mock clients, not simulated text output. Keep live exchange connectivity disabled. High-risk task: stop for review before merge and include lifecycle evidence.

High-risk task: stop at REVIEW_REQUIRED and do not enable auto-merge.

## Verification criteria

Command/example starts a sandbox LiveNode, reaches running, stops, reaches stopped, and emits a status artifact. Targeted cargo test/check for touched crates passes. scripts/ai/verify_fast.sh passes. Evidence proves external_venue_connection=false and real_orders_submitted=false.

Evidence must record changed files, commands run, command output summary, behavior impact, public API impact, migration note status, rollback plan, and a plain Chinese non-expert summary.

## Evidence required

Create `docs/rust-cutover/evidence/V02-004.md` with changed files, commands, validation results, behavior impact, public API impact, migration note status, rollback plan, and a plain Chinese summary.
