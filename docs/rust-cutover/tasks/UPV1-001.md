# UPV1-001 - 可用产品定位与首次 Backtest 入口

Date: 2026-08-25
Executor: Codex
GitHub issue: #1321
Risk: high
Owner role: Rust Product Surface Agent
Review role: Verification & Release Gatekeeper
Status: REVIEW_REQUIRED

## 目标

把 NTPRO 当前产品收敛为本地可部署的单策略工作台，并修复首次使用断点：经过验证的
StrategyVersion 即使没有历史 Run，也能进入内置确定性 Backtest 创建流程。

## 当前产品边界

- 一个用户、一个策略、一个交易品种、一个账户、一台机器；
- 一个 Supervisor + 一个 node；
- Backtest + Demo 是 Usable Product v1 的当前闭环；
- Live 继续使用独立准入，不从本任务继承任何真实交易能力。

## 范围

- 统一 README、`project.html` 和产品 Roadmap；
- 明确“当前可用产品、下一阶段、后置能力”；
- Backtest 页面在已验证空 Run 列表下从当前 StrategyVersion 解析内置来源；
- 增加空历史、来源不唯一和 stale/pending fail-closed 回归；
- 增加 production bundle 浏览器首次 Backtest 证据。

## 非目标

- 不增加真实历史数据目录；
- 不修改 Product API、Rust Runtime 或策略语义；
- 不开放策略 mutation、真实订单、自动重试或自动恢复；
- 不修改 v0.32.0 冻结发布文件。

## 验收

```bash
cd apps/strategy-workbench
npm run format:check
npm run lint
npm run typecheck
npm run test
npm run build

cd ../..
NTPRO_PLAYWRIGHT_CORE_PATH=apps/strategy-workbench/node_modules/playwright-core \
NTPRO_BROWSER_EVIDENCE_DIR=/tmp/ntpro-upv1-001-browser-evidence \
  node scripts/ai/test_strategy_workbench_browser.mjs
scripts/ai/check_mvp_freeze_baseline.sh
scripts/ai/check_rust_only_runtime.sh
scripts/ai/verify_fast.sh
git diff --check
```

## 完成条件

本任务为 high risk。必须取得独立审查批准并通过 hosted checks，随后手动合并；不得启用
auto-merge。PR 合并并关闭 Issue #1321 后才是 DONE。
