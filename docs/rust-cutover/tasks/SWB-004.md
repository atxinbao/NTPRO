# SWB-004 - 工作台首次启动与 Backtest/Demo 可用性收口

Date: 2026-08-14
Executor: Codex
GitHub issue: #1315
Risk: high
Owner role: Rust Product Surface Agent
Review role: Verification & Release Gatekeeper
Status: LOCAL_VALIDATION_PASSED / REVIEW_REQUIRED

## 目标

让全新 MVP 工作区无需手工创建目录即可读取 Run 并创建 Backtest；让已停止且没有活动 Run
所有权的 Sandbox node 可以创建 Demo Run；修复常用桌面视口下底部活动区遮挡主操作的问题。

## 范围

- MVP 初始化时建立受控 Backtest 工件根目录；
- stopped node 仅在所有 Run 所有权均已终态、不存在，或恰好一个与真实 Demo manifest、
  Sandbox 环境、node、策略实例和不可变哈希一致的标准未启动 claim 时视为稳定快照；
- 活动所有权、异常状态、身份/路径/边界工件继续 fail closed；
- 当前 claim 之后出现 started/stopped 时间即视为启动尝试；早于当前 claim 的上一 Run 历史时间
  不得阻塞新的标准 Created Run；
- Backtest 页面区分运行源错误、加载中和确实没有数据集；
- 1280x720 与移动视口下主内容保持独立滚动；Backtest 提交区固定在画布底部，桌面低高度
  活动区采用紧凑密度且不遮挡主操作。
- 修正 hosted CLI 全量测试暴露的 Live Runtime 外部退出 fixture 竞态，不改变生产 Live 行为。

## 非目标

- 不修改 S3-LV-011 / Issue #1312；
- 不开放真实订单、订单 mutation、自动重试、自动补救或交易控件；
- 不修改 v0.32.0 冻结发布文件。

## 验收

```bash
cargo fmt --all -- --check
cargo test -p nautilus-cli --lib mvp::tests
cargo test -p nautilus-cli --lib dashboard::product_api::tests
cargo clippy -p nautilus-cli --lib -- -D warnings

cd apps/strategy-workbench
npm run format:check
npm run lint
npm run typecheck
npm run test
npm run build

cd ../..
NTPRO_PLAYWRIGHT_CORE_PATH=/absolute/path/to/playwright-core \
NTPRO_BROWSER_EVIDENCE_DIR=/tmp/ntpro-swb004-browser-evidence \
  node scripts/ai/test_strategy_workbench_browser.mjs
scripts/ai/check_backend_runtime_risk_inventory.sh
scripts/ai/check_mvp_freeze_baseline.sh
git diff --check
```

## 完成条件

本任务为 high risk。必须取得独立审查批准并通过 hosted checks，随后手动合并；不得启用
auto-merge。PR 合并并关闭 Issue #1315 后才是 DONE。
