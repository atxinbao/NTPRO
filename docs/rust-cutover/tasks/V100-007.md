# V100-007 Define Execution Artifact Contract

Date: 2026-06-19
Executor: Codex
Status: IN PROGRESS

## Goal

Define the redacted v0.10 execution artifact contract for future manual Binance
testnet tiny submit-and-cancel proof without submitting or canceling orders in
offline automation.

## Scope

- Add a local CLI command that writes the execution artifact contract.
- Define request, order-test, submit-ack, cancel-ack, lifecycle, and
  reconciliation artifact entries.
- Record explicit counters:
  - `testnet_orders_submitted`
  - `testnet_orders_canceled`
  - `production_orders_submitted`
  - `production_orders_canceled`
- Keep offline counters at `0`.
- Record `manual_submit_cancel_proof_observed=false` in offline evidence.
- Preserve existing manual Binance testnet order gates and redaction rules.

## Out Of Scope

- No Binance API calls.
- No online submit or cancel.
- No `/api/v3/order` execution.
- No matching-engine submission.
- No production Binance endpoint.
- No Dashboard order controls.
- No claim that V100-006 manual proof has passed.

## Acceptance

- Missing manual gates fail before artifact construction.
- Missing API key or secret fails closed.
- Passing command writes the artifact contract JSON.
- Artifact contract distinguishes testnet counters from production counters.
- Artifact contract records request/order-test/submit/cancel/lifecycle/
  reconciliation artifact schemas.
- Artifact contract redacts API key value, secret, signature, signed query,
  signed URL, and request body.
- Artifact contract records `network_attempted=false` and
  `real_orders_submitted=false`.
- V100-006 remains manual-gated and is not completed by this task.
