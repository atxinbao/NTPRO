# V221-003 - Expand executable read-model replay coverage

Date: 2026-07-02
Executor: Codex
GitHub issue: `#707`
Milestone: `v0.22.1`
Risk: medium
Owner role: Verification & Release Gatekeeper
Review role: Control & Scope Agent
Auto-merge: allowed after local validation and hosted checks pass

## Goal

Promote high-risk Trader Terminal Workbench read-model rows from
`schema_only_scoped` to executable Rust projection replay.

Plain Chinese summary: 本任务把 positions、fills、order duplicate/readback
mismatch、risk 状态和 dashboard forbidden controls 等高风险 read-model 路径接入
Rust projection replay。v0.22.1 仍只声明 Workbench/runtime bridge；剩余 contract
baseline 和 account missing/redaction rows 继续作为 schema-only scope，不宣称完整
executable read-model runtime。

## Scope

Included:

- extend `golden_trace_read_model_projection` to cover position and fill
  projections;
- replay the remaining order lifecycle mismatch/duplicate rows and risk rows
  through the existing Rust harness;
- replay dashboard forbidden-controls blocked behavior;
- update `RELEASE_REPLAY_SCOPE.json` so promoted rows carry executable harness
  evidence;
- record the remaining read-model schema-only rows explicitly.

Not included:

- changing runtime trading semantics;
- enabling submit/cancel/retry/replace/amend/flatten controls;
- claiming v0.22.1 is a full product-grade executable read-model runtime;
- starting v0.23.0 multi-account / multi-strategy / multi-venue work.

## Acceptance Criteria

- Priority read-model rows move from `schema_only_scoped` to
  `executable_replay`.
- Rust projection replay covers positions, fills, order duplicate/readback
  mismatch, risk rows, and dashboard forbidden controls.
- Release scope validation passes.
- Evidence distinguishes Workbench/runtime bridge from complete executable
  runtime.

## Validation

```text
cargo fmt --all -- --check
python3 scripts/ai/validate_golden_trace_release_scope.py --manifest docs/rust-cutover/golden_trace/RELEASE_REPLAY_SCOPE.json --trace-glob 'tests/golden/*.jsonl'
cargo test -p nautilus-cli --test golden_trace_read_model_projection -- --nocapture
scripts/ai/verify_fast.sh
git diff --check
```
