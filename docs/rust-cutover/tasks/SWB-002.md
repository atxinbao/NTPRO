# SWB-002 - 策略总览与 Run 详情纵向切片

Date: 2026-08-06
Executor: Codex
GitHub issue: #1268
Risk: high
Owner role: Rust Product Surface Agent
Review role: Verification & Release Gatekeeper
Status: REVIEW_APPROVED_HOSTED_PENDING

## 依赖

- FEI-001 / Issue #1255 已通过 PR #1257 合并；
- S0-API-001 / Issue #1256 及四个子任务均已关闭；
- 生成的 TypeScript Product API 合同已通过 PR #1267 合并。

## 目标

让策略工作台的策略总览和 Run 详情正式消费只读 Product API，以 Strategy、不可变
StrategyVersion 和独立 Run 为产品资源展示 Backtest、Demo（Sandbox）与 Live 三种环境。

## 范围

- 通过 TanStack Query 调用生成的 `productApi`；
- 总览展示策略、默认版本和三模式 Run 汇总；
- 新增 `/runs/$runId` 类型化详情路由；
- 详情展示生命周期、风险、结果、账户、Venue、适配器、数据、配置、来源与能力边界；
- URL 保存 Run 身份，页面刷新可恢复同一详情；
- 区分加载、空数据、无权限、资源不存在、传输失败和合同阻断；
- 组件与浏览器测试覆盖桌面、移动端、深链和 fail-closed。
- 同步仓库级 Rust/Axum 生产 bundle 浏览器验收脚本的页面语义。

## 非目标

- 不修改 Rust Product API 或 MVP 状态合同；
- 不新增策略或 Run 写操作；
- 不开放真实 Venue、订单提交、订单修改、自动重试、自动补救或交易控件；
- 不修改 v0.32.0 冻结发布文件；
- 不提交构建和浏览器生成物。

## 验收

```bash
cd apps/strategy-workbench
npm run audit
npm run format:check
npm run lint
npm run typecheck
npm run test
npm run build
npm run test:e2e

cd ../..
scripts/ci/test-ci-change-classifier.sh
scripts/ai/check_rust_only_runtime.sh
scripts/ai/check_mvp_freeze_baseline.sh
NTPRO_PLAYWRIGHT_CORE_PATH=apps/strategy-workbench/node_modules/playwright-core \
  node scripts/ai/test_strategy_workbench_browser.mjs
git diff --check
```

## 完成条件

本任务为 high risk。必须取得独立 Verification & Release Gatekeeper 的明确批准并通过
hosted checks，随后手动合并；不得启用 auto-merge。PR 合并并关闭 Issue #1268 后才是 DONE。
