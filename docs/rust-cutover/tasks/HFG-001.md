# HFG-001 Govern and clean obsolete hidden root configuration

Date: 2026-07-28
Executor: Codex
GitHub issue: `#1161`
Risk: medium
Owner role: Rust Product Surface Agent
Review role: Verification & Release Gatekeeper
Auto-merge: not requested
Status: `READY_FOR_PR`

## Goal

梳理根目录隐藏文件和目录，删除无活动消费者的配置，保留项目基础设施和用户本地
状态，并通过 executable governance 防止已退役配置回流。

## Scope

- 删除 `.codecov.yml`、`.codespellrc`、`.dockerignore`、`.gitlint`、
  `.markdownlint.jsonc`、`.yamlfmt` 和 `.yamllint.yaml`；
- 清理对应 CODEOWNERS，并为仍在使用的隐藏配置补齐 ownership；
- 删除 developer guide 中 Gitlint 和 Codecov 过时说明；
- backend hygiene 拒绝本任务和 TCG-001 退役的九个根配置回流。

## Preserved

- `.git`、`.cargo`、`.config`、`.docker`、`.github`、
  `.pre-commit-hooks` 和 `.supply-chain`；
- `.env.example`、`.gitattributes`、`.gitignore`、`.lycheeignore`、
  `.pre-commit-config.yaml` 和 `.zizmor.yml`；
- 受保护的本地 `.agentflow`、`.codex` 和 `.understand-anything`。

## Not in scope

- Docker compose 服务、Cargo workspace、依赖或编译 profile；
- `.git` 历史重写或用户本地状态删除；
- runtime、API、adapter、schema、存储或交易语义；
- v0.32.0 冻结发布文件。

## Acceptance criteria

- 七个删除项无活动消费者或引用；
- 九个累计退役根配置被 backend hygiene 阻止回流；
- 保留项均有明确执行消费者或本地状态治理依据；
- docs、Cargo、governance、Rust-only 和 hosted security checks 通过；
- 无生成缓存或 v0.32.0 冻结文件差异。

## Local validation

- `cargo test -p ntpro-governance`: 35 passed；
- `cargo clippy --locked -p ntpro-governance --all-targets -- -D warnings`: passed；
- `scripts/ai/check_docs_examples_governance.sh`: passed；
- `scripts/ai/check_rust_only_runtime.sh`: passed；
- `scripts/ai/verify_release.sh current-governance`: passed；
- `scripts/ci/security-audit-gate.sh`: `audit_needed=false`；
- v0.32.0 frozen release diff: zero。
