# V031-001 - public README and release surface cleanup

Milestone: v0.3.1 Local Supervisor Control Console Hardening
Priority: P0
Owner role: Rust Product Surface Agent
Review role: Verification & Release Gatekeeper
Risk: medium

## Goal

Align the public README and release-facing wording with the actual
`ntpro-rust-only-v0.3.0` published state before the next patch release.

Plain language: the repo must stop telling users that the current release is
still `v0.2.0`.

## Dependencies

- none

## Scope

- Update `README.md` current source tag and capability wording.
- Replace the stale `ntpro-rust-only-v0.2.0` public release link where the
  README claims the current milestone.
- Update README capability boundary text so it no longer describes only the
  v0.2 local multi-node foundation.
- Add the current v0.3 local Supervisor console user entrypoints where the
  README should mention them.
- Keep the README explicit that production trading, real exchange connectivity,
  real orders, and remote multi-user dashboard operation remain unsupported.

## Non-goals

- Do not implement new CLI behavior.
- Do not change runtime semantics.
- Do not create a release tag.
- Do not publish a GitHub Release.

## Related files

- `README.md` (TO_MODIFY): public release surface.
- `docs/rust-cutover/release/v0_3_0_supervisor_control_readiness_report.md`
  (REFERENCE): current v0.3.0 readiness claim.
- `docs/rust-cutover/scope/v0_3_1_supervisor_control_hardening.md`
  (REFERENCE): v0.3.1 patch scope.
- `docs/rust-cutover/evidence/V031-001.md` (CREATE): task evidence.

## Implementation guidance

Keep this task documentation-only. Do not overstate the patch as a new product
milestone. The README should describe the current source release honestly and
reserve `v0.4` product expansion for later work.

## Verification criteria

- README no longer claims the current source tag is `ntpro-rust-only-v0.2.0`.
- README no longer claims the current capability is only
  `Local Multi-Node Runtime Foundation`.
- README keeps the local sandbox-only and non-production boundaries explicit.

## Required commands

```bash
rg -n "ntpro-rust-only-v0.2.0|Local Multi-Node Runtime Foundation|Local Supervisor Control Console" README.md
git diff --check
```

## Evidence required

Create `docs/rust-cutover/evidence/V031-001.md` with:

- task ID;
- plain Chinese summary;
- files changed;
- commands run;
- behavior impact;
- public API impact;
- migration note status;
- release-surface cleanup result;
- rollback plan.
