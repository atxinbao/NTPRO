# V240-003 rate-limit and throttle gate preview

Date: 2026-07-04
Executor: Codex
Task: `V240-003` / GitHub issue `#746`
Milestone: `v0.24.0`
Status: LOCAL VALIDATION PASSED

## Goal

Define preview-level rate-limit and throttle gate evidence for v0.24.0 order
control. The gate must prove that over-limit, missing-policy, and cross-scope
reuse cases fail closed before any runtime adapter or network operation.

Plain Chinese summary: 本任务只定义 rate-limit / throttle preview gate 证据。
它按 account、strategy、venue node、isolation_scope_key 绑定输入，生成确定性的只读
decision；不做真实生产限速执行，不调用 exchange endpoint。

## Scope

- Define per account / strategy / venue node / isolation scope rate-limit input.
- Define deterministic decisions: `allowed_preview`, `throttled`,
  `blocked_missing_limit`, and `blocked_scope_mismatch`.
- Cover burst, window, venue-specific cap, missing limit policy, and
  cross-scope leakage.
- Output read-only gate evidence for later Dashboard / Workbench preview.
- Add a fail-closed release verification stage.

## Non-Goals

- Do not implement production rate-limit execution.
- Do not call exchange endpoints.
- Do not call execution adapters.
- Do not add submit, cancel, retry, replace, amend, or flatten capability.

## Required Evidence

- `bash -n scripts/ai/verify_release.sh scripts/ai/verify_v24_rate_limit_throttle_gate.sh`
- `python3 scripts/ai/golden_trace_runner.py tests/golden/v240_rate_limit_throttle_gate.jsonl --mode validate-only`
- `scripts/ai/verify_release.sh v24-rate-limit-throttle-gate`
- `scripts/ai/verify_fast.sh`
- `git diff --check`

## Done Criteria

- Over-limit cases are `throttled`; they cannot be implicitly allowed.
- Missing limit policy is `blocked_missing_limit`.
- Cross-scope policy reuse is `blocked_scope_mismatch`.
- Gate output is read-only Dashboard-preview evidence.
- No network, adapter, mutation, signed payload, or secret artifact appears.
