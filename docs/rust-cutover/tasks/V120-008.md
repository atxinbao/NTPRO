# V120-008 Add Dual Release Gates for Offline and Owner-Gated Online

Date: 2026-06-21
Executor: Codex
Owner role: Verification & Release Gatekeeper
Risk level: medium
Status: BACKLOG
Depends on: V120-007

## Goal

Wire v0.12 production online read-only and persistent shadow work into release
verification without making production network access a default CI requirement.

Plain Chinese summary: 这次做的是“发版闸门”，不是新交易功能。默认 gate 只跑离线证据；
线上生产只读证明必须 owner 显式开关，CI 不会自动连生产，也不会读真实密钥。

## Scope

- Add `scripts/ai/verify_v12_offline_release_gates.sh`.
- Add `scripts/ai/verify_v12_manual_online_preflight.sh`.
- Expose both through `scripts/ai/verify_release.sh`.
- Run v12 gates from PR smoke when v0.12 scripts/docs/workflow/product files change.
- Add v12 release stages to `.github/workflows/release-tag.yml`.
- Keep online production reads owner-gated and optional.

## Non-Goals

- No production order submission.
- No production cancel, replace, amend, retry, or correction orders.
- No production order-state reads.
- No listenKey lifecycle access.
- No Dashboard order controls.
- No real-funds trading.
- No default CI production network dependency.

## Acceptance

- `scripts/ai/verify_v12_offline_release_gates.sh` passes.
- `scripts/ai/verify_v12_manual_online_preflight.sh` passes.
- `scripts/ai/verify_release.sh v12-offline-release-gates v12-manual-online-preflight` passes.
- `bash -n` covers all v12 release scripts.
- `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/rust-cutover-smoke.yml"); YAML.load_file(".github/workflows/release-tag.yml")'` passes.
- `git diff --check` passes.

## Auto-Merge

This task has an owner-approved workflow risk downgrade in Shrimp on
2026-06-20. Auto-merge is allowed only because the implementation remains
verification-only, read-only, fail-closed, and non-mutating.
