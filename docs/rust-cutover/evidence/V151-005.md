# V151-005 Evidence - Production order-test preview denial

Date: 2026-06-23
Executor: Codex
Milestone: v0.15.1
Status: local validation PASS; hosted smoke pending PR

## Summary

Denied production live-alpha `POST /api/v3/order/test` request preview for
v0.15.1 while keeping `POST /api/v3/order` as the only production redacted
request-preview candidate.

Plain Chinese summary: 生产 request preview 暂时只允许 `/api/v3/order`。
`/api/v3/order/test` 在 production preview 里直接拒绝；testnet/sandbox 的 order-test
预览不变。仍然不联网、不发生产请求、不调用生产 adapter、不真实下单。

## Files Changed

- `crates/cli/src/endpoint_classifier.rs`
- `crates/cli/src/live.rs`
- `scripts/ai/verify_v15_live_order_request_dry_run_builder.sh`
- `docs/rust-cutover/release/v0_15_0_mutation_scope_decision.md`
- `docs/rust-cutover/tasks/V151-005.md`
- `docs/rust-cutover/evidence/V151-005.md`

## Behavior Boundary

Included:

- production `POST /api/v3/order/test` is classified as forbidden;
- production live-alpha request preview rejects `/api/v3/order/test`;
- production `POST /api/v3/order` remains preview-only under owner/manual gates;
- testnet/sandbox order-test behavior remains separate and unchanged.

Not included:

- production request sending;
- production order submission;
- production order-test submission;
- production order mutation;
- production execution adapter call;
- real funds;
- production trading;
- Dashboard order controls.

## Validation

PASS:

```text
cargo fmt --all --check
cargo test -p nautilus-cli endpoint_classifier --lib
cargo test -p nautilus-cli production_live_alpha_order_request_preview --lib
cargo clippy -p nautilus-cli --all-targets -- -D warnings
scripts/ai/verify_v15_live_order_request_dry_run_builder.sh
scripts/ai/verify_release.sh v15-release-gates
scripts/ai/verify_fast.sh
git diff --check
```

Observed results:

```text
endpoint_classifier ... 14 passed
production_live_alpha_order_request_preview ... 8 passed
cargo clippy -p nautilus-cli --all-targets -- -D warnings PASS
v15_live_order_request_dry_run_builder status=ok
v15_release_gates status=ok
verify_fast complete
git diff --check PASS
production POST /api/v3/order/test classified as forbidden
production live-alpha /api/v3/order/test request preview rejected
production POST /api/v3/order remains redacted preview-only under gates
request_sent=false
network_attempted=false
production_orders_submitted=0
production_order_mutations_attempted=0
execution_adapter_called=false
dashboard_order_controls_enabled=false
```

## Rollback Plan

Revert this PR to restore production `/api/v3/order/test` request-preview
classification and remove the V151-005 regression checks and docs. No exchange
state, credentials, production orders, or runtime trading behavior require
rollback.
