# PAR-004 Emulator Routing Placeholder Replacement Evidence

Date: 2026-07-29
Executor: Codex
GitHub issue: #1172
Status: LOCAL VALIDATION PASSED / REVIEW_REQUIRED

## Ownership Finding

```text
historical placeholder location = crates/risk/tests/risk_engine.rs
current submit/list routing owner = crates/trading/src/strategy/mod.rs
current emulated modify routing owner = crates/trading/src/strategy/mod.rs
emulator endpoint = OrderEmulator.execute
risk endpoint = RiskEngine.queue_execute
```

The historical comments expected RiskEngine to route to the emulator. The
current executable architecture routes emulated strategy commands before they
enter RiskEngine. Keeping the tests in RiskEngine would either assert the wrong
contract or introduce a test-only route that does not exist in production.

## Repair Boundary

```text
production Rust changed = false
ignored empty placeholders removed = 3
default executable routing tests added = 3
emulator positive assertions = submit list + submit + modify
risk negative assertions = submit list + submit + modify
public API changed = false
production trading capability changed = false
v0.32.0 frozen files changed = false
```

## Validation

```text
cargo test -p nautilus-trading --lib emulat -- --nocapture
result = PASS (4 passed, including the three restored cases)

cargo test -p nautilus-trading -p nautilus-risk
result = PASS
  nautilus-trading = 179 passed, 0 ignored
  nautilus-risk risk_engine = 80 passed, 1 PAR-005 placeholder ignored

cargo test -p nautilus-trading --lib emulat \
  --features nautilus-model/high-precision
result = PASS (4 passed)

cargo test -p nautilus-risk --features nautilus-model/high-precision
result = PASS (80 risk_engine passed, 1 PAR-005 placeholder ignored)

cargo clippy -p nautilus-trading -p nautilus-risk \
  --all-targets --all-features \
  --features nautilus-model/high-precision -- -D warnings
result = PASS

REQUIRE_GOLDEN_REPLAY=1 scripts/ai/run_golden_traces.sh
result = PASS (289 cases: 98 executable, 186 validator executable,
5 schema-only scoped)

scripts/ai/check_backend_runtime_risk_inventory.sh
result = PASS (29,126 signals in 1,215 files)

scripts/ai/verify_release.sh current-governance backend-freeze-baseline
result = PASS

scripts/ai/verify_fast.sh
result = PASS

direct #[ignore] attribute scan
result = 20 (down from 23 before PAR-004)

git diff --check
result = PASS
```

The inventory conservatively classifies inline tests in
`crates/trading/src/strategy/mod.rs` as production-owned. Its production count
therefore increases by seven even though `#[cfg(test)]` is the only changed
section. PAR-011 already owns the planned path-versus-region attribution
improvement.

## Baseline Precision Finding

The combined full high-precision command reaches an unrelated existing TWAP
fixture failure:

```text
cargo test -p nautilus-trading -p nautilus-risk \
  --features nautilus-model/high-precision
result = FAIL outside PAR-004
test = algorithm::twap::tests::test_twap_calculates_size_schedule_with_remainder
left raw = 3333333333333333
right raw = 333333333
```

All PAR-004 cases pass in high precision and the complete high-precision risk
target passes. The TWAP test uses a standard-precision raw literal and requires
its own scoped precision-fixture task; it is registered as GitHub issue #1185
(`PAR-004A`) and is not modified in this PR.

## Review State

Independent gatekeeper review session:

```text
session = 019fabfa-1230-7583-8aac-d94d934e39da
first pass = P2 formal blocker row still described PAR-004 work as open
repair = DRG8-BLOCKER-004 now records Closed by PAR-004 with exact endpoint
         and no-RiskEngine evidence
rereview = pending
```

Hosted gate state remains pending. Auto-merge is prohibited.

## Rollback

Revert the PAR-004 PR and restore the three blocker entries. Do not restore
empty ignored tests as evidence; a rollback means emulator routing is again
unproven for capability-readiness purposes.
