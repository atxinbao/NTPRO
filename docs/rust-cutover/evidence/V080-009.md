# V080-009 Release Closure Evidence

Date: 2026-06-17
Executor: Codex
Task: V080-009 Record v0.8.0 release closure evidence after owner approval
Status: PASS

## Owner Approval

Owner-approved instruction in Codex thread:

```text
批准创建 ntpro-rust-only-v0.8.0 tag，并发布正式 GitHub Release，release name 使用 NTPRO Rust-only v0.8.0，说明使用当前 main 上的 v0.8.0 release notes。
```

## Release Commit

```text
commit = f0da02717a498ce237459ffa6053a2f95800d4bc
source = origin/main
tag = ntpro-rust-only-v0.8.0
tag type = annotated
tag message = NTPRO Rust-only v0.8.0
```

The tag points at the merged V080-008 release-candidate documentation commit:

```text
V080-008 prepare v0.8 release candidate docs (#346)
```

## Hosted Release Gates

Workflow-dispatch release gate:

```text
workflow = Rust Cutover Release Gate
event = workflow_dispatch
run = https://github.com/atxinbao/NTPRO/actions/runs/27637885139
headSha = f0da02717a498ce237459ffa6053a2f95800d4bc
status = completed
conclusion = success
createdAt = 2026-06-16T18:05:21Z
updatedAt = 2026-06-16T19:00:19Z
```

Tag-triggered release gate:

```text
workflow = Rust Cutover Release Gate
event = push tag ntpro-rust-only-v0.8.0
run = https://github.com/atxinbao/NTPRO/actions/runs/27641087223
headSha = f0da02717a498ce237459ffa6053a2f95800d4bc
status = completed
conclusion = success
createdAt = 2026-06-16T19:01:40Z
updatedAt = 2026-06-16T20:19:56Z
```

Note: the first tag-triggered attempt had a `full-rust-tests-workspace-adapters-a`
job-level failure without a Rust test failure log. Only failed jobs were rerun,
and the final run conclusion is `success`.

## GitHub Release

```text
tagName = ntpro-rust-only-v0.8.0
name = NTPRO Rust-only v0.8.0
url = https://github.com/atxinbao/NTPRO/releases/tag/ntpro-rust-only-v0.8.0
publishedAt = 2026-06-16T20:20:57Z
isDraft = false
isPrerelease = false
```

## Scope Boundary

This release is scoped to authenticated Binance testnet read-only proof only.

Included:

```text
authenticated Binance testnet read-only proof
manual-online-only authenticated account-shape validation
env-only credentials
redacted artifact evidence
synthetic secret leak checks
Dashboard read-only artifact display
```

Not included:

```text
testnet order placement
order cancel/replace/amend
account mutation
production Binance connectivity
production trading
real funds
production parity
Dashboard-started probes
Dashboard credential entry
raw account body persistence
balance/uid/account-identifying persistence
```

## Manual Online Proof

Manual online proof was not run during this release closure. This closure uses
the hosted offline/default gates plus the authenticated read-only preflight.

Therefore this release must not be described as real Binance production
readiness, real-funds readiness, production trading readiness, or order
submission readiness.

## Behavior Impact

No runtime behavior changed in this closure evidence update. The tag and GitHub
Release publish existing `main` artifacts and documents.

## Public API Impact

No public API changed.

## Migration Note Status

No migration note is required for this closure evidence update.

## Rollback Plan

If the release must be withdrawn:

1. Mark the GitHub Release as draft or delete it.
2. Delete or supersede the `ntpro-rust-only-v0.8.0` tag.
3. Add a corrective release note explaining the withdrawal.
4. Keep this evidence file as the audit record of the original publication.
