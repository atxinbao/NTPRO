# V090-014 Release Closure Evidence

Date: 2026-06-18
Executor: Codex
Task: V090-014
Status: PASS

## Goal

Record the exact owner-approved publication evidence for
`ntpro-rust-only-v0.9.0`.

## Owner Approval

The owner approved creating the `ntpro-rust-only-v0.9.0` tag and publishing a
formal GitHub Release with release name `NTPRO Rust-only v0.9.0`, using the
current `main` v0.9.0 release notes as the source.

## Release Identity

```text
Tag: ntpro-rust-only-v0.9.0
Release name: NTPRO Rust-only v0.9.0
Release URL: https://github.com/atxinbao/NTPRO/releases/tag/ntpro-rust-only-v0.9.0
Release commit: 83b333503a5c8e8436c98f54a4d94c4a50f919a8
Published at: 2026-06-18T07:57:04Z
Draft: false
Prerelease: false
```

The remote annotated tag resolves to the release commit:

```text
83b333503a5c8e8436c98f54a4d94c4a50f919a8 refs/tags/ntpro-rust-only-v0.9.0^{}
```

## Hosted Release Gates

Workflow-dispatch release gate before tag publication:

```text
Workflow: Rust Cutover Release Gate
Run: https://github.com/atxinbao/NTPRO/actions/runs/27738080665
Event: workflow_dispatch
Head SHA: 83b333503a5c8e8436c98f54a4d94c4a50f919a8
Result: success
Jobs: 34/34 successful
```

Tag-triggered release gate after pushing `ntpro-rust-only-v0.9.0`:

```text
Workflow: Rust Cutover Release Gate
Run: https://github.com/atxinbao/NTPRO/actions/runs/27742550316
Event: push
Head branch/tag: ntpro-rust-only-v0.9.0
Head SHA: 83b333503a5c8e8436c98f54a4d94c4a50f919a8
Result: success
Jobs: 34/34 successful
```

## Local Validation

Before creating the tag, Codex ran the release-critical v0.9 local gates:

```bash
scripts/ai/verify_release.sh release-build-product-surface rust-only-gates v09-strategy-runtime-smoke v09-shadow-mode-no-order-gate
scripts/ai/verify_full.sh rust-tests-workspace-runtime
```

Observed result:

```text
verify_release targeted gates: PASS
verify_full rust-tests-workspace-runtime: PASS
```

## Scope Boundary

This release remains scoped to the v0.9.0 Strategy Runtime Foundation:

```text
Included: local Strategy Session runtime, fixture/mock market stream, signal
artifacts, shadow order-intent artifacts, shadow rejected risk-decision
artifacts, supervisor read-only status, Dashboard read-only artifact/status
display.

Excluded: Binance testnet order submission, production order submission,
real funds, production trading, Dashboard order buttons, Dashboard credential
input, and automatic authenticated exchange probes.
```

Plain Chinese summary: v0.9.0 已正式发布，但它只代表本地策略运行时地基完成。
它不是 Binance testnet 下单版本，不支持真实资金，不支持生产交易，也不允许
Dashboard 下单。

## Behavior Impact

No runtime code changed in this closure task. The tag and GitHub Release publish
the already-merged v0.9.0 Strategy Runtime Foundation release surface.

## Public API Impact

No new public API is introduced by this closure task.

## Migration Note

No migration action is required. The release notes keep the v0.9.0 boundary and
defer Binance testnet order lifecycle proof to v0.10.0.

## Rollback Plan

If publication must be rolled back:

1. Delete or mark the GitHub Release as withdrawn.
2. Delete the remote tag only with explicit owner approval.
3. Revert this evidence PR after the release surface is withdrawn.

