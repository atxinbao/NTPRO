# DRG-003 Evidence - Full verification gate

Date: 2026-06-06
Executor: Codex
Task ID: DRG-003

## 修改目标

把 v0.2 Design Readiness 的 G2 full verification gate 从未执行状态推进为
明确 `PASS`，并记录完整本地验证证据。这个任务只证明当前完整验证链路可跑通，
不把后续产品设计、CLI runtime、dashboard 或 adapter 能力标记为已完成。

## 文件变更

- `.agentflow/state/task_status.json`：将 `DRG-003` 推进到 `PR_OPEN`。
- `.agentflow/leases/DRG-003.json`：补充本次实际触达路径和验证摘要。
- `docs/rust-cutover/release/v0_2_readiness_report.md`：记录 `G2 Full verification: PASS`。
- `docs/rust-cutover/evidence/DRG-003.md`：新增本 evidence。
- `crates/cli/src/backtest.rs`：内部 helper 改为借用 option 参数，消除 clippy `needless_pass_by_value`。
- `crates/cli/src/config.rs`：内部 helper 改为借用 option 参数，消除 clippy `needless_pass_by_value`。
- `crates/cli/src/data.rs`：内部 helper 改为借用 option 参数，消除 clippy `needless_pass_by_value`。
- `crates/cli/src/sandbox.rs`：内部 helper 改为借用 option 参数，消除 clippy `needless_pass_by_value`。
- `crates/execution/tests/matching_engine.rs`：移除测试里的冗余 `clone`。
- `crates/live/src/node.rs`：把 live startup helper 的控制项和事件接收器分组，消除 clippy `too_many_arguments`。

## 命令输出摘要

```text
cargo fmt --check
结果：PASS
```

```text
cargo check --workspace --features "arrow,ffi,high-precision,streaming,defi"
结果：PASS
摘要：workspace full-feature check 完成，无编译失败。
```

```text
cargo clippy --workspace --lib --tests --features "arrow,ffi,high-precision,streaming,defi" -- -D warnings
结果：PASS
摘要：修复 DRG-003 本轮暴露的 redundant clone、too_many_arguments 和 needless_pass_by_value 后通过。
```

```text
scripts/ai/verify_fast.sh
结果：PASS
摘要：fast verification 完成，无失败。
```

```text
scripts/ai/verify_full.sh
结果：PASS
摘要：完整门禁完成。覆盖 fast checks、workspace clippy、workspace Rust tests、
golden trace schema validation、golden trace replay tests，以及
cargo doc --workspace --features arrow,ffi,high-precision,streaming,defi --no-deps。
最后输出：== verify_full complete ==
```

## 行为影响

没有修改交易语义、订单生命周期、adapter 行为、runtime wiring 或 CLI 对外命令契约。
本轮源码改动都是为通过当前 clippy 和 full verification gate 的机械性清理。

`verify_full` 运行期间仍观察到 runtime 日志 banner 里保留 upstream
`NAUTILUS TRADER` / `Nautech Systems Pty Ltd.` 字样。这属于后续公开身份清理
或 runtime banner cleanup 范围，不影响 DRG-003 对完整验证门禁的 PASS 判断。

## Public API 影响

无。CLI public command shape、Rust crate public API 和 adapter contract 均未改变。

## Migration note 状态

不需要 migration note。本任务未引入 public API 或配置迁移。

## 已知边界

- 本 evidence 不表示 G3-G9 已通过。
- 本 evidence 不表示 v0.2 已 tag-ready。
- pre-existing ignored tests 没有在本任务中新增或删除。

## Rollback plan

回滚本 PR 可恢复 DRG-003 的状态、readiness 报告和本次 clippy 机械修正。
如回滚后 full verification 再次失败，应按失败输出重新记录 blocker，而不是把
G2 标记为 PASS。
