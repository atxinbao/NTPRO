# V041-003 Evidence - explicit v0.4 Binance sandbox smoke gate

Date: 2026-06-13
Executor: Codex
Task ID: V041-003

## Plain Chinese Summary

本任务新增一个明确命名的 v0.4 Binance sandbox smoke 脚本。

大白话说：以前 v0.4 的证据分散在 Binance replay、EMA、RSI、mock order、
risk rejection 和 Dashboard 测试里。现在有一个入口可以一次性跑这些 scoped
证据，方便 reviewer 看清楚这条发布路径。它没有新增实盘能力，也没有扩大测试矩阵。

## Files Changed

- `scripts/ai/verify_v04_binance_sandbox.sh`
  - Runs the existing V04 fixture replay test.
  - Runs the existing EMA and RSI smoke tests.
  - Runs the existing mock order lifecycle test.
  - Runs the existing risk rejection smoke test.
  - Runs the existing Dashboard Binance sandbox read-model test.

## Commands Run

```bash
scripts/ai/verify_v04_binance_sandbox.sh
```

Result: PASS.

Summary:

- Binance fixture replay: 2 passed.
- EMA strategy smoke: 4 passed.
- RSI strategy smoke: 4 passed.
- Mock order lifecycle: 3 passed.
- Risk rejection smoke: 1 passed.
- Dashboard Binance sandbox read-model: 1 passed.
- Final boundary line:
  `scope=Binance sandbox-only no_real_funds=true no_production_trading=true real_orders_submitted=false`.

```bash
scripts/ai/verify_fast.sh
```

Result: PASS. The script confirmed Cargo/Rust `1.95.0`, ran `cargo fmt
--check`, and reported that workspace `cargo check`, clippy, and release gates
are outside the fast-smoke default.

```bash
git diff --check
```

Result: PASS.

## Behavior Impact

No runtime behavior changed.

No trading-semantic behavior changed.

No adapter behavior changed.

## Public API Impact

No public API change.

No CLI command shape changed.

## Migration Note Status

No migration note required. This task adds a verification script only.

## Rollback Plan

Revert:

- `scripts/ai/verify_v04_binance_sandbox.sh`
- `docs/rust-cutover/evidence/V041-003.md`

Rollback removes the named v0.4 smoke entrypoint while leaving the underlying
V04 tests unchanged.
