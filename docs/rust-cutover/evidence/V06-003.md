# V06-003 Evidence - dry-run and optional testnet runtime

Date: 2026-06-13
Executor: Codex
Task ID: V06-003

## Plain Chinese Summary

本任务新增了 workflow mode 和 optional testnet-network intent flag。

大白话说：`binance-testnet` 默认是 `dry-run`。CLI 也能解析
`--mode connectivity-probe` 和 `--allow-testnet-network`，但 v0.6 release gate 仍然只生成
离线 artifact，不打开网络连接。

## Files Changed

- `crates/cli/src/opt.rs`
- `crates/cli/src/workflow.rs`
- `docs/rust-cutover/tasks/V06-003.md`
- `docs/rust-cutover/evidence/V06-003.md`

## Commands Run

```bash
cargo test -p nautilus-cli workflow --lib
```

## Validation Summary

Result: PASS.

Covered parser test:

- `parses_workflow_binance_testnet_options`

## Behavior Impact

Adds CLI parsing and offline artifact behavior only.

## Public API Impact

Adds `--mode`, `--config`, and `--allow-testnet-network` to `workflow run`.

## Migration Note Status

No migration note required.

## Rollback Plan

Revert the workflow option additions.
