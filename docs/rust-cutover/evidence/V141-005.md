# V141-005 Evidence - CLI Help Text Boundary

Date: 2026-06-22
Executor: Codex

## Summary

V141-005 updates `nautilus live` help text so the command group is not described
as sandbox-only after production read-only and live-alpha dry-run proof commands
were added.

Plain Chinese summary: 这次只改 CLI 帮助文案和测试。用户看到 `live` 命令时，会知道这里既有
本地 live/sandbox smoke，也有生产只读证据和 dry-run 证据命令；所有 production 命令都明确
写着不允许 production mutation。

## Changed

- Replaced the `live` command group summary with:
  `Local live, production read-only, and dry-run proof commands`.
- Added `no production mutation` wording to every production read-only, shadow,
  and live-alpha dry-run command description.
- Added a clap help-rendering test covering the new wording and rejecting the
  old sandbox-only group description.

## Not Changed

- No production order submission.
- No production order mutation, cancel, replace, amend, retry, or correction.
- No listenKey lifecycle.
- No Dashboard order controls.
- No real funds or real orders.
- No runtime behavior changes.

## Validation

Planned / executed commands for this task:

```text
cargo test -p nautilus-cli live_help_describes_production_readonly_and_dry_run_boundaries --lib
cargo test -p nautilus-cli live_help --lib
cargo check -p nautilus-cli
scripts/ai/verify_fast.sh
cargo fmt --all --check
git diff --check
```
