# V210-001 Evidence - Unified read model contract and schema

Date: 2026-06-30
Executor: Codex
Task: `V210-001` / GitHub issue `#652`
Milestone: `v0.21.0`
Status: LOCAL VALIDATION PASSED

## Summary

V210-001 defines the v0.21 unified read model contract and schema for account,
position, order, fill, risk, and lifecycle status. It adds schema-only golden
trace smoke coverage and a local verifier that proves missing lineage, source
provenance, or freshness cannot be marked healthy.

Plain Chinese summary: 本任务建立 v0.21 read model 的契约和 schema。它只定义
snapshot identity、source provenance、lineage、freshness、redaction 和 fail-closed
规则，不接入新 submit，也不新增 Dashboard 操作控件。

## Task Source

```text
GitHub issue = #652 V210-001 Unified read model contract and schema
Depends on = #651 V210-000, closed at 2026-06-30T16:56:06Z
Task file = not present in docs/rust-cutover/tasks as of 2026-06-30
Owner role = verification_release_gatekeeper / control scope contract work
Review role = control_scope_agent
Risk level = medium docs, schema, golden trace validator, verifier target
Runtime behavior change = none
```

## Contract Evidence

```text
contract doc = docs/rust-cutover/release/v0_21_0_unified_read_model_contract.md
JSON schema = docs/rust-cutover/release/v0_21_0_unified_read_model_schema.json
golden trace smoke = tests/golden/read_model_contract_schema.jsonl
verifier = scripts/ai/verify_v21_read_model_contract.sh
verify target = scripts/ai/verify_release.sh v21-read-model-contract
golden trace category = read_model
```

## Boundary

```text
new_submit_capability = false
production_order_submission_allowed = false
production_order_mutation_allowed = false
dashboard_order_controls_enabled = false
dashboard_approval_controls_enabled = false
dashboard_cancel_controls_enabled = false
dashboard_retry_controls_enabled = false
retry_replace_amend_flatten_allowed = false
product_grade_trading_terminal_claim = false
```

## Validation

```text
bash -n scripts/ai/verify_v21_read_model_contract.sh scripts/ai/verify_release.sh = PASS
jq empty docs/rust-cutover/release/v0_21_0_unified_read_model_schema.json docs/rust-cutover/golden_trace/RELEASE_REPLAY_SCOPE.json = PASS
scripts/ai/golden_trace_runner.py tests/golden/read_model_contract_schema.jsonl --mode validate-only = PASS, 2 rows
scripts/ai/verify_release.sh v21-read-model-contract = PASS
RUN_RUST_*_TRACE_REPLAY=0 scripts/ai/run_golden_traces.sh = PASS, all JSONL schema validation only
python3 scripts/ai/validate_golden_trace_release_scope.py --manifest docs/rust-cutover/golden_trace/RELEASE_REPLAY_SCOPE.json --trace-glob 'tests/golden/*.jsonl' = PASS, 53 cases, 50 executable replay, 3 schema-only scoped
scripts/ai/verify_fast.sh = PASS
git diff --check = PASS
```

## Behavior Impact

No runtime behavior change, no adapter behavior change, no public API change,
and no trading capability expansion. This task adds a contract, schema,
schema-only golden trace rows, and verifier coverage.

## Public API Impact

None. The contract is a v0.21 internal release/read-model contract baseline for
later V210 implementation tasks.

## Rollback Plan

Revert this PR to remove the v0.21 read model contract, schema, read_model
golden trace category, schema-only smoke rows, and verifier target.
