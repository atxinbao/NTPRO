# BFH-007 Repository Hygiene Guard And Phase-1 Closeout Evidence

Date: 2026-07-19
Executor: Codex
GitHub issue: #1118
Milestone: post-freeze-backend-hygiene
Status: PRE-MERGE VALIDATION PASSED

## Summary

BFH-007 adds one active Rust guard for all phase-1 repository hygiene
decisions. It validates generated output, stale routes, cleanup authority, and
large fixture identity, then proves fail-closed behavior with negative
mutations.

Plain Chinese summary: guard 已覆盖 BFH-002 至 BFH-006 的关键规则。它不会自动删除
文件，而是在生成物被提交、旧入口回流、清理范围扩大或 fixture 权威漂移时直接失败。
本任务不修改 runtime、fixture 内容或 v0.32.0 冻结发布文件。

## Guard Coverage

```text
tracked generated roots = forbidden
.DS_Store outside protected tool state = forbidden
required generated-output ignore rules = required
retired ci-pr-wheel and stale active routes = forbidden
current Rust-only contributor routes = required
Make build/generated cleanup allowlists = exact
Make protected local-state allowlist = exact and disjoint
git clean and retired cleanup aliases = forbidden
large fixture inventory = schema/owner/policy/summary validated
tracked large fixtures = size/hash/baseline Git reconstruction validated
ignored large fixtures = ignore/checksum/metadata validated
existing local ignored caches = size/hash validated when present
unregistered existing fixture at or above 1 MiB = forbidden
size-only or network-substitute deletion = forbidden
```

## Source And Live GitHub State

At pre-merge capture:

```text
source baseline = origin/main 6a1651b36b7832b3b1e08ebdd0bfc975a8a23da3
BFH-001 through BFH-006 issues closed = 6/6
BFH-001 through BFH-006 PRs merged = #1127-#1132
prior hosted smoke runs completed/success = 6/6
BFH-007 issue #1118 = open
milestone #34 = open, open issues=1, closed issues=6
open overlapping PRs before BFH-007 = 0
audit strategy = source_tree_plus_github_remote
```

The current PR cannot contain its own final merge SHA or post-merge hosted
result. After merge, live GitHub must prove issue #1118 closed, the current PR
merged, its smoke run succeeded, milestone #34 has 0 open and 7 closed issues,
and the milestone is closed.

## Change Boundary

```text
runtime or adapter behavior changed = false
fixture content changed or deleted = false
Cargo dependency or lockfile changed = false
public API or schema changed = false
docs/rust-cutover/release/v0_32_0_* changed = false
registered backend boundary changed = false
new backend capability or patch release = false
```

## Validation

```text
ntpro-governance unit tests = PASS, 30/30
targeted ntpro-governance Clippy = PASS, -D warnings
backend hygiene guard = PASS, tracked_files=5272, fixture_entries=17
tracked fixture hashes = PASS, 14/14
local ignored fixture hashes = PASS, 3/3
backend hygiene negative selftest = PASS, cases=10
current governance = PASS
docs/examples governance = PASS, markdown_files=117, local_links=293
zero-Python closeout = PASS, tracked_files=5272, active_scripts=56,
  workflow_actions=17, historical_docs=1946, negative_cases=10
backend freeze baseline = PASS, boundaries=27, source_hashes=4
backend freeze negative selftest = PASS, cases=20
historical release retirement = PASS, retired=263, restored_blobs=263
scripts/ai/verify_fast.sh = PASS
git diff --check = PASS
repository target after validation = absent
external BFH-007 Cargo target after final validation = cleaned, 927.6 MiB removed
protected local state after cleanup = retained, 5/5
```

Commands:

```bash
CARGO_TARGET_DIR="${TMPDIR:-/tmp}/ntpro-bfh007-target" \
  cargo test --locked -p ntpro-governance
CARGO_TARGET_DIR="${TMPDIR:-/tmp}/ntpro-bfh007-target" \
  cargo clippy --locked -p ntpro-governance --all-targets -- -D warnings
CARGO_TARGET_DIR="${TMPDIR:-/tmp}/ntpro-bfh007-target" \
  scripts/ai/check_backend_hygiene.sh
CARGO_TARGET_DIR="${TMPDIR:-/tmp}/ntpro-bfh007-target" \
  scripts/ai/verify_release.sh current-governance backend-freeze-baseline
CARGO_TARGET_DIR="${TMPDIR:-/tmp}/ntpro-bfh007-target" \
  scripts/ai/verify_fast.sh
git diff --check
```
