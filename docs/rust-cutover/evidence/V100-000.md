# V100-000 Evidence

Date: 2026-06-19
Executor: Codex
Task: V100-000 Define Binance testnet order boundary

## Goal

Define the v0.10.0 Binance Testnet Order Proof boundary before any order-path
implementation starts.

## Changes

- Added `docs/rust-cutover/tasks/V100-000.md`.
- Added `docs/rust-cutover/release/v0_10_0_order_boundary.md`.
- Updated `docs/rust-cutover/release/README.md` to index the v0.10.0 boundary.
- Updated `README.md` to point at the v0.10.0 boundary without claiming release.

## Boundary Confirmed

```text
v0.10.0 = Binance Testnet Order Proof
default = offline fail-closed
manual online submit/cancel = owner-gated
production Binance = excluded
real funds = excluded
Dashboard order controls = excluded
```

Plain Chinese summary: 这次只写清楚边界。v0.10.0 未来可以证明 Binance
testnet 上的一笔小额订单提交后撤单，但必须手动批准、手动开 gate、只用 testnet。
本 PR 没有实现下单，也没有打开生产交易能力。

## Commands

```bash
scripts/ai/verify_fast.sh
rg -n "v0\\.10\\.0|Binance Testnet Order Proof|owner/manual|manual-gated|no real funds|production trading|Dashboard order controls|/api/v3/order/test|/api/v3/order|testnet\\.binance\\.vision" \
  README.md \
  docs/rust-cutover/release/README.md \
  docs/rust-cutover/release/v0_10_0_order_boundary.md \
  docs/rust-cutover/tasks/V100-000.md \
  docs/rust-cutover/evidence/V100-000.md
rg -n "production trading ready|real funds ready|Dashboard order controls enabled|automatic order submission enabled" \
  docs/rust-cutover/release/v0_10_0_order_boundary.md \
  docs/rust-cutover/tasks/V100-000.md
git diff --check
```

## Validation Result

- PASS: `scripts/ai/verify_fast.sh`
- PASS: boundary keyword check confirmed v0.10.0 sequencing, testnet endpoint
  allowlist, manual gate wording, and no-production/no-real-funds boundaries.
- PASS: forbidden wording check found no misleading readiness claims.
- PASS: `git diff --check`

## Behavior Impact

No runtime behavior changes. This is documentation and release-boundary
definition only.

## Public API Impact

No public API impact.

## Migration Note

No migration note required.

## Rollback Plan

Revert the PR to remove the v0.10.0 boundary document, task record, evidence,
and index entries.
