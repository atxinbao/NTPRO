# MVP-012 - 单节点故障矩阵与人工恢复验收证据

Date: 2026-08-04
Executor: Codex
GitHub issue: #1231
Status: REVIEW_REQUIRED

## 实现状态

- 新增标准 Node.js 故障矩阵验收器，只调用真实 `nautilus` 和 `ntpro-node`；
- 每个进程案例使用独立、初始不存在的临时 workspace；
- 工件案例在真实节点 SIGSTOP 后注入，随后恢复原始可信工件并 SIGCONT；
- 外部 SIGTERM 使用既有 operator start，hard-kill 使用既有 Supervisor CLI 显式 start；
- hard-kill 后先验证 registry stale、共享状态 fail closed、控制中心 stop 失败，以及观察窗口
  内没有自动新 PID；
- 初次真实运行确认 invalid status 会按预期把仍存活节点标记 stale，但可信 status 恢复后
  从不重新验证，导致节点永久卡死；任务已提升为 high risk，并限定修复为原 PID 存活且
  PID/进程/status 身份、完整 started_at 和 OS 进程启动时间指纹全部匹配时重新归类
  running，不放宽 hard-kill、PID 复用或身份失败边界；
- 所有结果写入 `ntpro.mvp_fault_matrix_evidence.v1`，日志和 JSON 写入前执行 token/cookie
  脱敏检查。

## 本地验收证据

- 完整故障矩阵：`11/11` 通过，其中工件故障 9 个、进程故障 2 个；
- 人工恢复边界：通过，未观察到自动 retry、restart、remediation 或 recovery；
- fail-closed 边界：通过，故障状态下交易就绪保持 blocked，required-false 字段保持 false；
- MVP Rust 测试：`47 passed`；
- Supervisor Rust 测试：`38 passed`；审查修正后增加 14 类严格身份反例、首次启动身份
  补全和已建立身份不可重新补全回归，定向 refresh status 为 `5 passed`；
- Dashboard Rust 测试：`172 passed`；
- 全 target / 全 feature Clippy：通过，warnings denied；
- risk inventory：通过，`29367` signals / `1221` files，production ownership 保持 `2697`；
- Rust-only、docs/examples governance、v0.32.0 backend freeze、current governance：全部通过；
- `result.json` SHA-256：
  `7faa9c4916c567c28f025005e07c4f81babc974c8768b35a5927ef12baed43df`；
- `mvp-fault-matrix.log` SHA-256：
  `203f9ac3ad6944a72d55918534e9d9b53f852321ed285e098217b172912b48d6`；
- token/cookie 扫描：0 命中；强制 token failure 返回非零且只写入 `[REDACTED]`。

## 首轮独立审查修正

Rust Product Surface Agent 对 head `e60386c152ecca53cea31937def15c85897e4096` 输出
`CHANGES_REQUESTED`：stale 恢复仍接受不完整身份、无自动恢复观察窗口不足、失败清理未
追踪全部 PID、cargo build 失败前没有 artifact。修正后：

- stale 恢复额外要求 PID artifact/status 完整、running + spawned_process、artifact root、
  started_at 与 OS 进程启动时间全部一致；14 类反例均保持 stale；
- SIGTERM 和 SIGKILL 各执行超过 6 秒、12 个样本的无动作观察，registry/PID/共享状态和
  lifecycle events 均保持不变，再允许人工 start；
- 每个 session 追踪所有观察过的节点 PID，删除临时 workspace 前逐个确认退出；
- hosted step 在 cargo build 前预创建 fail evidence，构建失败也能由 `always()` 上传。

第二次复审对 head `6f412a4986bab3a468b40bf05a75edef90ad7ba1` 指出首次启动补全
可能被运行后裁剪 `started_at` 重新触发。最终修正要求 PID identity 自身也必须处于从未
建立 `started_at` 的 initial pending 状态；已建立身份后，status 缺失或更换 started_at
都会保持 stale，且 PID identity 保留原值。完整序列回归已通过。

首个获批 head `3b643a5e50` 的 hosted smoke run `30924697127` 在 Linux hard-kill
投影超时。失败 artifact 已成功上传并脱敏。根因是 Linux zombie 仍可由 sysinfo 返回 OS
启动时间，而 stale 严格 matcher 尚未同时调用 `process_is_alive`；该函数已明确把 Zombie
判为不存活。最终修正要求“存活检查 + OS 启动时间 + 完整身份”同时成立，并为等待失败
增加最后一次共享投影诊断。修正后本地 11-case 矩阵再次通过，等待新 hosted run。

首次构建由 shell 中 Homebrew Rust 1.87.0 拒绝；随后将
`/Users/mac/.rustup/toolchains/1.95.0-aarch64-apple-darwin/bin` 置于 `PATH` 首位，使用仓库固定
Rust 1.95.0 完成全部 Rust 验证。该失败不属于产品代码失败。

## 待完成证据

- Rust Product Surface Agent 对最终提交的独立 review；
- hosted `mvp-fault-matrix-<PR_NUMBER>` artifact；
- PR 全部 required checks 通过并手动合并。

本文件在上述证据完成前不得标记 DONE，也不得据此关闭 M4。
