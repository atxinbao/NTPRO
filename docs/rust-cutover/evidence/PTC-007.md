# PTC-007 Python Toolchain And Packaging Removal Evidence

Date: 2026-07-17
Executor: Codex
GitHub issue: #1102
Milestone: python-tooling-closeout
Status: REVIEW_REQUIRED

## Removal Result

```text
tracked .py/.pyi/.pyx/.pxd/.ipynb files = 0
pyproject.toml = absent
uv.lock = absent
local .venv = absent (136 MiB removed)
Python cache directories = absent
wheel composite actions = absent
Python test shell entrances = absent
Python package/container publication workflows = absent
tracked change set = 44 deleted, 32 modified, 3 added files
```

## Retained Rust Authority

```text
Rust workspace build/check/clippy workflow = retained
split Rust workspace tests = retained
Rust CLI binary workflow = retained
Rust docs and feature checks = retained
Miri and deterministic simulation tests = retained
Rust security audit = retained
Rust cutover smoke and release gates = retained
local Postgres/Redis integration services = retained
```

## Validation

```text
shell syntax = pass
workflow/action/pre-commit YAML parse = pass
staged GitHub Action SHA verification = pass (portable parser corrected)
Makefile parse = pass
cargo-tool-version zizmor = pass (1.26.1)
tracked Python execution scan = pass (only fail-closed pattern fixtures remain)
cargo fmt --all -- --check = pass
cargo test -p ntpro-governance --locked = pass (22 tests)
cargo clippy -p ntpro-governance --all-targets -- -D warnings = pass
scripts/clippy-changed.sh = pass (full workspace)
scripts/doc-changed.sh = pass (full workspace)
scripts/ai/v02_two_node_supervisor_smoke.sh = pass (two nodes, running/stopped jq assertions)
scripts/ai/check_rust_only_runtime.sh = pass
scripts/ai/check_cython_removed.sh = pass
scripts/ai/verify_fast.sh = pass
current governance/backend freeze = pass
live v0.32.0 GitHub release reconstruction = pass
publish-after-gate deterministic matrix = pass (live mutation disabled)
cargo audit = pass with existing unmaintained warnings
cargo deny = pass with existing warnings
cargo vet = pass (serde_with and serde_with_macros 3.20.0 -> 3.21.0 deltas reviewed)
Zizmor 1.26.1 = pass (38 configured suppressions, 0 findings)
release-tag cache poisoning finding = fixed by disabling release-context cache
OSV Scanner = pass after `serde_with` 3.21.0 advisory fix
GHSA-7gcf-g7xr-8hxj = fixed (`serde_with` 3.20.0 -> 3.21.0)
prek configuration and targeted local hooks = pass
hosted smoke and security audit = pending
docs/rust-cutover/release/v0_32_0_* diff = empty
```

The first hosted security run found `GHSA-7gcf-g7xr-8hxj` in
`serde_with 3.20.0`. The lockfile was updated to 3.21.0, local OSV scanning
then reported no issues, and the affected blockchain dependency path compiled
successfully. The dependency and proc-macro deltas were reviewed and recorded
as `safe-to-deploy` cargo-vet audits; neither adds unsafe, build, network,
filesystem, or process behavior. Hosted checks must rerun on the corrected
commit.

This high-risk deletion set must not enable auto-merge. Issue #1102 remains
open until owner review and merge complete.
