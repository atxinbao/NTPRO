# PAR-009 Dashboard CLI Ownership Boundary Split Evidence

Date: 2026-07-29
Executor: Codex
GitHub issue: #1177
Status: LOCAL VALIDATION PASSED / REVIEW_APPROVED / HOSTED_CHECKS_PENDING

## Ownership Result

```text
baseline crates/cli/src/dashboard.rs = 25,680 lines
current crates/cli/src/dashboard.rs = 12,731 lines
current dashboard/rendering.rs = 1,836 lines
current dashboard/tests.rs = 11,106 lines
HTTP transport owner = dashboard/server.rs
read-only v0.28 API owner = dashboard/trader_terminal_api.rs
read-model and projection owner = dashboard.rs
static asset owner = dashboard/rendering.rs
behavioral regression owner = dashboard/tests.rs
```

The split changes source ownership only. It does not change route registration,
snapshot projection, serialized contracts, rendered content, or capability
boundaries.

## Move Fidelity

The current three files were mechanically reconstructed from the exact
`origin/main` parent source, then formatted with the repository toolchain:

```text
dashboard.rs reconstruction diff = empty
rendering.rs extraction diff = empty
tests.rs extraction diff =
  one required include_str relative-path adjustment
  one module ownership structural regression
unwrap calls before/after = 372 / 372
expect calls before/after = 6 / 6
panic! calls before/after = 0 / 0
```

The `include_str!` target is unchanged; only the relative path gains one parent
segment because the test source moved under `dashboard/`.

## Risk Inventory Rebase

Moving inline tests from a mixed production-owned source file to
`dashboard/tests.rs` changes path-scoped ownership without changing signal
counts:

```text
total signals = 29,132 -> 29,132
matched files = 1,216 -> 1,217
production signals = 13,351 -> 12,988
test signals = 14,461 -> 14,824
signal counts by type = unchanged
canonical hash =
  9abcec1adcc97c57a4fae27d4e18426baae0a5b35f3d30f04817731cf1b58866
```

## Validation

```text
nautilus-cli library compile after extraction = PASS
module ownership regression = PASS
dashboard shell/render marker regression = PASS
dashboard HTTP route regression = PASS
v0.28 read-only HTTP method and response regression = PASS
complete nautilus-cli standard-precision library suite =
  PASS (508 passed, 0 failed, 0 ignored)
complete nautilus-cli high-precision library suite =
  PASS (508 passed, 0 failed, 0 ignored)
Clippy all targets/all features/high precision = PASS
mechanical extraction and reconstruction = PASS
golden replay = PASS
  289 total
  98 executable
  186 validator executable
  5 schema-only scoped
backend runtime risk inventory =
  PASS (29,132 signals / 1,217 files)
current governance + backend freeze baseline = PASS
backend freeze negative self-test = PASS (20 cases)
verify_fast with workspace cargo check = PASS
cargo fmt --all --check = PASS
git diff --check = PASS
v0.32.0 frozen file diff = empty
```

## API And Migration

Rust public API changed: false.

HTTP route, method, schema, snapshot, rendering, and output contract changed:
false.

Migration note required: false. This is an internal ownership-only module move.

## Review

Independent high-risk review checked move-only fidelity, module visibility,
HTTP/API/schema/rendering preservation, forbidden-control and redaction
fail-closed behavior, risk inventory ownership, and the v0.32.0 freeze
boundary.

```text
independent reviewer = 019fabfa-1230-7583-8aac-d94d934e39da
reviewed implementation head = 592cb1b4ca04e68f700067ed5e305920fb9dc894
review result = REVIEW_APPROVED
remaining P1/P2/P3 findings = 0
```

Final-head hosted checks are required before manual merge. Auto-merge is
forbidden.

## Rollback

Revert the PAR-009 PR as one unit. Do not use rollback to add trading controls,
mutation paths, retry, remediation, or alter the frozen capability boundary.
