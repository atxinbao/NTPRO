# BPO-008 Tag-Push Publication Binding Evidence

Date: 2026-07-21
Executor: Codex
GitHub issue: #1141
GitHub PR: #1142
Milestone: v0.33.0-backend-maintenance
Status: LOCAL VALIDATION PASSED; INDEPENDENT REVIEW PENDING

## Summary

BPO-008 closes a release-governance gap discovered after PR #1140 merged and
before any v0.33.0 tag or GitHub Release was created.

中文摘要：本任务只收紧发布依据。成功的 release gate 还必须由准确的 v0.33.0 tag
push 触发；同 SHA 的手动 run 或 `main` ref 都不能授权发布。

## Change Boundary

```text
runtime or adapter behavior changed = false
public API or schema changed = false
trading semantics changed = false
frozen v0.32.0 release files changed = false
v0.33.0 tag exists before corrective merge = false
v0.33.0 GitHub Release exists before corrective merge = false
forbidden capability inherited = false
```

## Negative Matrix

| Gate state | Expected |
| --- | --- |
| success + push + exact tag ref + exact SHA | accept |
| failure + push + exact tag ref + exact SHA | reject |
| success + workflow_dispatch + exact SHA | reject |
| success + push + main ref + exact SHA | reject |
| success + push + exact tag ref + wrong SHA | reject |

## Validation

```text
current-release-gates = PASS
current-governance = PASS
backend-freeze-baseline = PASS, boundaries=27, negative cases=20
v33-maintenance-release = PASS, exact issues=8, exact PR slots=8
v33 manifest boundaries = 27 explicit false
v33 manifest negative cases = 2/2 PASS
v33-strict-provenance = PASS, files=6
release-publish-after-gate positive scenarios = PASS
failed gate negative case = PASS
workflow_dispatch negative case = PASS
mismatched tag ref negative case = PASS
post-publication gate selector valid/manual/wrong-ref/wrong-SHA selftest = PASS, 4 cases
pre-gate public release and asset negative cases = PASS
shell syntax, JSON, workflow YAML, cargo fmt, git diff check = PASS
v0.33.0 tag before corrective merge = absent
v0.33.0 GitHub Release before corrective merge = absent
frozen v0.32.0 release files changed = false
```

Commands:

```bash
NTPRO_RELEASE_SURFACE_ALLOW_MISSING_TAG=1 \
NTPRO_RELEASE_PUBLICATION_ALLOW_OFFLINE=1 \
NTPRO_RELEASE_PUBLISH_AFTER_GATE_LIVE_CURRENT=0 \
NTPRO_V33_STRICT_OUTPUT=/tmp/ntpro-bpo008/current-release-strict.json \
  scripts/ai/verify_release.sh current-release-gates

NTPRO_RELEASE_SURFACE_ALLOW_MISSING_TAG=1 \
NTPRO_RELEASE_PUBLISH_AFTER_GATE_LIVE_CURRENT=0 \
  scripts/ai/verify_release_publish_after_gate.sh
```

## Rollback

Revert this corrective PR before tag creation. Do not create or rewrite a tag.
