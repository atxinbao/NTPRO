# V02-002 Node And Supervisor Product Contract Evidence

Date: 2026-06-06
Executor: Codex
Task ID: V02-002
Branch: `codex/V02-002-node-and-supervisor-product-contract`

## Plain Chinese Summary

这次把 v0.2 本地多节点底座的“产品合同”写清楚了，没有写代码。

合同里定义了 `ntpro-node` 是一个本地 Rust 运行时目标，`ntpro-supervisor`
是本地管理器，`node_id` 是本地唯一节点 ID；还规定了配置归属、进程模型、
本地目录布局、start/stop/status/logs/metrics 的边界，以及第一轮只支持
sandbox/mock/fixture 证据。

没有实现 Dashboard、control API、真实交易所连接、人工下单、分布式部署或
release/tag。

## Goal

Define the product contract for local node and supervisor behavior before V02
runtime implementation tasks begin.

## Changed Files

- `docs/architecture/local_multi_node_runtime_contract.md`
  - New node/supervisor product contract.
- `docs/rust-cutover/evidence/V02-002.md`
  - This evidence file.
- `.agentflow/state/task_status.json`
  - V02-002 execution state and previous task closeout state.
- `.agentflow/leases/V02-002.json`
  - V02-002 lease and validation summary.

## Validation Commands

Planned commands for this task:

```text
python3 -m json.tool .agentflow/state/task_status.json
python3 -m json.tool .agentflow/leases/V02-002.json
rg -n "ntpro-node|ntpro-supervisor|node_id|runs/local-nodes|sandbox-only|Local Multi-Node Runtime Foundation" docs/architecture/local_multi_node_runtime_contract.md docs/rust-cutover/evidence/V02-002.md
scripts/ai/verify_fast.sh
scripts/ai/validate_agentflow_roles.py
git diff --check
```

## Command Output Summary

```text
python3 -m json.tool .agentflow/state/task_status.json
PASS: agentflow task status JSON parsed successfully.
```

```text
python3 -m json.tool .agentflow/leases/V02-002.json
PASS: V02-002 lease JSON parsed successfully.
```

```text
rg -n "ntpro-node|ntpro-supervisor|node_id|runs/local-nodes|sandbox-only|Local Multi-Node Runtime Foundation" docs/architecture/local_multi_node_runtime_contract.md docs/rust-cutover/evidence/V02-002.md
PASS: found the required product contract markers.
```

```text
scripts/ai/verify_fast.sh
PASS: cargo 1.95.0, rustc 1.95.0, rustfmt fast smoke passed.
The script explicitly reports that cargo check, clippy, release gate, and
golden trace gate are outside fast-smoke scope.
```

```text
scripts/ai/validate_agentflow_roles.py
PASS: agentflow role protocol validation passed.
```

```text
git diff --check
PASS: no whitespace or patch-format errors.
```

## Behavior Impact

No runtime behavior changed. This task only adds architecture documentation,
evidence, and agentflow task state.

## Public API Impact

No public API changed. No CLI command, Rust crate API, adapter behavior, or
trading semantic changed.

## Migration Note Status

No migration note is required because no user runtime behavior changed.

## Rollback Plan

Revert this PR to remove the local multi-node runtime contract and V02-002
evidence. No runtime rollback is needed.

## Scope Boundary

V02-002 does not:

- implement `ntpro-node`;
- implement `ntpro-supervisor`;
- add Dashboard UI;
- add control API endpoints;
- connect to production venues;
- submit real orders;
- implement manual order entry;
- create release tags;
- publish GitHub Releases.
