# V04-010 Evidence - Dashboard exchange / strategy / order / risk panels

Date: 2026-06-13
Executor: Codex

## Task ID

`V04-010` - Dashboard exchange / strategy / order / risk panels

## Plain Chinese Summary

这次把本地 Dashboard 补成了能看懂 v0.4 Binance sandbox 业务状态的页面。页面新增了 `Binance 沙盒业务状态` 区块，分成交易场所、策略、订单、风控四个面板。

这些面板不是手写占位值：交易和策略状态来自已合并的 EMA/RSI smoke helper 与 Binance bar fixture；订单状态来自已合并的 mock order lifecycle JSONL；风控状态来自 mock 拒单事件和 v0.4 风控拒绝原因。它仍然只显示本地 sandbox 证据，不连接 Binance，不提交真实订单，也不新增手动下单能力。

## Files Changed

- `crates/cli/src/dashboard.rs`
  - Adds `sandbox_business` to the dashboard snapshot.
  - Adds exchange, strategy, order, and risk panel DTOs.
  - Loads deterministic v0.4 Binance sandbox evidence from checked-in fixture data and V04 strategy smoke helpers.
  - Renders the new business-facing panel group in the no-framework dashboard shell.
  - Extends Rust unit and HTTP snapshot tests to assert the new dashboard data.

## Dashboard Verification Summary

The dashboard now exposes:

- Exchange panel:
  - venue: `BINANCE`
  - instrument: `BTCUSDT.BINANCE`
  - bar type: `BTCUSDT.BINANCE-1-MINUTE-LAST-EXTERNAL`
  - fixture: `v04-binance-spot-bars`
  - external venue connection: `false`
- Strategy panel:
  - EMA smoke summary
  - RSI smoke summary
  - final signal and signal count from deterministic fixture replay
  - real orders submitted: `false`
- Order panel:
  - lifecycle: `v04-binance-mock-order-lifecycle`
  - event counts for submitted, accepted, filled, canceled, rejected
  - combined mock strategy order requests: `7`
  - real orders submitted: `false`
- Risk panel:
  - smoke: `v04-binance-risk-rejection-smoke`
  - rejected client order: `O-V04-003`
  - risk reason: `TradingState::HALTED`
  - forwarded to execution: `false`

## Commands Run

```bash
cargo fmt
source scripts/ai/toolchain_env.sh && cargo test -p nautilus-cli dashboard --lib
scripts/ai/v03_dashboard_smoke.sh
git diff --check
source scripts/ai/toolchain_env.sh && cargo clippy -p nautilus-cli --tests -- -D warnings
scripts/ai/verify_fast.sh
```

## Command Output Summary

- `cargo fmt`: passed.
- `cargo test -p nautilus-cli dashboard --lib`: passed, 25 tests.
- `scripts/ai/v03_dashboard_smoke.sh`: passed, local dashboard served and smoke completed with `status=ok`.
- `git diff --check`: passed.
- `cargo clippy -p nautilus-cli --tests -- -D warnings`: passed.
- `scripts/ai/verify_fast.sh`: passed. This is the project fast smoke only; it runs toolchain and rustfmt by default and does not replace release verification.

## Behavior Impact

- Adds local dashboard read-model and rendering for v0.4 Binance sandbox business evidence.
- Does not change trading semantics.
- Does not connect to Binance.
- Does not submit real orders.
- Does not add manual order entry.
- Does not add a frontend framework.

## Public API Impact

Additive Rust-only dashboard JSON shape:

- `DashboardSnapshot.sandbox_business`
- `SandboxBusinessStatus`
- `SandboxExchangePanel`
- `SandboxStrategyPanel`
- `SandboxOrderPanel`
- `SandboxRiskPanel`

No existing CLI command contract was removed.

## Migration Note Status

No migration note required. This is an additive local dashboard surface for the v0.4 sandbox product foundation.

## Rollback Plan

Revert the `crates/cli/src/dashboard.rs` changes from this task and remove this evidence file. The previous dashboard snapshot and rendering behavior will be restored.
