# V05-003 Evidence - atomic artifact writer

Date: 2026-06-13
Executor: Codex
Task ID: V05-003

## Plain Chinese Summary

本任务让 workflow artifact 复用已有的 CLI 原子写入工具。

大白话说：每个 JSON/JSONL 文件都是先写临时文件，再 rename 到目标路径。最后才写
`manifest.json`，所以 manifest 可以作为本次 workflow 完成的收口标记。

## Files Changed

- `crates/cli/src/workflow.rs`
- `docs/rust-cutover/tasks/V05-003.md`
- `docs/rust-cutover/evidence/V05-003.md`

## Commands Run

```bash
cargo test -p nautilus-cli workflow --lib
find /tmp/ntpro-v05-workflow-smoke -type f | sort
```

## Validation Summary

- Unit test `workflow_run_writes_manifest_last_and_all_artifacts`: PASS.
- CLI smoke generated all expected files.
- No new artifact writer framework was added.

## Behavior Impact

Workflow artifact writes are deterministic and use existing CLI artifact helpers.

## Public API Impact

No public API change beyond the new CLI command.

## Migration Note Status

No migration note required.

## Rollback Plan

Revert `crates/cli/src/workflow.rs` and remove the workflow command routing.
