# V03-001 Evidence - Scope decision / Dashboard system-status MVP

Date: 2026-06-07
Executor: Codex
Task ID: V03-001
Branch: `codex/V03-001-dashboard-scope`

## Plain Chinese Summary

本任务把 v0.3 的方向先锁住：v0.3 做本地 Dashboard MVP，也就是一个本地系统状态驾驶舱。

大白话说：这一步不是写页面、不是接交易所、不是做下单界面，而是先把边界说清楚。后续 Dashboard 只能展示本地 supervisor 管理的 node 状态、日志、指标、数据源、执行、风控和运行模块状态；可以做本地 sandbox node 的 start/stop 边界；不能做真实交易、手动下单、改单、撤单、复杂前端框架或分布式控制。

## Goal

Create the active v0.3 scope decision and record the V03 task sequence for the Dashboard MVP.

## Files Changed

- `docs/rust-cutover/scope/v0_3_dashboard_mvp.md`
  - Defines the active v0.3 Dashboard MVP scope.
  - Records in-scope, out-of-scope, product boundary, required panels, control policy, validation gates, and release boundary.
- `docs/rust-cutover/tasks/V03-001.md` through `docs/rust-cutover/tasks/V03-010.md`
  - Defines the ordered V03 task sequence.
- `.agentflow/state/task_status.json`
  - Adds V03 task metadata for V03-001 through V03-010.
  - Marks V03-001 as `RUNNING`.
  - Leaves V03-002 through V03-010 as not completed.
- `.agentflow/leases/V03-001.json`
  - Records the V03-001 lease and allowed touched paths.
- `/Users/mac/.codex/shrimp-data/NTPRO/tasks.json`
  - Marks V03-001 as `in_progress` in the NTPRO isolated Shrimp queue.

## Commands Run

```bash
git diff --check
```

Result: PASS.

```bash
python3 -m json.tool .agentflow/state/task_status.json >/dev/null
python3 -m json.tool .agentflow/leases/V03-001.json >/dev/null
python3 -m json.tool /Users/mac/.codex/shrimp-data/NTPRO/tasks.json >/dev/null
```

Result: PASS. All updated JSON files parse.

```bash
scripts/ai/validate_agentflow_roles.py
```

Result: PASS. Agentflow role protocol validation passed.

```bash
rg -n "V03-001|Dashboard MVP|Tag creation|GitHub Release|DashboardSnapshot|Runtime Modules" \
  docs/rust-cutover/scope/v0_3_dashboard_mvp.md \
  docs/rust-cutover/tasks/V03-*.md
```

Result: PASS. Required v0.3 scope and release-boundary markers are present.

```bash
scripts/ai/verify_fast.sh
```

Result: PASS.

Key output:

```text
== verify_fast: toolchain ==
cargo 1.95.0 (f2d3ce0bd 2026-03-21)
rustc 1.95.0 (59807616e 2026-04-14)
== verify_fast: rust fmt ==
== verify_fast complete: fast smoke only; release work still requires stronger verification ==
```

## Behavior Impact

No runtime behavior changed.

This task only records v0.3 scope, task sequence, and task-management state.

## Public API Impact

No public API changed.

The scope document defines future Dashboard product boundaries, but this task does not add DTOs, HTTP endpoints, UI, CLI commands, or runtime controls.

## Migration Note Status

No migration note is required.

This task does not change user-facing runtime behavior or remove compatibility surfaces.

## Review / Merge Status

V03-001 is low-risk control/scope work. It is ready for PR review after the branch is pushed.

## Rollback Plan

Revert the V03-001 PR to remove the v0.3 scope document, V03 task documents, V03-001 evidence, agentflow V03 metadata, and V03-001 lease. Reverting this PR does not affect the v0.2 release tag or runtime code.
