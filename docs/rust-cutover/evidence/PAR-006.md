# PAR-006 dYdX Reconnect Subscription Restoration Evidence

Date: 2026-07-29
Executor: Codex
GitHub issue: #1174
Status: LOCAL VALIDATION PASSED / REVIEW_REQUIRED

## Reproduction

The historical test failed under its original ignored mode:

```text
cargo test -p nautilus-dydx --test websocket \
  test_subscription_restoration_tracking -- --ignored --exact --nocapture
result = FAIL
error = Timeout waiting for condition after 5.0s
```

## Ownership Finding

```text
production reconnect owner = nautilus-network WebSocket controller
production replay owner = dydx FeedHandler::replay_subscriptions
failed behavior owner = dydx Axum test server disconnect_trigger
```

The production controller transitions the replacement socket to Active and
sends an internal `RECONNECTED` message. FeedHandler then replays preserved
subscription messages. The mock server checked a persistent boolean before
every receive, so the replacement socket inherited the same injected failure.

## Repair Boundary

```text
production Rust changed = false
fixture disconnect semantics = level-triggered to one-shot
ignored tests restored = 1
real credentials or exchange access = false
public API changed = false
live trading capability changed = false
v0.32.0 frozen files changed = false
```

## Validation

```text
target regression after repair = PASS
target regression repeated 20 times = PASS 20/20
complete websocket fixture = PASS (76 passed, 0 ignored)
complete dYdX adapter standard precision =
  PASS (325 unit + 24 data_client + 21 exec_client + 1 fixture_manifest +
        38 grpc + 61 http + 76 websocket)
complete dYdX adapter high precision =
  PASS (325 unit + 24 data_client + 21 exec_client + 1 fixture_manifest +
        38 grpc + 61 http + 76 websocket)
Clippy all targets/all features/high precision = PASS
golden replay = PASS
  289 total
  98 executable
  186 validator executable
  5 schema-only scoped
backend runtime risk inventory = PASS (29,137 signals / 1,215 files)
current governance + backend freeze baseline = PASS
verify_fast = PASS
direct ignored-test attributes = 18
git diff --check = PASS
v0.32.0 frozen file diff = empty
```

The complete golden replay remains green after the adapter fixture change.
No production source, credential, endpoint, or exchange interaction was used.

## Review

Independent critical review and hosted checks are required before manual
merge. Auto-merge is forbidden.

## Rollback

Revert the PAR-006 PR and restore the blocker entry. Do not restore the ignored
test as evidence; rollback means deterministic dYdX reconnect replay is again
unproven.
