# PAR-001 Matching Engine Contingent State Repair Evidence

Date: 2026-07-29
Executor: Codex
GitHub issue: #1169
Status: LOCAL VALIDATION PASSED / HOSTED REVIEW PENDING

## Baseline Reproduction

```text
cargo test -p nautilus-execution --test matching_engine \
  test_updating_of_contingent_orders -- --ignored --nocapture
result = FAIL: event count left 3, right 4

cargo test -p nautilus-execution --test matching_engine \
  test_ouo_child_cancelled_when_parent_leaves_zero -- --ignored --nocapture
result = FAIL: expected OrderCanceled, observed OrderUpdated quantity=0.400
```

## Repair Boundary

```text
parent snapshot source before = pre-event local clone
parent snapshot source after = current cache entry
contingent quantity propagation changed = corrected
zero-leaves child cancellation changed = corrected
public API changed = false
production trading capability changed = false
v0.32.0 frozen files changed = false
```

## Executable Evidence

The two restored `nautilus-execution` integration tests replay deterministic
order-book, order-submit, partial-fill, and modify event streams through the
real Rust matching engine. They assert the emitted order-event sequence and
quantities directly; schema validation is not used as a substitute.

## Validation

```text
cargo test -p nautilus-execution --test matching_engine \
  test_updating_of_contingent_orders
result = PASS

cargo test -p nautilus-execution --test matching_engine \
  test_ouo_child_cancelled_when_parent_leaves_zero
result = PASS

cargo test -p nautilus-execution --features high-precision \
  --test matching_engine <each restored test>
result = PASS (2/2)

cargo test -p nautilus-execution
result = PASS

cargo test -p nautilus-execution --features high-precision
result = PASS

cargo clippy -p nautilus-execution --all-targets --all-features -- -D warnings
result = PASS

scripts/ai/check_backend_runtime_risk_inventory.sh
result = PASS (28,973 signals in 1,215 files)

scripts/ai/verify_release.sh current-governance backend-freeze-baseline
result = PASS

scripts/ai/verify_fast.sh
result = PASS

direct #[ignore] scan
result = 26 (down from 28)

git diff --check
result = PASS
```

The risk inventory decreased by two test-owned `TODO` signals because PAR-001
removed the two stale TODO comments alongside the ignored attributes. No
production-owned signal count increased.

## Review State

This is a high-risk trading-semantic repair. It stops at `REVIEW_REQUIRED`;
hosted checks and a reviewer independent from the implementation must approve
the PR before manual merge.

## Rollback

Reverting the implementation would restore stale parent reads and reintroduce
the two default-suite gaps. A rollback must also restore the ignored-test
register counts and blocker states.
