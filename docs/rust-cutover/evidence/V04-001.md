# V04-001 Evidence - Binance product boundary contract

Date: 2026-06-13
Executor: Codex
Task ID: V04-001

## Plain Chinese Summary

这次只锁定 v0.4 的产品边界，没有改任何 Rust 运行时代码。

大白话说：v0.4 不是生产实盘版本，也不是“已经支持 Binance 真实交易”。
它只准备做 Binance sandbox 产品基础：本地 fixture replay、mock order、
EMA/RSI 示例策略、风险拒单证据和本地 Dashboard 状态展示。默认验证路径
是 fixture / testnet / mock first，不能用真实资金、真实账户或生产下单来
包装成 release 能力。

## Files Changed

- `README.md`
- `docs/rust-cutover/scope/v0_4_0_binance_sandbox_product_foundation.md`
- `docs/rust-cutover/evidence/V04-001.md`

## Commands Run

```bash
rg -n "sandbox|real funds|production trading|Binance Spot|Binance USDT-M" docs/rust-cutover/scope/v0_4_0_binance_sandbox_product_foundation.md
git diff --check
scripts/ai/verify_fast.sh
```

## Validation Summary

- `rg` PASS: scope contract explicitly mentions sandbox, real funds,
  production trading, Binance Spot, and Binance USDT-M boundaries.
- `git diff --check` PASS.
- `scripts/ai/verify_fast.sh` PASS: pinned Rust toolchain and `cargo fmt
  --check` passed. Cargo check and clippy were skipped by the fast-smoke
  default, as documented by the script output.

## Scope Decision Result

- `v0.4.0` product claim is `Binance Sandbox Product Foundation`.
- Binance Spot can be claimed only as a sandbox product foundation after local
  fixture/mock/risk/strategy/dashboard evidence exists.
- Binance USDT-M remains deferred unless later tasks add explicit sandbox
  evidence.
- Real funds, production trading, real order submission, production venue
  behavior, and production parity claims are out of scope.

## Behavior Impact

No runtime behavior change. No trading semantic change. No CLI behavior change.

## Public API Impact

None.

## Migration Note Status

Not required. This is a scope and release-surface documentation task, not a
runtime or public API migration.

## Rollback Plan

Revert this PR to remove the v0.4 Binance sandbox scope contract, the README
pointer, and this evidence file.
