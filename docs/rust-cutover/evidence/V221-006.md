# V221-006 Evidence - v0.22.1 release gates and strict provenance

Date: 2026-07-03
Executor: Codex
Task: `V221-006` / GitHub issue `#710`
Milestone: `v0.22.1`
Status: LOCAL VALIDATION PASSED

## Summary

V221-006 prepares the final `v0.22.1` release package. It adds release notes,
readiness report, manifest, release gate wiring, strict provenance, and the
post-gate publication evidence path for `ntpro-rust-only-v0.22.1`.

Plain Chinese summary: 本任务是 v0.22.1 的发版收口。它把 V221-001 到 V221-005
的证据合并进 release notes、readiness report、manifest、release gates 和 strict
provenance，并要求公开 release 只能在 hosted gate 成功之后发布。

## Files Changed

```text
.github/workflows/release-tag.yml
scripts/ai/verify_fast.sh
scripts/ai/verify_release.sh
scripts/ai/verify_v21_1_read_model_projection_replay.sh
scripts/ai/verify_v22_1_release_gates.sh
scripts/ai/verify_v22_1_strict_provenance.sh
docs/rust-cutover/tasks/V221-006.md
docs/rust-cutover/release/v0_22_1_release_notes.md
docs/rust-cutover/release/v0_22_1_readiness_report.md
docs/rust-cutover/release/v0_22_1_release_manifest.json
docs/rust-cutover/release/README.md
docs/rust-cutover/evidence/V221-006.md
verification.md
```

## Release Boundary

```text
hardening_patch_only = true
trader_terminal_workbench = true
read_only_first = true
required_false_runtime_boundary = true
workbench_render_smoke = true
gate_before_publish = true
complete_executable_read_model_runtime = false
product_grade_live_trading_terminal = false
new_submit_capability = false
production_order_mutation_allowed = false
v0.23.0_implementation_started = false
```

## Validation

```text
bash -n scripts/ai/verify_v21_1_read_model_projection_replay.sh scripts/ai/verify_v22_1_release_gates.sh scripts/ai/verify_v22_1_strict_provenance.sh scripts/ai/verify_release.sh scripts/ai/verify_fast.sh = PASS
python3 -m json.tool docs/rust-cutover/release/v0_22_1_release_manifest.json >/dev/null = PASS
ruby -e 'require "yaml"; YAML.load_file(".github/workflows/release-tag.yml")' = PASS
scripts/ai/verify_release.sh v22.1-release-gates = PASS, v221_evidence=complete, workbench_render_smoke=required, gate_before_publish=required, current_issue_state=OPEN
scripts/ai/verify_release.sh v22.1-strict-provenance = PASS, pre-tag mode tag_exists=false, source_dirty=true, manifest=target/ntpro-v221/v0_22_1_strict_release_manifest.json
scripts/ai/verify_fast.sh = PASS, fast smoke only
git diff --check = PASS
```

During validation, the v21.1 replay gate still expected exactly eight
`V211-003` read-model executable rows. V221-003 had already expanded that set
to 28 read-model executable rows and 4 remaining schema-only read-model rows.
The gate now preserves the V211 baseline while explicitly validating the V221
expanded promotion set.

GitHub dependency proof also hit transient GraphQL `EOF` responses while
reading live issue state. The V221 release gate now retries GitHub CLI reads
for release, milestone, auth, and issue proof before failing.

## Hosted Release Evidence

```text
pending until PR merge, tag push, hosted release gate success, and public release publication
PR merge does not auto-close #710; #710 must be manually closed only after tag, hosted gate, release URL, publication timestamp, and strict provenance proof are recorded.
```

## Behavior Impact

No runtime trading behavior changes. The change adds release gates and release
evidence only. It does not enable submit, cancel, retry, replace, amend,
flatten, funds transfer, account mutation, automatic action, production
mutation, or product-grade live terminal behavior.

## Public API Impact

No public API changed.

## Migration Note

No user migration note is required. Release operators must publish v0.22.1 only
through `scripts/ai/publish_ntpro_release_after_gate.sh` or the
`Rust Cutover Publish Release` workflow after the hosted release gate succeeds.

## Rollback Plan

Revert this PR to remove the v0.22.1 release package and gate wiring before
tagging. If the tag has already been created, do not move it; stop publication
and create a superseding patch only after a separate release decision.
