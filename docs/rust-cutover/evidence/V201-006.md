# V201-006 Evidence

Date: 2026-06-30
Executor: Codex
Task: `V201-006` / GitHub issue `#648`
Milestone: `v0.20.1`

## Plain Chinese Summary

本次补强 Dashboard v20 订单生命周期审计只读面板的 foundation boundary
诊断。面板现在显式显示 `foundation_only_no_adapter_runtime`，把已关闭的本地
foundation evidence 与未来 adapter-integrated runtime evidence 分开。missing
evidence、stale evidence、source/provenance mismatch、retry/auto remediation
flag、adapter runtime claim mismatch 都会进入 degraded 或 boundary violation，
不会被渲染成交易员可用的实盘终端能力。

## Scope

Changed files:

```text
crates/cli/src/dashboard.rs
scripts/ai/verify_v20_release_gates.sh
docs/rust-cutover/release/v0_20_0_dashboard_order_lifecycle_audit.md
```

## Behavior Evidence

```text
foundation_boundary_status = foundation_only_no_adapter_runtime | adapter_integrated_runtime | adapter_runtime_claim_mismatch | exchange_truth_claim_without_adapter_runtime | mixed_or_unknown_foundation_boundary
foundation_boundary_diagnostics = fail-closed diagnostics for mismatched boundary claims
manual_structured adapter_runtime_integrated=true = boundary_violation
stale evidence = boundary_violation, not audit_closed
submit/cancel/approval controls remain absent from the v20 audit renderer
v0.20.1 Dashboard = read-only evidence foundation, not product-grade live trading
```

## Validation

```text
cargo test -p nautilus-cli production_order_lifecycle_audit --lib = PASS, 9 passed
cargo clippy -p nautilus-cli --lib -- -D warnings = PASS
bash -n scripts/ai/verify_v20_release_gates.sh = PASS
scripts/ai/verify_release.sh v20-release-gates = PASS
scripts/ai/verify_fast.sh = PASS
git diff --check = PASS
```
