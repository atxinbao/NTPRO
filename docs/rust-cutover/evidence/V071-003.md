# V071-003 Evidence - HTTP Probe Artifact Contract

Date: 2026-06-15
Executor: Codex
Task: V071-003 Normalize HTTP probe artifact contract

## Plain Chinese Summary

本任务修的是 v0.7 HTTP probe 工件合同，不是交易能力。

大白话：`testnet/http_connectivity_probe.json` 是 HTTP probe 的正式工件路径，
`testnet/connectivity_probe.json` 继续保留给旧读取方和汇总状态使用。之前 writer
和 Dashboard reader 都各自写了路径字符串，这次把这三个 testnet probe 路径提到同
一个 Rust 合同文件里，避免以后 writer 写一个路径、Dashboard 读另一个路径。

## Change Summary

- Added shared testnet probe artifact path constants to
  `crates/cli/src/workflow_contract.rs`.
- Updated the workflow writer to use the shared path constants for event
  records and artifact path generation.
- Updated the Dashboard reader and Dashboard tests to use the shared path
  constants when matching and writing test artifacts.
- Added this V071-003 evidence record.
- Added a dedicated V071-003 task record.

## Files Changed

```text
crates/cli/src/dashboard.rs
crates/cli/src/workflow.rs
crates/cli/src/workflow_contract.rs
docs/rust-cutover/evidence/V071-003.md
docs/rust-cutover/tasks/V071-003.md
```

## Behavior Impact

No behavior change is intended. The same artifacts are still written and read:

```text
testnet/connectivity_probe.json
testnet/http_connectivity_probe.json
testnet/ws_connectivity_probe.json
```

The change reduces contract drift risk between the CLI writer and Dashboard
reader.

## Public API Impact

No public Rust API, CLI flag, trading behavior, or external network behavior
change.

## Migration Note Status

No migration note is required. Artifact paths remain unchanged.

## Boundary Confirmation

V071-003 keeps these non-goals explicit:

```text
no authenticated account access
no order submission
no real funds
no production Binance connectivity
no production trading
no v0.8 capability work
no tag creation
no GitHub Release publication
```

## Validation Commands

```bash
cargo fmt --check
cargo test -p nautilus-cli workflow --lib
cargo test -p nautilus-cli dashboard --lib
scripts/ai/verify_v06_binance_testnet_dry_run.sh
scripts/ai/verify_v07_default_offline_gate.sh
scripts/ai/verify_fast.sh
git diff --check
```

## Validation Result

PASS.

```text
cargo fmt --check:
PASS after applying rustfmt to the new imports/constants.

cargo test -p nautilus-cli workflow --lib:
PASS - 33 passed, 0 failed.

cargo test -p nautilus-cli dashboard --lib:
PASS - 33 passed, 0 failed.

scripts/ai/verify_v06_binance_testnet_dry_run.sh:
PASS - artifact_count=11, canonical HTTP probe artifact present, no network,
       no real orders.

scripts/ai/verify_v07_default_offline_gate.sh:
PASS - dry-run and blocked connectivity-probe paths remain offline and
       fail-closed.

scripts/ai/verify_fast.sh:
PASS - toolchain and cargo fmt smoke passed.

git diff --check:
PASS - no whitespace errors.
```

Additional contract scan:

```text
rg '"testnet/(connectivity_probe|http_connectivity_probe|ws_connectivity_probe).json"'
PASS - duplicate Rust writer/reader path literals are removed; the remaining
       matches are the shared contract constants in workflow_contract.rs.
```

## Rollback Plan

Revert the V071-003 commit. Artifact paths themselves are unchanged, so rollback
only restores duplicated string literals in the writer and Dashboard reader.
