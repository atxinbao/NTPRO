# V031-003 Evidence - Hosted Release Gate Triage

Date: 2026-06-12
Executor: Codex
Task ID: V031-003
Task: hosted release gate triage and evidence closeout

## Plain Chinese Summary

这次没有改运行时代码，也没有重新发布版本。做的事情是把 GitHub 托管
`Rust Cutover Release Gate` 的真实结果记清楚。

大白话说：本地验证和部分 PR smoke 可以通过，但 GitHub 上的完整 release
gate 没有拿到干净 PASS。它在托管 runner 上出现了链接阶段失败、磁盘空间不足
或长时间卡住后取消。因此，在 V031 修正任务完成并重新跑出托管 PASS 前，不能
把当前状态说成“远端 release gate 已通过”。

## Goal

Close the hosted GitHub Actions evidence gap for the current v0.3.x release
line by recording exact workflow runs, trigger types, commits, conclusions, and
failure modes.

## Hosted Workflow Evidence

Workflow name:

```text
Rust Cutover Release Gate
```

### Run 27384342541

| Field | Value |
| --- | --- |
| URL | `https://github.com/atxinbao/NTPRO/actions/runs/27384342541` |
| Trigger | `push` |
| Ref / branch | `ntpro-rust-only-v0.3.0` |
| Commit SHA | `2822ef8c29771de8ef1b90b96507ac6f1bcefcb3` |
| Status | `completed` |
| Conclusion | `failure` |
| Job | `verify-release` |
| Job result | `failure` |
| Started | `2026-06-11T23:36:37Z` |
| Completed | `2026-06-12T00:07:56Z` |

Failure summary:

```text
rustc-LLVM ERROR: IO failure on output stream: No space left on device
No space left on device (os error 28)
error: linking with `cc` failed: exit status: 1
collect2: fatal error: ld terminated with signal 7 [Bus error], core dumped
```

Affected compile targets included:

```text
nautilus-architect-ax test "exec_client"
nautilus-architect-ax bin "ax-flatten"
nautilus-architect-ax bin "ax-ws-orders"
nautilus-architect-ax test "websocket"
```

Conclusion: hosted release gate failed. The failure mode points to hosted
runner resource pressure during heavy Rust linking, not a clean product PASS.

### Run 27421121134

| Field | Value |
| --- | --- |
| URL | `https://github.com/atxinbao/NTPRO/actions/runs/27421121134` |
| Trigger | `workflow_dispatch` |
| Ref / branch | `main` |
| Commit SHA | `afc805396ad731e93f99252fbf3ca9e81010753a` |
| Status | `completed` |
| Conclusion | `failure` |
| Job | `verify-release` |
| Job result | `failure` |
| Started | `2026-06-12T14:12:19Z` |
| Completed | `2026-06-12T14:24:33Z` |

Failure summary:

```text
error: linking with `cc` failed: exit status: 1
collect2: fatal error: ld terminated with signal 7 [Bus error], core dumped
```

Affected compile targets included:

```text
nautilus-event-store test "verifier"
nautilus-event-store test "writer"
```

Conclusion: hosted release gate failed again during test linking. This confirms
that the release gate was still too heavy or unstable for the hosted runner in
this form.

### Run 27423501016

| Field | Value |
| --- | --- |
| URL | `https://github.com/atxinbao/NTPRO/actions/runs/27423501016` |
| Trigger | `workflow_dispatch` |
| Ref / branch | `main` |
| Commit SHA | `5bc497e6e7aa93d615e2d3580c61757de9eb7fbe` |
| Status | `completed` |
| Conclusion | `cancelled` |
| Job | `verify-release` |
| Job result | `cancelled` |
| Started | `2026-06-12T14:53:18Z` |
| Completed | `2026-06-12T16:38:19Z` |

Closure summary:

```text
Verify release gate remained in progress for more than 90 minutes.
The run was cancelled after the hosted gate continued to block.
```

Conclusion: no hosted PASS was produced. This run cannot be used as release
approval evidence.

## Local vs Hosted Status

Local and PR-level checks can pass while the hosted release gate is still not
clean:

- local v0.3 smoke can pass;
- release binaries can pass local smoke;
- PR smoke can pass;
- hosted `Rust Cutover Release Gate` still failed or was cancelled.

Therefore the release evidence must say:

```text
Local verification: available for scoped smoke evidence.
Hosted release gate: not clean PASS as of the runs above.
Release decision: defer tag/release claims until V031 fixes complete and hosted gate reruns successfully.
```

## Commands Run

```bash
perl -e 'alarm 45; exec @ARGV' gh run list --repo atxinbao/NTPRO --limit 20
perl -e 'alarm 60; exec @ARGV' gh run view 27384342541 --repo atxinbao/NTPRO --json name,event,headBranch,headSha,status,conclusion,url,createdAt,updatedAt,jobs
perl -e 'alarm 60; exec @ARGV' gh run view 27421121134 --repo atxinbao/NTPRO --json name,event,headBranch,headSha,status,conclusion,url,createdAt,updatedAt,jobs
perl -e 'alarm 60; exec @ARGV' gh run view 27423501016 --repo atxinbao/NTPRO --json name,event,headBranch,headSha,status,conclusion,url,createdAt,updatedAt,jobs
perl -e 'alarm 60; exec @ARGV' gh run view 27384342541 --repo atxinbao/NTPRO --log-failed | tail -n 140
perl -e 'alarm 60; exec @ARGV' gh run view 27421121134 --repo atxinbao/NTPRO --log-failed | tail -n 140
perl -e 'alarm 60; exec @ARGV' gh run view 27423501016 --repo atxinbao/NTPRO --log-failed | tail -n 80
```

Note: macOS did not provide GNU `timeout`, so the GitHub commands were bounded
with `perl` `alarm`.

## Behavior Impact

No runtime behavior changed.

## Public API Impact

No public Rust API changed.

## Migration Note

No migration note is required. This task records hosted release evidence only.

## Next Action

Finish the V031 hardening queue before rerunning the hosted release gate:

```text
V031-004
V031-005
V031-006
V031-007
V031-008
V031-009
V031-010
```

After V031-010, rerun `Rust Cutover Release Gate` and require a hosted PASS
before creating or publishing a v0.3.1 release.

## Rollback Plan

If this evidence is incorrect, revert this documentation PR and replace it with
updated GitHub run evidence. No code rollback is required because this task does
not modify runtime behavior.
