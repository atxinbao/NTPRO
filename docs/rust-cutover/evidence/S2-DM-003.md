# S2-DM-003 - Demo 模拟成交、持仓、资金曲线与跨环境比较证据

Date: 2026-08-11
Executor: Codex
GitHub issue: #1284
Status: DONE_ON_MERGE

## 验收合同

- 模拟成交必须来自仓库现有模拟执行引擎，不从订单意图推断或伪造；
- 交易、持仓和权益必须绑定 Run、StrategyVersion、输入数据、manifest 和 checksum；
- running 与 frozen snapshot 必须消费同一严格工件；
- Backtest/Demo 比较必须分别验证两侧来源，不允许缺失工件时降级；
- 所有真实交易、自动动作和外部连接边界继续为 false。

## 验证记录

### 运行时与工件

- `StrategySession` 复用仓库现有 `BacktestEngine`、`SimulatedVenue` 与 `EmaCross`，不从
  订单意图伪造成交；
- Demo 每次发布 12 个策略工件，新增 `simulation_summary.json`、
  `simulated_fills.jsonl`、`simulated_positions.jsonl` 与 `equity_curve.jsonl`；
- manifest 对每个工件绑定规范路径、记录数、字节数与 SHA-256；Golden fixture
  `tests/golden/demo_simulation_schema.jsonl` 证明相同输入可重放出相同模拟结果；
- summary、成交、持仓、权益四层都必须显式 `simulation_only=true`。任一层缺失、为 false，
  或任何真实交易边界为 true，Product API 均 fail closed。

### Product API 与前端

- Demo snapshot 升级为 `ntpro.product_api.demo_run_snapshot.response.v2`，running/frozen
  结果返回严格校验后的模拟摘要和明细；
- Run comparison 升级为 `ntpro.product_api.run_comparison.response.v2`，支持已完成
  Backtest 与已停止 Demo，分别校验两侧不可变来源后再计算可比性；
- `behaviorally_comparable` 要求策略、版本、参数、标的和币种一致；
  `directly_comparable` 还要求数据哈希一致；不同策略或版本仍允许并列查看，但不会被标记为
  行为可比；Demo 不支持确定性复现；
- Demo 比较项的 `config_sha256` 绑定实际 `request.json`，策略 manifest 来源指向实际注册的
  `supervisor-nodes/{node_id}/strategy/manifest.json`，不使用推测路径；
- 策略工作台 Run 详情展示 Demo 模拟成交、持仓和资金曲线；比较页明确显示两侧环境和来源，
  生产页面消费真实 Product API，测试 fixture 不进入生产路径；
- 迁移说明位于 `docs/rust-cutover/migration/S2-DM-003-demo-simulation-comparison.md`。

### 本地自动验证

- Rust 1.95.0 工具链 pin：PASS；`scripts/ai/verify_fast.sh`：PASS；
- `cargo test -p nautilus-cli --lib -- --test-threads=1`：PASS，661/661；
- Demo 仿真 Golden replay、策略 session、Product API 生命周期和四层
  `simulation_only=false` 负向测试：PASS；
- workspace 全 feature Clippy：PASS，`-D warnings`；
- OpenAPI 生成与漂移检查、TypeScript typecheck、oxlint、生产 build：PASS；
- Vitest：PASS，91/91；Playwright：PASS，8/8；npm audit：0 vulnerabilities；
- Rust/Axum 与真实 Chrome 联合验收：PASS，桌面 `1440x1000`、移动 `390x844`，确认
  running/frozen Demo、模拟结果、Backtest/Demo 比较和 Live disabled；结果记录在
  `/tmp/ntpro-s2-dm-003-browser-evidence/result.json`；
- backend runtime risk inventory：PASS，29862 signals / 1225 files；production 保持 2703，
  本轮审查修复新增 1 条 test-owned 信号；canonical SHA-256
  `44f1e61c35554f82935d286e3e6890b53e1591109077e560dd1789a92fb58dec`；
- current governance、backend freeze baseline、MVP freeze、Rust-only 和 hygiene gates：PASS；
- cargo audit 使用官方新克隆数据库：PASS，3 项仓库已允许 warning；cargo deny：PASS，
  仅输出既有配置/yanked transitive warning；cargo vet：PASS，182 fully audited、
  26 partially audited、693 exempted。

### 验证过程中的非代码失败

- `npm test -- --runInBand` 使用了 Vitest 不支持的 Jest 参数；改用权威 `npm test` 后
  88/88 通过；
- 首次联合 Cargo filter 命令传入两个位置参数，被 Cargo 拒绝；拆分为独立测试后全部通过；
- 首次浏览器验收缺少 Playwright core/binary 显式路径；构建准确 Rust binaries 并指定
  Chrome 后通过；
- 首次全量 Rust 测试误并行启动两份进程，产生不可采信的资源竞争结果；停止重复进程、同步
  新的 12 工件 fixture 和 4 条策略计数后，单进程全量 661/661 通过；
- 默认 RustSec 缓存包含重复 advisory ID `RUSTSEC-2026-0244`；使用官方 advisory-db
  新克隆目录复验通过。
- 首次审查修复后的浏览器复验拾取了旧 `target/debug/nautilus`，前端因响应缺少新必需字段
  `same_parameters` 正确 fail closed；重新构建 `nautilus` 与 `ntpro-node` 后，同一真实浏览器
  验收通过。

## 行为与边界结论

S2 产品退出条件已在代码和本地自动验证层满足：用户可对同一不可变 StrategyVersion 创建、
启动、停止和查看 Demo，并将模拟成交、持仓与资金曲线同 Backtest 统一比较。真实 Live、
外部 Venue、真实订单、撤改单、自动重试、自动补救和交易控件仍全部关闭，Demo 结果不会继承
或开启 Live 权限。未修改 `docs/rust-cutover/release/v0_32_0_*` 冻结文件。

#1284 是 MVP 冻结后独立授权的产品任务；其 Demo fixture 扩展修改了冻结源
`crates/cli/src/mvp.rs`。按 `mvp_freeze_manifest.json` 的 post-freeze policy，已同步该文件的
SHA-256 登记；19 项禁止能力边界未变，未继承任何真实交易权限。

## 审查与合并状态

首轮独立 Gatekeeper 审查发现四项问题：前端错误强制同策略、比较未核对实际参数、Demo
manifest 来源 URI 与实际存储不一致，以及 `config_sha256` 错绑外层 manifest。当前修复已改为
逐项重算兼容性、加入 `same_parameters`、绑定实际 request SHA 和实际 Supervisor 节点路径，并
补充跨策略、跨版本、参数矛盾及来源绑定测试。

任务风险为 high。Gatekeeper 已对 exact head
`1a9c23d03123c0bc2c14e5aa5f55c6f3b87ef333` 输出 `REVIEW_APPROVED`；Rust Cutover Smoke
run `31491189891`、Backend Performance run `31491190070` 的 6 项基准和 security-audit
run `31491189918` 均成功。PR #1285 已手动合并，merge SHA 为
`e97b8d78b6343c2600d3f5e1562a9f77d294c927`；未启用 auto-merge。本状态随 S2-CLS-001
合并生效为 `DONE`。
