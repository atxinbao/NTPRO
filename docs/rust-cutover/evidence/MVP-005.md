# MVP-005 - 双门户共享只读状态 API 证据

Date: 2026-08-02
Executor: Codex
GitHub issue: #1207
Status: REVIEW_REQUIRED

## 实现结果

- 新增 `GET /api/mvp/v1/status`；
- 新增 `ntpro.mvp_shared_status_api.v1` 和版本化响应 schema；
- 聚合 MVP-003 身份合同、MVP-004 四轴状态合同和受控 Unified Read Model 摘要；
- 同一响应明确供 `institution_workbench` 和 `control_center` 使用；
- 对合同 schema、身份、运行来源、边界、错误信封和时效进行请求时复核；
- 顶层或单轴陈旧时旧 healthy 会降级，不依赖源文件中的静态 freshness 声明；
- Unified Read Model 账户/Venue 串线时不返回业务摘要；
- Unified Read Model 缺失时返回 missing，交易就绪保持 blocked；
- 原始 Event Store、原始 Venue payload、写操作、重试和补救能力保持关闭。

## 定向验证

```text
cargo test -p nautilus-cli mvp_status_api --lib -j 2
PASS: 11 passed, 0 failed

cargo test -p nautilus-cli mvp_shared_status_route_is_get_only --lib -j 2
PASS: 1 passed, 0 failed

cargo test -p nautilus-cli trader_terminal_v28_http_routes_serve_read_only_contracts --lib -j 2
PASS: 1 passed, 0 failed

cargo test -p nautilus-cli dashboard --lib -j 2
PASS: 149 passed, 0 failed

cargo test -p nautilus-cli mvp --lib -j 2
PASS: 34 passed, 0 failed

cargo clippy -p nautilus-cli --all-targets --all-features -- -D warnings
PASS

cargo fmt --all -- --check
PASS

scripts/ai/check_docs_examples_governance.sh
PASS

scripts/ai/check_rust_only_runtime.sh
PASS

scripts/ai/check_backend_freeze_baseline.sh
PASS: baseline and 20 negative cases

scripts/ai/verify_fast.sh
PASS

git diff --check
PASS
```

覆盖场景：

- 两个门户共享同一合同；
- Unified Read Model 缺失；
- status contract identity 不一致；
- 身份字段缺失或 contract ID 伪造；
- 只读边界字段被改为 true；
- 顶层状态合同陈旧；
- 单轴状态陈旧；
- 顶层或单轴时间戳为零；
- 顶层未来时间戳；
- error 与 availability 自相矛盾；
- Supervisor 运行来源路径被篡改；
- Unified Read Model 账户/Venue 身份不一致；
- POST、PUT、PATCH、DELETE 返回 405。

## 真实 HTTP 冒烟

使用 `configs/nodes/btc-ema-shadow.toml` 启动真实 `nautilus mvp serve` 和
`ntpro-node`，请求 `/api/mvp/v1/status`：

```text
schema_version=ntpro.mvp_shared_status_api.response.v1
contract_version=ntpro.mvp_shared_status_api.v1
consumers=institution_workbench,control_center
node_id=mvp-node-001
strategy_instance_id=btc-ema-shadow-001
account_id=SANDBOX-001
runtime=running
runtime_freshness=fresh
technical_health=healthy
technical_freshness=fresh
trading_readiness=blocked
business_availability=missing
status_contract_refreshed=true
POST=405 PUT=405 PATCH=405 DELETE=405
forbidden_true_count=0
```

Ctrl-C 后：

```text
runtime=stopped
technical_health=not_running
trading_readiness=blocked
supervisor_process_state=stopped
node_lifecycle=stopped
ntpro-node processes=0
nautilus processes=0
```

当前仅待独立 Verification & Release Gatekeeper 审查与 hosted required checks。

## 行为与边界

本任务新增只读查询，不新增前端、多节点、真实交易或生产准入。HTTP 成功不覆盖
研究、运行、技术健康和交易就绪的独立语义；所有交易与自动动作字段继续为 false。
