# BFH-004 Rust-Only Contributor Route Repair Evidence

Date: 2026-07-19
Executor: Codex
GitHub issue: #1115
Milestone: post-freeze-backend-hygiene
Status: PRE-MERGE VALIDATION PASSED

## Summary

BFH-004 replaces stale current contributor instructions with tracked Rust-only
repository authorities. Historical cutover evidence and the v0.32.0 release
package remain unchanged.

Plain Chinese summary: 当前贡献入口不再要求已删除的 Python 工具链、旧
developer-guide 安装页、`develop` 分支或不存在的 CLA 自动化。所有替代入口都来自
当前仓库中的 Rust 配置、Make target、验证脚本和治理协议。

## Route Replacement

```text
base branch = main
branch and PR unit = one issue, one branch, one PR
Rust toolchain authority = rust-toolchain.toml
Cargo tool authority = Cargo.toml [workspace.metadata.tools]
standalone tool authority = tools.toml
dependency fetch = make install-deps
tool install = make install-tools
fast validation = scripts/ai/verify_fast.sh
full validation = scripts/ai/verify_full.sh
docs validation = make docs-check-links
zero-Python validation = scripts/ai/check_zero_python_closeout.sh
backend freeze validation = scripts/ai/verify_release.sh backend-freeze-baseline
review authority = docs/rust-cutover/TASK_EXECUTION.md
CLA authority = CLA.md
```

## Change Boundary

```text
runtime crates changed = false
Cargo.toml or Cargo.lock changed = false
workflow or hook changed = false
public API or schema changed = false
docs/rust-cutover/release/v0_32_0_* changed = false
registered backend boundary changed = false
historical audit wording changed = false
```

## Validation

```text
modified local links = PASS, checked=6, missing=0
stale current contributor routes = PASS, count=0
documented Make targets = PASS, install-deps/install-tools/docs-check-links
docs/examples governance = PASS, markdown_files=116, local_links=293
Rust examples integrity = PASS, required_paths=14
zero-Python closeout = PASS, tracked_files=5260, active_scripts=55,
  workflow_actions=17, historical_docs=1936, negative_cases=10
backend freeze baseline = PASS, boundaries=27, source_hashes=4
backend freeze negative selftest = PASS, cases=20
current governance = PASS
scripts/ai/verify_fast.sh = PASS
git diff --check = PASS
runtime/Cargo/workflow/frozen release path changes = 0
```

Commands:

```bash
scripts/ai/check_docs_examples_governance.sh
scripts/ai/check_zero_python_closeout.sh
CARGO_TARGET_DIR="${TMPDIR:-/tmp}/ntpro-bfh001-target" \
  scripts/ai/verify_release.sh backend-freeze-baseline
CARGO_TARGET_DIR="${TMPDIR:-/tmp}/ntpro-bfh001-target" \
  scripts/ai/verify_release.sh current-governance
CARGO_TARGET_DIR="${TMPDIR:-/tmp}/ntpro-bfh001-target" \
  scripts/ai/verify_fast.sh
make -n install-deps
make -n install-tools
make -n docs-check-links
local Markdown link resolution over the four modified documents
git diff --check
```

`lychee` is not installed locally, so the scoped link proof used a deterministic
shell resolver and the repository-owned docs/examples governance gate. An
additional whole-repository `.pre-commit-hooks/check_docs_conventions.sh` run
reported 1,199 pre-existing table-row hyphen findings across untouched current
and historical documents. None came from a line added by BFH-004; changing
those files would violate this issue's narrow contributor-route scope.
