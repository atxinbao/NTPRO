# V190-009 Evidence - Actual Cancel Golden Trace And Fixture Coverage

Date: 2026-06-28
Executor: Codex
Milestone: v0.19.0
GitHub issue: `#585`
Status: REVIEW_REQUIRED - local validation complete before PR

## Summary

V190-009 adds executable golden trace coverage for the v0.19 owner-approved
actual cancel line. The trace covers success, blocked pre-send, rejected,
timeout, unknown, already-cancelled, and partial-fill outcomes and proves every
case carries request, response, readback, audit, and provenance references.

Plain Chinese summary: 这次补齐 v0.19 真实撤单的 golden trace/fixture 覆盖。大白话：
成功、审批缺失、审批复用、risk 不匹配、adapter 不支持、被拒绝、超时、unknown、already
cancelled、partial fill 都有固定 trace，并且每条都能看到 request/response/readback/audit/
provenance references。它不接真实交易所，不改真实撤单执行逻辑，不增加 retry、二次撤单或
Dashboard 撤单按钮。

## Trace / Harness

```text
trace = tests/golden/actual_cancel_schema.jsonl
cases = 10
category = execution
fixture_contract = ntpro.v190_actual_cancel_golden_fixture.v1
harness = cargo test -p nautilus-cli --test golden_trace_actual_cancel
release scope = 10 executable_replay entries in RELEASE_REPLAY_SCOPE.json
```

## Covered Outcomes

```text
success = cancel_confirmed
approval_missing = blocked_source_artifact / owner_approval_required
approval_reused = blocked_source_artifact / single_use_owner_approval
risk_mismatch = blocked_source_artifact / risk_gate_order_lineage_binding
adapter_unsupported = blocked_source_artifact / adapter_actual_cancel_capability
cancel_rejected = rejected
timeout = timeout
unknown = unknown
already_cancelled = already_cancelled recovered terminal state
partial_fill = partial_success with residual risk and decimal-string quantities
```

## Files Changed

```text
tests/golden/actual_cancel_schema.jsonl
crates/cli/tests/golden_trace_actual_cancel.rs
scripts/ai/run_golden_traces.sh
scripts/ai/verify_v19_actual_cancel_golden_traces.sh
docs/rust-cutover/golden_trace/SCHEMA.md
docs/rust-cutover/golden_trace/GATE_EVIDENCE.md
docs/rust-cutover/golden_trace/RELEASE_REPLAY_SCOPE.json
docs/rust-cutover/release/v0_19_0_actual_cancel_golden_trace_fixtures.md
docs/rust-cutover/release/README.md
docs/rust-cutover/evidence/V190-009.md
verification.md
```

## Validation

```text
cargo fmt -p nautilus-cli = PASS
python3 scripts/ai/golden_trace_runner.py tests/golden/actual_cancel_schema.jsonl --mode validate-only = PASS, 10 rows
TRACE_GLOB=tests/golden/actual_cancel_schema.jsonl ... scripts/ai/run_golden_traces.sh = PASS
cargo test -p nautilus-cli --test golden_trace_actual_cancel = PASS, 1 test
scripts/ai/verify_v19_actual_cancel_golden_traces.sh = PASS
scripts/ai/run_golden_traces.sh = PASS
python3 scripts/ai/validate_golden_trace_release_scope.py --manifest docs/rust-cutover/golden_trace/RELEASE_REPLAY_SCOPE.json --trace-glob 'tests/golden/*.jsonl' = PASS, 45 cases, 44 executable replay, 1 schema-only scoped
scripts/ai/verify_release.sh v19-release-gates = UNAVAILABLE, unknown verify_release stage: v19-release-gates
cargo fmt --check -p nautilus-cli = PASS
cargo clippy -p nautilus-cli --all-targets -- -D warnings = PASS
scripts/ai/verify_fast.sh = PASS
git diff --check = PASS
```

## Boundary

```text
real venue network dependency = not added
live broker connection = not added
actual cancel runtime behavior = not changed
production order submit lifecycle = not expanded
retry / remediation / second cancel = not added
Dashboard cancel controls = not added
raw request/response/readback persistence = not added
credential persistence = not added
quantity/price arithmetic = not changed
```

## Review / Merge Status

```text
PR = pending
Hosted Rust Cutover Smoke = pending
Review status = REVIEW_REQUIRED because this issue extends release-gate golden trace coverage for actual cancel
Issue closeout = pending until review/merge closes #585
```

## Rollback Plan

Revert this PR to remove the V190-009 JSONL trace, Rust harness, verifier
script, golden trace runner wiring, release scope entries, release document,
release index entry, evidence file, and verification entry. This does not
require exchange, credential, order, cancel, adapter runtime, tag, GitHub
Release, or binary cleanup because the change only adds local/offline fixtures
and validation.
