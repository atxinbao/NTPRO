# BFG-004 Generated Artifact Hygiene Evidence

Date: 2026-07-15
Executor: Codex
GitHub issue: #1071
Milestone: backend-freeze-governance
Status: LOCAL VALIDATION PASSED

## Summary

This task adds root ignore coverage and a governance policy for reproducible
post-baseline local outputs. Existing local files are preserved.

Plain Chinese summary: 生成物仍可由检查脚本生成和读取，但不会污染 Git 状态，也不会
被误当作正式 release 证据。本任务没有删除本地文件或历史 evidence。

## Validation

```text
git check-ignore -v --no-index declared generated paths = PASS
tracked generated artifact scan = PASS, zero files
publication + freeze + strict provenance + fast smoke status stability = PASS
scripts/ai/check_github_release_published.sh = PASS
scripts/ai/check_backend_freeze_baseline.sh = PASS, negative_selftest=20
scripts/ai/verify_release.sh v32-strict-provenance = PASS
scripts/ai/verify_fast.sh = PASS
git diff --check = PASS
```

Ignore proof:

```text
/release-publication-evidence/ -> .gitignore root rule
/graphify-out/ -> .gitignore root rule
target/ntpro-v320/ -> existing *target/ rule
.codex/ -> existing .codex/ rule
.agentflow/ -> existing .agentflow/ rule
```

## Behavior Impact

None. Repository hygiene and governance documentation only.
