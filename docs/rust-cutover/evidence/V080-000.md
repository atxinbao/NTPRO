# V080-000 Evidence - Authenticated Read-Only Boundary and Endpoint Allowlist

Date: 2026-06-16
Executor: Codex
Task: V080-000 Define authenticated read-only boundary and endpoint allowlist

## Plain Chinese Summary

这个任务只写 v0.8.0 的边界，不写代码。v0.8.0 的方向是“带 Binance testnet
API key 的只读账号接口证明”，但只能手动 opt-in，只能 testnet，只能读，不能下单、
撤单、改单、改账号、连生产 Binance、用真实资金或声称生产交易。初始 allowlist 只有
`GET /api/v3/account`，其它没有明确 allowlist 的 endpoint 默认禁止。

## Change Summary

- Added the v0.8.0 authenticated read-only boundary document.
- Defined the endpoint allowlist and denylist.
- Defined env-only credential handling and redaction rules.
- Defined allowed and forbidden artifact fields.
- Defined Dashboard artifact-read-only behavior.
- Defined offline and manual-online release gate expectations.

## Files Changed

```text
docs/rust-cutover/release/v0_8_0_authenticated_readonly_boundary.md
docs/rust-cutover/tasks/V080-000.md
docs/rust-cutover/evidence/V080-000.md
```

## Behavior Impact

No runtime behavior change. This is a design/boundary gate only.

## Public API Impact

No Rust API, CLI flag, artifact schema implementation, or trading semantic
change.

## Migration Note Status

No migration note is required.

## Validation Commands

```bash
rg -n "GET.*\\/api\\/v3\\/account|POST.*\\/api\\/v3\\/order|DELETE.*\\/api\\/v3\\/order|listenKey|NTPRO_BINANCE_TESTNET_API_KEY|NTPRO_BINANCE_TESTNET_API_SECRET|raw API key value|raw signature|raw account response body|Dashboard may read and display completed artifacts only|production trading = not included|real funds = not included" docs/rust-cutover/release/v0_8_0_authenticated_readonly_boundary.md
if rg -n "order submission = included|account mutation = included|production trading = included|real funds = included|production Binance connectivity = included|Dashboard may start authenticated probes" docs/rust-cutover/release/v0_8_0_authenticated_readonly_boundary.md; then exit 1; fi
scripts/ai/verify_fast.sh
git diff --check
```

## Validation Result

PASS.

- Positive scan found the `GET /api/v3/account` allowlist, order/listenKey
  denylist, credential env vars, forbidden raw secret fields, Dashboard
  artifact-only boundary, and no-real-funds/no-production-trading text.
- Negative scan found no included order submission, account mutation,
  production trading, real funds, production connectivity, or
  Dashboard-started probe claim.
- `scripts/ai/verify_fast.sh` passed.
- `git diff --check` passed.

## Rollback Plan

Revert the V080-000 commit. This removes the boundary document and task
evidence. No runtime behavior depends on it.
