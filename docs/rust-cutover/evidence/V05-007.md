# V05-007 Evidence - Dashboard artifact UI

Date: 2026-06-13
Executor: Codex
Task ID: V05-007

## Plain Chinese Summary

本任务在 Dashboard 页面上加了 Workflow 工件区域。

大白话说：打开 Dashboard 后，页面可以看到本地 workflow manifest 的 run id、状态、
manifest 路径、artifact 数量，以及“沙盒/外部连接/真实资金/生产交易/真实订单”等边界。
它只是展示本地 artifact，不提供按钮去跑真实交易。

## Files Changed

- `crates/cli/src/dashboard.rs`
- `docs/rust-cutover/tasks/V05-007.md`
- `docs/rust-cutover/evidence/V05-007.md`

## Commands Run

```bash
cargo test -p nautilus-cli dashboard --lib
```

## Validation Summary

Result: PASS.

```text
27 passed; 0 failed; 0 ignored
```

Covered UI contract checks:

- Dashboard shell contains `workflow-artifacts`.
- Dashboard JavaScript contains `renderWorkflowArtifacts`.
- Empty state says `没有 workflow manifest 工件`.

## Behavior Impact

Dashboard UI can render local workflow artifact status.

## Public API Impact

No new HTTP endpoint. Existing `/api/snapshot` includes the new
`workflow_artifacts` data used by the static UI.

## Migration Note Status

No migration note required.

## Rollback Plan

Revert this PR to remove the Dashboard workflow artifact UI section.
