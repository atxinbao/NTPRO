# FEA-001 - 策略工作台前端架构文档证据

Date: 2026-08-05
Executor: Codex
GitHub issue: #1251
Status: QA_PASSED_PENDING_MERGE

## 文档结果

- 新增策略工作台前端产品架构权威文档；
- 记录当前页面只是 Rust 内嵌框架、只消费 MVP 共享状态的代码事实；
- 选择 React + TypeScript + Vite，并将 Node.js 限定为开发与构建工具；
- 生产运行时继续由 Rust/Axum 提供静态资源和 API；
- 固定 Strategy、不可变 StrategyVersion、Run 产品资源及首批只读 API；
- 固定页面信息架构、状态所有权、视觉原则、测试要求和 CI 路径分类；
- 固定 FEA-001、FEF-001、S0-API-001、SWB-002、S1-S4 的交付顺序；
- 明确真实 Live 是必达目标，但当前真实交易能力和控件继续关闭。

## 行为影响

本任务只修改文档，不修改 Rust runtime、Axum 路由、API、前端资产、CI 或发布文件。

## 自动验证

```text
git diff --check
PASS

scripts/ai/check_docs_examples_governance.sh
PASS: markdown_files=134, local_links=315, image_links=20

scripts/ai/check_rust_only_runtime.sh
PASS

scripts/ai/check_mvp_freeze_baseline.sh
PASS: tasks=19, phases=5, boundaries=19, frozen_sources=13
```

验证同时确认 v0.32.0 后端冻结基线的 27 个边界和 4 个 source hash 继续成立。本任务没有
生成 `target/`、`node_modules/` 或前端构建产物。
