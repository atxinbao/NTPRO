# PAR-005 Partial/Full Fill Account-Balance Regression Evidence

Date: 2026-07-29
Executor: Codex
GitHub issue: #1173
Status: LOCAL VALIDATION PASSED / REVIEW_REQUIRED

## Ownership Finding

```text
historical placeholder location = crates/risk/tests/risk_engine.rs
current balance owner = crates/portfolio/src/manager.rs::AccountsManager
balance update = AccountsManager::update_balances
reservation update = AccountsManager::update_orders
```

RiskEngine does not consume fill events or recalculate account balances. The
empty placeholder was therefore moved to the current production owner instead
of adding a test-only path to RiskEngine.

## Defect Reproduction

Before the repair, the new event-sequence regression failed after the partial
fill:

```text
left = Some(Money(80000.00, USD))
right = Some(Money(48000.00, USD))
```

The cash reservation path used the original order quantity after a partial
fill. It now uses `order.leaves_qty()`, so released capital is reflected while
the remaining order stays reserved.

## Repair Boundary

```text
production Rust changed = one cash-reservation quantity input
ignored empty placeholders removed = 1
default executable event-sequence tests added = 1
margin/betting reservation behavior changed = false
public API changed = false
live trading capability changed = false
v0.32.0 frozen files changed = false
```

## Validation

```text
cargo test -p nautilus-portfolio --lib \
  test_partial_fill_and_full_fill_account_balance_correct
result = PASS (1 passed)

cargo test -p nautilus-portfolio --lib \
  test_partial_fill_and_full_fill_account_balance_correct \
  --features nautilus-model/high-precision
result = PASS (1 passed)

cargo test -p nautilus-portfolio -p nautilus-risk
result = PASS
  portfolio unit = 23 passed, 0 ignored
  portfolio integration = 66 passed
  risk engine = 80 passed, 0 ignored

cargo test -p nautilus-portfolio -p nautilus-risk \
  --features nautilus-model/high-precision
result = PASS with the same test inventory

cargo clippy -p nautilus-portfolio -p nautilus-risk \
  --all-targets --all-features \
  --features nautilus-model/high-precision -- -D warnings
result = PASS

REQUIRE_GOLDEN_REPLAY=1 scripts/ai/run_golden_traces.sh
result = PASS (289 cases: 98 executable, 186 validator executable,
5 schema-only scoped)

scripts/ai/check_backend_runtime_risk_inventory.sh
result = PASS (29,137 signals in 1,215 files)

scripts/ai/verify_release.sh current-governance backend-freeze-baseline
result = PASS

scripts/ai/verify_fast.sh
result = PASS

direct #[ignore] attribute scan
result = 19 (down from 20 before PAR-005)

git diff --check
result = PASS
```

The event-sequence regression is executable Rust evidence at the production
owner and the complete release golden replay remains green. No new release
golden case is claimed for account balances.

The inventory conservatively classifies inline tests in
`crates/portfolio/src/manager.rs` as production-owned. Its production count
therefore increases even though most added signals are inside `#[cfg(test)]`.
PAR-011 owns the planned path-versus-region attribution improvement.

## Review State

Independent gatekeeper review:

```text
session = 019fabfa-1230-7583-8aac-d94d934e39da
result = REVIEW_APPROVED
findings = none
```

Hosted checks remain pending. Auto-merge is prohibited.

## Rollback

Revert the PAR-005 PR and restore the blocker entries. Do not restore the empty
ignored test as evidence; rollback means partial-fill cash reservation and
account-balance behavior are again unproven.
