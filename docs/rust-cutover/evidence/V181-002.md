# V181-002 Evidence - Current Release Surface Alignment

Date: 2026-06-27
Executor: Codex
Milestone: v0.18.1
GitHub issue: `#571`
Status: PASS - local validation complete before PR

## Summary

V181-002 aligns the public release surface to the published
`ntpro-rust-only-v0.18.0` baseline. The root README now shows v0.18.0 in the
first viewport as the current release, identifies v0.18.1 as the provenance
hardening patch, and identifies v0.19.0 as the next capability line for
owner-approved single-shot actual cancel.

Plain Chinese summary: 这次只改公开版本说明。大白话：README 和 versioning
文档现在都说 v0.18.0 已经是当前正式 release，v0.18.1 只是发版面和证据加固补丁，
v0.19.0 才是后续真实撤单能力线；不发布新 tag，也不改运行时撤单行为。

## Files Changed

```text
README.md
ROADMAP.md
docs/versioning.md
docs/rust-cutover/versioning.md
docs/rust-cutover/release/README.md
docs/rust-cutover/evidence/V181-002.md
scripts/ai/check_release_surface_current.sh
verification.md
```

## Path Note

Issue #571 named `docs/rust-cutover/versioning.md`, but the existing
authoritative versioning document was `docs/versioning.md`. This task updates
`docs/versioning.md` and adds `docs/rust-cutover/versioning.md` as a thin
rust-cutover validation entry that points back to the authoritative document.

## Boundary

The release-surface alignment preserves the v0.18 preview-only boundary:

```text
actual cancel send = not included
automatic cancel = disabled
automatic remediation = disabled
Dashboard order controls = disabled
Dashboard cancel controls = disabled
next patch = v0.18.1 release surface and provenance hardening
next capability = v0.19.0 owner-approved single-shot actual cancel
```

## Validation

```text
rg -n "v0\\.17\\.0|v0\\.17\\.1|v0\\.18\\.0|v0\\.18\\.1|v0\\.19\\.0" README.md docs/rust-cutover/versioning.md docs/rust-cutover/release = PASS
stale current/latest v0.17 or future/unpublished v0.18 surface scan = no matches in current release-surface files
NTPRO_RELEASE_SURFACE_ALLOW_MISSING_TAG=1 scripts/ai/check_release_surface_current.sh = PASS, current_release_version=v0.18.0
scripts/ai/verify_release.sh release-surface-current-guard = PASS, current_release_version=v0.18.0
bash -n scripts/ai/check_release_surface_current.sh = PASS
scripts/ai/verify_fast.sh = PASS
git diff --check = PASS
```

## Review / Merge Status

```text
PR = pending
Hosted Rust Cutover Smoke = pending
Issue closeout = pending until PR merge closes #571
```

## Rollback Plan

Revert this PR to restore the previous public release-surface wording and
remove `docs/rust-cutover/versioning.md` plus this evidence file. This does not
require exchange, credential, order, cancel, artifact, or runtime cleanup
because the change is documentation and evidence only.
