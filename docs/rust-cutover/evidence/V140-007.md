# V140-007 Evidence

Date: 2026-06-22
Executor: Codex

## Summary

V140-007 wires v0.14 release gates into local release verification, PR smoke,
and the release-tag workflow. The gate aggregates existing v0.14 proofs and
keeps default execution offline and no-mutation.

Plain Chinese summary: 这次是“发布门禁收口”，不是开放实盘交易。v0.14 gate 会检查
订单状态只读 proof、Supervisor shadow runtime、live-alpha dry-run order gate、risk
preflight 和 reconciliation golden traces，并且继续证明没有真实下单、没有生产改单、
没有 Dashboard 下单控件、没有真实资金交易。

## Changed

- Added `scripts/ai/verify_v14_release_gates.sh`.
- Added `scripts/ai/verify_release.sh v14-release-gates`.
- Added `release-v14-release-gates` to `.github/workflows/release-tag.yml`.
- Added v14 change classification and `v14-release-gates` step to
  `.github/workflows/rust-cutover-smoke.yml`.
- Added task/evidence documentation for V140-007.

## Not Changed

- No production order submission.
- No production cancel, replace, amend, retry, correction, or flatten.
- No live-alpha execution adapter call.
- No production network action by default.
- No listenKey lifecycle.
- No Dashboard order controls.
- No credential entry.
- No trading semantic change.
- No tag creation or GitHub Release publication.

## Validation

Planned / executed commands for this task:

```text
bash -n scripts/ai/verify_v14_release_gates.sh scripts/ai/verify_release.sh
ruby -e 'require "yaml"; YAML.load_file(".github/workflows/rust-cutover-smoke.yml"); YAML.load_file(".github/workflows/release-tag.yml"); puts "yaml ok"'
scripts/ai/verify_v14_release_gates.sh
scripts/ai/verify_release.sh v14-release-gates
scripts/ai/verify_fast.sh
git diff --check
```

## Required No-Mutation Evidence

The v14 release gate must keep these values true in local/CI/default release
execution:

```text
network_default_offline=true
production_order_submission_allowed=false
production_order_mutation_allowed=false
production_order_state_reads_allowed=owner_gated_only
production_order_state_reads_attempted=0 by default offline
production_orders_submitted=0
production_order_mutations_attempted=0
listen_key_lifecycle_allowed=false
dashboard_order_controls_enabled=false
execution_adapter_called=false
order_endpoint_access_attempted=false
real_orders_submitted=false
real_funds_enabled=false
production_trading_enabled=false
values_are_exchange_truth=false by default offline
```
