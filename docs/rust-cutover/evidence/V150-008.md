# V150-008 Evidence

Date: 2026-06-22
Executor: Codex
Task: V150-008 Dashboard live alpha mutation preflight panel

## Summary

Implemented a read-only Dashboard view for the v0.15 live-alpha mutation
preflight artifact chain. The panel displays owner approval state, redacted
request preview metadata, dry-run execution adapter state, kill switch runtime
gate state, order-state age, missing gates, and read-only boundary counters.

Plain Chinese summary: Dashboard 现在能看见 v0.15 mutation preflight 的状态，但仍然
不能在页面上下单、撤单、改单、重试或重连。只要工件里出现 request sent 或 Dashboard
order control 的痕迹，面板就会降级成 boundary violation。

## Files Changed

- `crates/cli/src/dashboard.rs`
- `.github/workflows/rust-cutover-smoke.yml`
- `docs/rust-cutover/tasks/V150-008.md`
- `docs/rust-cutover/evidence/V150-008.md`
- `docs/rust-cutover/release/v0_15_0_dashboard_mutation_preflight_panel.md`
- `docs/rust-cutover/release/README.md`

## Behavior Impact

Added read-only Dashboard DTO fields and static HTML rendering for:

- manual approval lifecycle;
- order request preview;
- execution dry-run;
- kill switch runtime gate;
- artifact paths and read-only boundary counters.

No trading behavior changed. No production order submission, cancel, replace,
amend, retry, reconnect, production adapter call, real funds, or production
trading path was added.

## Validation

Passed:

```text
cargo fmt --all --check
cargo test -p nautilus-cli live_alpha_v15_dashboard --lib
cargo test -p nautilus-cli dashboard --lib
scripts/ai/verify_fast.sh
cargo check -p nautilus-cli
cargo clippy -p nautilus-cli --all-targets -- -D warnings
cargo test -p nautilus-cli --test golden_trace_live_alpha_mutation_dry_run
scripts/ai/verify_v15_incident_rollback_artifact.sh
ruby -e 'require "yaml"; YAML.load_file(".github/workflows/rust-cutover-smoke.yml"); puts "rust-cutover-smoke yaml ok"'
git diff --check
```

Pending final closeout validation:

```text
hosted PR smoke
```
