# V230-003 Evidence - multi-strategy supervisor isolation

Date: 2026-07-03
Executor: Codex
Task: `V230-003` / GitHub issue `#714`
Milestone: `v0.23.0`
Status: LOCAL VALIDATION PASSED

## Summary

V230-003 adds executable Rust replay coverage for strategy supervisor identity
partitioning. It proves isolated strategy supervisor partitions,
cross-strategy component mismatch fail-closed behavior, and unknown strategy
identity fail-closed behavior.

Plain Chinese summary: 本任务把 #714 的多策略隔离落到 Rust 可执行 replay。新增 fixture
和测试要求 `strategy_key`、`account_key`、`venue_node_key`、`isolation_scope_key`
明确存在；跨策略串线和 unknown strategy identity 都会 fail closed。本任务不新增实盘
submit、不新增策略驱动生产执行、不新增 Dashboard 操作控件。

## Files Changed

```text
crates/cli/tests/golden_trace_read_model_projection.rs
tests/golden/read_model_strategy_supervisor_schema.jsonl
docs/rust-cutover/golden_trace/RELEASE_REPLAY_SCOPE.json
docs/rust-cutover/tasks/V230-003.md
docs/rust-cutover/release/v0_23_0_multi_strategy_supervisor_isolation.md
docs/rust-cutover/evidence/V230-003.md
docs/rust-cutover/release/README.md
verification.md
```

## Contract Traceability

```text
V230-001 Identity Model = satisfied by strategy_key, account_key, venue_node_key, and isolation_scope_key replay assertions
V230-001 Strategy Boundary = satisfied by cross_strategy_component_mismatch fail-closed fixture
V230-001 Future Owner-Approved Control Paths = satisfied by per-scope approval boundary assertions and disabled strategy-driven production execution
V230-001 Logs And Evidence Boundary = satisfied by release replay scope entries and source_provenance assertions
```

## Boundary

```text
runtime_behavior_change = read_model_projection_test_harness_only
adapter_behavior_change = false
dashboard_behavior_change = false
release_publication = false
new_submit_capability = false
strategy_driven_production_execution_allowed = false
production_order_submission_allowed = false
dashboard_operation_controls_enabled = false
venue_node_lifecycle_started = false
```

## Validation

```text
cargo test -p nautilus-cli --test golden_trace_read_model_projection -- --nocapture = PASS, 3 tests passed
python3 -c 'import json,pathlib; [json.loads(line) for line in pathlib.Path("tests/golden/read_model_strategy_supervisor_schema.jsonl").read_text().splitlines() if line.strip()]' = PASS
jq . docs/rust-cutover/golden_trace/RELEASE_REPLAY_SCOPE.json = PASS
python3 scripts/ai/validate_golden_trace_release_scope.py = PASS, 89 cases, 84 executable replay, 5 schema-only scoped
rg -n "read_model.strategy_supervisor|strategy_key|isolation_scope_key|cross_strategy_component_mismatch|missing_strategy_key|rust_cli_read_model_projection_replays_v230_strategy_supervisor_paths" tests/golden/read_model_strategy_supervisor_schema.jsonl crates/cli/tests/golden_trace_read_model_projection.rs docs/rust-cutover/golden_trace/RELEASE_REPLAY_SCOPE.json docs/rust-cutover/evidence/V230-003.md = PASS
scripts/ai/verify_fast.sh = PASS, fast smoke only
git diff --check = PASS
```

## Behavior Impact

Adds executable read-model replay coverage for multi-strategy supervisor
partitioning. It does not add production submit, order mutation,
strategy-driven production execution, venue node lifecycle behavior, or
Dashboard operation controls.

## Public API Impact

None.

## Rollback Plan

Revert this PR to remove V230 strategy supervisor replay fixtures, harness
logic, and evidence. The V230-001 contract and V230-002 account partitioning
remain intact.
