# TTR-002 Evidence

Date: 2026-07-28
Executor: Codex
GitHub issue: `#1151`
Branch: `codex/ttr-002-event-store-terminal-boundary`
Status: `REVIEW_REQUIRED`

## 中文摘要

本任务处理 `project.html` 的 Event Store Medium 审查项。Event Store 仍是
Early alpha 的单实例嵌入式权威日志，但 README 现在明确规定：Trader Terminal
和浏览器只能消费经过可信 Rust 服务投影、脱敏、携带 provenance/freshness 且
独立版本化的只读 DTO，不能直接读取 crate API、原始 payload、run 目录或
`.redb` 文件。

Dashboard HTTP 回归测试新增了五个代表性禁止路径，并要求它们始终返回
`404 Not Found`。六个 v0.28 Trader Terminal GET 路由及其非 GET 拒绝行为继续
由同一真实 HTTP 测试覆盖。

本任务没有修改 Event Store 持久化、编码、回放或 kernel 语义；没有修改
v0.32.0 冻结文件，也没有增加任何交易写能力。

## Files Changed

- `crates/event_store/README.md`
- `crates/cli/src/dashboard/server/tests.rs`
- `docs/rust-cutover/tasks/TTR-002.md`
- `docs/rust-cutover/evidence/TTR-002.md`

## Negative HTTP Boundary

The following representative paths must remain unmounted:

```text
GET /api/event-store
GET /api/event-store/raw
GET /api/redb
GET /api/runs/example/events
GET /event_store/trader-001/run.redb
```

Expected result for every path: `404 Not Found`.

## Local Validation

```text
cargo fmt --all -- --check
result = pass

cargo test -p nautilus-cli --lib \
  dashboard::server::tests::trader_terminal_v28_http_routes_serve_read_only_contracts \
  -- --nocapture
result = pass; 1 passed, 0 failed

cargo clippy --locked -p nautilus-cli --lib --tests -- -D warnings
result = pass

scripts/ai/verify_fast.sh
result = pass

scripts/ai/check_backend_runtime_risk_inventory.sh
result = pass; 28,985 signals in 1,215 files; no baseline drift

scripts/ai/verify_release.sh current-governance backend-freeze-baseline
result = pass; current v0.33.0 governance and frozen v0.32.0 baseline

git diff --check
result = pass
```

## Behavior Impact

- Documentation and regression hardening only.
- No new route, DTO, Event Store operation, or runtime capability.
- Existing Dashboard and Trader Terminal behavior remains unchanged.

## Public API and Migration

- Public API impact: none.
- Storage/schema impact: none.
- Migration note: not required.

## Rollback

Remove the README boundary section and the five negative HTTP assertions. No
data, schema, runtime state, or deployment rollback is required.

## Review Status

Implementation and local validation are complete. The owner role has not
approved its own work; Verification & Release Gatekeeper review remains
required before merge.
