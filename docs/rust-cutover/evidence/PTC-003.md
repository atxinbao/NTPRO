# PTC-003 Rust Read-Model Schema Evidence

Date: 2026-07-15
Executor: Codex
GitHub issue: #1098
Milestone: python-tooling-closeout
Status: PRE-MERGE VALIDATION PASSED

## Hosted Gate Remediation

The first hosted security run (`29435532227`) found two dependency-governance
gaps after the Rust JSON Schema migration: `MIT-0` was not in the repository
license allowlist, and the 16 new `jsonschema` dependency-closure crates did not
have exact-version cargo-vet coverage. The remediation changes governance data
only; runtime, trading behavior, schema behavior, and resolver features remain
unchanged.

```text
cargo-deny MIT-0 allowlist entry = added
cargo-vet exact-version safe-to-deploy exemptions = 16
exemption review suggestions = retained
source policy relaxation = none
advisory policy relaxation = none
```

## Corrected Authority Scope

```text
all read-model snapshots in current golden traces = 124
v0.21 contract snapshots validated = 36
non-v0.21 snapshots excluded by contract version = 88
later event-only read-model rows excluded = 66
negative mutations = 8
```

The retired Python command with its default recursive glob currently fails at
`read_model.slo_freshness_diagnostics.ready.001` because that later event does
not carry `payload.snapshot`. This was an authority-scope defect, not a failing
v0.21 snapshot.

## Change Boundary

```text
Rust JSON Schema implementation = jsonschema 0.47.0, Draft 2020-12
external schema resolver features = disabled
retired Python validators in this issue = 1
tracked Python files before = 6
tracked Python files after = 5
runtime or public API impact = none
trading-semantic impact = none
docs/rust-cutover/release/v0_32_0_* changed = false
```

## Validation

```text
cargo fmt --all -- --check = PASS
cargo test -p ntpro-governance --locked = PASS, 9 tests
cargo clippy -p ntpro-governance --all-targets -- -D warnings = PASS
cargo deny --all-features check advisories licenses sources bans = PASS
cargo vet --locked = PASS, 169 fully audited, 27 partially audited, 696 exempted
v0.21.1 schema boundary gate = PASS
validated read-model snapshots = 36
negative mutations = 8, intended reason asserted
modified shell syntax = PASS
jsonschema network resolver dependencies = 0
retired validator executable references = 0
docs/examples governance = PASS, 109 markdown files
backend freeze baseline = PASS, 27 boundaries and 4 source hashes
backend freeze negative selftest = PASS, 20 cases
scripts/ai/verify_fast.sh = PASS
tracked Python files after deletion = 5
git diff --check = PASS
docs/rust-cutover/release/v0_32_0_* changed = false
```

Commands:

```bash
cargo fmt --all -- --check
cargo test -p ntpro-governance --locked
cargo clippy -p ntpro-governance --all-targets -- -D warnings
cargo deny --all-features check advisories licenses sources bans
cargo vet --locked
scripts/ai/verify_v21_1_read_model_schema_boundary.sh
cargo tree -p ntpro-governance --edges normal
scripts/ai/check_docs_examples_governance.sh
scripts/ai/check_backend_freeze_baseline.sh
scripts/ai/verify_fast.sh
git diff --check
```
