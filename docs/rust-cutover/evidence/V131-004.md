# V131-004 Evidence - Kill switch dry-run runtime propagation

Date: 2026-06-22
Executor: Codex

## Summary

V131-004 propagates the v0.13 kill-switch dry-run approval artifact into
read-only local runtime status surfaces.

Plain Chinese summary: 现在 kill switch dry-run 不只是落在 JSON 工件里，也会出现在
Supervisor metrics、Supervisor risk 和 Dashboard 生产阴影状态面板里。所有字段仍然
表达“禁止生产提交/撤改/订单状态读取/listenKey/Dashboard 下单控件”，没有打开实盘交易。

## Runtime Status Surface

The propagated fields include:

```text
kill_switch_artifact_status
kill_switch_active
kill_switch_dry_run
manual_approval_recorded
approval_state
production_order_submission_allowed
production_order_mutation_allowed
production_order_state_reads_allowed
listen_key_lifecycle_allowed
production_order_submissions_attempted
production_orders_submitted
production_order_mutations_attempted
production_order_state_reads_attempted
dashboard_order_controls_enabled
real_orders_submitted
network_attempted
values_are_exchange_truth
```

## Boundary

The status propagation remains read-only:

```text
production_order_submission_allowed = false
production_order_mutation_allowed = false
production_order_state_reads_allowed = false
listen_key_lifecycle_allowed = false
dashboard_order_controls_enabled = false
real_orders_submitted = false
network_attempted = false
values_are_exchange_truth = false
```

## Validation

Local validation completed:

```bash
cargo test -p nautilus-cli node_metrics_expose_kill_switch_dry_run_artifact_status --lib = PASS
cargo test -p nautilus-cli production_shadow_v13_kill_switch_artifact_populates_dashboard_boundary_panel --lib = PASS
cargo test -p nautilus-cli production_kill_switch_approval_artifact --lib = PASS
cargo test -p nautilus-cli production_shadow --lib = PASS
cargo test -p nautilus-cli dashboard --lib = PASS
cargo test -p nautilus-cli supervisor --lib = PASS
scripts/ai/verify_v13_kill_switch_approval_artifact.sh = PASS
scripts/ai/verify_v13_dashboard_control_boundary.sh = PASS
scripts/ai/verify_release.sh v13-no-production-mutation-gate = PASS
scripts/ai/verify_fast.sh = PASS
cargo fmt --all --check = PASS
git diff --check = PASS
```

## Status

Ready for PR review. Hosted smoke is required before merge.
