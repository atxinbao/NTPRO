# V130-002 Evidence - Shadow Preflight Session Loop

Date: 2026-06-21
Executor: Codex

## Summary

Implemented a local v0.13 guarded-live-alpha shadow preflight session loop.

Plain Chinese summary: 本任务新增了一个本地 preflight loop。它能写 heartbeat，
能通过 stop-file 停止，能发现 shadow portfolio runtime 过期。它仍然不联网、
不下生产订单、不撤单、不改订单，也不启用 Dashboard 下单控件。

## Files

- `crates/cli/src/opt.rs`
- `crates/cli/src/live.rs`
- `scripts/ai/verify_v13_shadow_preflight_session.sh`
- `docs/rust-cutover/tasks/V130-002.md`
- `docs/rust-cutover/release/v0_13_0_shadow_session_preflight.md`
- `docs/rust-cutover/evidence/V130-002.md`

## Boundary Evidence

Every `ntpro.v130_shadow_preflight_session_event.v1` event records:

```text
session_network_attempted=false
production_order_submissions_attempted=0
production_orders_submitted=0
production_order_mutations_attempted=0
production_order_state_reads_attempted=0
listen_key_lifecycle_attempted=0
cancel_replace_amend_attempted=false
dashboard_order_controls_enabled=false
real_orders_submitted=false
values_are_exchange_truth=false
```

## Validation

Completed locally:

```text
cargo test -p nautilus-cli production_shadow_preflight_session --lib = PASS
scripts/ai/verify_v13_shadow_preflight_session.sh = PASS
scripts/ai/verify_fast.sh = PASS
cargo check -p nautilus-cli = PASS
cargo clippy -p nautilus-cli --lib --tests -- -D warnings = PASS
target/debug/nautilus live --help | rg -n "production-shadow-preflight-session" = PASS
git diff --check = PASS
```
