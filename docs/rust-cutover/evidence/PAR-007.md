# PAR-007 Production Mutation Executor Retirement Evidence

Date: 2026-07-29
Executor: Codex
GitHub issue: #1175
Status: LOCAL VALIDATION PASSED / REVIEW_REQUIRED

## Finding

BFR-001 rejected `--manual-online` before input or environment access, but
`crates/cli/src/live.rs` still compiled:

- a blocking production POST executor for guarded send;
- a blocking production DELETE executor for actual cancel;
- credential environment constructors and signed-request builders;
- HTTP result types and approval-consumption behavior.

The wrapper protected the current command path, but the current product binary
still retained dormant production mutation reachability.

## Repair Boundary

```text
production POST executor retained = false
production DELETE executor retained = false
credential CLI inputs retained = false
production signing path retained = false
approval consumption retained = false
historical artifact schemas/readers retained = true
current network/mutation counters = zero
v0.32.0 frozen files changed = false
```

Current artifacts use:

```text
mode = retired_*_offline_evaluation
credential_material = retired_not_read
production_signing_material_env_read = false
error/source = not_attempted_executor_retired / executor_retired_offline
```

Downstream readback and failure-evidence tests use an explicitly synthesized,
test-only historical attempt artifact. The current command cannot emit that
attempt state.

## Validation

```text
cargo test -p nautilus-cli --lib --no-run = PASS
targeted retired source/parser/guarded-send tests = PASS
targeted actual-cancel tests = PASS
complete nautilus-cli library suite = PASS (506 passed, 0 failed, 0 ignored)
high-precision dependency compile =
  PASS (nautilus-cli with nautilus-model/high-precision)
Clippy all targets/all features/high precision = PASS
current nautilus binary forbidden executor markers = absent
retired command help online/credential flags = absent
golden replay = PASS
  289 total
  98 executable
  186 validator executable
  5 schema-only scoped
backend runtime risk inventory current summary =
  29,135 signals / 1,215 files / canonical hash
  95e94c4b85b984777cc3c35679b4333a511fa50de531c5f09ee63dbe650cd967
current governance + backend freeze baseline = PASS
backend freeze negative self-test = PASS (20 cases)
verify_fast with workspace cargo check = PASS
git diff --check = PASS
v0.32.0 frozen file diff = empty
```

## Review

Critical risk. Independent review and all hosted checks are required before
manual merge. Auto-merge is forbidden.

## Rollback

Do not restore the retired executors as a rollback. Revert only if the offline
historical reader contract is defective, then keep the network executor
deletion and repair the reader separately.
