# V160-002 Evidence - owner-approved production mutation runtime gates

Date: 2026-06-23
Executor: Codex
Milestone: v0.16.0
Status: local validation PASS; hosted smoke pending PR

## Summary

V160-002 adds the v0.16 production mutation runtime gate artifact. The gate
aggregates the v0.15 dry-run order gate, risk preflight, request preview, and
kill-switch runtime gate before any production mutation send can be considered.

Plain Chinese summary: 已加 v0.16 “发送前总闸门”。大白话：它会把前面所有证据
合在一起检查，但现在还没有 V160-003 的 signing approval，所以默认仍然 blocked，
不会发请求、不会联网、不会真实下单。

## Files

```text
crates/cli/src/opt.rs
crates/cli/src/live.rs
scripts/ai/verify_v16_runtime_gates.sh
docs/rust-cutover/tasks/V160-002.md
docs/rust-cutover/evidence/V160-002.md
```

## Artifact Contract

```text
schema_version = ntpro.v160_production_mutation_runtime_gate.v1
capability = Minimum Owner-Approved Production Order Mutation Candidate
capability_expansion_from_v15 = true
default_fail_closed = true
owner_approval_required = true
owner_approval_consumed = true
kill_switch_checked_before_send = true
production_order_submission_allowed_policy = owner_approved_single_limit_gtc_only
production_order_submissions_attempted = 0
production_orders_submitted = 0
production_order_mutations_attempted = 0
order_type = LIMIT
time_in_force = GTC
request_redacted = true
response_redacted = true
signature_recorded = false
signed_query_recorded = false
signed_url_recorded = false
api_key_value_recorded = false
api_secret_value_recorded = false
dashboard_order_controls_enabled = false
automatic_remediation_attempted = false
retry_attempted = false
cancel_attempted = false
replace_attempted = false
amend_attempted = false
flatten_attempted = false
listen_key_lifecycle_attempted = 0
```

## Boundary

The task intentionally keeps the send path closed:

```text
runtime_gate_open = false
send_consideration_allowed = false
signing_approval_required = true
signing_approval_ready = false
explicit_send_gate_open = false
request_sent = false
network_attempted = false
real_orders_submitted = false
production_trading_enabled = false
```

## Validation

Local validation completed:

```text
cargo fmt --all --check = PASS
cargo test -p nautilus-cli parses_live_production_mutation_runtime_gate_options --lib = PASS
cargo test -p nautilus-cli production_mutation_runtime_gate --lib = PASS
cargo clippy -p nautilus-cli --all-targets -- -D warnings = PASS
scripts/ai/verify_v16_runtime_gates.sh = PASS
scripts/ai/verify_fast.sh = PASS
git diff --check = PASS
target/debug/nautilus live production-mutation-runtime-gate --help = PASS
```

`scripts/ai/verify_v16_runtime_gates.sh` generated and checked these blocked
paths:

```text
blocked_missing_gate
blocked_kill_switch_active
blocked_request_preview
blocked_signing_approval
```

## Rollback Plan

Revert this PR to remove the v0.16 runtime gate CLI command, artifact builder,
verification script, task, and evidence file. No exchange state, credentials,
network request, production order, or trading runtime behavior requires
rollback.
