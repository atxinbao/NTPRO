# V171-007 Evidence - Dashboard Artifact Diagnostics

Date: 2026-06-26
Executor: Codex
Status: PASS
Issue: #537

## Summary

The v0.17 Dashboard reconciliation/orphan panel now surfaces explicit read-only
diagnostics for missing artifacts, schema mismatches, provenance/hash mismatches,
and stale evidence.

Plain Chinese summary: Dashboard 现在会说清楚 v0.17 对账证据哪里坏了。大白话：不是只显示
degraded，而是直接列出缺哪个 artifact、哪个 schema 版本不对、哪个 source ref 的
sha256/bytes 对不上，以及哪个证据过期需要重新生成。

## Files Changed

```text
crates/cli/src/dashboard.rs
scripts/ai/verify_v17_dashboard_reconciliation_panel.sh
docs/rust-cutover/evidence/V171-007.md
```

## Evidence Contract

```text
missing_artifacts_displayed = true
schema_expected_actual_displayed = true
provenance_sha256_or_bytes_mismatch_displayed = true
stale_artifacts_displayed = true
dashboard_order_controls_enabled = false
dashboard_cancel_controls_enabled = false
no_order_button = true
no_cancel_button = true
no_fetch_or_credential_input = true
```

## Validation

```text
PASS bash -n scripts/ai/verify_v17_dashboard_reconciliation_panel.sh
PASS cargo fmt --check -p nautilus-cli
PASS scripts/ai/verify_v17_dashboard_reconciliation_panel.sh
PASS cargo clippy -p nautilus-cli --all-targets -- -D warnings
PASS scripts/ai/verify_fast.sh
PASS git diff --check
PASS cargo clippy --workspace --lib --tests --features "arrow,ffi,high-precision,streaming,defi" -- -D warnings
```

## Behavior Impact

No trading behavior changes. The Dashboard remains a local read-only evidence
viewer and does not expose order, cancel, retry, remediation, credential, or
network controls.

## Rollback Plan

Revert this PR to remove the additional read-only diagnostics fields, renderer
rows, verifier expansion, and evidence doc.
