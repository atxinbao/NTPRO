# BPO-006 Measured Runtime Hotspot Evidence

Date: 2026-07-20
Executor: Codex
GitHub issue: #1125
Milestone: v0.33.0-backend-maintenance
Status: LOCAL VALIDATION PASSED; HOSTED VALIDATION PENDING

## Scope And Threshold

BPO-006 selects one benchmark-proven hotspot family: the default-quota path in
`RateLimiter::check_key`. No other runtime path is changed.

The acceptance threshold was fixed before the implementation:

```text
decision metric = median of three Criterion sessions
required improvement = at least 10%
baseline and candidate CV = at most 5%
correctness = default, keyed, dynamic-add, boundary, property, and concurrency tests pass
material regression elsewhere = none
```

## Baseline And Profile

Baseline commit:
`73d6feac35859eab7063610a59d4f6c796ad0517`

Command:

```bash
scripts/ai/run_backend_benchmark_workload.sh \
  network_rate_limiter /Users/mac/Documents/NTPRO \
  73d6feac35859eab7063610a59d4f6c796ad0517 \
  /tmp/ntpro-bpo006-baseline/network-rate-limiter.json \
  /tmp/ntpro-bpo006-target
```

Result:

```text
observations_ns = [41.890624927749045, 44.838881897466564, 46.02628241693385]
median_ns = 44.838881897466564
coefficient_of_variation_pct = 3.928957198511956
```

A 10-second `/usr/bin/sample` capture of the same bench-lto binary recorded
7,959 benchmark-loop samples. The keyed-quota lookup at `mod.rs:219` accounted
for 1,819 samples before the default GCRA path was entered. This confirms the
unnecessary `DashMap` lookup as a concrete cost center when the limiter has no
keyed quotas.

The pre-commit candidate capture used the same binary target and methodology:

```text
observations_ns = [38.46950294892685, 39.07904510624826, 42.57309069230463]
median_ns = 39.07904510624826
coefficient_of_variation_pct = 4.51538857669139
improvement = 12.845629836152849%
threshold result = PASS
```

A second 10-second sample of the candidate recorded no samples on the
keyed-quota lookup branch. The final candidate capture is repeated from the
committed SHA so the evidence does not treat a dirty-worktree identifier as
release authority.

## Implementation

`RateLimiter` records whether any keyed quota has been published. The common
default-only path uses one acquire load and directly evaluates the default
GCRA. `add_quota_for_key` inserts the GCRA before publishing the flag with a
release store, so a completed add remains visible before keyed lookup is
enabled.

The rate algorithm, clock read, state-store CAS, quota values, waiting behavior,
public method signatures, and keyed lookup behavior remain unchanged.

## Change Boundary

```text
trading semantics changed = false
public API or schema changed = false
network attempts or adapter sends added = false
retry, recovery, or remediation added = false
docs/rust-cutover/release/v0_32_0_* changed = false
forbidden backend capability inherited = false
```

## Validation

```text
cargo fmt --all -- --check = PASS
git diff --check = PASS

cargo test --locked -p nautilus-network ratelimiter --lib =
PASS, 24 tests

cargo test --locked -p nautilus-network --lib =
PASS, 308 tests

cargo nextest run --locked -p nautilus-network -E 'test(/ratelimiter/)' =
PASS, 24 tests across 4 binaries
nextest run ID = 3267b88d-11d1-4a2d-bdc7-ba0a328ff634

dynamic keyed-quota publication soak =
PASS, 100 isolated iterations

cargo clippy --locked -p nautilus-network --all-targets -- -D warnings =
PASS

backend runtime risk inventory =
PASS with rg and grep implementations
signals = 28,985, files = 1,215
signal and ownership counts changed = false
canonical source-location hash =
f7a23543e4463626fe6363c7da67628d991849deca18c762b5cd9e433d34ab48

scripts/ai/verify_release.sh current-governance backend-freeze-baseline \
  backend-performance-baseline rust-only-gates =
PASS

repository target after direct Criterion profile output = 36 KiB
repository target cleanup = pending final validation
```

Golden validation is included by `current-governance` through the validate-only
schema trace. No new golden trace is required because the GCRA algorithm,
clock, ordering, quota decisions, adapter behavior, and trading semantics are
unchanged.

Hosted benchmark, smoke, security checks, final independent review, and merge
remain required. Auto-merge is forbidden for this high-risk issue.

## Rollback

Revert the BPO-006 PR. No data, schema, deployment, or configuration migration
is required.
