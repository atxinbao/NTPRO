# PTC-008 Zero-Python Repository Tooling Closeout Evidence

Date: 2026-07-17
Executor: Codex
GitHub issue: #1103
Milestone: python-tooling-closeout
Status: PRE-MERGE LOCAL VALIDATION PASSED

## Delivered Guard

```text
implementation = crates/governance/src/zero_python.rs
CLI = ntpro-governance zero-python-closeout
shell entry = scripts/ai/check_zero_python_closeout.sh
release integration = scripts/ai/verify_release.sh current-governance
hosted integration = Rust Cutover Smoke
authority = source_tree_plus_github_remote
```

## Dependency Proof

```text
PTC-007 PR = #1110 merged
PTC-007 merge commit = c1af9d4e3021b1ca60314adc05cc8581d2ec8ed4
PTC-007 hosted smoke = run 29521582112 attempt 2 completed/success
PTC-007 security audit = run 29521582221 completed/success
PTC-007 issue #1102 = closed
```

## Local Contract

```text
tracked .py/.pyi/.pyx/.pxd/.ipynb/.pyc = 0 required
Python manifests/locks at any tracked path = absent required
.venv/venv/__pycache__/.pyc = absent required
supported script Python/uv/pytest/Ruff/pip execution = 0 required
workflow/action Python setup or wheel execution = 0 required
historical docs/rust-cutover evidence = retained required
v0.32.0 frozen release files changed = false required
negative selftests = direct Python, env/path Python, uv+pytest, Make prefix, timeout/env wrapper, Python shebang, setup-python action, wheel action, workflow Python shell, historical non-executable mention
```

## Validation

```text
cargo fmt --all -- --check = pass
cargo test -p ntpro-governance --locked = pass (27 tests)
cargo clippy -p ntpro-governance --all-targets --locked -- -D warnings = pass
scripts/ai/check_zero_python_closeout.sh = pass
guard summary = tracked_files=5253 active_scripts=55 workflow_actions=17 historical_docs=1929 negative_cases=10
alternate Git index negative integration = pass (tracked .py, executable Python script, setup-python workflow rejected)
bash syntax = pass
Rust Cutover Smoke YAML parse = pass
hosted smoke/security = pending PR
docs/rust-cutover/release/v0_32_0_* diff = empty
```

The first final hosted smoke run reached and passed workspace check, clippy,
product-path lint, current governance, and all 496 CLI tests, then exposed a
false positive in the older Rust-only runtime grep: it interpreted the new
guard's `maturin` detector pattern as a product dependency. Detector-definition
lines are now explicitly marked and excluded from that legacy grep; real
product-path references remain fail closed.

## Post-Merge Requirement

The PR cannot contain its own merge SHA, final hosted conclusion, issue close,
or milestone close without circular proof. After merge, GitHub live state must
show issues #1096-#1103 closed, every PTC PR merged, required hosted checks
successful, no open milestone PR, and milestone #33 closed.
