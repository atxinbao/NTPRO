# V080-008 Evidence - v0.8.0 readiness and release notes candidate

Date: 2026-06-16
Executor: Codex
Task: V080-008
Branch: `codex/V080-008-readiness-release-notes`

## 中文摘要

本任务把 v0.8.0 的发布候选口径收口成文档：v0.8.0 只代表
authenticated Binance testnet read-only proof，不代表真实 Binance 实盘、不代表生产交易、
不代表真实资金，也不包含下单、撤单、改单、账户变更或 Dashboard 主动联网。

本任务不创建 tag，不发布 GitHub Release。正式发布必须等 V080-009 owner approval。

## 修改目标

- 对齐 v0.8 boundary 文档中的实际 env 名和 artifact schema。
- 新增 readiness report，列出 V080 task gate 状态。
- 新增 release notes candidate。
- 明确 V080-009 仍是 owner-approved release closure，不在本 PR 内完成。

## 文件变更

- `docs/rust-cutover/release/v0_8_0_authenticated_readonly_boundary.md`
  - 对齐实际 env 名：`BINANCE_TESTNET_API_KEY` /
    `BINANCE_TESTNET_API_SECRET`。
  - 对齐实际 artifact schema：
    `ntpro.v08_binance_testnet_authenticated_readonly_probe.v1`。
  - 删除旧 planning-only artifact field 示例。
- `docs/rust-cutover/release/v0_8_0_authenticated_readonly_readiness_report.md`
  - 新增 v0.8 readiness report。
- `docs/rust-cutover/release/v0_8_0_release_notes.md`
  - 新增 v0.8 release notes candidate。
- `docs/rust-cutover/evidence/V080-008.md`
  - 记录本任务证据。

## 行为影响

无运行时代码变更，无交易语义变更，无 CI 执行语义变更。

## Public API 影响

无。

## Migration note 状态

不需要迁移。文档只对齐当前实现和发布候选口径。

## Rollback plan

回滚本 PR 即可移除 v0.8 readiness/release-notes candidate 文档，并恢复 boundary
文档原表述。运行时代码不受影响。

## 验证记录

通过：

```bash
scripts/ai/verify_fast.sh
git diff --check
rg -n "ntpro.v08_binance_testnet_authenticated_readonly_probe.v1|BINANCE_TESTNET_API_KEY|BINANCE_TESTNET_API_SECRET|V080-009|not yet published|RELEASE CANDIDATE" docs/rust-cutover/release/v0_8_0_authenticated_readonly_boundary.md docs/rust-cutover/release/v0_8_0_authenticated_readonly_readiness_report.md docs/rust-cutover/release/v0_8_0_release_notes.md docs/rust-cutover/evidence/V080-008.md
if rg -n "tag is [c]reated|GitHub Release is [p]ublished|production trading = [i]ncluded|real funds = [i]ncluded|production Binance connectivity = [i]ncluded|order submission = [i]ncluded|account mutation = [i]ncluded|Dashboard-started probes = [i]ncluded" docs/rust-cutover/release/v0_8_0_authenticated_readonly_readiness_report.md docs/rust-cutover/release/v0_8_0_release_notes.md docs/rust-cutover/evidence/V080-008.md; then exit 1; fi
```

结果摘要：

- `scripts/ai/verify_fast.sh`：通过；该脚本仍是 fast smoke，不是完整 release gate。
- `git diff --check`：通过。
- 正向 `rg`：确认 readiness/report/release notes candidate 使用实际 v0.8 artifact schema、
  testnet env 名、V080-009 closure 边界和 release-candidate wording。
- 负向 `rg`：没有发现 tag/GitHub Release 已发布、生产交易、真实资金、生产 Binance、
  下单、账户变更或 Dashboard-started probes 被写成 included/published 的文案。

## 剩余风险

- V080-009 仍需 owner approval 后才能创建 tag 和发布 GitHub Release。
- 本任务没有运行 manual online gate；manual online proof 仍是显式人工步骤。
