# MVP-013 - 浏览器总验收、性能基线与 MVP 最终冻结证据

Date: 2026-08-05
Executor: Codex
GitHub issue: #1233
GitHub PR: #1236
Status: REVIEW_REQUIRED

## 实现状态

- 新增 `ntpro.mvp_freeze_manifest.v1`，固定 19 个任务/issue/PR、M0-M4、单节点拓扑、
  六个产品合同入口、四类真实验收器、六工作负载性能合同、19 个 required-false 边界和
  13 个关键冻结源 hash；
- 新增冻结守卫及 7 类不可跳过负向自测：true/missing/substituted boundary、M4 提前
  complete、source hash 漂移、缺失或非法类型都必须非零退出；权威文件路径不可由环境替换；
- 新增中文发布/人工回滚说明，明确停止条件、空 workspace 重验、禁止自动 restart/retry
  和冻结后独立 intake；
- hosted smoke 在治理文件或任一 manifest 冻结源变化时强制复跑确定性闭环、11-case
  故障矩阵、机构工作台和控制中心双视口 Chrome，并输出聚合 final acceptance artifact；
- backend performance 在冻结合同变化时强制运行既有六组 same-runner 对比；
- MVP-012 的独立审查、hosted artifact、merge 和 issue closure 事实已回写。

## 本地验收

在吸收 TCG-002 / PR #1235 的 Rust 1.95.0 治理 merge 后重新执行：

- toolchain pin、MVP freeze guard、shell/Node/YAML/JSON syntax：PASS；freeze guard 仅依赖
  runner 基础命令，从 PATH 移除 ripgrep 后仍通过；
- deterministic acceptance：PASS，2 次结果一致，单节点生命周期
  `running,stopped,running,stopped`；
- fault matrix：PASS，11/11，其中 artifact faults 9、process faults 2，全部 fail closed，
  仅允许人工恢复；
- 机构工作台与控制中心 browser smoke：PASS，分别覆盖 `1440x1000`、`390x844`，
  身份/角色/事件/陈旧数据/HTTP error/required-false 边界成立；
- 四张主视口截图已人工检查，无空白、横向溢出或跨门户身份串线；
- `cargo test -p nautilus-cli mvp --lib -j 2`：47 passed；
- `cargo test -p nautilus-cli supervisor --lib -j 2`：39 passed；
- `cargo clippy -p nautilus-cli --all-targets --all-features -- -D warnings`：PASS；
- Rust-only、backend risk inventory、v0.32.0 backend freeze、current governance：PASS。

本地生成证据位于 `/tmp/ntpro-mvp013-1785869497-*`，不提交 `target/`、浏览器截图、
access token、cookie 或 generated artifact。

## Hosted 与独立审查

待 PR #1236 final head、五类 MVP artifact、六组性能 job、required checks 和独立
`REVIEW_APPROVED` 完成后回写。完成前不得把本文件标记 DONE，也不得把 PR 阶段结果
解释为 M4 已冻结。

## 保持关闭的能力

外部 Venue、真实订单、订单提交/修改、自动 retry、remediation、recovery、多节点、
生产 IAM 和产品级实盘终端全部保持关闭。本任务不创建 tag 或 GitHub Release。
