# V04-002 Evidence - Binance capability matrix

Date: 2026-06-13
Executor: Codex
Task ID: V04-002

## Plain Chinese Summary

这次只做 Binance v0.4 能力矩阵，不改 adapter 代码。

大白话说：矩阵把 Binance Spot 和 Binance USDT-M 分清楚了。Spot 可以作为
v0.4 的 sandbox 产品基础继续往下做，但也只能是 fixture / mock /
sandbox-only。USDT-M 虽然仓库里有 Rust adapter 和 fixture 代码，但这次不把
它包装成 v0.4 产品能力，默认先标记为 deferred / fixture-only / partial。

## Files Changed

- `docs/rust-cutover/inventory/binance_capability_matrix.md`
- `docs/rust-cutover/evidence/V04-002.md`

## Commands Run

```bash
rg -n "supported|partial|sandbox-only|fixture-only|deferred|removed" docs/rust-cutover/inventory/binance_capability_matrix.md
git diff --check
scripts/ai/verify_fast.sh
```

## Capability Classification Summary

- Binance Spot: `sandbox-only` as the v0.4 product foundation target.
- Binance Spot market data replay: `fixture-only` until `V04-005` proves the
  deterministic replay path.
- Binance Spot execution, risk, and strategy behavior: `sandbox-only` after the
  later V04 evidence tasks prove them.
- Binance Spot runtime data adapter and config surface: `partial`.
- Binance USDT-M: `deferred` for the v0.4 product claim.
- Binance USDT-M existing fixtures: `fixture-only` adapter evidence, not a user
  product claim.
- Real credentials: `removed` from the v0.4 product input surface.

## Validation Summary

- `rg` PASS: the matrix uses all required classification terms:
  `supported`, `partial`, `sandbox-only`, `fixture-only`, `deferred`, and
  `removed`.
- `git diff --check` PASS.
- `scripts/ai/verify_fast.sh` PASS: pinned Rust toolchain and `cargo fmt
  --check` passed. Cargo check and clippy were skipped by the fast-smoke
  default, as documented by the script output.

## Behavior Impact

No runtime behavior change. No trading semantic change. No adapter behavior
change.

## Public API Impact

None.

## Migration Note Status

Not required. This is an inventory and release-boundary documentation task.

## Rollback Plan

Revert this PR to remove the Binance capability matrix and V04-002 evidence
file.
