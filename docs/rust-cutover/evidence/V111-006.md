# V111-006 Evidence

Date: 2026-06-20
Executor: Codex
Task: V111-006 Fix openOrders wording conflict
Risk level: medium

## Goal

Align v0.11 release-facing wording so `/api/v3/openOrders` is not presented as
included in the v0.11 authenticated account snapshot support.

Plain Chinese summary: 这次只修公开文档的说法。v0.11 只支持离线的
`/api/v3/account` 账户快照合同，不支持生产 open orders 或订单状态读取。

## Files Changed

- `docs/rust-cutover/release/v0_11_0_authenticated_account_snapshot_contract.md`
  - Clarifies `/api/v3/openOrders`, `/api/v3/allOrders`, and `/api/v3/order`
    order-state reads are out of scope.
- `docs/rust-cutover/release/v0_11_0_boundary.md`
  - Narrows authenticated account snapshot wording to `/api/v3/account`.
  - Adds production order-state reads to forbidden v0.11 surfaces.
- `README.md`
  - Adds explicit v0.11 exclusion for production open-order/order-state reads.
- `docs/versioning.md`
  - Adds the same explicit v0.11 exclusion.
- `docs/rust-cutover/tasks/V111-006.md`
  - Records the task contract.
- `docs/rust-cutover/evidence/V111-006.md`
  - Records this evidence.

## Behavior Impact

Documentation is clearer. Runtime behavior is unchanged. The existing Rust
endpoint classifier already denies `GET /api/v3/openOrders` in v0.11.

No production network access, credential read, account mutation, order
submission, order cancel/replace/amend, Dashboard order control, or trading
semantic behavior is added.

## Public API Impact

No public API or CLI syntax changes.

## Migration Note

No migration note is required. This is release wording only.

## Validation

PASS:

```bash
rg -n "/api/v3/openOrders|open-order|order-state" README.md docs/versioning.md docs/rust-cutover/release -S
```

Result: v0.11 hits now explicitly say production open-order/order-state reads
are out of scope. The remaining v0.10 hits are historical sandbox order-boundary
documentation, not v0.11 authenticated account snapshot support.

PASS:

```bash
NTPRO_RELEASE_SURFACE_ALLOW_MISSING_TAG=1 scripts/ai/check_release_surface_current.sh
```

Result: release surface current guard passed for current `v0.11.0`, active
patch `v0.11.1`, and next capability `v0.12.0`.

PASS:

```bash
scripts/ai/verify_fast.sh
```

Result: toolchain and rustfmt fast smoke passed.

PASS:

```bash
git diff --check
```

Result: no whitespace errors.

## Rollback Plan

Revert this PR to restore the previous v0.11 wording. No exchange state,
credentials, orders, or published release artifacts require rollback.
