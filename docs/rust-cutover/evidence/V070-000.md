# V070-000 Evidence - v0.7 Read-Only Testnet Boundary

Date: 2026-06-14
Executor: Codex
Task ID: V070-000
Shrimp task ID: `64ff9f75-d6c8-4553-93ef-41cfe7a0c34e`
Branch: `codex/v070-000-readonly-boundary`
Risk: high

## Plain Chinese Summary

这次没有实现任何真实联网代码，只先把 v0.7 的边界写清楚。

v0.7 允许做的事情只有一件：在人工打开 online gate 后，做真实 Binance testnet
只读连通性证明。它不下单，不交易，不用真实资金，不做生产交易声明。

因为 V070-000 是 high risk 任务，本 PR 只能停在 `REVIEW_REQUIRED`，不能自动合并；
Shrimp 也不能在人工 review/merge 前标 completed。

## Goal

Define the v0.7.0 real Binance testnet read-only boundary, threat model,
fail-closed network opt-in contract, and artifact schemas before implementation.

## Files Changed

- `docs/rust-cutover/release/v0_7_0_readonly_testnet_boundary.md`
  - Added v0.7 read-only testnet boundary, fail-closed contract, gate split,
    HTTP/WebSocket schemas, secret policy, Dashboard boundary, and threat model.
- `docs/rust-cutover/release/README.md`
  - Added release evidence index entry.
- `docs/rust-cutover/tasks/V070-000.md`
  - Added task record.
- `docs/rust-cutover/evidence/V070-000.md`
  - Added this evidence record.

## Validation

| Command | Result | Summary |
| --- | --- | --- |
| `scripts/ai/verify_fast.sh` | PASS | Toolchain and fmt smoke passed. |
| `rg -n "read-only|no orders|no secret|secret|fail-closed|manual online gate|--allow-testnet-network|NTPRO_ALLOW_TESTNET_NETWORK=1|environment = testnet|order_submission = disabled|real_orders_submitted = false|Dashboard|HTTP|WebSocket|default CI|production trading" ...` | PASS | Boundary document and task record contain required safety terms. |
| `git diff --check` | PASS | No whitespace errors. |

## Behavior Impact

No runtime behavior change. No network behavior change. No Dashboard behavior
change. No trading semantic change.

## Public API Impact

No public API change. This is a design-gate document only.

## Migration Note

No migration note is required. The task does not change existing v0.6.1
artifact paths, JSON fields, CLI behavior, or Dashboard behavior.

## Review / Merge Status

High risk. Stop at `REVIEW_REQUIRED`.

Do not auto-merge. Do not mark Shrimp `completed` until the PR is reviewed,
merged, and the manual gate is closed.

## Rollback Plan

Revert this PR to remove the v0.7 boundary document and index/task/evidence
records. Runtime behavior remains unchanged because no code path was modified.
