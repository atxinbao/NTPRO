# PAR-008 Live CLI Ownership Boundary Split Evidence

Date: 2026-07-29
Executor: Codex
GitHub issue: #1176
Status: LOCAL VALIDATION PASSED / REVIEW_APPROVED / HOSTED_CHECKS_PENDING

## Ownership Result

```text
baseline crates/cli/src/live.rs = 36,780 lines
current crates/cli/src/live.rs = 21,857 lines
current live/node_runtime.rs = 802 lines
current live/tests.rs = 14,050 lines
top-level command dispatch owner = live/command.rs
node lifecycle owner = live/node_runtime.rs
behavioral regression owner = live/tests.rs
```

`node_runtime` owns live/sandbox node execution, status, metrics, heartbeat, and
shutdown handling. The parent module retains configuration and artifact
contracts. The move changes module ownership only; it does not change CLI or
runtime behavior.

## Risk Inventory Rebase

Moving inline tests from a mixed source file to `live/tests.rs` changes the
path-scoped ownership classification:

```text
production signals = 14,260 -> 13,351
test signals = 13,555 -> 14,461
matched files = 1,215 -> 1,216
total textual signal rows = 29,135 -> 29,132
```

The source contains 1,019 `.unwrap(` calls both before and after the move. The
three-row scan difference comes from line-based counting after formatting, not
from runtime hardening or removed assertions.

## Validation

```text
nautilus-cli library compile after test extraction = PASS
nautilus-cli library compile after node_runtime extraction = PASS
complete nautilus-cli library suite before structural regression =
  PASS (506 passed, 0 failed, 0 ignored)
module ownership regression = PASS
retired production mutation executor source guard = PASS
complete nautilus-cli standard-precision library suite =
  PASS (507 passed, 0 failed, 0 ignored)
complete nautilus-cli high-precision library suite =
  PASS (507 passed, 0 failed, 0 ignored)
Clippy all targets/all features/high precision = PASS
mechanical parent-module reconstruction diff = empty
mechanical node_runtime extraction diff = docs/blank lines only
mechanical tests extraction diff =
  ownership regression + complete production-source guard only
CLI help and parser regressions = PASS through complete library suites
live/sandbox runtime artifact regressions = PASS through complete library suites
golden replay = PASS
  289 total
  98 executable
  186 validator executable
  5 schema-only scoped
backend runtime risk inventory =
  PASS (29,132 signals / 1,216 files / canonical hash
  8084a0d876d9a9de0038c33bd14c2371cc09ed0e4587d5ae244e09d736e4a88f)
current governance + backend freeze baseline = PASS
backend freeze negative self-test = PASS (20 cases)
verify_fast with workspace cargo check = PASS
git diff --check = PASS
v0.32.0 frozen file diff = empty
```

## API And Migration

Rust public API changed: false.

CLI command, option, parser, help, serialization, and output contract changed:
false.

Migration note required: false. This is an internal ownership-only module move.

## Review

Independent high-risk review checked move-only fidelity, module visibility,
CLI/help/parser/output/runtime preservation, the complete retired mutation
executor source guard, path-scoped risk inventory ownership, and the v0.32.0
freeze boundary.

```text
independent reviewer = 019fabfa-1230-7583-8aac-d94d934e39da
reviewed implementation head = fc88122d36f9ece2911e76c6d6eb04ea8c6665cf
review result = REVIEW_APPROVED
remaining P1/P2/P3 findings = 0
```

Final-head hosted checks are required before manual merge. Auto-merge is
forbidden.

## Rollback

Revert the PAR-008 PR as one unit. Do not use rollback to restore production
mutation executors or alter the frozen capability boundary.
