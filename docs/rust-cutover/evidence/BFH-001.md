# BFH-001 Post-Freeze Backend Hygiene Authority Evidence

Date: 2026-07-19
Executor: Codex
GitHub issue: #1112
Milestone: post-freeze-backend-hygiene
Status: PRE-MERGE VALIDATION PASSED

## Summary

BFH-001 adds the cleanup authority map and captured inventory only. It does not
delete files, alter runtime behavior, change public APIs, or modify the frozen
v0.32.0 release package.

Plain Chinese summary: 已完成后端冻结后的仓库分类盘点。当前生成物目录均不存在，
8 个 `.DS_Store` 交给 BFH-002；`project.html`、本地分析目录和大型测试数据先保留并
确认所有权；tracked 配置、文档和 fixture 的后续清理由独立 issue 执行。

## Captured Inventory

```text
baseline commit = 0a265368ab
tracked repository files = 5253
tracked Rust files = 1948
tracked docs files = 2044
tracked docs/rust-cutover files = 1929
tracked crates files = 2700
tracked tests files = 339
tracked scripts files = 54
tracked GitHub files = 26
tracked examples files = 14
tracked Python/Cython/stub/notebook files = 0
v0.32.0 frozen release files = 22
local .DS_Store files = 8
local project.html = present, 52 KiB, untracked
local .agentflow = present, 1.0 MiB, ignored
local .codex = present, 37 MiB, ignored
local .understand-anything = present, 14 MiB, ignored
local tests/test_data/large = present, 237 MiB, ignored
target and target-v2 = absent
release-publication-evidence = absent
graphify-out = absent
.venv and Python caches = absent
```

## Change Boundary

```text
tracked files deleted = 0
runtime crates changed = false
public API or schema changed = false
docs/rust-cutover/release/v0_32_0_* changed = false
registered backend boundary changed = false
local user-owned paths deleted = 0
```

## Validation

```text
release surface current guard = PASS
docs/examples governance = PASS, markdown_files=116, local_links=293,
  image_links=20, integration_pages=15, concept_pages=9, tutorial_assets=20
Rust examples integrity = PASS, required_paths=14, toml_files=7,
  readme_paths=7
backend freeze baseline = PASS, boundaries=27, source_hashes=4
backend freeze negative selftest = PASS, cases=20
zero-Python closeout = PASS, tracked_files=5253, active_scripts=55,
  workflow_actions=17, historical_docs=1929, negative_cases=10
control-plane retirement = PASS, retired_tools=5, authority_files=76
historical release retirement = PASS, retired=263, tags=49,
  restored_blobs=263, negative_cases=4
golden schema smoke = PASS, rows=1
scripts/ai/verify_fast.sh = PASS
git diff --check = PASS
tracked files deleted = 0
runtime crates changed = false
v0.32.0 frozen release files changed = false
```

Commands:

```bash
CARGO_TARGET_DIR="${TMPDIR:-/tmp}/ntpro-bfh001-target" \
  scripts/ai/verify_release.sh current-governance backend-freeze-baseline
CARGO_TARGET_DIR="${TMPDIR:-/tmp}/ntpro-bfh001-target" \
  scripts/ai/check_zero_python_closeout.sh
CARGO_TARGET_DIR="${TMPDIR:-/tmp}/ntpro-bfh001-target" \
  scripts/ai/verify_fast.sh
git diff --check
git status --short --ignored
```

The external `CARGO_TARGET_DIR` keeps validation output outside the repository.
The existing untracked `project.html` and ignored local analysis/test-data
paths were inspected but not modified or deleted.
