# PTC-006 Historical Release Executable Gate Retirement Evidence

Date: 2026-07-16
Executor: Codex
GitHub issue: #1101
Milestone: python-tooling-closeout
Status: REVIEW_REQUIRED

## Retirement Inventory

```text
pre-retirement source commit = 15debbfb394b99f525be2bc8510aaa66b8f20360
retired verify_v*.sh files = 262
retired strict release script = 1
total retired executables = 263
retired executables containing Python/tooling = 233
immutable release tags = 49
reconstructable Git blobs = 263
negative manifest cases = 4
```

The machine-readable identities and restore commands are recorded in
`docs/rust-cutover/governance/historical_release_executable_retirement.json`.

## Preserved Authority

```text
docs/rust-cutover historical evidence retained = true
tests/golden fixtures retained = true
Rust integration tests retained = true
immutable Git tags retained = true
published GitHub Releases retained = true
v0.32.0 frozen release files changed = false
```

## Validation

```text
cargo fmt --all -- --check = pass
cargo test -p ntpro-governance --locked = pass (22 tests)
cargo clippy -p ntpro-governance --all-targets -- -D warnings = pass
scripts/ai/check_historical_release_retirement.sh = pass (263 blobs, 49 tags, 4 negative cases)
scripts/ai/check_current_governance_rust_only.sh = pass (17 authority files, 2 negative cases)
scripts/ai/check_backend_freeze_baseline.sh = pass (27 boundaries, 4 hashes, 20 negative cases)
scripts/ai/check_release_surface_current.sh = pass
scripts/ai/check_github_release_published.sh = pass (live v0.32.0 GitHub release)
scripts/ai/verify_release_publish_after_gate.sh = pass (live disabled; deterministic matrix passed)
scripts/ai/verify_release.sh current-governance backend-freeze-baseline = pass
scripts/ai/verify_release.sh v32-release-gates = expected fail (historical stage retired)
scripts/ai/verify_fast.sh = pass
scripts/ai/check_rust_only_runtime.sh = pass
scripts/ai/check_cython_removed.sh = pass (`.venv` temporarily moved outside repository and restored)
cargo vet --locked = pass
cargo deny --all-features check advisories licenses sources bans = pass with existing warnings
workflow YAML parse = pass
action SHA checks = pass (no staged SHA updates)
supported authority reference scan = pass (0 retired executable references)
docs/rust-cutover/release/v0_32_0_* diff = empty
git diff --check = pass
```

Hosted checks and independent review must pass before owner merge approval. This
high-risk task does not enable auto-merge and does not close issue #1101 while
the PR remains in `REVIEW_REQUIRED`.
