# V071-002 Evidence - Roadmap And Release-Facing Docs Alignment

Date: 2026-06-15
Executor: Codex
Task: V071-002 Align roadmap and release-facing docs

## Plain Chinese Summary

本任务只清理发布文档口径，不改运行时代码。

大白话：当前正式公开源码版本还是 `ntpro-rust-only-v0.7.0`，`v0.7.1`
只是发布门禁和产物合同的加固轨道，不是新交易能力版本。下一阶段能力目标是
`v0.8.0` 的 authenticated read-only testnet proof。文档里不能提前把尚未创建的
`ntpro-rust-only-v0.7.1` 写成已经存在的 current tag。

## Change Summary

- Added this V071-002 evidence record.
- Added a dedicated V071-002 task record.
- Updated the v0.7.1 draft release notes from `Current source tag` to
  `Planned source tag`.
- Added an explicit note that `ntpro-rust-only-v0.7.1` remains planned until
  release closure creates and publishes it after the hosted gate passes.

## Files Changed

```text
docs/rust-cutover/evidence/V071-002.md
docs/rust-cutover/release/v0_7_1_release_notes.md
docs/rust-cutover/tasks/V071-002.md
```

## Behavior Impact

No runtime behavior changes. This is release-facing documentation and evidence
only.

## Public API Impact

No public Rust API, CLI flag, artifact schema, or trading behavior change.

## Migration Note Status

No migration note is required. The task changes release wording only.

## Boundary Confirmation

V071-002 keeps these non-goals explicit:

```text
no authenticated account access
no order submission
no real funds
no production Binance connectivity
no production trading
no tag creation
no GitHub Release publication
```

## Validation Commands

```bash
rg -n "current public.*v0\\.6|current release.*v0\\.6|active hardening.*v0\\.6|Current source tag: ntpro-rust-only-v0\\.7\\.1" ROADMAP.md docs/rust-cutover/release
rg -n "ntpro-rust-only-v0\\.7\\.0|v0\\.7\\.1|v0\\.8\\.0|order submission|production trading|real funds" ROADMAP.md docs/rust-cutover/release/v0_7_1_release_notes.md docs/rust-cutover/release/v0_7_1_release_gate_hardening_readiness_report.md
scripts/ai/verify_fast.sh
git diff --check
```

## Validation Result

PASS.

```text
rg forbidden wording:
PASS - no forbidden matches for stale v0.6 current-release wording or
       "Current source tag: ntpro-rust-only-v0.7.1".

rg expected release-boundary wording:
PASS - expected v0.7.0 current release, v0.7.1 hardening, v0.8.0 next
       capability, and no order / no production / no real funds boundaries are
       present in Roadmap and v0.7.1 release-facing docs.

scripts/ai/verify_fast.sh:
PASS - toolchain and cargo fmt smoke passed.

git diff --check:
PASS - no whitespace errors.
```

Note: `verify_fast.sh` is intentionally fast smoke only. It does not replace
the v0.7.1 hosted release gate or `verify_release.sh` release evidence.

## Rollback Plan

Revert the V071-002 commit. Because this task only changes release-facing docs
and evidence, rollback does not affect runtime behavior or published artifacts.
