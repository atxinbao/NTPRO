# MVP-007 - 控制中心共享状态消费证据

Date: 2026-08-03
Executor: Codex
GitHub issue: #1213
Status: DONE

## 实现结果

- 新增 `/control-center` 独立平台运维入口，以及专属 CSS 和 JavaScript；
- 页面读取 `GET /api/mvp/v1/status` 和最小化的
  `GET /api/mvp/v1/control-center`；
- 共享事实仅来自版本化 MVP 合同，本地运维细节仅来自现有 Dashboard 投影；
- 专用运维 DTO 不序列化 controls、订单/生产操作证据、账户引用或原始错误文本；
- 校验 `control_center` consumer、单节点身份、registry provenance 严格等值、生命周期对齐和
  API/身份/状态/运维四层关闭边界；
- 异常刷新先清空节点、组件、观测、告警和证据，再进入 fail-closed；
- 页面不渲染 Supervisor action，也不包含交易、审批、重试或补救控件；
- 三个页面资源路由显式拒绝 HEAD 和其他非 GET 方法；
- CI 对控制中心执行合同回放、真实 MVP 进程浏览器 smoke，并上传独立证据工件。

## 当前自动验证

```text
node scripts/ai/test_control_center_contract.mjs
PASS: valid=1 fail_closed=29 stale_clear=29
PASS: 嵌套 schema、双 registry、运行/转换/停止生命周期、overview 计数、脱敏值、原始错误和超范围字段均有负向用例

Node --check over contract/browser scripts and embedded JavaScript
PASS

cargo fmt --all -- --check
PASS

cargo test -p nautilus-cli control_center --lib
PASS: 4 passed, 0 failed

cargo test -p nautilus-cli mvp_status_api --lib -j 2
PASS: 20 passed, 0 failed

cargo test -p nautilus-cli dashboard:: --lib -j 2
PASS: 156 passed, 0 failed

cargo clippy -p nautilus-cli --all-targets --all-features -- -D warnings
PASS

node scripts/ai/test_control_center_browser.mjs
PASS: 真实 `nautilus mvp serve` + `ntpro-node` + Chrome
PASS: 1440x1000 / 390x844 无横向溢出和状态栏遮挡
PASS: valid=1 shared_boundary=1 node_mismatch=1 ops_http_error=1 stale_clear=3
PASS: 专用运维响应不含 controls、订单/生产操作证据、账户引用或原始错误文本
PASS: 页面只有刷新按钮，不暴露 Supervisor 或交易动作
PASS: 中文 glyph canvas 像素签名与优雅停止证据均通过

node scripts/ai/test_institution_workbench_browser.mjs
PASS: 既有机构工作台桌面/移动、边界、HTTP error 和优雅停止回归

scripts/ai/check_backend_runtime_risk_inventory.sh
scripts/ai/test_backend_runtime_risk_inventory.sh
PASS: 29,292 signals / 1,221 files; production=2,697; self-test cases=8

scripts/ai/check_docs_examples_governance.sh
scripts/ai/check_rust_only_runtime.sh
scripts/ai/check_backend_freeze_baseline.sh
scripts/ai/verify_fast.sh
scripts/ai/verify_release.sh current-governance backend-freeze-baseline
git diff --check
PASS
```

独立 Verification & Release Gatekeeper 在最终 head 上返回 `REVIEW_APPROVED`。PR #1214
的 14 项 hosted checks 全部成功，Rust Cutover Smoke run `30828100856` completed / success。
`control-center-browser-1214` artifact digest 为
`sha256:bf63d2fa4fe419703d685e24edd831915516d2f6183306ec93b09d1cb43fb8da`，
`institution-workbench-browser-1214` digest 为
`sha256:2a262493538cb242e90490b3fcf81dfc0c8eb810bc3db948e0472e60ca033011`。
PR 于 2026-08-03 合并，merge SHA 为
`5caf48ee228803af7d26b07b09881ae21b5d4207`；issue #1213 自动以 completed 关闭。

新增 Rust 回归改变了 canonical 风险清单的行位置哈希，并在测试所有权下增加 3 条信号；
当前为 29,292 / 1,221，production ownership 保持 2,697。已将同一 scanner 生成的哈希
登记为 MVP-007 rebase，不把测试断言变化解释为生产风险变化。

## 行为与边界

本任务不改变共享 API schema、Supervisor 生命周期动作、交易状态机、adapter 或后端
冻结基线。控制中心仅把同一个策略实例/节点的共享合同与本地只读运维投影关联起来。
所有提交、变更、外部 Venue、自动重试、自动补救和真实订单能力继续为 false。

MVP-007 已合并，M2 双门户消费退出条件成立。M3 继续开放，后续处理跨门户事件跳转、
服务端角色边界和生命周期动作产品化。
