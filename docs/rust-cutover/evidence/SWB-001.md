# SWB-001 - 策略工作台页面框架证据

Date: 2026-08-05
Executor: Codex
GitHub issue: #1249
Status: REVIEW_REQUIRED

## 实现结果

- 新增 `/strategy-workbench` 主产品入口及独立 CSS/JavaScript；
- 页面结构固定为左侧导航、顶部策略上下文、三模式栏目、主画布、右侧隐藏详情栏、
  底部持仓/活动/成交/日志 Dock 和状态栏；
- 仅消费 `GET /api/mvp/v1/status`，当前呈现已冻结的单节点 Demo / Sandbox 状态；
- Backtest、Demo、Live 使用同一策略版本的产品语言，但本任务不创建任何 Run；
- Live 可见但保持 disabled，并明确标记未开放；
- schema、身份、环境、来源、四轴状态或只读边界异常时清空旧状态并 fail closed；
- 旧机构工作台、控制中心、共享 API、Supervisor/node 与交易引擎行为未修改。

## 冻结例外

Issue #1249 已添加 `backend-freeze-exception`，并记录用户授权、允许范围和回滚方式。
冻结源仅修改 `crates/cli/src/dashboard/server.rs`，增加 URL 输出和三个 GET-only 路由；
`docs/product/mvp_freeze_manifest.json` 已更新该文件 SHA-256。冻结守卫确认 13 个源文件、
19 个边界和 M0-M4 状态合同继续成立。

## 自动验证

```text
cargo test -p nautilus-cli strategy_workbench --lib -j 2
PASS: 2 passed

cargo test -p nautilus-cli portal_access --lib -j 2
PASS: 3 passed

node scripts/ai/test_strategy_workbench_contract.mjs
PASS: valid=1, fail_closed=7, stale_clear=7

node scripts/ai/test_strategy_workbench_browser.mjs
PASS: 1440x1000 / 390x844
PASS: valid=1, boundary=1, http_error=1
PASS: Dock、详情栏、Live disabled、URL token cleanup 与移动端默认收栏

scripts/ci/test-ci-change-classifier.sh
PASS: 16 cases

cargo clippy -p nautilus-cli --all-targets --all-features -- -D warnings
PASS

scripts/ai/check_backend_runtime_risk_inventory.sh
PASS: 29,367 signals in 1,221 files；production ownership 仍为 2,697

scripts/ai/test_backend_runtime_risk_inventory.sh
PASS: 8 cases

scripts/ai/check_docs_examples_governance.sh
scripts/ai/check_rust_only_runtime.sh
scripts/ai/verify_fast.sh
scripts/ai/check_mvp_freeze_baseline.sh
cargo fmt --all -- --check
git diff --check
PASS
```

本地真实浏览器证据位于 `/tmp/ntpro-swb-001-browser-evidence-final/`，包含桌面、移动、
边界阻断截图、结构化结果和脱敏服务日志。最终 hosted 结论以 PR 当前 head 的
`Rust Cutover Smoke` 与上传的 `strategy-workbench-browser-*` artifact 为准。

## 行为影响

新增一个机构访问角色保护的只读产品页面，不改变公开 Rust API、数据格式、交易语义或
真实交易能力。路由不接受 HEAD 或其他非 GET 方法。回滚时删除新增模块、路由、测试和
CI 分类，并恢复 manifest 中 `server.rs` 的上一 SHA-256。
