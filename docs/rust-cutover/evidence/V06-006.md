# V06-006 Evidence - optional runtime wiring

Date: 2026-06-13
Executor: Codex
Task ID: V06-006

## Plain Chinese Summary

本任务把 optional testnet runtime 的“意图开关”接进 CLI。

大白话说：CLI 有 `--allow-testnet-network`，用于明确表达“我知道这是 testnet network
相关操作”。但 v0.6 仍然不会打开网络连接；release gate 只跑 offline dry-run。

## Files Changed

- `crates/cli/src/opt.rs`
- `crates/cli/src/workflow.rs`
- `docs/rust-cutover/tasks/V06-006.md`
- `docs/rust-cutover/evidence/V06-006.md`

## Commands Run

```bash
cargo test -p nautilus-cli workflow --lib
```

## Validation Summary

Result: PASS.

Parser coverage confirms `--allow-testnet-network` is accepted and represented
in workflow options.

## Behavior Impact

Adds an explicit CLI flag. No network runtime is started by v0.6.

## Public API Impact

Adds `--allow-testnet-network` to `workflow run`.

## Migration Note Status

No migration note required.

## Rollback Plan

Remove the optional runtime flag and associated artifact fields.
