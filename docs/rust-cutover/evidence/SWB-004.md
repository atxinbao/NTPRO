# SWB-004 - 工作台首次启动与 Backtest/Demo 可用性证据

Date: 2026-08-14
Executor: Codex
GitHub issue: #1315
Status: LOCAL_VALIDATION_PASSED / REVIEW_REQUIRED

## 缺陷复现

- 全新 `mvp serve` 工作区的 Run API 返回 `503 product_source_unavailable`，字段为
  `result_root`；手工创建 `artifacts/backtests` 后立即恢复 200；
- node 运行时 Demo 创建按合同返回 `409 supervisor_node_state`；通过控制中心停止 node 后，
  Run 与 Demo 创建均返回 `503 product_source_stale`，字段为 `node_status`；
- 1280x720 浏览器中，Backtest 的“创建并运行”按钮被底部活动区拦截；1440x1000 可点击；
- 手工绕过目录问题后，真实 BacktestEngine 成功生成 120 条行情、5 笔成交、5 个持仓与完整
  收益/风险/事件结果，证明引擎本身可用，阻塞位于产品启动和页面布局合同。

## 修复边界

- 初始化空 Backtest 根目录不伪造任何结果；
- MVP prepare 现在显式创建空 `artifacts/backtests`，不生成或伪造任何 Run 结果；
- stopped node 仅在全部 Run 所有权均已终态、不存在，或恰好一个与 Demo create 输出一致的
  标准未启动 claim 时跳过时间 freshness；原先“任意一个终态”即通过的混合所有权缺口
  同时被收紧；
- 独立审查发现该标准 claim 仍只依赖 registry 形态，可能由伪造 ownership 绕过 freshness；
  corrective 将 claim 绑定到真实 `run-manifest.json`、request、StrategyVersion snapshot、Sandbox、
  node、策略实例、身份合同和精确 SHA-256，缺失或漂移统一返回 `demo_run_ownership` fail closed；
- 当前 claim 之后的 started/stopped 时间代表启动尝试并保持非静止；上一 Run 早于当前 claim 的
  历史时间允许复用同一 node。创建流程改为先收口旧 ownership，再捕获新 Run 时间，保证
  Supervisor baseline 不晚于 claim；
- Backtest 页面先显示 Run 数据源错误和验证中状态，不再把 503 误写成“没有数据集”；
- AppShell 主工作区与画布显式允许收缩，并提供 64px 底部滚动安全区，1280x720 下按钮
  完整位于活动区上方；
- Backtest 提交区同时固定为画布内粘性操作栏，消除 Linux CJK 字体度量变化导致按钮落到
  底部活动区之后的问题；
- hosted CLI 全量测试首次暴露 Live Runtime 外部退出 fixture 在 Supervisor stop 前已退出的竞态；
  用例改为直接终止并等待 fixture 进程，测试锁可从单项 panic poison 恢复，生产 Live 逻辑不变；
- hosted fault matrix 暴露“Demo 已创建但尚未启动”的标准 ownership 被误判为运行中；现在仅允许
  无 PID、没有当前 claim 启动时间且恰好一个 manifest-bound active claim 的标准创建态继续启动，
  曾启动 ownership 和多 active ownership 仍 fail closed；
- hosted Ubuntu/Noto CJK browser smoke 暴露 1280x720 下按钮底部 594px、活动区顶部 509px 的
  字体相关遮挡；粘性提交区加入 12px 明确安全距离，低高度桌面模式同时把活动区缩为 144px，
  其内容仍保持独立滚动；
- running、stale、unknown、活动 Run 所有权和所有真实交易能力继续 fail closed。

## 本地验证

- `cargo test -p nautilus-cli --lib`：并行全量 783/783 通过；
- `cargo test -p nautilus-cli --lib dashboard::product_api::tests`：91/91 通过；新增 no manifest、
  双 active claim、manifest hash/node/environment 漂移、当前 claim 启动时间但无 PID，以及上一
  Run 历史时间不阻塞新 Run 的直接回归；
- `cargo test -p nautilus-cli --lib mvp::tests -- --test-threads=1`：19/19 通过；并行首次运行曾有
  一个既有 fixture node 启动超时，单独重跑和串行整组均通过；
- `cargo clippy -p nautilus-cli --lib -- -D warnings`、`cargo fmt --all -- --check`：通过；
- 前端 `format:check`、`lint`、`typecheck`、`build`：通过；单元测试 188/188 通过；
- `scripts/ai/test_mvp_fault_matrix.mjs`：11/11 通过，覆盖 9 个工件故障和 2 个进程故障；
- production bundle 浏览器 smoke：1440x1000、1280x720、390x844 全部通过；覆盖 Demo 创建、
  启动、模拟结果、停止冻结、Backtest 创建、真实结果和移动端指标；
- 1280x720 截图：`/tmp/ntpro-swb004-browser-compact-gap-20260814-a/strategy-workbench-backtest-create-1280.png`；
- `scripts/ai/check_backend_runtime_risk_inventory.sh`：30,495 个信号、1,230 个文件，通过；新增
  13 条均归属 test，production 计数保持 2,703；
- `scripts/ai/check_mvp_freeze_baseline.sh`：19 个任务、5 个阶段、19 个边界、13 个冻结来源，
  负向自测 7/7 通过；
- `scripts/ai/check_rust_only_runtime.sh`、`git diff --check`：通过。

## 后冻结治理与兼容性

- Issue #1315 已登记用户继续修复的 owner 指令和后冻结允许路径；
- `mvp_freeze_manifest.json` 仅更新 `crates/cli/src/mvp.rs` 的精确 SHA-256，不改拓扑、阶段、
  接口、能力边界、tag 或 release；
- 公共 API schema 无变化，无数据迁移；
- 回滚：还原 MVP 初始化、manifest-bound 稳定终态判断、新 Run 时间基线、前端错误态/布局、
  测试和两个治理摘要；已有 Run 工件不删除、不改写；
- 行为影响：全新工作区无需手工目录即可使用 Backtest，正常 stopped node 可创建 Demo；
- 审查状态：high risk；独立审查首轮为 `REVIEW_CHANGES_REQUIRED`，上述 corrective 已本地验证，
  等待同一审查者复审和新 head hosted checks，禁止 auto-merge。
