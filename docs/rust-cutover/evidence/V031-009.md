# V031-009 Evidence - Ignored Tests Closure Batch 1

Date: 2026-06-12
Executor: Codex
Task: V031-009
Branch: `codex/V031-009-ignored-tests-closure-batch-1`

## Plain Chinese Summary

这次没有假装把所有 ignored tests 都修好了，也没有删除测试来制造绿色结果。
我做的是第一批发布范围收口：把高影响的 execution/risk/dYdX ignored tests
明确标成“不属于 v0.3.1 本地控制台补丁发布能力”，同时说明 live stress
测试仍是 release/perf 手动项。这样 v0.3.1 不会把这些还没修好的交易语义、
真实 adapter reconnect、吞吐/饥饿压力测试当成发布证据。

## Goal

Close the first ignored-test hardening batch for `v0.3.1` by classifying which
ignored tests are outside the Local Supervisor Control Console Hardening release
claim, while keeping the unresolved runtime/adapter/stress debt visible.

## Files Changed

- `docs/rust-cutover/tasks/V031-009.md`
  - corrected the risk-register path to the actual project documents.
- `docs/rust-cutover/verification/ignored_tests_risk_register.md`
  - added the V031-009 batch-1 closure decision.
- `docs/rust-cutover/quality/ignored_tests_register.md`
  - updated the current state for the high-impact execution/risk/dYdX batch and
    the live stress tests.
- `docs/rust-cutover/evidence/V031-009.md`
  - records this evidence.

## Ignored-Test Closure Summary

| Group | Decision |
| --- | --- |
| `IGN-HIGH-003` through `IGN-HIGH-011` | Scoped out for `v0.3.1`; still blockers for future runtime/trading releases. |
| `IGN-HIGH-012` / `IGN-DYDX-001` | Scoped out for `v0.3.1`; still a future adapter reconnect blocker. |
| `IGN-LIVE-001`, `IGN-LIVE-002` | Kept as release/perf-only stress tests; deterministic v0.3 supervisor/dashboard smokes cover the current patch boundary. |

No ignored test was removed. No empty placeholder test was promoted as release
evidence.

## Commands Run

```bash
rg -n "^\s*#\[ignore|ignore\s*=|Production bug|ignored test|ignored tests|risk register" crates tests docs .github scripts -g '!target'
rg -n "ignored|ignore|Production bug" docs/rust-cutover docs/testing crates/common/src/cache/tests.rs crates/execution crates/risk crates/adapters -g '!target'
sed -n '1,180p' docs/rust-cutover/verification/ignored_tests_risk_register.md
sed -n '1,120p' docs/rust-cutover/quality/ignored_tests_register.md
sed -n '1,140p' docs/rust-cutover/scope/v0_3_1_supervisor_control_hardening.md
```

Notes:

- The second `rg` command exited with status 2 because `docs/testing` does not
  exist in this repository. That discovery was used to correct the task file to
  the actual ignored-test register paths under `docs/rust-cutover/`.

## Validation Results

```bash
cargo test -- --ignored
scripts/ai/verify_full.sh
git diff --check
```

Result summary:

- `cargo test -- --ignored`: **failed as expected for scope evidence**.
  - The command compiled the workspace and ran ignored tests.
  - Several external/manual ignored tests passed on this machine, including
    Betfair fixture loaders, BitMEX rate limiting, and Bybit live ticker checks.
  - The command failed at `nautilus-dydx`:
    `test_subscription_restoration_tracking` timed out after 5.1s while waiting
    for a 5.0s condition.
  - This confirms the ignored-test suite is not a valid `v0.3.1` release gate
    and must remain recorded as runtime/adapter/stress debt instead of being
    counted as release evidence.
- `scripts/ai/verify_full.sh`: **passed**.
  - Fast checks passed.
  - Workspace clippy passed.
  - Default Rust tests passed while preserving ignored tests as ignored.
  - Golden trace validation passed.
  - Rust docs generation passed.
- `git diff --check`: **passed**.

## Behavior Impact

No runtime behavior changed.

No trading-semantic behavior changed.

No adapter behavior changed.

The only product effect is release documentation clarity: `v0.3.1` cannot rely
on unresolved ignored runtime/adapter/stress tests.

## Public API Impact

No public API change.

No CLI command shape changed.

## Migration Note Status

No migration note required. This is verification and release-scope
classification work only.

## Rollback Plan

Revert this PR to remove the V031-009 scope classification and restore the prior
ignored-test register wording. This does not affect runtime behavior because no
runtime code is changed.
