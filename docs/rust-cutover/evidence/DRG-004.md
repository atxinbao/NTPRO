# DRG-004 Evidence - Core Crates Full Targeted Tests

Date: 2026-06-06
Executor: Codex
Task ID: DRG-004
Branch: `codex/DRG-004-core-crates-full-targeted-tests`

## Goal

证明 v0.2 Design Readiness Gate 的 G3 core crate targeted tests 可以通过。

本任务只做验证和状态收口，不修改交易语义、不修改 runtime 行为、不新增 CLI
能力，也不改变 public API。

## Files Changed

- `.agentflow/state/task_status.json`
- `.agentflow/leases/DRG-004.json`
- `docs/rust-cutover/evidence/DRG-004.md`
- `docs/rust-cutover/release/v0_2_readiness_report.md`

## Commands Run

```bash
cargo test -p nautilus-core
cargo test -p nautilus-model
cargo test -p nautilus-data
cargo test -p nautilus-execution
cargo test -p nautilus-risk
cargo test -p nautilus-portfolio
cargo test -p nautilus-backtest
cargo test -p nautilus-live
```

Result summary:

| Package | Result | Summary |
| --- | --- | --- |
| `nautilus-core` | PASS | 957 unit tests, 3 value type gate tests, and 3 doc-tests passed. |
| `nautilus-model` | PASS | 2559 unit tests and 3 value type gate tests passed; doc-tests had 0 tests. |
| `nautilus-data` | PASS | 209 unit tests, 48 client tests, and 242 engine tests passed; doc-tests had 0 tests. |
| `nautilus-execution` | PASS | 388 unit tests, 149 execution engine tests, 169 matching engine tests, and 1 order emulator test passed. |
| `nautilus-risk` | PASS | 10 unit tests and 78 risk engine tests passed. |
| `nautilus-portfolio` | PASS | 22 unit tests and 66 portfolio tests passed. |
| `nautilus-backtest` | PASS | 79 unit tests, 59 backtest engine tests, golden trace/parity tests, and option-chain smoke passed. |
| `nautilus-live` | PASS | 129 unit tests, 1 golden trace sandbox test, 129 manager tests, 29 node tests, and 61 plugin dispatch tests passed. |

Additional validation for the evidence/status update:

```bash
cargo fmt --check
scripts/ai/verify_fast.sh
python3 -m json.tool .agentflow/state/task_status.json
python3 -m json.tool .agentflow/leases/DRG-004.json
scripts/ai/validate_agentflow_roles.py
git diff --check
```

## Ignored Tests Observed

These ignored tests were pre-existing and were not changed by DRG-004:

- `nautilus-execution` matching engine tests: 3 ignored.
- `nautilus-risk` risk engine tests: 6 ignored.
- `nautilus-live` stress tests: 2 ignored.

DRG-004 records package-level targeted test pass status. It does not close the
separate ignored-test risk work.

## Behavior Impact

No runtime behavior changed. This PR is verification evidence plus gate state
only.

## Public API Impact

No public API changed.

## Migration Note Status

No migration note is required because no user-facing API, CLI contract, runtime
behavior, or data format changed.

## Known Boundaries

- G3 is PASS after this task.
- G4-G9 remain unpassed until their own DRG tasks run.
- Existing ignored tests remain visible as follow-up risk and are not treated as
  fixed by this task.
- Existing runtime logs still include upstream NautilusTrader banner text in
  tests; this task does not clean runtime branding output.

## Rollback Plan

Revert this PR to remove the DRG-004 evidence and restore G3 to unknown/failed
readiness state. No code rollback is needed because no source behavior changed.
