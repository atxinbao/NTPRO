# V141-002 Evidence - Split exchange-truth semantics

Date: 2026-06-22
Executor: Codex

## Summary

V141-002 separates order-state read-only exchange-truth from shadow and
portfolio truth semantics while keeping the legacy `values_are_exchange_truth`
field as a compatibility summary.

Plain Chinese summary: 已把“订单状态真值”和“Shadow/Portfolio 真值”拆开。订单状态只读查询成功可以
说明订单状态来自交易所，但不能让 Dashboard 误判 shadow/portfolio 也变成交易所真实状态。

## Files

```text
crates/cli/src/live.rs
crates/cli/src/dashboard.rs
scripts/ai/verify_v141_order_state_owner_evidence.sh
docs/rust-cutover/tasks/V141-002.md
docs/rust-cutover/evidence/V141-002.md
```

## Boundary Markers

```text
order_state_values_are_exchange_truth = true only for validated order-state read-only proof success
shadow_values_are_exchange_truth = false for shadow/dry-run artifacts
portfolio_values_are_exchange_truth = false for shadow portfolio artifacts
values_are_exchange_truth = compatibility summary only
Dashboard shadow boundary violation excludes order_state_values_are_exchange_truth
Dashboard live-alpha boundary violation excludes order_state_values_are_exchange_truth
production_order_submission_attempted = false
production_order_mutation_attempted = false
dashboard_order_controls_enabled = false
```

## Validation

Local validation completed:

```bash
cargo check -p nautilus-cli = PASS
cargo test -p nautilus-cli production_order_state_readonly_proof_records_owner_gated_online_success --lib = PASS
cargo test -p nautilus-cli dashboard_keeps_order_state_truth_separate_from_shadow_truth --lib = PASS
cargo test -p nautilus-cli live_alpha_dry_run_artifacts_populate_readonly_dashboard_panel --lib = PASS
bash -n scripts/ai/verify_v141_order_state_owner_evidence.sh = PASS
scripts/ai/verify_v141_order_state_owner_evidence.sh = PASS
NTPRO_V141_ORDER_STATE_EVIDENCE_ROOT=<temp-owner-fixture> scripts/ai/verify_v141_order_state_owner_evidence.sh = PASS
cargo fmt --all --check = PASS
scripts/ai/verify_fast.sh = PASS
git diff --check = PASS
```

## Status

Ready for PR review. Hosted smoke is required before merge.
