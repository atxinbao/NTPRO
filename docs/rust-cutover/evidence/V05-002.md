# V05-002 Evidence - artifact contract

Date: 2026-06-13
Executor: Codex
Task ID: V05-002

## Plain Chinese Summary

本任务定义了 v0.5 workflow artifact 的格式。

大白话说：workflow 会输出 `manifest.json`、`summary.json`、`events.jsonl` 和
market/strategy/order/risk 组件 JSON。Dashboard 后续可以读这些文件，但这些文件
只代表本地 sandbox 证据，不代表真实 Binance 连接或实盘能力。

## Files Changed

- `docs/rust-cutover/product/V05_WORKFLOW_ARTIFACT_CONTRACT.md`
- `crates/cli/src/workflow.rs`
- `docs/rust-cutover/tasks/V05-002.md`
- `docs/rust-cutover/evidence/V05-002.md`

## Commands Run

```bash
cargo test -p nautilus-cli workflow --lib
jq -r '.schema_version, .workflow, .run_id, .summary.sandbox_only, .summary.real_orders_submitted, .artifact_count' /tmp/ntpro-v05-workflow-smoke/manifest.json
wc -l /tmp/ntpro-v05-workflow-smoke/events.jsonl
```

## Validation Summary

- Manifest schema is `ntpro.workflow_manifest.v1`.
- Workflow is `binance-sandbox`.
- `summary.sandbox_only=true`.
- `summary.real_orders_submitted=false`.
- Manifest artifact count is `9`.
- Events JSONL has `7` lines.

## Behavior Impact

Adds deterministic local artifact output for one workflow.

## Public API Impact

Defines the v0.5 local workflow artifact contract.

## Migration Note Status

No migration note required. This is additive Rust-only artifact output.

## Rollback Plan

Revert the workflow artifact contract and generated artifact writer code.
