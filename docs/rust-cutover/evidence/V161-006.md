# V161-006 Evidence - CLI and classifier wording drift

Date: 2026-06-24
Executor: Codex
Milestone: v0.16.1
Status: local validation PASS; hosted smoke PASS

## Summary

V161-006 aligns the current CLI and endpoint-classifier wording with the
v0.16/v0.16.1 owner-gated production mutation candidate boundary. It does not
change endpoint classification decisions, request execution, order submission,
or Dashboard controls.

Plain Chinese summary: 这个任务只修公开文案。大白话：CLI 和 classifier 现在会说清楚
“默认关闭，但存在 owner-gated mutation-candidate 路径”，不再把当前 v0.16/v0.16.1
口径写成旧的 v0.15-only 或绝对 no-mutation 说法。

## Files

```text
crates/cli/src/opt.rs
crates/cli/src/endpoint_classifier.rs
docs/rust-cutover/tasks/V161-006.md
docs/rust-cutover/evidence/V161-006.md
```

## Boundary

This is a wording hardening task only:

```text
no classifier decision change
no authorization change
no HTTP send behavior change
no production order submission
no cancel/replace/amend/retry/correction/flatten/remediation
no Dashboard order controls
no new production trading platform claim
```

## Validation

Local validation completed:

```text
cargo test -p nautilus-cli live_help_describes_owner_gated_mutation_candidate_boundary --lib = PASS
cargo test -p nautilus-cli endpoint_classifier --lib = PASS
cargo test -p nautilus-cli --lib = PASS
! rg -n "Local live, production read-only, and dry-run proof commands \\(no production order mutation\\)|v0\\.15 dry-run request-preview" crates/cli/src/opt.rs crates/cli/src/endpoint_classifier.rs = PASS
cargo fmt --all --check = PASS
cargo clippy -p nautilus-cli --all-targets -- -D warnings = PASS
scripts/ai/verify_fast.sh = PASS
git diff --check = PASS
```

## Rollback Plan

Revert this PR to restore the previous CLI/classifier wording and remove the
V161-006 task/evidence files. Because this PR does not change runtime behavior
or exchange interaction, rollback requires no exchange-state or credential
cleanup.
