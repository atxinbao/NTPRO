# V05-010 Evidence - release gate integration

Date: 2026-06-13
Executor: Codex
Task ID: V05-010

## Plain Chinese Summary

本任务把 v0.5 workflow artifact smoke 接进 release gate。

大白话说：以后 release gate 里会多跑一项
`v05-workflow-artifacts-smoke`。它会用 release 版 `nautilus` binary 运行本地
Binance sandbox workflow，再检查 artifact 和 no-real-funds/no-production/no-real-orders
边界。

这不会让 release gate 连接 Binance，也不会提交真实订单。

## Files Changed

- `scripts/ai/verify_release.sh`
- `scripts/ai/verify_v05_workflow_artifacts.sh`
- `.github/workflows/release-tag.yml`
- `.github/workflows/rust-cutover-smoke.yml`
- `docs/rust-cutover/tasks/V05-010.md`
- `docs/rust-cutover/evidence/V05-010.md`

## Commands Run

```bash
bash -n scripts/ai/verify_v05_workflow_artifacts.sh scripts/ai/verify_release.sh
ruby -e 'require "yaml"; YAML.load_file(".github/workflows/release-tag.yml"); YAML.load_file(".github/workflows/rust-cutover-smoke.yml"); puts "syntax ok"'
scripts/ai/verify_release.sh v05-workflow-artifacts-smoke
```

## Validation Summary

Result: PASS.

Key release-stage output:

```text
== verify_release: v0.5 workflow artifact smoke ==
== verify_release: release CLI binary build ==
Finished `release` profile [optimized] target(s) in 4m 08s
v05_workflow_artifact_assertions status=ok run_id=v05-smoke artifact_count=9 events=7
v05_workflow_artifacts_smoke status=ok
== verify_release complete ==
```

## Behavior Impact

Release verification now includes the v0.5 local workflow artifact smoke.

## Public API Impact

No public API change.

## Migration Note Status

No migration note required. This is a release verification change.

## Rollback Plan

Remove the `v05-workflow-artifacts-smoke` stage from `verify_release.sh` and
`.github/workflows/release-tag.yml`, then remove the v0.5 smoke script.
