# V06-004 Evidence - testnet CLI and config contract

Date: 2026-06-13
Executor: Codex
Task ID: V06-004

## Plain Chinese Summary

本任务新增了 checked-in Binance testnet dry-run config，并让 CLI 校验这个配置。

大白话说：`examples/rust/binance/testnet_dry_run.toml` 会被 workflow 读取。代码会确认
它是 Binance Spot testnet、dry-run、order_submission disabled，并且 URL 和 Binance
adapter 的 testnet URL resolver 一致。

## Files Changed

- `examples/rust/binance/testnet_dry_run.toml`
- `crates/cli/src/workflow.rs`
- `docs/rust-cutover/tasks/V06-004.md`
- `docs/rust-cutover/evidence/V06-004.md`

## Commands Run

```bash
scripts/ai/verify_v06_binance_testnet_dry_run.sh
cargo test -p nautilus-cli workflow --lib
```

## Validation Summary

Result: PASS.

The first smoke run caught a config URL mismatch and the checked-in config was
fixed to `wss://stream.testnet.binance.vision/ws`.

## Behavior Impact

Adds config validation for the offline testnet dry-run workflow.

## Public API Impact

No Rust library API change.

## Migration Note Status

No migration note required.

## Rollback Plan

Remove the config sample and testnet workflow config parser.
