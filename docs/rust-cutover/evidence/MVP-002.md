# MVP-002 - 单节点 MVP 与双门户信息架构证据

Date: 2026-08-02
Executor: Codex
GitHub issue: #1201
Status: REVIEW_REQUIRED

## 文档结果

- README 用中文固定“一个平台、两个门户、一个共享控制平面”；
- Roadmap 将 #1198 标为已交付的单节点运行基线，不再误写暂停/恢复或完整产品状态；
- `project.html` 分离机构工作台与控制中心的一级导航、页面上下文和状态职责；
- 策略、回测、节点、策略实例、账户、Venue 和工作空间身份保持独立；
- 多节点、真实交易和完整前端继续明确后置。

## 验证

### 文档与治理检查

```bash
source scripts/ai/toolchain_env.sh
export CARGO_TARGET_DIR=/tmp/ntpro-mvp002-target
scripts/ai/check_docs_examples_governance.sh
scripts/ai/check_rust_only_runtime.sh
scripts/ai/check_backend_freeze_baseline.sh
git diff --check
```

结果：全部通过。文档治理检查覆盖 124 个 Markdown 文件、306 个本地链接和
20 个图片链接；Rust-only runtime 检查通过；v0.32.0 冻结基线 27 个边界和
20 个负向自测通过；diff 无空白错误。

### 浏览器检查

使用 Playwright CLI 打开本地静态页面并验证：

- 桌面视口 `1440x1000`：页面宽度与视口一致，无横向溢出；第七版和
  `91c74e6f133f` 基线信息正确；控制台 0 error / 0 warning；
- 机构工作台右侧详情栏可以收起和展开，`aria-expanded` 在 `false/true` 间切换；
- 底部“活动 4”页签切换后，表头更新为“活动 / 对象 / 类型 / 状态 / 业务影响 /
  更新时间”；
- 移动视口 `390x844`：页面 `scrollWidth=390`，两个门户框架均为 354px，
  无整页或门户横向溢出；控制台 0 error / 0 warning；
- 验证过程中发现并修复旧架构图与 MVP 结构图的固定最小宽度问题。

Playwright 截图和临时 Cargo target 仅用于本地检查，验证后已删除，未进入提交。
