# SWB-005 - 默认基线 Run 产物一致性证据

Date: 2026-08-15
Executor: Codex
GitHub issue: #1317
Status: LOCAL_VALIDATION_PASSED / REVIEW_REQUIRED

## 缺陷复现

- `GET /api/product/v1/runs/ema-cross-btcusdt-baseline-v1`：200；
- `GET /api/product/v1/runs/ema-cross-btcusdt-baseline-v1/metrics`：503；
- 错误码：`product_source_unavailable`；字段：`result_run_root`；
- 静态配置声明 `completed/available`，但工作区不存在同名结果目录；
- React 详情上下文把 `metrics.error` 提升为顶层错误，因此整页不可用。

## 修复证据

- 节点配置通过 `[mvp].baseline_backtest_config` 绑定默认 Backtest 配置，并固定
  `summary.json`、`details.json`、`analysis.json` 三个不可变 SHA-256；
- 全新工作区启动时先在受控暂存目录生成默认基线，逐文件校验哈希和 `run_id` 后原子发布；
- 已存在目录只验证、不覆盖。篡改内容、缺失文件和符号链接目录均阻断启动；
- MVP 重启测试确认基线字节保持不变，四个 Run 读取入口均返回 200；
- 前端把指标错误降级为结果区域局部错误，Run 身份、环境和生命周期继续显示，并提供显式重试；
- production bundle 浏览器结果中
  `product_run_baseline_bootstrap=1`、`product_run_metrics=1`、
  `product_run_metrics_mobile=1`。

## 本地验证

```text
cargo fmt --all -- --check
PASS

cargo test -p nautilus-cli --lib -- --test-threads=1
PASS: 786 passed; 0 failed

cargo clippy -p nautilus-cli --lib -- -D warnings
PASS

cd apps/strategy-workbench
npm run format:check && npm run lint && npm run typecheck && npm run test && npm run build
PASS: 189 passed; production bundle built

NTPRO_PLAYWRIGHT_CORE_PATH=apps/strategy-workbench/node_modules/playwright-core \
NTPRO_BROWSER_EVIDENCE_DIR=/tmp/ntpro-swb005-browser-evidence-final \
  node scripts/ai/test_strategy_workbench_browser.mjs
PASS: strategy_workbench_browser=pass

scripts/ai/check_mvp_freeze_baseline.sh
PASS: tasks=19 phases=5 boundaries=19 frozen_sources=13

scripts/ai/check_backend_runtime_risk_inventory.sh
PASS: 30518 signals in 1230 files; production=2703

scripts/ai/check_rust_only_runtime.sh
PASS

git diff --check
PASS
```

## 故障恢复回归

`node scripts/ai/test_mvp_fault_matrix.mjs` 的最终隔离复现通过 11/11：9 类工件故障和
2 类进程故障均 fail closed，且只允许人工恢复。前两次在本机高负载下出现
`artifact-faults server did not stop after SIGINT` 固定 30 秒等待超时；服务日志已写出
`mvp.serve status=stopped`，清理逻辑未报告节点泄漏，第三次相同代码完整通过。该既有脚本
不在 Issue #1317 的允许修改路径内，本 PR 不混入其超时策略调整。

## 行为与边界

- 行为变化：官方 MVP 配置不再发布一个没有结果产物却宣称 available 的默认基线 Run；
- API 变化：无 schema 或路由变化；
- 迁移：自定义节点配置未声明 `baseline_backtest_config` 时保持原行为；
- 交易边界：外部 Venue、真实订单、订单 mutation、自动重试和自动补救继续关闭；
- 回滚：回退本 PR 后需恢复启动前人工生成基线产物，否则旧错误会再次出现。

## 审查状态

本任务为 high risk。当前仅完成本地验证，必须取得独立审查批准并通过 hosted checks，
随后手动合并；禁止 auto-merge。
