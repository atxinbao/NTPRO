# BFG-005 GitHub Intake Hardening Evidence

Date: 2026-07-15
Executor: Codex
GitHub issue: #1072
Milestone: backend-freeze-governance
Status: LOCAL AND LIVE VALIDATION PASSED

## Summary

This task adds backend-freeze declarations to issue and PR intake and
materializes the required routing labels in live GitHub state.

Plain Chinese summary: issue 和 PR 现在必须明确后端冻结分类、禁止能力和例外/v0.33+
证据。labels 只负责路由，不能替代正文、task、验证和审计证据。

## Validation

```text
GitHub issue-form YAML parse and required-ID validation = PASS, ids=19
PR template backend-freeze marker scan = PASS
live required label inventory = PASS, labels=7
milestone #31 label application = PASS, issues=6, required base labels=4
scripts/ai/verify_release.sh backend-freeze-baseline = PASS, negative_selftest=20
scripts/ai/verify_fast.sh = PASS
git diff --check = PASS
```

Live labels:

```text
backend-freeze
backend-freeze-exception
v32-baseline
v33-separately-scoped
trading-control-forbidden
rust-cutover
agent-ready
```

Issues #1068-#1073 carry the four base governance labels. Open execution issues
#1072 and #1073 also carry `agent-ready`; #1073 carries
`v33-separately-scoped`.

The first local YAML assertion used `Array#filter_map`, which the installed
Ruby does not support. YAML parsing itself succeeded; the assertion was rerun
with the compatible `map.compact` equivalent and passed.

## Behavior Impact

None. GitHub intake, repository templates, and governance documentation only.
