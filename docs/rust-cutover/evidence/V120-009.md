# V120-009 Evidence

Date: 2026-06-21
Executor: Codex
Task: V120-009 Prepare v0.12 readiness and release notes
Risk level: medium

## Goal

Prepare v0.12 readiness, release notes, and release-surface accounting after
V120-000 through V120-008 pass.

Plain Chinese summary: 这次只收口 v0.12 的发版材料。它不创建 tag，不发布
GitHub Release，也不把 v0.12 写成实盘交易能力。

## Files Changed

- `docs/rust-cutover/release/v0_12_0_readiness_report.md`
  - Records v0.12 scope, task accounting, gate evidence, and release decision
    boundary.
- `docs/rust-cutover/release/v0_12_0_release_notes.md`
  - Prepares release-note material for an owner-approved v0.12 release.
- `README.md`
  - Aligns the next capability track with Production Online Read-Only +
    Persistent Shadow and moves Guarded Live Alpha to v0.13.
- `ROADMAP.md`
  - Adds v0.12 candidate capability track wording and corrected sequence.
- `docs/versioning.md`
  - Defines v0.12 as the candidate capability boundary and v0.13 as the
    earliest Guarded Live Alpha candidate.
- `docs/rust-cutover/release/README.md`
  - Adds v0.12 readiness and release notes to the release evidence index.
- `docs/rust-cutover/tasks/V120-009.md`
  - Records the task contract.
- `docs/rust-cutover/evidence/V120-009.md`
  - Records this evidence.

## Behavior Impact

Documentation and release accounting only.

No production network access, credential value persistence, account mutation,
order submission, order cancel/replace/amend, production order-state read,
listenKey lifecycle, Dashboard order control, or trading semantic behavior is
added.

## Public API Impact

No public API or CLI syntax changes.

## Migration Note

No migration note is required. This is release material only.

## Validation

PASS:

```bash
scripts/ai/verify_release.sh v12-offline-release-gates v12-manual-online-preflight
```

Result: v0.12 offline release gates and manual-online fail-closed preflight
passed. The gate built the release CLI binary, ran public and authenticated
production read-only preflights with `network_attempted=false`, validated
redacted response shape, shadow portfolio runtime, persistent shadow strategy
session, read-only reconciliation, Dashboard v0.12 production shadow panel, and
release boundary markers.

PASS:

```bash
scripts/ai/verify_fast.sh
```

Result: toolchain and rustfmt fast smoke passed.

PASS:

```bash
NTPRO_RELEASE_SURFACE_ALLOW_MISSING_TAG=1 scripts/ai/check_release_surface_current.sh
```

Result: current release surface guard passed with current release `v0.11.0`,
next patch `v0.11.1`, and next capability `v0.12.0`.

PASS:

```bash
rg -n 'v0\.12\.0.*Guarded|Guarded Live Alpha.*v0\.12\.0|v0\.12\.0 = Guarded|Status: RELEASED|Tag: `ntpro-rust-only-v0\.12\.0`|formal tag = ntpro-rust-only-v0\.12\.0|formal GitHub Release' README.md ROADMAP.md docs/versioning.md docs/rust-cutover/release/v0_12_0_readiness_report.md docs/rust-cutover/release/v0_12_0_release_notes.md docs/rust-cutover/release/README.md; test $? -eq 1
```

Result: no stale `v0.12.0 = Guarded Live Alpha` wording and no false v0.12
released/tagged claim were found.

PASS:

```bash
git diff --check
```

Result: no whitespace errors.

## Rollback Plan

Revert this PR to remove v0.12 readiness/release-note material and document
links. No exchange state, credentials, orders, or published release artifacts
require rollback.
