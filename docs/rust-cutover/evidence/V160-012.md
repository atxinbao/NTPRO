# V160-012 Evidence - v0.16 release gates

Date: 2026-06-23
Executor: Codex
Milestone: v0.16.0
Task: V160-012 Add v0.16 release gates

## Summary

Added an aggregate v0.16 release gate for the minimum owner-approved production
order mutation candidate. The gate runs the v0.16 local/offline evidence chain
and scans the resulting logs for forbidden default production mutation markers.

Plain Chinese summary: 这次只是接发版门禁，不开放实盘交易。默认 gate 证明 v0.16
证据链能跑通，但仍然不联网、不发生产请求、不真实下单、不撤单改单、不重试，也没有
Dashboard 下单按钮。

## Files Changed

```text
scripts/ai/verify_v16_release_gates.sh
scripts/ai/verify_release.sh
.github/workflows/rust-cutover-smoke.yml
.github/workflows/release-tag.yml
docs/rust-cutover/tasks/V160-012.md
docs/rust-cutover/evidence/V160-012.md
```

## Behavior Impact

```text
local release verification gains v16-release-gates
PR smoke can run v16-release-gates for v0.16-relevant changes
PR smoke avoids fanning out legacy v12-v15.1 gates for v0.16 release wiring
tag-triggered release workflow gains release-v16-release-gates
default execution remains offline and fail-closed
no production network behavior changed
no trading semantics changed
```

## Boundary

The aggregate gate rejects forbidden default markers:

```text
request_sent=true
network_attempted=true
manual_online_requested=true
production_order_submissions_attempted > 0
production_orders_submitted > 0
production_order_mutations_attempted > 0
production_order_state_reads_attempted > 0
listen_key_lifecycle_attempted > 0
retry/cancel/replace/amend/correction/flatten/remediation attempted
dashboard_order_controls_enabled=true
real_orders_submitted=true
production_trading_enabled=true
secret/signature/raw request/raw response persistence
```

## Validation

```text
bash -n scripts/ai/verify_v16_release_gates.sh scripts/ai/verify_release.sh scripts/ai/verify_v16_*.sh = PASS
scripts/ai/verify_v16_release_gates.sh = PASS
scripts/ai/verify_release.sh v16-release-gates = PASS
scripts/ai/verify_v16_dashboard_readonly_evidence.sh = PASS
ruby -e 'require "yaml"; YAML.load_file(".github/workflows/release-tag.yml"); YAML.load_file(".github/workflows/rust-cutover-smoke.yml")' = PASS
scripts/ai/verify_fast.sh = PASS
git diff --check = PASS
PR smoke classifier simulation for this change set = PASS
  v12=false
  v13=false
  v14=false
  v15=false
  v151=false
  v16=true
Hosted run 28016721339 = FAIL, fixed
  failure: scripts/ai/verify_v16_dashboard_readonly_evidence.sh used rg on hosted runner
  fix: replaced rg static checks with grep/awk
```

## Review / Merge Status

```text
PR = TODO
Hosted Rust Cutover Smoke = TODO
Merge commit = TODO
```

## Rollback Plan

Revert the PR to remove the v0.16 aggregate gate script and workflow/dispatcher
wiring. This does not affect trading behavior because the change is release
verification wiring only.
