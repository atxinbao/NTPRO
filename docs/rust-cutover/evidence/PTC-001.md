# PTC-001 Python Tooling Authority Evidence

Date: 2026-07-15
Executor: Codex
GitHub issue: #1096
Milestone: python-tooling-closeout
Status: PRE-MERGE VALIDATION PASSED

## Baseline Results

```text
baseline commit = 20455c86be25c5b7d083ce0a67d4053a844352d2
tracked Python files = 8
tracked shell files = 339
shell Python/tooling execution files = 263
versioned verify scripts = 262
versioned Python execution files = 236
inline Python heredoc shell files = 233
Python workflow/action files = 18
Python/tooling invocation lines = 695
uv.lock packages = 55
local .venv size = 136 MiB
```

The initial extension-only inventory was incomplete because executable Python
also exists inside shell heredocs, Make targets, workflows, and composite
actions. The corrected scope is captured in
`governance/python_tooling_baseline.json`.

## Change Boundary

```text
tracked Python files deleted = 0
shell/workflow/action files deleted = 0
pyproject.toml or uv.lock changed = false
docs/rust-cutover/release/v0_32_0_* changed = false
runtime or public API impact = none
```

## Validation

```text
python_tooling_baseline.json parse and exact-count assertions = PASS
baseline count reproduction = PASS
docs/examples governance = PASS, markdown_files=107, local_links=293,
  image_links=20, integration_pages=15, concept_pages=9, tutorial_assets=20
Rust examples integrity = PASS, required_paths=14, toml_files=7,
  readme_paths=7
backend freeze baseline = PASS, boundaries=27, source_hashes=4
backend freeze negative selftest = PASS, cases=20
scripts/ai/verify_fast.sh = PASS
git diff --check = PASS
deleted files = 0
pyproject.toml and uv.lock changed = false
runtime crates changed = false
v0.32.0 frozen release files changed = false
```

Commands:

```bash
jq empty docs/rust-cutover/governance/python_tooling_baseline.json
git ls-files '*.py'
rg -l '(python3?|uv run|pytest|ruff|pip-audit)' scripts -g '*.sh' -g '*.bash'
rg -l '(python3?|uv run|pytest|ruff|pip-audit)' scripts/ai -g 'verify_v*.sh'
rg -l 'python3?([^[:alnum:]_]|$).*<<' scripts -g '*.sh' -g '*.bash'
rg -l '(python3?|setup-python|uv run|pytest|ruff|pip-audit|wheel|maturin)' .github/workflows .github/actions -g '*.yml' -g '*.yaml'
scripts/ai/check_docs_examples_governance.sh
scripts/ai/verify_fast.sh
git diff --check
```

An untracked `crates/graphify-out/` directory appeared during relationship
inspection. It contained only 26 generated AST cache files, no report or source
content, and was removed under the generated-artifact policy before review.
