# V180-010 Evidence - v0.18 release gates

Date: 2026-06-26
Executor: Codex
Milestone: v0.18.0
Status: PASS - local validation complete before PR

## Summary

V180-010 wires the v0.18 owner-approved cancel recovery preview evidence chain
into aggregate release verification and hosted release gate routing.

Plain Chinese summary: 这次只处理 v0.18 发版门禁。大白话：把撤单预览、风险门禁、
Owner 审批、响应脱敏、撤单后回查、incident/audit closeout 和 Dashboard 只读面板串成
一个本地/托管都能跑的门禁；它仍然不发真实撤单、不重试、不自动补救，也不开 Dashboard
撤单按钮。

## Files

```text
scripts/ai/verify_v18_release_gates.sh
scripts/ai/verify_release.sh
.github/workflows/rust-cutover-smoke.yml
.github/workflows/release-tag.yml
docs/rust-cutover/evidence/V180-010.md
verification.md
```

## Evidence Contract

The aggregate gate proves:

```text
cancel_request_preview_gate = included
cancel_risk_gate = included
manual_owner_approval_lifecycle_gate = included
cancel_response_redaction_gate = included
post_cancel_readback_gate = included
incident_audit_closeout_gate = included
dashboard_cancel_recovery_panel_gate = included
verify_release_stage = v18-release-gates
hosted_pr_smoke_stage = v18-release-gates
hosted_release_tag_stage = release-v18-release-gates
actual_cancel_send_allowed = false
cancel_attempted = false
cancel_requests_sent = 0
network_cancel_endpoint_attempted = false
retry_attempted = false
replace_attempted = false
amend_attempted = false
flatten_attempted = false
remediation_attempted = false
automatic_cancel_allowed = false
automatic_remediation_allowed = false
dashboard_order_controls_enabled = false
dashboard_cancel_controls_enabled = false
production_order_mutation_allowed = false
raw_secrets_recorded = false
raw_responses_recorded = false
production_trading_enabled = false
```

## Not Implemented

```text
no actual cancel send
no retry, replace, amend, flatten, or remediation action
no automatic cancel
no Dashboard cancel or order controls
no production trading claim
no raw secret or raw response persistence
no release tag
```

## Validation

Local validation completed:

```text
bash -n scripts/ai/verify_v18_release_gates.sh scripts/ai/verify_release.sh = PASS
ruby -e 'require "yaml"; YAML.load_file(".github/workflows/release-tag.yml"); YAML.load_file(".github/workflows/rust-cutover-smoke.yml")' = PASS
scripts/ai/verify_v18_release_gates.sh = PASS
scripts/ai/verify_release.sh v18-release-gates = PASS
PR smoke classifier simulation for this change set = PASS
  v18_smoke = true
  heavy_rust = true
  heavy_rust_reason = verification.md
cargo fmt --check -p nautilus-cli = PASS
cargo clippy -p nautilus-cli --all-targets -- -D warnings = PASS
scripts/ai/verify_fast.sh = PASS
git diff --check = PASS
```

## Rollback Plan

Revert this PR to remove the v0.18 aggregate release gate script, dispatcher
stage, hosted workflow routing, evidence document, and verification update.
This does not affect trading behavior because the change is release
verification and documentation only.
