# V110-000 Evidence

Date: 2026-06-19
Executor: Codex
Task: V110-000 Define v0.11 Production Read-Only + Shadow Portfolio boundary
Risk level: medium

## Goal

Define the v0.11.0 Production Read-Only + Shadow Portfolio boundary before
implementation tasks begin.

## Files Changed

- `docs/rust-cutover/tasks/V110-000.md`
  - Adds the task contract and acceptance criteria.
- `docs/rust-cutover/release/v0_11_0_boundary.md`
  - Defines v0.11.0 read-only/shadow-only scope, forbidden production mutation
    surfaces, artifact counters, and gate ladder.
- `docs/rust-cutover/evidence/V110-000.md`
  - Records this evidence.

## Boundary Confirmed

```text
v0.11.0 = Production Read-Only + Shadow Portfolio
production public reads = owner/local gated
production authenticated account reads = owner gated and redacted
shadow execution intents = local artifact only
shadow portfolio = local artifact only
production order mutation = forbidden
Dashboard order controls = forbidden
v0.12.0 = earliest possible Guarded Live Alpha
```

Plain Chinese summary: 这次只把 v0.11 的边界写清楚。v0.11 可以读取生产环境信息
并在本地做影子组合，但不能提交、撤销、替换、修改生产订单，也不能在 Dashboard
上加下单按钮。

## Behavior Impact

Documentation-only. No runtime code, trading semantics, credentials, network
behavior, order submission behavior, or Dashboard controls changed.

## Public API Impact

No public Rust API change.

## Migration Note

No migration note is required.

## Validation

PASS:

```bash
rg -n "v0\\.11\\.0|Production Read-Only|Shadow Portfolio|production order mutation|production order submission|real-funds mutation|Dashboard order controls|v0\\.12\\.0|Guarded Live Alpha|submission_allowed=false" docs/rust-cutover/tasks/V110-000.md docs/rust-cutover/release/v0_11_0_boundary.md docs/rust-cutover/evidence/V110-000.md
```

PASS:

```bash
rg -n "production order mutation = allowed|production order submission = allowed|real funds mutation = allowed|Dashboard order controls = true|submission_allowed=true" docs/rust-cutover/tasks/V110-000.md docs/rust-cutover/release/v0_11_0_boundary.md docs/rust-cutover/evidence/V110-000.md && exit 1 || true
```

PASS:

```bash
scripts/ai/verify_fast.sh
```

PASS:

```bash
git diff --check
```

## Rollback Plan

Revert this PR to remove the v0.11.0 boundary document, task file, and evidence
file. Runtime behavior and trading semantics do not require rollback.
