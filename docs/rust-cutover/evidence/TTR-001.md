# TTR-001 Evidence

Date: 2026-07-27
Executor: Codex
GitHub issue: `#1147`
Branch: `codex/ttr-001-v28-terminal-http-routes`
PR-stage status: `REVIEW_REQUIRED`
Post-merge status: `DONE`

## 中文摘要

本任务修复了 `project.html` 指出的 Trader Terminal 阻塞：v0.28 定义的六个
只读合同现在已经是 Dashboard Axum 服务的真实 HTTP 路由。响应从冻结的
source-controlled artifacts 做显式投影，并在 malformed、未脱敏或 required-false
边界漂移时 fail closed。

本任务没有实现 Trader Terminal 前端、实时流或任何交易写能力；没有修改
v0.32.0 冻结文件，也没有增加 submit、cancel、retry、replace、amend、flatten、
adapter send、live exchange 或自动修复能力。

## Routes

```text
GET /api/v28/backend-closure/status
GET /api/v28/provenance/drilldown
GET /api/v28/audit/entries
GET /api/v28/telemetry/health
GET /api/v28/permissions/snapshot
GET /api/v28/deployment/state
```

`POST`, `PUT`, `PATCH`, and `DELETE` return `405 Method Not Allowed` for all six
routes.

## Files changed

- `crates/cli/src/dashboard.rs`
- `crates/cli/src/dashboard/server.rs`
- `crates/cli/src/dashboard/server/tests.rs`
- `crates/cli/src/dashboard/trader_terminal_api.rs`
- `crates/cli/src/dashboard/trader_terminal_api/tests.rs`
- `docs/rust-cutover/tasks/TTR-001.md`
- `docs/rust-cutover/evidence/TTR-001.md`

## Contract behavior

- Required response fields are validated directly against
  `v0_28_0_trader_terminal_backend_api_contract_artifact.json`.
- Every response carries `read_only=true`.
- Every response carries explicit false values for operation controls, trading
  controls, order ticket, and manual operation submission.
- Source-specific required-false maps are checked before projection.
- Audit, telemetry, identity, and provenance sources require redacted status.
- Error responses use the v0.28 fail-closed semantic codes.

## Local validation

```text
cargo fmt --all -- --check
result = pass

cargo clippy --workspace --lib --tests \
  --features "arrow,ffi,high-precision,streaming,defi" -- -D warnings
result = pass; exact Rust Cutover Smoke workspace Clippy command

cargo test -p nautilus-cli dashboard::trader_terminal_api::tests -- --nocapture
result = pass; 4 passed, 0 failed

cargo test -p nautilus-cli dashboard::server::tests::trader_terminal_v28_http_routes_serve_read_only_contracts -- --nocapture
result = pass; 1 passed, 0 failed

scripts/ai/verify_fast.sh
result = pass

scripts/ai/check_backend_runtime_risk_inventory.sh
result = pass; 28,985 signals in 1,215 files; no baseline drift

scripts/ai/verify_release.sh current-governance backend-freeze-baseline
result = pass

git diff --check
result = pass
```

The first direct Cargo test attempt used the Homebrew `rustc 1.87.0` and stopped
before compilation because the workspace requires Rust `1.95.0`. All recorded
passing Cargo commands used `scripts/ai/toolchain_env.sh` and the repository
toolchain.

The governance gate initially found local `.DS_Store` files. They were deleted
and the gate was rerun. The risk inventory also detected test assertions in
mixed production paths; the tests were moved to dedicated `tests/` modules and
rewritten without adding direct panic or unwrap signals, restoring the exact
canonical inventory.

The first hosted Rust Cutover Smoke run failed because four assertion-based
tests returned `Result`, which triggered `clippy::panic_in_result_fn` when CI
linted `--lib --tests`. Those tests now return `()` and use explicit result
assertions plus pattern matching. The exact CI workspace Clippy command and both
targeted test groups pass after the correction.

## Behavior impact

- Additive read-only local Dashboard HTTP surface.
- Existing Dashboard shell, snapshot, node query, and lifecycle action routes
  are unchanged.
- No trading semantics, precision, adapter behavior, persistence format, or
  public Rust API changed.

## Migration note

No migration note is required because the change only implements previously
declared additive GET routes. Existing endpoints and DTOs are not changed.

## Rollback

Remove the six route registrations, the two `trader_terminal_api` source/test
modules, the server HTTP test module, and the `mod trader_terminal_api`
declaration. No data migration or runtime state rollback is required.

## PR-stage review status

Implementation and local validation are complete. The owner role has not
approved its own work; Verification & Release Gatekeeper review remains
required before merge.

The paragraph above records the handoff state at the end of implementation. It
is retained as historical PR-stage evidence and is not the current task state.

## Post-merge closeout

- Pull request: [#1148](https://github.com/atxinbao/NTPRO/pull/1148)
- Merged at: `2026-07-27T15:37:09Z`
- Merge commit: `4a57979d6a9a914493a07b8b217ea6074500dff2`
- GitHub issue `#1147` closed at: `2026-07-27T15:37:10Z`
- Hosted checks: `14/14` completed with `SUCCESS`

Under the active GitHub control-plane contract, the merged PR and closed issue
make the current task state `DONE`.
