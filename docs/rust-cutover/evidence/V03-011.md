# V03-011 Evidence - v0.3.0 Supervisor Control Release Contract

Date: 2026-06-12
Executor: Codex
Task: V03-011

## Plain Chinese Summary

本次只把 v0.3.0 的发布口径先锁清楚：v0.3.0 不能只说有
Dashboard 状态页，而要证明本地 sandbox Supervisor 控制台可以覆盖
start、stop、pause、resume、reconnect data source、reconnect execution
gateway 这些基础控制。

这次没有实现 pause/resume/reconnect 的代码，也没有创建 tag 或发布
GitHub Release。后续 V03-012 到 V03-016 必须逐项补实现、补 smoke、补
release gate，全部通过后才允许发布 v0.3.0。

## Files Changed

- `docs/rust-cutover/scope/v0_3_supervisor_control_console.md`
  - Added the v0.3.0 Local Supervisor Control Console scope addendum.
  - Marked pause/resume/reconnect controls as release blockers.
  - Kept production trading, real venue connectivity, real orders, manual order
    entry, and production reconnect out of scope.
- `docs/rust-cutover/tasks/V03-011.md`
  - Added the V03-011 task contract.
- `docs/rust-cutover/tasks/V03-012.md`
  - Added the pause/resume follow-up task contract.
- `docs/rust-cutover/tasks/V03-013.md`
  - Added the reconnect data source follow-up task contract.
- `docs/rust-cutover/tasks/V03-014.md`
  - Added the reconnect execution gateway follow-up task contract.
- `docs/rust-cutover/tasks/V03-015.md`
  - Added the Dashboard controls and API smoke follow-up task contract.
- `docs/rust-cutover/tasks/V03-016.md`
  - Added the release gate and readiness follow-up task contract.
- `docs/rust-cutover/evidence/V03-011.md`
  - Added this evidence record.

## Commands Run

```bash
rg -n "pause node|resume node|reconnect data source|reconnect execution gateway|real_orders_submitted=false" docs/rust-cutover/scope/v0_3_supervisor_control_console.md
```

Result: pending at evidence creation time; final PR result must be recorded in
the PR body.

Observed result:

- found required release-control strings at lines 32-35, 43-46, 112, 114,
  116, 118, and 121.
- confirmed `real_orders_submitted=false` is part of the required release gate.

```bash
git diff --check
```

Result: pending at evidence creation time; final PR result must be recorded in
the PR body.

Observed result: passed.

## Behavior Impact

No runtime behavior changed. This task is documentation-only.

## Public API Impact

No public API changed.

## Migration Note Status

No migration note is required because no runtime behavior or public API changed.

## Release Blocker Decision

V03-011 records the release blocker decision:

- v0.3.0 release is blocked until V03-011 through V03-016 have evidence.
- v0.3.0 release is blocked until supervisor control smoke passes.
- v0.3.0 must not claim production trading, real venue connectivity, real order
  submission, manual order entry, production reconnect, tag creation, or GitHub
  Release approval before the release gate passes.

Owner override for this run: the user explicitly downgraded V03-011 through
V03-016 risk to allow automated execution and auto-merge for this batch.

## Rollback Plan

Revert the PR to remove the v0.3.0 supervisor control scope addendum, the
V03-011 through V03-016 task documents, and this evidence file.
