# V160-010 Evidence - failure-mode and no-retry semantics

Date: 2026-06-23
Executor: Codex
Milestone: v0.16.0
Status: local validation PASS; hosted smoke pending PR

## Summary

V160-010 adds the v0.16 failure/no-retry semantics artifact for the minimum
owner-approved production mutation candidate. It consumes the redacted V160-009
audit trail and records failure handling as evidence-only stop behavior.

Plain Chinese summary: 已增加“失败后怎么处理”的 evidence artifact。大白话：失败后不
自动重试、不自动撤单改单、不自动补救、不继续策略，只写证据并停住。

## Artifact Contract

```text
schema_version = ntpro.v160_production_mutation_failure_semantics.v1
artifact_type = production_mutation_failure_semantics
status = ready_failure_semantics_evidence
terminal_action = write_evidence_and_stop
evidence_written = true
stop_after_evidence = true
strategy_continuation_allowed = false
retry_allowed = false
retry_attempted = false
retry_attempts = 0
max_retry_attempts = 0
cancel_attempted = false
replace_attempted = false
amend_attempted = false
correction_attempted = false
flatten_attempted = false
remediation_attempted = false
automatic_remediation_allowed = false
dashboard_order_controls_enabled = false
listen_key_lifecycle_allowed = false
production_order_mutations_attempted = 0
listen_key_lifecycle_attempted = 0
```

Supported failure modes:

```text
timeout
http-4xx
http-5xx
malformed-response
readback-mismatch
kill-switch-transition
```

## Boundary

This task does not add any automated failure recovery.

```text
no retry
no cancel/replace/amend
no correction/flatten
no automatic remediation
no Dashboard order controls
no listenKey lifecycle
no strategy continuation after evidence
```

## Validation

Local validation completed:

```text
cargo fmt --all --check = PASS
cargo test -p nautilus-cli parses_live_production_mutation_failure_semantics_options --lib = PASS
cargo test -p nautilus-cli production_mutation_failure_semantics --lib = PASS
cargo clippy -p nautilus-cli --all-targets -- -D warnings = PASS
scripts/ai/verify_v16_failure_no_retry_semantics.sh = PASS
scripts/ai/verify_fast.sh = PASS
git diff --check = PASS
target/debug/nautilus live production-mutation-failure-semantics --help = PASS
```

Observed verifier evidence:

```text
v16_failure_no_retry_semantics status=ok root=/var/folders/4t/qzfq_xwj43vbx6ty4tkg_w100000gn/T//ntpro-v16-failure-semantics.eKkfBZ modes=timeout http-4xx http-5xx malformed-response readback-mismatch kill-switch-transition retry_attempted=false remediation_attempted=false strategy_continuation_allowed=false dashboard_order_controls_enabled=false
```

Remaining validation before merge:

```text
hosted Rust Cutover Smoke
```

## Rollback Plan

Revert this PR to remove the failure-semantics command, artifact builder,
verifier, and docs. Default/offline validation performs no network call, so no
exchange state, credentials, real funds, or production order requires rollback.
