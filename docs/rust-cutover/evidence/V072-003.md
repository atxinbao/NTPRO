# V072-003 Evidence - Online Boundary Notes

Date: 2026-06-15
Executor: Codex
Task: V072-003 Fix online boundary notes

## Plain Chinese Summary

这个任务修正 Binance testnet workflow 的 artifact 边界说明。以前即使已经明确
opt-in 并执行了公共 HTTP 只读探测，`boundary.json` 的 notes 仍写“没有打开
socket”，这和实际 artifact 字段冲突。现在离线路径仍写没有打开 socket；只要
HTTP 只读探测被尝试过，boundary notes 就明确写“已在显式 opt-in 后打开
Binance testnet 公共 HTTP 只读 socket”。

## Change Summary

- Added dynamic Binance testnet boundary notes based on
  `connectivity_probe.network_attempted`.
- Kept offline dry-run boundary notes unchanged for no-socket evidence.
- Added unit assertions for offline shared-contract artifacts.
- Added unit assertions for HTTP success and HTTP failure artifacts so online
  attempt boundary notes no longer claim that no socket was opened.

## Files Changed

```text
crates/cli/src/workflow.rs
docs/rust-cutover/evidence/V072-003.md
docs/rust-cutover/tasks/V072-003.md
```

## Behavior Impact

Artifact wording is corrected. Runtime gate behavior is unchanged:

```text
default dry-run = offline
blocked manual online probe = no socket
explicit opt-in HTTP read-only probe = public HTTP read-only socket may open
order submission = disabled
real funds = false
production trading = false
production venue connection = false
credential values recorded = false
```

## Public API Impact

No Rust public API, CLI flag, artifact schema, or trading semantic change.
Only `boundary.json` note text is more accurate for manual online HTTP
read-only probe attempts.

## Migration Note Status

No migration note is required.

## Boundary Confirmation

```text
No default networking added.
No network gate permission relaxed.
No authenticated account access added.
No order submission added.
No production Binance connectivity added.
No real funds added.
No production trading added.
```

## Validation Commands

```bash
cargo fmt --check
cargo test -p nautilus-cli workflow --lib
scripts/ai/verify_v07_default_offline_gate.sh
scripts/ai/verify_v07_manual_online_gate.sh
scripts/ai/verify_fast.sh
rg -n "No socket is opened|public HTTP read-only socket was opened after explicit opt-in" crates/cli/src/workflow.rs
git diff --check
```

## Validation Result

PASS.

- Initial `cargo fmt --check` reported formatting differences only; `cargo fmt`
  was run and the formatted diff was kept.
- `cargo test -p nautilus-cli workflow --lib` passed:
  `35 passed; 0 failed; 0 ignored; 107 filtered out`.
- `scripts/ai/verify_v07_default_offline_gate.sh` passed.
- `scripts/ai/verify_v07_manual_online_gate.sh` passed with expected
  `blocked_missing_env` default because manual online proof still requires
  explicit environment opt-in.
- `scripts/ai/verify_fast.sh` passed.
- `rg` shows `No socket is opened` remains only in offline/blocked diagnostic
  paths and tests, while online boundary tests assert it is absent from HTTP
  success/failure notes.
- `git diff --check` passed.

## Rollback Plan

Revert the V072-003 commit. That restores the previous fixed boundary notes,
including the known inaccurate wording for manual online HTTP attempts.
