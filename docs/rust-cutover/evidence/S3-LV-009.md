# S3-LV-009 - 策略意图绑定的逐笔 Live 执行证据

Date: 2026-08-14
Executor: Codex
GitHub issue: #1306
Status: MERGED

## 实现证据

- Product API 从已停止且结果冻结的 Demo Run 读取最新订单意图，同时校验 StrategyVersion、
  manifest/result 哈希、风险拒绝和零真实发送；
- 策略意图作为不可变工件写入 Live candidate，三角色审批、最终 admission、Runtime config 和
  execution order state 均绑定其哈希；
- instrument、side、quantity 由来源意图锁定。策略工作台只读展示来源 Demo Run、intent、方向
  和数量，用户仅填写 LIMIT 价格、最大名义金额并明确确认；
- 工作区扫描阻止同一 Demo 结果中的同一 intent 被另一个 Live Run 重放；
- Runtime 在注册执行客户端前回读 `strategy-order-intent.json`，身份、数量、哈希或 schema 漂移
  均 fail closed；订单状态 v3 保留完整来源链；
- Owner、Risk、Operator 审批发布使用持久化 stage，外部锚点、本地审批/receipt/workspace head
  以及最终 admission/state/commit/state head 均可幂等恢复；19 个写入边界逐一故障注入并恢复；
- mutation lock 使用标准库 OS advisory file lock，文件描述符持有所有权且进程退出时由内核释放；
  PID 与进程启动时间仅作诊断。真实 `SIGKILL` 恢复及两个并发恢复者竞争均保持单 writer；
- Demo `f64` 数量先转换为最短十进制文本再进入 Decimal，`0.1`、`0.001`、`0.00001`
  均与前端请求一致；`strategy_intent_id` 的 OpenAPI/后端上限统一为 128；
- 追加订单、改单、自动重试、自动恢复和补偿交易继续关闭。

## 本地验证

- `cargo +1.95.0 check -p nautilus-cli --tests`：PASS；
- 策略意图身份、哈希和跨 Run 防重放定向测试：2 passed；
- Live Product API 定向回归：37 passed；
- 审批发布 19 个故障边界恢复测试：PASS，来源策略意图漂移在恢复前 fail closed；
- mutation lock 活 owner 拒绝、真实 `SIGKILL` 恢复和两个并发恢复者单 writer：5 passed；
- Decimal 规范化回归：`0.1`、`0.001`、`0.00001` 全部 PASS；
- `cargo +1.95.0 test -p nautilus-cli --lib -- --test-threads=1`：771 passed；
- `cargo +1.95.0 clippy -p nautilus-cli --lib --tests -- -D warnings`：PASS；
- Strategy Workbench Vitest：180 passed；
- Strategy Workbench Playwright：10 passed，覆盖桌面、移动端、Live 单笔准入和部分成交人工撤单；
- `npm run format:check`、`npm run lint`、`npm run typecheck`、`npm run build`：PASS；
- `npm audit --audit-level=high`：0 vulnerabilities；传递依赖 `nanoid` 已从 3.3.17
  锁定升级到 3.3.18；
- Rust 1.95.0 pin、Rust-only runtime、backend runtime risk inventory、MVP/backend
  freeze 与 current governance：PASS；risk inventory production ownership 保持 2,703，
  新增信号仅属于 inline tests。

## 审查与合并

风险等级为 critical。必须在独立 Gatekeeper 审批和 hosted checks 全绿后手动合并，禁止
auto-merge。独立 Gatekeeper 已批准，hosted checks 全绿，PR #1307 已手动合并，merge commit
为 `2ffcc25ef1701df874726e849910e4372b6384cf`，Issue #1306 已关闭。
