# TCG-002 - Rust 1.95.0 工具链统一证据

Date: 2026-08-05
Executor: Codex
GitHub issue: #1234
Status: REVIEW_REQUIRED

## 根因

仓库已有 1.95.0 rustup override，但当前登录环境把 `/opt/homebrew/bin` 放在
`$HOME/.local/bin` 之前。Homebrew `cargo/rustc` 不读取 rustup override，因此部分未加载
`toolchain_env.sh` 的治理脚本实际使用 Rust 1.87.0。

## 修复

- `toolchain_env.sh` 从 `rust-toolchain.toml` 读取版本，并要求与 Cargo.toml
  `rust-version` 完全一致；
- 环境变量不能覆盖 canonical 版本；rustc 与 cargo 都必须报告 1.95.0；
- governance、product lint、benchmark 和 Makefile 全部绑定该环境；
- smoke、performance、release gate 和 release publication workflow 在 checkout 后读取 canonical 版本，不再复制
  `1.95.0` literal；
- `verify_fast.sh` 首先执行 toolchain pin guard；
- 负向测试拒绝环境覆盖、floating stable 和 Cargo.toml 版本不一致。

## 验证

```text
check_rust_toolchain_pin.sh
PASS: toolchain=1.95.0, direct_cargo_scripts=9, workflow_literals=0

negative selftest
PASS: environment override, floating stable, Cargo.toml mismatch 均被拒绝

verify_fast.sh
PASS: cargo 1.95.0 / rustc 1.95.0 / cargo fmt

Makefile parse
PASS: CARGO/RUSTC/RUSTUP_TOOLCHAIN 全部解析到 1.95.0

Cargo metadata
PASS

cargo clippy -p ntpro-governance --all-targets -- -D warnings
PASS

Rust-only / backend runtime risk inventory
PASS: 29367 signals / 1221 files

current-governance / backend-freeze-baseline
PASS: v0.32.0 frozen commit、27 boundaries、20 negative cases 保持成立

workflow YAML
PASS

git diff --check
PASS
```

本机登录 shell 验证：NTPRO 主目录和关联 worktree 的 plain `cargo/rustc` 都是 1.95.0；
`/tmp` 非 NTPRO 目录继续使用 Homebrew 1.87.0。该 path-scoped shell 修复位于用户环境，
不进入 Git；仓库自身不依赖该本机配置，所有脚本仍独立强制 canonical 工具链。

待 hosted checks 和独立复审完成。PR 合并前保持 REVIEW_REQUIRED。
