# RDG-002 Evidence

Date: 2026-07-28
Executor: Codex
GitHub issue: `#1157`
Branch: `codex/rdg-002-compress-chinese-readme`
Status: `READY_FOR_PR`

## 中文摘要

README 从 630 行、31,078 字节的历史累积页面，改为以中文为主的项目入口。
历史版本逐条清单、重复边界和过时的 v0.20.1 最新发布声明不再出现在根入口；
它们仍由 changelog、roadmap、release index 和 source-controlled evidence 保存。

发布治理所依赖的机器字段原文保留，避免文档精简破坏自动 gate。

## Behavior impact

- 仅修改文档和任务证据。
- 不改变运行时、API、adapter、schema、存储、交易语义或发布事实。
- 不修改 v0.32.0 冻结发布文件。

## Validation

- README：630 行压缩为 156 行，31,078 字节压缩为 5,502 字节。
- `scripts/ai/check_docs_examples_governance.sh`：通过，122 个 Markdown 文件、
  304 个本地链接、20 个图片链接和 15 个 integration 页面均有效。
- `scripts/ai/verify_release.sh current-governance`：通过，包括 current release
  surface、backend freeze、zero-Python、backend hygiene、risk inventory、
  control-plane retirement、historical release retirement 和 golden trace。
- `scripts/ai/check_rust_only_runtime.sh`：通过。
- release surface 确认 `v0.33.0`、`backend-maintenance` 和 `v0.34.0+`。
- backend freeze 确认 27 个边界、四个 source hashes 和 20 个 negative cases。
- README 中不再包含过时的 v0.20.1 latest release 声明。
- `git diff -- docs/rust-cutover/release/v0_32_0_*`：空。
- `git diff --check`：通过。

## Rollback

回退本任务提交即可恢复旧 README；不需要数据、schema、部署或 release 回滚。
