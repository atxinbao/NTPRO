# V05-001 Evidence - workflow CLI contract

Date: 2026-06-13
Executor: Codex
Task ID: V05-001

## Plain Chinese Summary

本任务新增了 `nautilus workflow run` 这个 Rust CLI 入口。

大白话说：用户现在可以用 CLI 跑一个本地 Binance sandbox workflow，并得到一组
JSON/JSONL artifact。它只是把本地 fixture、策略 smoke、mock order lifecycle 和
risk rejection 证据串起来，不连接真实 Binance，不用真实资金，也不提交真实订单。

## Files Changed

- `crates/cli/src/opt.rs`
- `crates/cli/src/lib.rs`
- `crates/cli/src/workflow.rs`
- `crates/cli/Cargo.toml`
- `docs/rust-cutover/product/V05_WORKFLOW_ARTIFACT_CONTRACT.md`
- `docs/rust-cutover/tasks/V05-001.md`
- `docs/rust-cutover/evidence/V05-001.md`

## Commands Run

```bash
cargo fmt --check
cargo test -p nautilus-cli workflow --lib
cargo run -p nautilus-cli --bin nautilus -- workflow run --workflow binance-sandbox --run-id v05-smoke --output /tmp/ntpro-v05-workflow-smoke
```

## Validation Summary

- `cargo fmt --check`: PASS.
- `cargo test -p nautilus-cli workflow --lib`: PASS, 7 tests passed.
- CLI smoke: PASS, generated `manifest.json`, `summary.json`, `events.jsonl`,
  and component artifacts.

## Behavior Impact

Adds one local artifact command. Existing CLI commands are unchanged.

## Public API Impact

Public CLI surface adds `workflow run`. No Rust library API is exposed.

## Migration Note Status

No migration note required. This is a new Rust-only local workflow command.

## Rollback Plan

Revert the PR to remove the workflow CLI command and its documentation.
