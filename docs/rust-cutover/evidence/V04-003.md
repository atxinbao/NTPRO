# V04-003 Evidence - EMA / RSI strategy contracts

Date: 2026-06-13
Executor: Codex
Task ID: V04-003

## Plain Chinese Summary

这次只定义 EMA 和 RSI 两个内置策略的 v0.4 合约，没有改运行时代码。

大白话说：后续 DTO、fixture replay、mock order、risk smoke、EMA/RSI smoke
和 Dashboard 都要按同一套规则说话。EMA/RSI 只用于 Binance Spot sandbox
路径，不代表策略盈利，不支持任意用户策略加载，也不连接真实账户或真实下单。

## Files Changed

- `docs/rust-cutover/specs/v0_4_strategy_contracts.md`
- `docs/rust-cutover/evidence/V04-003.md`

## Commands Run

```bash
rg -n "EMA|RSI|input|output|evidence|non-goal" docs/rust-cutover/specs/v0_4_strategy_contracts.md
git diff --check
scripts/ai/verify_fast.sh
```

## Validation Summary

- `rg -n "EMA|RSI|input|output|evidence|non-goal" docs/rust-cutover/specs/v0_4_strategy_contracts.md`
  passed and showed the EMA/RSI input, output, evidence, and non-goal sections.
- `git diff --check` passed with no whitespace errors.
- `scripts/ai/verify_fast.sh` passed. It ran toolchain and `cargo fmt --check`;
  workspace `cargo check` and clippy are intentionally skipped by the fast
  smoke default.

## Strategy Contract Summary

- EMA contract defines `fast_period`, `slow_period`, `signal_mode`, and
  `warmup_bars`.
- RSI contract defines `period`, `oversold_threshold`,
  `overbought_threshold`, and `warmup_bars`.
- Both contracts require deterministic fixture replay, mock execution, scoped
  sandbox risk, and comparable smoke evidence.
- Both contracts explicitly exclude production trading, strategy profitability,
  arbitrary user strategy loading, real credentials, and real orders.

## Behavior Impact

No runtime behavior change. No trading semantic change. No strategy code
change.

## Public API Impact

None.

## Migration Note Status

Not required. This is a product contract document for later v0.4
implementation tasks.

## Rollback Plan

Revert this PR to remove the v0.4 strategy contract document and V04-003
evidence file.
