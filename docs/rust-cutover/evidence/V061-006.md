# V061-006 Evidence - PR-stage v0.6 workflow smoke

Date: 2026-06-14
Executor: Codex
Task ID: V061-006

## Plain Chinese Summary

本任务只调整 PR 阶段 CI 覆盖，不改运行时代码、不连接 Binance、不下单、不读取
真实密钥，也不替代正式 release gate。

大白话说：以前 v0.6 的 Binance testnet dry-run smoke 更多是在 release gate
里暴露问题。现在只要 PR 改到 workflow、Dashboard、CLI option、v0.6 配置、v0.6
验证脚本或 v0.6 用户文档，PR smoke 里就会出现一个明确步骤：
`v06-binance-testnet-dry-run-smoke`，提前跑 offline dry-run。

## Files Changed

- `.github/workflows/rust-cutover-smoke.yml`
- `docs/rust-cutover/tasks/V061-006.md`
- `docs/rust-cutover/evidence/V061-006.md`

Local-only state:

- `.agentflow/leases/V061-006.json` was written locally but is ignored by git
  and is not part of the PR.

## Commands Run

```bash
ruby -e 'require "yaml"; YAML.load_file(".github/workflows/rust-cutover-smoke.yml"); puts "rust-cutover-smoke yaml ok"'
tmp=$(mktemp); printf '%s\n' 'crates/cli/src/dashboard.rs' > "$tmp"; if grep -E '^(crates/cli/src/(workflow|dashboard|opt)\.rs|examples/rust/binance/testnet_dry_run\.toml|scripts/ai/verify_v06_binance_testnet_dry_run\.sh|docs/rust-cutover/product/V06_BINANCE_TESTNET_DRY_RUN_USER_GUIDE\.md)$' "$tmp" >/dev/null; then echo 'v06_smoke=true'; else echo 'v06_smoke=false'; exit 1; fi
scripts/ai/verify_v06_binance_testnet_dry_run.sh
scripts/ai/verify_fast.sh
git diff --check
```

## Command Output Summary

- Workflow YAML parse:

```text
rust-cutover-smoke yaml ok
```

- Representative classifier smoke:

```text
v06_smoke=true
```

- V06 dry-run smoke:

```text
v06_binance_testnet_dry_run_assertions status=ok run_id=v06-smoke artifact_count=9 events=7 testnet_connection=false real_orders_submitted=false
v06_binance_testnet_dry_run_smoke status=ok
```

## Validation Summary

Result: PASS.

- PR changed-file classifier now emits `v06_smoke=true` for representative
  v0.6 paths.
- PR smoke now has an explicit `v06-binance-testnet-dry-run-smoke` step.
- `scripts/ai/verify_v06_binance_testnet_dry_run.sh`: PASS.
- `scripts/ai/verify_fast.sh`: PASS.
- `git diff --check`: PASS.

## Behavior Impact

- PR-stage CI coverage is stricter for v0.6 workflow/dashboard/option/config
  and documentation changes.
- The v0.6 smoke remains offline-only.
- Formal release verification remains unchanged.
- No runtime behavior changed.
- No network behavior changed.
- No order behavior changed.

## Public API Impact

No public CLI or runtime API changes.

## Migration Note Status

No migration note is required. This is CI coverage only.

## Rollback Plan

Revert this PR to remove the PR-stage v0.6 smoke trigger and return v0.6 smoke
coverage to release-gate usage only.
