# V031-010 Evidence - v0.3.1 Readiness Report

Date: 2026-06-12
Executor: Codex
Task: V031-010
Branch: `codex/V031-010-v0-3-1-readiness-report`

## Plain Chinese Summary

这次是 v0.3.1 修正任务的最后一项：生成最终 readiness report。

大白话说：V031 前面 9 项已经把 v0.3.0 后的问题收口了，本地
`verify_release.sh` 也完整跑通了。所以现在可以进入 GitHub hosted release
gate。

但现在还不能直接发布。因为 GitHub hosted release gate 在 V031 收口后还没有
重新跑出绿色。结论是：本地 readiness 通过，可以跑 hosted gate；正式 tag /
GitHub Release 必须等 hosted gate 通过。

## Files Changed

- `docs/rust-cutover/tasks/V031-010.md`
  - adds the task record for this readiness report.
- `docs/rust-cutover/release/v0_3_1_supervisor_control_hardening_readiness_report.md`
  - creates the final v0.3.1 readiness report.
- `docs/rust-cutover/evidence/V031-010.md`
  - records this evidence.

## Commands Run

```bash
scripts/ai/verify_release.sh
scripts/ai/check_rust_only_runtime.sh
git diff --check
gh run list --workflow "Rust Cutover Release Gate" --limit 5 --json databaseId,status,conclusion,headBranch,headSha,event,createdAt,updatedAt,url
```

## Validation Results

- `scripts/ai/verify_release.sh`: **passed**.
  - `verify_full` passed.
  - release build passed.
  - release build duration: `11m 54s`.
  - release CLI help checks passed.
  - `check_rust_only_runtime.sh` passed inside release verification.
  - `check_cython_removed.sh` passed inside release verification.
  - v0.2 two-node supervisor smoke passed.
  - v0.3 supervisor control smoke passed against release binaries.
  - v0.3 dashboard control smoke passed against release binaries.
- `scripts/ai/check_rust_only_runtime.sh`: **passed**.
- `git diff --check`: **passed**.

## v0.3.1 Readiness PASS / FAIL

| Decision item | Result |
| --- | --- |
| V031 task queue ready for hosted release gate | PASS |
| Local readiness | PASS |
| Hosted release gate | FAIL / PENDING RERUN |
| Tag / GitHub Release publication | FAIL until hosted release gate PASS |

## Hosted Release-Gate Result

No hosted `Rust Cutover Release Gate` PASS exists after the full V031 closeout.

Latest hosted runs checked:

- `27423501016`: `workflow_dispatch` on `main`, conclusion `cancelled`.
- `27421121134`: `workflow_dispatch` on `main`, conclusion `failure`.
- `27418815065`: `workflow_dispatch` on `main`, conclusion `cancelled`.
- `27384342541`: `push` on `ntpro-rust-only-v0.3.0`, conclusion `failure`.

This means hosted release approval remains pending. The next step after this PR
is merged is to rerun hosted `Rust Cutover Release Gate` on `main`.

## Behavior Impact

No runtime behavior changed.

No trading-semantic behavior changed.

No adapter behavior changed.

## Public API Impact

No public API change.

No CLI command shape changed.

## Migration Note Status

No migration note required. This is a readiness report and evidence update only.

## Rollback Plan

Revert this PR to remove the V031-010 readiness report, evidence file, and task
record. Runtime behavior is unaffected because this task changes only
documentation and release evidence.
