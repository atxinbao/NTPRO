# V141-006 Evidence - Dashboard Order-State Read-Only Panel

Date: 2026-06-22
Executor: Codex

## Summary

V141-006 adds the production order-state read-only proof to the Dashboard
live-alpha readiness surface.

Plain Chinese summary: 这次只把已有的生产订单状态只读证据展示到 Dashboard。用户能看到
读取状态、端点、是否发生网络读取、shape 是否有效、openOrders 数量、是否观察到非空订单、
订单生命周期是否就绪，以及这些值的真值来源。Dashboard 仍然没有任何生产下单、撤单、改单、
重试、重连或 listenKey 控制。

## Changed

- Added Dashboard constants for `ntpro.v140_production_order_state_readonly_proof.v1`
  and `v0_14/production_order_state_readonly_proof.json`.
- Extended `LiveAlphaDryRunStatus` with order-state read-only proof fields.
- Rendered the proof fields in the Dashboard `Order State` column.
- Added the proof artifact path to the Dashboard artifact column.
- Added unit coverage for empty `openOrders`:
  - endpoint is readable;
  - response shape is valid;
  - open order count is `0`;
  - non-empty order state is `false`;
  - lifecycle readiness is `false`.

## Not Changed

- No production order submission.
- No production order mutation, cancel, replace, amend, retry, or correction.
- No listenKey lifecycle.
- No Dashboard order controls.
- No trading semantics changes.
- No network execution from Dashboard.

## Validation

Planned / executed commands for this task:

```text
cargo test -p nautilus-cli live_alpha_dry_run_artifacts_populate_readonly_dashboard_panel --lib
cargo test -p nautilus-cli live_alpha_dry_run_boundary_violation_degrades_dashboard_panel --lib
cargo test -p nautilus-cli dashboard --lib
cargo check -p nautilus-cli
cargo clippy -p nautilus-cli --all-targets -- -D warnings
scripts/ai/verify_fast.sh
cargo fmt --all --check
git diff --check
```
