# BFH-002 Local Filesystem Hygiene Evidence

Date: 2026-07-19
Executor: Codex
GitHub issue: #1113
Milestone: post-freeze-backend-hygiene
Status: PRE-MERGE VALIDATION PASSED

## Summary

BFH-002 removed confirmed Finder metadata and made the retained local project
review report invisible to unrelated Git status. No tracked product, test,
fixture, schema, release, or governance authority was deleted.

Plain Chinese summary: 8 个 `.DS_Store` 已完全清除。`project.html` 是本地 Codex
审查蓝图，不是正式代码或发布证据，现保留并本地排除。分析工具状态和大型测试数据均
保持原样。

## Before And After

```text
.DS_Store before = 8 files, 84,000 bytes
.DS_Store after = 0 files
project.html before = untracked, 52,457 bytes
project.html after = retained, locally ignored by .git/info/exclude
project.html sha256 =
  3da72394f6f961b9808a665ac65fac460c2417a639ed993cee59d6ae75ab2347
project.html credential-like text matches = 0
.agentflow = retained, 217 files, 1.0 MiB
.codex = retained, 158 files, 37 MiB
.understand-anything = retained, 8 files, 14 MiB
tests/test_data/large = retained, 9 files, 237 MiB
tests/test_data/local = absent
target and target-v2 = absent
release-publication-evidence = absent
graphify-out = absent
.venv and Python caches = absent
```

## Ownership Decision

- `.DS_Store`: reproducible Finder metadata; removed.
- `project.html`: user-local Codex review/roadmap report; retained and excluded
  by the local `/project.html` rule in `.git/info/exclude`.
- `.agentflow/`, `.codex/`, `.understand-anything/`: ignored local tool state;
  retained.
- `tests/test_data/large/`: expensive ignored test input; retained pending
  BFH-006.

The `.git/info/exclude` change is deliberately local-only and is not part of the
tracked PR.

## Change Boundary

```text
tracked files deleted = 0
tracked runtime/source/test/fixture files changed = false
docs/rust-cutover/release/v0_32_0_* changed = false
registered backend boundary changed = false
user-owned local paths deleted = 0
```

## Validation

```text
.DS_Store recount = PASS, 0 files
project.html local ignore = PASS, .git/info/exclude:8:/project.html
tracked deletion count = PASS, 0
runtime/test/schema/frozen path changes = PASS, 0
release surface current guard = PASS
docs/examples governance = PASS, markdown_files=116, local_links=293
Rust examples integrity = PASS, required_paths=14
zero-Python closeout = PASS, tracked_files=5256, active_scripts=55,
  workflow_actions=17, historical_docs=1932, negative_cases=10
backend freeze baseline = PASS, boundaries=27, source_hashes=4
backend freeze negative selftest = PASS, cases=20
golden schema smoke = PASS, rows=1
scripts/ai/verify_fast.sh = PASS
git diff --check = PASS
```

Commands:

```bash
find . -type d \( -path './.git' -o -path './target' -o \
  -path './target-v2' \) -prune -o -name .DS_Store -print
git check-ignore -v project.html
git status --short --ignored
CARGO_TARGET_DIR="${TMPDIR:-/tmp}/ntpro-bfh001-target" \
  scripts/ai/verify_release.sh current-governance backend-freeze-baseline
CARGO_TARGET_DIR="${TMPDIR:-/tmp}/ntpro-bfh001-target" \
  scripts/ai/verify_fast.sh
git diff --check
```

The existing external Cargo target was reused. No build output was written to
the repository.
