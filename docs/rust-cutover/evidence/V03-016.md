# V03-016 Evidence - v0.3.0 Release Gate and Readiness Report

Date: 2026-06-12
Executor: Codex
Task: V03-016

## Plain Chinese Summary

本次没有再改 runtime 逻辑，而是把 v0.3.0 的最终发布门禁补完整。

大白话说：现在 `verify_release.sh` 不只是看旧的 v0.2 两节点 smoke，也会强制
跑 v0.3 的本地 Supervisor 控制 smoke 和本地 Dashboard 控制 smoke；同时我补了
最终的 `v0.3.0` readiness report，把已支持和未支持的边界写清楚，避免发布时把
本地 sandbox 控制台说成生产交易系统。

这次没有创建 tag，没有发布 GitHub Release，也没有新增真实交易所连接、真实订单
或手工下单能力。

## Files Changed

- `scripts/ai/verify_release.sh`
  - Extended supervisor help checks to include:
    - `pause`
    - `resume`
    - `reconnect-data`
    - `reconnect-execution`
  - Added v0.3 local supervisor control smoke.
  - Added v0.3 local Dashboard control smoke.
- `docs/rust-cutover/release/v0_3_0_supervisor_control_readiness_report.md`
  - Added the final v0.3.0 local supervisor control readiness report.
- `docs/rust-cutover/evidence/V03-016.md`
  - Added this evidence record.

## Commands Run

```bash
cargo fmt --check
```

Result: passed.

```bash
cargo check -p nautilus-cli
```

Result: passed.

```bash
cargo test -p nautilus-cli dashboard --lib
```

Result: passed, 24 tests passed.

```bash
cargo test -p nautilus-cli supervisor --lib
```

Result: passed, 24 tests passed.

```bash
scripts/ai/v03_supervisor_control_smoke.sh
```

Result: passed.

Observed summary:

- `status=ok`
- `lifecycle_state=paused` after pause
- `lifecycle_state=running` after resume
- `data_connection=not_supported`
- `execution_connection=not_supported`
- `external_venue_connection=false`
- `real_orders_submitted=false`

```bash
scripts/ai/v03_dashboard_smoke.sh
```

Result: passed.

Observed summary:

- final states `sandbox-a=running`, `sandbox-b=stopped`
- reconnect buttons returned `not_supported`
- desktop/mobile layout assertions passed

```bash
scripts/ai/verify_release.sh
```

Result: passed.

```bash
scripts/ai/check_rust_only_runtime.sh
```

Result: passed.

```bash
git diff --check
```

Result: passed.

## v0.3.0 Readiness PASS/FAIL

PASS.

PASS criteria met:

- CLI control surface covers the six scoped commands.
- Dashboard control surface covers the same local-only control actions.
- Release gate now includes both v0.3 control smokes.
- The release report states supported and unsupported boundaries explicitly.

## Release Gate Result

PASS.

`scripts/ai/verify_release.sh` now covers:

- v0.2 local two-node supervisor smoke
- v0.3 local supervisor control smoke
- v0.3 local Dashboard control smoke

## Behavior Impact

No trading semantics changed in this task.

The behavior impact is on release validation only: the v0.3.0 release gate is
now strict enough to prevent publishing a local control-console release without
proving the local control flows still work.

## Public API Impact

No Rust library API changed.

The only public-facing effect is stronger release verification and a new final
readiness report documenting the actual v0.3.0 control-console boundary.

## Migration Note Status

No migration note is required. This task updates release verification and
readiness documentation only.

## Rollback Plan

Revert this PR to remove the stricter release gate checks, the v0.3.0
readiness report, and this evidence file.
