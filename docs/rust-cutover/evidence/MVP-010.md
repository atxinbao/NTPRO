# MVP-010 - 控制中心单节点生命周期动作产品化证据

Date: 2026-08-04
Executor: Codex
GitHub issue: #1225
Status: DONE

## 中文摘要

控制中心现在只允许运维角色对唯一的本地 sandbox 节点执行启动和停止。页面不会直接
修改节点，也不会开放暂停、恢复、重连或任何交易动作；它调用版本化 API，由既有
Supervisor 路径完成状态转换，再重新读取共享状态和运维投影确认结果。

## 实现结果

- 运维快照升级为 v2，服务端只投影 start/stop、目标节点、POST 方法、enabled 和稳定
  reason code；
- 新增两个 operator-only 版本化 POST 路由，复用既有 `control_action_response`；
- 动作 envelope 显式关闭外部 Venue、外部网络、订单提交/变更、自动重试/补救和真实订单；
- 控制中心要求二次点击确认，动作期间禁用全部生命周期按钮；
- 浏览器不信任 action 响应，严格校验 schema、contract、node、action、状态和边界；
- 动作完成后以 200ms 间隔、最多 5 秒等待 MvpRuntime 四轴状态合同与 Supervisor 工件
  对齐，并要求最终状态等于本次动作目标；超时或反向状态即阻断并移除动作按钮；
- Dashboard 所有生命周期入口共享单一串行锁，直接并发 stop 恰好一个成功、一个 409；
- Supervisor 在 registry 文件锁内拒绝已存在的 `stop.request`，不同 store/process 入口也不能
  重复写入同一次 stop；
- 版本化 action 在锁内重新验证单节点、sandbox-only 及全部外部/真实交易 false 边界，
  不能绕过 GET 投影直接操作额外节点；
- shared status 与 event correlation 继续保持 read-only，机构工作台继续无 action。

## 负向边界

- 无会话或机构角色调用 action：403；
- action 端点 GET/HEAD/PUT/PATCH/DELETE 等：405；
- pause 等未注册 action：404；
- 错误节点：404，错误生命周期：409；
- schema、contract、node、action、状态或任一 false 边界漂移：浏览器 fail closed；
- 控制中心状态读取失败时清空旧节点、组件和动作能力；
- 不包含 submit、cancel、replace、amend、flatten、retry 或 remediation 控件。

## 本地验证

```text
cargo fmt --all -- --check
PASS

cargo test -p nautilus-cli dashboard:: --lib -j 2
PASS: 164 passed

cargo test -p nautilus-cli mvp::tests --lib -j 2
PASS: 14 passed

cargo test -p nautilus-cli supervisor::tests --lib -j 2
PASS: 25 passed

cargo clippy -p nautilus-cli --all-targets --all-features -- -D warnings
PASS

test_control_center_contract.mjs
PASS: valid=1 lifecycle_action=1 post_action_target_mismatch=1
      fail_closed=60 stale_clear=49

test_institution_workbench_contract.mjs
PASS: valid=1 fail_closed=18 stale_clear=18

test_control_center_browser.mjs
PASS: desktop=1440x1000 mobile=390x844 lifecycle_action=1
      lifecycle_operator_only=1 running->stopped->running=1
      unauthorized=1 wrong_role=1 method_boundary=1 unsupported_action=1
      stale_clear=5 cjk_glyphs=1 graceful_shutdown=1

test_institution_workbench_browser.mjs
PASS: desktop=1440x1000 mobile=390x844 valid=1 boundary=1
      unauthorized=1 wrong_role=1 stale_clear=4 cjk_glyphs=1 graceful_shutdown=1

backend runtime risk inventory
counts unchanged: total=29295 matched_files=1221 production=2697
canonical hash updated only because source line positions changed

check_docs_examples_governance.sh
PASS: markdown_files=132 local_links=311 image_links=20

check_rust_only_runtime.sh
PASS

check_backend_freeze_baseline.sh
PASS: boundaries=27 source_hashes=4 negative_cases=20

verify_release.sh current-governance backend-freeze-baseline
PASS

git diff --check
PASS
```

首次 Chrome 回归在 stop 后立即读取到 Supervisor `stopped` 与尚未到下一个 heartbeat 的
共享 `running`，页面按合同阻断且移除按钮。实现随后增加有界状态对齐轮询，第二次真实
Chrome 回归完整通过。这证明 HTTP action 成功不会被直接当作最终产品状态。

验证统一使用仓库固定 Rust 1.95.0 工具链。系统默认 PATH 首次命中 Rust 1.87.0 时，治理
脚本因最低版本约束拒绝执行；修正 PATH 后完整重跑通过，未据此放宽任何门禁。

## 浏览器证据

```text
control center artifact:
/tmp/ntpro-mvp010-final-control.Kn7dsp/result.json
sha256=2d3fbc07e4c7df4101af527867e2f10f8fcca8981b3467e95ddece4f3b5133a5

institution workbench artifact:
/tmp/ntpro-mvp010-final-institution.aLNfCn/result.json
sha256=9df96384eb5f164edfd1effff5c57e95bdcdf6aeb315e2a5bf76c73e036bb096

access_token pattern scan:
PASS: no token value found in either evidence directory; `[REDACTED]` log markers are allowed
```

桌面与 390x844 移动截图已人工检查：按钮、状态栏和内容无重叠或横向溢出；机构工作台
未出现 lifecycle action 控件。上述 `/tmp` 路径是本地验证证据，hosted run 必须另行上传
并核对同类 artifact，不能由本地结果替代。

## 行为影响

新增本地 operator-only start/stop 产品入口。没有交易语义、订单状态机、adapter、外部
Venue、数据格式或 v0.32.0 冻结面变化。旧 Dashboard unversioned action 保持兼容。

## 公共 API 与回滚

新增 v2 运维投影和版本化 action API，迁移说明见
`docs/rust-cutover/migration/MVP-010-control-center-lifecycle-actions.md`。回滚本任务提交即可
恢复 v1 只读控制中心，不需要迁移 Supervisor registry。

## 独立审查修复

独立 Gatekeeper 对首个 head `a0ad1ea9f9` 给出 `REVIEW_REQUIRED`，指出三个 P1：版本化
action 可绕过单节点 sandbox 投影、并发 stop 存在重复成功窗口、动作后未验证目标最终状态。
修复结果如下：

- action 路径与 GET 投影复用同一 scope validator，并在串行锁内执行；
- 旧 Dashboard 与版本化 action 共用同一锁；Supervisor 同时在 registry 锁内拒绝重复 stop
  marker，并发 stop 回归固定为 1 个 200、1 个 409；
- 浏览器刷新接受 `expectedLifecycleState`，投影虽一致但不等于动作目标时继续 fail closed；
- 增加多节点 503、错误目标 404、并发 stop 和错误最终状态回归。

独立复审随后发现版本化 action 在 scope 校验后再次读取 registry，第二份快照没有重新执行
scope 校验，存在 TOCTOU 窗口。实现已改为把锁内通过 scope 校验的同一份快照直接交给动作
状态判断与执行入口，版本化路径不再二次读取未校验快照。

首个 head 的 hosted 14/14 及 artifacts 只证明当时提交，修复后必须对新 head 全部重跑，不能
沿用旧 head 的通过状态。

修复后第一次 Chrome 负向场景回放暴露测试自身的按钮时序竞争：页面先写入“已阻断”，
`finally` 随后才重新启用刷新按钮，测试在两者之间点击会被浏览器忽略。browser smoke 已改为
等待 `refreshControlCenter()` Promise 完成并直接核对标题/诊断；产品 fail-closed 逻辑未放宽，
随后双门户完整回归通过。

新 head 推送后，hosted `cargo-audit` 与 OSV 因 2026-08-04 新增的
`RUSTSEC-2026-0235` 阻断。`rkyv 0.7.46` 仅由 `rust_decimal 1.42.x` 作为未启用的可选依赖
写入 `Cargo.lock`；`cargo tree --all-features --target all -i rkyv@0.7.46` 没有活动路径，且
最新 `rust_decimal 1.42.1` 仍未提供 `rkyv >= 0.8.17` 迁移路径。因此只在
`.cargo/audit.toml` 与 `osv-scanner.toml` 登记精确例外，并保留 cargo-deny 不忽略该 advisory：
未来一旦 workspace 激活 rkyv 路径，`cargo deny --all-features` 将重新 fail closed。

本地复核：cargo-audit JSON 报告 `vulnerability_count=0`，OSV 2.3.8 报告
`No issues found`，cargo-deny advisories 通过。该例外应在 `rust_decimal` 支持
`rkyv >= 0.8.17` 或扫描器排除未激活可选依赖后删除。

corrective head 的 hosted 安全检查全部通过，随后 smoke 按预期捕获审计配置变更导致的风险
清单 canonical hash 漂移；matched files、signals 和 production ownership 计数均未变化。已只同步
`backend_runtime_risk_inventory.json` 的 canonical hash 为
`553083d2c5d5c04fd9f12f513b5381f3cfeb334ea54f64af010fd11654890bed`。首次 smoke 在浏览器步骤前
退出，因此没有 artifacts，不得作为浏览器证据；最终 head 必须完整重跑。

## 最终远端证据

- issue #1225：closed at `2026-08-04T11:06:41Z`；
- PR #1226：merged at `2026-08-04T11:06:40Z`；
- final head：`a41a9b722a289765047562f2100640a7642d60fa`；
- merge SHA：`97fdd88a82fae342b4a909e1b3f25e8d9feef20e`；
- 独立 Verification & Release Gatekeeper：exact final head `REVIEW_APPROVED`，无 P1/P2/P3；
- Rust Cutover Smoke：run `30902101122` / success；
- Backend Performance：run `30902101201` / 6/6 jobs success；
- security-audit：run `30902101202` / success；
- hosted checks：14/14 success；
- control center artifact id `8889719905`，`result.json` pass，sha256
  `2d3fbc07e4c7df4101af527867e2f10f8fcca8981b3467e95ddece4f3b5133a5`；
- institution workbench artifact id `8889719146`，`result.json` pass，sha256
  `9df96384eb5f164edfd1effff5c57e95bdcdf6aeb315e2a5bf76c73e036bb096`；
- hosted artifact 令牌扫描：未发现真实 `access_token` 值，仅存在 `[REDACTED]` 标记；
- 桌面与 390x844 截图核验：无重叠，机构工作台无 lifecycle action 控件。

Risk 为 high，已按规则在精确 final head 独立审查、全部 hosted checks 和双门户 browser
artifacts 成功后手动合并，未启用 auto-merge。MVP-010 已完成；M3 由 MVP-010A 独立收口。
