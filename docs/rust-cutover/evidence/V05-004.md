# V05-004 Evidence - workflow pipeline wiring

Date: 2026-06-13
Executor: Codex
Task ID: V05-004

## Plain Chinese Summary

本任务把 v0.4 的 Binance sandbox 本地证据串成一个 workflow。

大白话说：它复用了已经存在的 Binance bar fixture、EMA/RSI strategy smoke、
mock order lifecycle 和 risk rejection summary。没有新增交易语义，也没有启动真实
Binance 连接。

## Files Changed

- `crates/cli/src/workflow.rs`
- `crates/cli/Cargo.toml`
- `docs/rust-cutover/tasks/V05-004.md`
- `docs/rust-cutover/evidence/V05-004.md`

## Commands Run

```bash
cargo run -p nautilus-cli --bin nautilus -- workflow run --workflow binance-sandbox --run-id v05-smoke --output /tmp/ntpro-v05-workflow-smoke
jq -r '.summary.market_fixture_id, .summary.order_lifecycle_id, .summary.risk_smoke_id, .summary.external_venue_connection, .summary.production_trading' /tmp/ntpro-v05-workflow-smoke/manifest.json
```

## Validation Summary

- CLI smoke completed with `runtime_status=completed`.
- Artifact output keeps `external_venue_connection=false`.
- Artifact output keeps `production_trading=false`.
- Artifact output keeps `real_orders_submitted=false`.

## Behavior Impact

Adds a local workflow smoke path that stitches existing v0.4 evidence.

## Public API Impact

Adds CLI-accessible local workflow artifact generation.

## Migration Note Status

No migration note required.

## Rollback Plan

Revert the workflow module, CLI routing, and this evidence.
