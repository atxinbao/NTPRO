# P1-005 Evidence - Plugin Unsafe Alpha Boundary

Date: 2026-06-11
Executor: Codex
Task: P1-005

## Plain Chinese Summary

这次没有改插件加载代码，也没有启用新的插件能力。改动只是把插件系统的边界写清楚：v0.2 里的 plug-in/cdylib 只能算“可信本地 alpha 能力”，不是生产级、不是沙箱、不是可以加载不可信第三方库的产品能力。后续要做 allowlist、sha256 强制校验、路径规范、ABI/build-id 策略、panic/crash 隔离等工作，已经放进登记表。

## Goal

Keep the plug-in system documented as a trusted local alpha boundary and outside v0.2 production product claims.

## Files Changed

- `crates/plugin/README.md`
  - Added a trusted local alpha boundary section.
  - Clarified that plug-in cdylibs execute in the host process and are not an untrusted-code safe product surface.
- `docs/rust-cutover/release/v0_2_0_known_limits.md`
  - Added the v0.2 plug-in cdylib known limit.
- `docs/rust-cutover/security/plugin_unsafe_register.md`
  - Added follow-up items for allowlist, canonical paths, symlink handling, mandatory sha256, ABI/build-id policy, panic boundary, crash isolation, and lifecycle cancellation.
- `docs/rust-cutover/evidence/P1-005.md`
  - Records this evidence.

## Commands Run

```bash
rg -n "plugin|cdylib|trusted|alpha" crates/plugin/README.md docs/rust-cutover/release/v0_2_0_known_limits.md docs/rust-cutover/security/plugin_unsafe_register.md
```

Result: passed. The command found the expected trusted local alpha plug-in boundary language in all target documents.

```bash
scripts/ai/verify_fast.sh
```

Result: passed. The fast smoke checked toolchain and `cargo fmt --check`; workspace cargo check and clippy remain intentionally skipped by default in this script.

## Behavior Impact

No runtime behavior changed. The change only clarifies release and plug-in product boundaries.

## Public API Impact

No public API change.

## Migration Note Status

No migration note required. The plug-in system remains alpha and trusted-local only for v0.2 product claims.

## Rollback Plan

Revert the documentation updates and remove `docs/rust-cutover/security/plugin_unsafe_register.md` plus this evidence file.
