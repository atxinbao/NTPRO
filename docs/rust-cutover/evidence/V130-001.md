# V130-001 Evidence - Guarded Live Alpha Preflight Scope Decision

Date: 2026-06-21
Executor: Codex
Task: V130-001 Guarded Live Alpha Preflight scope decision
Risk level: medium

## Goal

Define `v0.13.0` as Guarded Live Alpha Preflight only, without enabling
production order mutation.

Plain Chinese summary: 这次只定 v0.13.0 的边界。v0.13.0 是 live alpha 前置检查，
不是生产下单版本；不下单、不撤单、不改单、不重试、不纠错、不碰真实资金。

## Files Changed

- `docs/rust-cutover/tasks/V130-001.md`
  - Records the Shrimp task contract, scope, non-goals, acceptance, and
    auto-merge policy.
- `docs/rust-cutover/release/v0_13_0_scope_decision.md`
  - Defines the v0.13.0 preflight-only product claim, task ladder, default
    no-mutation posture, and evidence required before any later mutation
    release.
- `ROADMAP.md`
  - Aligns the v0.13 line with Guarded Live Alpha Preflight only.
- `docs/versioning.md`
  - Records the v0.13.0 preflight boundary in the versioning guide.
- `docs/rust-cutover/release/README.md`
  - Indexes the v0.13.0 scope decision for release documentation discovery.

## Behavior Impact

Documentation and release-boundary accounting only.

No runtime code, production network request, credential loading, signing,
production order submission, production cancel/replace/amend/retry/correction,
listenKey lifecycle, real-funds behavior, production trading, or Dashboard
order control was added.

## Public API Impact

No public API or CLI syntax change.

## Migration Note

No migration note is required. This is a scope decision.

## Validation

PASS:

```bash
rg -n "Guarded Live Alpha Preflight|preflight_only|production_order_mutation = forbidden|kill-switch|Decimal/string|Dashboard order controls" docs/rust-cutover/tasks/V130-001.md docs/rust-cutover/release/v0_13_0_scope_decision.md ROADMAP.md docs/versioning.md docs/rust-cutover/release/README.md docs/rust-cutover/evidence/V130-001.md
```

Result: the task, scope decision, roadmap, versioning guide, release index, and
evidence record the v0.13.0 preflight-only boundary, no-mutation default
posture, kill-switch dry-run evidence, Decimal/string amount boundary, and
Dashboard order-control non-goal.

PASS:

```bash
if rg -n "production order submission = included|production order mutation = included|real funds = included|production trading = included|Dashboard order controls = included" docs/rust-cutover/tasks/V130-001.md docs/rust-cutover/release/v0_13_0_scope_decision.md ROADMAP.md docs/versioning.md docs/rust-cutover/release/README.md; then exit 1; else echo "negative v0.13 scope check PASS"; fi
```

Result: no v0.13.0 scope document claims production order submission,
production order mutation, real funds, production trading, or Dashboard order
controls as included.

PASS:

```bash
scripts/ai/verify_release.sh release-surface-current-guard
```

Result: current v0.12.1 release surface guard remains PASS after the v0.13.0
scope wording update.

PASS:

```bash
scripts/ai/verify_fast.sh
```

Result: toolchain and rustfmt fast smoke passed.

PASS:

```bash
git diff --check
```

Result: no whitespace errors.

## Rollback Plan

Revert the V130-001 PR to remove the v0.13.0 scope decision, task contract,
evidence document, and roadmap/versioning wording. No runtime state, exchange
state, credentials, order artifacts, tag, or GitHub Release requires rollback.
