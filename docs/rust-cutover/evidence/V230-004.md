# V230-004 Evidence - multi-venue node lifecycle boundary

Date: 2026-07-03
Executor: Codex
Task: `V230-004` / GitHub issue `#715`
Milestone: `v0.23.0`
Status: LOCAL VALIDATION PASSED

## Summary

V230-004 adds executable Rust replay coverage for venue node registry and
lifecycle identity partitioning. It proves isolated venue node partitions,
cross-node component mismatch fail-closed behavior, and unknown venue node
identity fail-closed behavior.

Plain Chinese summary: 本任务把 #715 的多 venue node registry/lifecycle 边界落到 Rust
可执行 replay。新增 fixture 和测试要求 `venue_node_key`、`account_key`、
`strategy_key`、`isolation_scope_key`、`adapter_instance_id` 明确存在；跨 node 串线和
unknown venue node identity 都会 fail closed。本任务不新增凭证处理扩展、不新增实盘
submit、不新增 order mutation、不新增 Dashboard 操作控件。

## Files Changed

```text
crates/cli/tests/golden_trace_read_model_projection.rs
tests/golden/read_model_venue_node_lifecycle_schema.jsonl
docs/rust-cutover/golden_trace/RELEASE_REPLAY_SCOPE.json
docs/rust-cutover/tasks/V230-004.md
docs/rust-cutover/release/v0_23_0_multi_venue_node_lifecycle_boundary.md
docs/rust-cutover/evidence/V230-004.md
docs/rust-cutover/release/README.md
verification.md
```

## Contract Traceability

```text
V230-001 Identity Model = satisfied by venue_node_key, account_key, strategy_key, and isolation_scope_key replay assertions
V230-001 Venue Node Boundary = satisfied by cross_node_component_mismatch fail-closed fixture
V230-001 Adapter Boundary = satisfied by adapter_instance_id and source_provenance registry assertions
V230-001 Logs And Evidence Boundary = satisfied by release replay scope entries and source_provenance assertions
```

## Node Registry Contract

```text
venue_node_key = required
venue = required label but not sufficient as identity
environment = required label but not sufficient as identity
adapter_instance_id = required
source_provenance = required
venue_only_fallback = forbidden
credential_alias_fallback = forbidden
process_id_fallback = forbidden
```

## Boundary

```text
runtime_behavior_change = read_model_projection_test_harness_only
adapter_behavior_change = false
dashboard_behavior_change = false
release_publication = false
credential_handling_expansion_allowed = false
production_order_mutation_allowed = false
production_order_submission_allowed = false
dashboard_operation_controls_enabled = false
cross_venue_implicit_operation_allowed = false
lifecycle_control_requires_owner_approval = true
```

## Validation

```text
cargo test -p nautilus-cli --test golden_trace_read_model_projection -- --nocapture = PASS, 4 tests passed
python3 -c 'import json,pathlib; [json.loads(line) for line in pathlib.Path("tests/golden/read_model_venue_node_lifecycle_schema.jsonl").read_text().splitlines() if line.strip()]' = PASS
jq . docs/rust-cutover/golden_trace/RELEASE_REPLAY_SCOPE.json = PASS
python3 scripts/ai/validate_golden_trace_release_scope.py = PASS, 92 cases, 87 executable replay, 5 schema-only scoped
rg -n "read_model.venue_node_lifecycle|venue_node_key|adapter_instance_id|cross_node_component_mismatch|missing_venue_node_key|rust_cli_read_model_projection_replays_v230_venue_node_lifecycle_paths" tests/golden/read_model_venue_node_lifecycle_schema.jsonl crates/cli/tests/golden_trace_read_model_projection.rs docs/rust-cutover/golden_trace/RELEASE_REPLAY_SCOPE.json docs/rust-cutover/evidence/V230-004.md = PASS
scripts/ai/verify_fast.sh = PASS, fast smoke only
git diff --check = PASS
```

## Behavior Impact

Adds executable read-model replay coverage for multi-venue node registry and
lifecycle partitioning. It does not add credential handling expansion,
production submit, order mutation, cross-node orchestration, or Dashboard
operation controls.

## Public API Impact

None.

## Rollback Plan

Revert this PR to remove V230 venue node lifecycle replay fixtures, harness
logic, registry contract note, and evidence. The preceding V230 account and
strategy isolation evidence remains intact.
