# V150-001 Evidence - Production mutation endpoint classifier gate

Date: 2026-06-22
Executor: Codex

## Summary

V150-001 extends the central endpoint classifier with v0.15 production mutation
dry-run request-preview classifications while preserving default deny behavior.

Plain Chinese summary: 已把 `POST /api/v3/order` 和 `POST /api/v3/order/test`
放进 v0.15 的“请求预览候选”分类。默认仍然拒绝；只有显式 owner/manual signed preview
上下文才允许 `request_preview_allowed=true`。这仍然不是实盘下单能力，不发请求、不撤改、
不调用 production adapter，也没有 Dashboard 订单控制。

## Files

```text
crates/cli/src/endpoint_classifier.rs
docs/rust-cutover/tasks/V150-001.md
docs/rust-cutover/evidence/V150-001.md
```

## Boundary Markers

```text
production_mutation_scope_candidate = default denied
production_mutation_owner_approved_manual_only = request preview only
request_preview_allowed = true only in signed owner/manual context
mutation_allowed = false
read_allowed = false
dashboard_order_controls_allowed = false
production HTTP request execution = not implemented
production order submission = not implemented
production cancel/replace/amend/retry/correction = not implemented
listenKey lifecycle = not implemented
```

## Validation

Local validation completed:

```bash
cargo fmt --all --check = PASS
cargo test -p nautilus-cli endpoint_classifier --lib = PASS
cargo check -p nautilus-cli = PASS
cargo clippy -p nautilus-cli --all-targets -- -D warnings = PASS
scripts/ai/verify_fast.sh = PASS
git diff --check = PASS
```

## Status

Ready for PR review. Hosted smoke is required before merge.
