# V120-003 Evidence - Production Read-Only Response Shape

Date: 2026-06-20
Executor: Codex
Task: V120-003 Validate production read-only response shape

## Summary

V120-003 adds redacted response-shape evidence for the production authenticated
account snapshot path. The report now includes `response_shape_summary`, which
records only field presence, type checks, entry counts, validation status, and
redaction flags.

## Non-Mutating Boundary

- No new production endpoint.
- No production order submission.
- No production cancel, replace, amend, retry, correction, or listenKey
  lifecycle mutation.
- No order endpoint access.
- No Dashboard order controls.
- No raw account response, balances, asset symbols, permission values, API key,
  secret, signature, signed query, or signed URL persistence.

## Commands Run

```bash
cargo fmt -p nautilus-cli
cargo check -p nautilus-cli --lib
cargo test -p nautilus-cli production_account_snapshot_shape --lib
cargo test -p nautilus-cli production_account_snapshot --lib
scripts/ai/verify_v12_response_shape.sh
scripts/ai/verify_v12_authenticated_account_snapshot_online_read.sh
git diff --check
```

## Results

- `cargo check -p nautilus-cli --lib`: passed.
- `cargo test -p nautilus-cli production_account_snapshot_shape --lib`: passed.
- `cargo test -p nautilus-cli production_account_snapshot --lib`: passed.
- `scripts/ai/verify_v12_response_shape.sh`: passed.
- `scripts/ai/verify_v12_authenticated_account_snapshot_online_read.sh`: passed
  default preflight; `network_attempted=false`.
- `git diff --check`: passed.
