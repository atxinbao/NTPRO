# UPV1-002 - 真实本地历史数据目录证据

Date: 2026-08-26
Executor: Codex
GitHub issue: #1323
Status: REVIEW_REQUIRED

## 实现结论

- 产品标准目录固定为 `<workspace>/catalog`，继续使用仓库现有 Rust
  `ParquetDataCatalog`，没有建立第二套数据引擎；
- Product API 新增策略版本兼容数据集读取合同，只返回品种、Venue、完整时间范围、记录数、
  文件统计和内容 SHA-256，不暴露宿主机绝对路径；
- 创建 Backtest 时，服务端重新解析数据引用和 SHA-256，生成绑定绝对目录、指纹、范围与
  记录数的不可变 `request.toml`；
- Backtest 由真实 `BacktestNode + BacktestDataConfig + BacktestEngine` 读取 QuoteTick，
  执行前后再次扫描相同数据，运行期间发生漂移即失败；
- 页面默认选择兼容的已验证本地数据，并把 `data_ref` 与 `data_sha256` 一起提交；内置
  `synthetic-quotes` 仍作为明确标识的确定性流程来源；
- 服务重启后，Run 继续从不可变工件读取相同数据引用与来源指纹。

## Fail-closed 证据

- catalog 缺失返回 `product_source_unavailable`；
- Parquet 损坏、目录类型异常或文件逃逸返回 `product_source_invalid`；
- 数据指纹不匹配不能进入 BacktestEngine；
- 策略版本不是 QuoteTick、品种/Venue 不兼容或数据引用不唯一时不列出可用数据；
- 引擎执行前校验记录数、SHA-256 和完整起止时间，执行后再次校验同一快照；
- Product API 和页面继续保持外部 Venue、真实订单、订单 mutation、自动重试和自动补救为
  `false`。

## 本地验证

- `cargo test -p nautilus-cli --lib local_quote_catalog`：通过，2 项；
- Rust OpenAPI exact route 测试：通过，1 项；
- Rust 路由到前端固定夹具一致性测试：通过，1 项；
- 前端 Product API 与 AppShell 定向测试：通过，2 个文件、188 项；
- 前端 `format`、`typecheck`、`lint`、`build`：通过；lint 仅保留生成代码中的既有
  `no-useless-escape` 非阻断提示，Vite 仅保留既有 bundle size 提示；
- Playwright Backtest 创建用例：通过，确认页面显示“已验证本地历史数据”，且创建请求
  同时冻结本地 `data_ref` 和 `data_sha256`。
- 完整 `nautilus-cli` 库测试以单线程运行：通过，793/793；并行全量运行曾出现一项既有
  Live Runtime 清理测试的时序波动，该测试单独重跑通过，串行全量无失败；
- production bundle 浏览器验收：通过，证据目录
  `/tmp/ntpro-upv1-002-browser-evidence-r3`；同时确认空 catalog 继续显示并可使用内置
  确定性来源；
- `cargo clippy -p nautilus-cli --lib -- -D warnings`：通过；
- MVP freeze baseline：通过，7 项负向自测、19 项任务、19 项边界和 13 个冻结来源；
- Rust-only runtime 与 Rust 1.95.0 fast smoke：通过。
- backend runtime risk inventory 仅增加 20 个 test-owned `expect`；production、unsafe、
  unwrap 和 panic 计数未增加，机器清单已同步。
- `verify_release.sh current-governance backend-freeze-baseline`：通过，包含当前发布面、文档、
  Rust-only、hygiene、风险清单、控制面退役、S3 边界和 v0.32.0 冻结基线。

独立审查和 hosted checks 结论将在 PR 阶段继续补充。

## 行为与迁移

- 行为影响：Backtest 新增可选择的本地真实历史数据来源；已有内置确定性来源继续有效；
- 公共 API：新增兼容数据集 GET 路由，并为 Backtest 创建请求增加可选 `data_sha256`；
- 迁移说明：已有工作区由 `mvp prepare/serve` 创建空 `catalog/`；需要真实历史数据时，将
  标准 Rust Parquet catalog 放入该目录，禁止直接放 CSV 或手工编辑 Parquet；
- 回滚：回退本 PR 即恢复 synthetic-only 产品面；已创建的本地数据 Run 工件必须保留用于
  审计，不能由旧代码伪装成可复现来源。

本任务按冻结后独立 Issue 政策更新 `mvp_freeze_manifest.json` 中三项来源哈希：workspace
初始化、Axum 路由和节点策略版本配置。M0-M4 拓扑、任务集合、发布标签、产品门户和 19 项
required-false 边界均未改变。

## 审查状态

本任务为 high risk，当前为 `REVIEW_REQUIRED`。必须取得独立审查批准并通过 hosted
checks 后手动合并，不得 auto-merge。
