# HFG-001 Evidence

Date: 2026-07-28
Executor: Codex
GitHub issue: `#1161`
Branch: `codex/hfg-001-hidden-root-governance`
Status: `LOCAL_VALIDATION_PASSED`

## 中文摘要

根目录共有 23 个隐藏文件或目录。本任务逐项核对 tracked/ignored 状态、磁盘占用、
仓库引用、Makefile、workflow、hook、已安装工具和治理保护规则。

七个配置只有 CODEOWNERS/旧文档引用或完全没有消费者，对应工具均未安装：

| Removed path | Removal proof |
| --- | --- |
| `.codecov.yml` | 无 Codecov workflow/upload，配置仍指向已退役 `master` |
| `.codespellrc` | 无 codespell 工具、hook、workflow 或脚本 |
| `.dockerignore` | 仓库无 Dockerfile，compose 只有 image、没有 build context |
| `.gitlint` | 仅由已退出纯 Rust 工具链的 Python/uv 可选文档引用 |
| `.markdownlint.jsonc` | 无 markdownlint 工具、hook、workflow 或脚本 |
| `.yamlfmt` | 无 yamlfmt 工具、hook、workflow 或脚本 |
| `.yamllint.yaml` | 无 yamllint 工具、hook、workflow 或脚本 |

## Preserved authority

- `.git`：Git 历史和对象数据库，无 garbage；不是普通缓存目录。
- `.cargo`、`.config`：Cargo audit/config 和 nextest 配置。
- `.docker`：Makefile 本地服务入口和 Postgres integration test 依赖。
- `.github`：issue、PR、workflow、action 和 ownership 控制面。
- `.pre-commit-hooks`：`prek` 当前 Rust-only 开发检查。
- `.supply-chain`、`.zizmor.yml`：hosted security audit authority。
- `.lycheeignore`：Makefile external-link audit 消费。
- `.env.example`：Rust data/catalog 环境变量模板。
- `.agentflow`、`.codex`、`.understand-anything`：backend hygiene 明确保护的
  ignored local state，本任务不删除。

## Behavior impact

- 删除未执行的质量/报告配置，避免向贡献者声明不存在的 gate。
- Rust coverage 文档改为真实的本地 `cargo llvm-cov` 路径。
- backend hygiene 对九个累计退役配置 fail closed。
- 不改变产品运行时、API、交易语义或发布能力。
- 不修改 v0.32.0 冻结发布文件。

## Validation

| Command | Result |
| --- | --- |
| `cargo test -p ntpro-governance backend_hygiene -- --nocapture` | 4 passed |
| `cargo test -p ntpro-governance` | 35 passed |
| `cargo clippy --locked -p ntpro-governance --all-targets -- -D warnings` | passed |
| `scripts/ai/check_docs_examples_governance.sh` | passed |
| `scripts/ai/check_rust_only_runtime.sh` | passed |
| `scripts/ai/verify_release.sh current-governance` | passed |
| `scripts/ci/security-audit-gate.sh` | `audit_needed=false` |
| active retired-config reference audit | zero |
| retired root configuration existence audit | absent, 9/9 |
| v0.32.0 frozen release diff | zero |

清理后根目录保留 16 个隐藏项。`.agentflow`、`.codex` 和
`.understand-anything` 均保留；`target/` 和其他生成缓存会在提交前清理，
不进入 Git。

## Rollback

回退本任务提交即可恢复旧配置；不需要数据、schema、部署或 release 回滚。
