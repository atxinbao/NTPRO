# V140-005 Evidence

Date: 2026-06-22
Executor: Codex

## Summary

V140-005 adds executable golden traces for live-alpha dry-run reconciliation.
The traces cover local order-state and risk-preflight scenarios and replay
through the Rust `nautilus-cli` binary.

Plain Chinese summary: 这次补的是“证据”，不是开放实盘交易。现在可以用 golden
trace 回放 7 个本地场景：无 open orders、有 open order、order-state 过期、account
读取失败、order-state 读取失败、kill switch 打开、风险限额超限。每个场景都证明：
没有真实订单、没有生产下单/改单、没有 execution adapter 调用、没有网络访问。

## Changed

- Added `tests/golden/live_alpha_reconciliation_schema.jsonl`.
- Added `crates/cli/tests/golden_trace_live_alpha_reconciliation.rs`.
- Added optional order-state freshness fields to the live-alpha risk preflight
  input and report.
- Added `order_state_stale` rejection evidence.
- Wired the new harness into `scripts/ai/run_golden_traces.sh`.
- Updated `docs/rust-cutover/golden_trace/RELEASE_REPLAY_SCOPE.json`.
- Updated `docs/rust-cutover/golden_trace/SCHEMA.md` and `scripts/ai/verify_full.sh`.

## Not Changed

- No production order submission.
- No production order mutation, cancel, replace, amend, retry, or correction.
- No execution adapter call.
- No production network access.
- No listenKey lifecycle.
- No Dashboard order controls.
- No real funds or real orders.

## Validation

Planned / executed commands for this task:

```text
python3 scripts/ai/golden_trace_runner.py tests/golden/live_alpha_reconciliation_schema.jsonl --mode validate-only
python3 scripts/ai/validate_golden_trace_release_scope.py
cargo test -p nautilus-cli --test golden_trace_live_alpha_reconciliation
cargo test -p nautilus-cli production_live_alpha_risk_preflight --lib
scripts/ai/verify_v14_live_alpha_risk_preflight.sh
scripts/ai/verify_full.sh golden-traces-files
scripts/ai/verify_full.sh golden-traces-live-alpha-reconciliation
cargo check -p nautilus-cli
cargo clippy -p nautilus-cli --lib --tests -- -D warnings
scripts/ai/verify_fast.sh
cargo fmt --all --check
git diff --check
```

## Required No-Mutation Evidence

Every new expected trace event records:

```text
production_order_submission_allowed=false
production_order_mutation_allowed=false
production_order_state_reads_allowed=false
production_order_submissions_attempted=0
production_orders_submitted=0
production_order_mutations_attempted=0
production_order_state_reads_attempted=0
execution_adapter_called=false
order_endpoint_access_attempted=false
matching_engine_submission=false
actual_submission_count=0
automatic_correction_orders_submitted=0
dashboard_order_controls_enabled=false
network_attempted=false
real_orders_submitted=false
real_funds=false
production_trading_enabled=false
values_are_exchange_truth=false
```
