# P1-006 Evidence - Ignored Tests Follow-up Register

Date: 2026-06-11
Executor: Codex
Task: P1-006

## Plain Chinese Summary

这次没有打开或修改任何测试，也没有改运行时代码。改动只是把当前仓库里被 `#[ignore]` 跳过的测试登记下来：每一项都写明原因、影响、owner、目标阶段和关闭条件。这样以后不会再出现“测试被跳过但没人跟”的情况。

## Goal

Turn ignored production, integration, adapter, and slow-test coverage into tracked follow-up work.

## Files Changed

- `docs/rust-cutover/quality/ignored_tests_register.md`
  - Adds a register for 28 actual ignored test attributes under `crates` and `tests`.
  - Records owner, impact, target milestone, close condition, and state.
- `docs/rust-cutover/evidence/P1-006.md`
  - Records this evidence.

## Commands Run

```bash
rg -n "#\[ignore" crates tests
```

Result: passed. The command found the existing ignored-test lines, including one explanatory comment in `crates/live/tests/stress.rs`; the register tracks the 28 actual `#[ignore]` attributes.

```bash
scripts/ai/verify_fast.sh
```

Result: passed. The fast smoke checked toolchain and `cargo fmt --check`; workspace cargo check and clippy remain intentionally skipped by default in this script.

## Behavior Impact

No runtime behavior changed. No tests were unignored, removed, or rewritten.

## Public API Impact

No public API change.

## Migration Note Status

No migration note required.

## Rollback Plan

Remove `docs/rust-cutover/quality/ignored_tests_register.md` and this evidence file.
