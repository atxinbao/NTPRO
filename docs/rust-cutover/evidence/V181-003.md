# V181-003 Evidence - v0.18 Release Guard Defaults

Date: 2026-06-27
Executor: Codex
Milestone: v0.18.1
GitHub issue: `#572`
Status: PASS - local validation complete before PR

## Summary

V181-003 completes the guard migration so the default release surface and
publication guards validate the real current release, `ntpro-rust-only-v0.18.0`.
The release surface guard was moved to v0.18.0 during V181-002 because the
required hosted smoke gate depends on it. This task finishes the publication
guard by changing its default release version to v0.18.0 and adding v0.18.0 to
the supported release-body matrix.

Plain Chinese summary: 这次只修发版 guard。大白话：默认检查现在会看 v0.18.0
的 README/ROADMAP/release notes 和真实 GitHub Release，不再默认按 v0.17.0 判断；
不创建 v0.18.1 release，也不改变运行时撤单行为。

## Files Changed

```text
scripts/ai/check_github_release_published.sh
docs/rust-cutover/evidence/V181-003.md
verification.md
```

## Guard Defaults

```text
release surface current guard default current_release_version = v0.18.0
release publication guard default current_release_version = v0.18.0
release publication guard default current_release_tag = ntpro-rust-only-v0.18.0
release publication guard default release name = NTPRO Rust-only v0.18.0
release publication guard supported matrix includes v0.18.0
```

## Validation

```text
scripts/ai/verify_release.sh release-surface-current-guard release-publication-guard = PASS
NTPRO_CURRENT_RELEASE_VERSION=v0.18.0 NTPRO_NEXT_PATCH_VERSION=v0.18.1 NTPRO_NEXT_CAPABILITY_VERSION=v0.19.0 NTPRO_CURRENT_RELEASE_CAPABILITY='Owner-Approved Cancel Recovery Preview' scripts/ai/check_release_surface_current.sh = PASS
NTPRO_CURRENT_RELEASE_VERSION=v0.18.0 NTPRO_CURRENT_RELEASE_NAME='NTPRO Rust-only v0.18.0' scripts/ai/check_github_release_published.sh = PASS
bash -n scripts/ai/check_github_release_published.sh scripts/ai/check_release_surface_current.sh scripts/ai/verify_release.sh = PASS
scripts/ai/verify_fast.sh = PASS
stale v0.17 default / unsupported v0.18 publication guard scan = no matches
git diff --check = PASS
```

## Boundary

The guard migration preserves the v0.18 preview-only boundary:

```text
actual cancel send = not included
automatic cancel = disabled
automatic remediation = disabled
Dashboard order controls = disabled
Dashboard cancel controls = disabled
v0.18.1 release creation = not included
GitHub Actions runner configuration change = not included
```

## Review / Merge Status

```text
PR = pending
Hosted Rust Cutover Smoke = pending
Issue closeout = pending until PR merge closes #572
```

## Rollback Plan

Revert this PR to restore the previous publication guard defaults and remove
`docs/rust-cutover/evidence/V181-003.md`. This does not require exchange,
credential, order, cancel, artifact, or runtime cleanup because the change is
guard configuration and evidence only.
