# V130-004 Evidence - Kill switch dry-run and manual approval artifact contract

Date: 2026-06-21
Executor: Codex

## Summary

V130-004 adds a local v0.13 kill-switch dry-run/manual-approval artifact command
and validation script.

Plain Chinese summary: 已新增本地 artifact 能力：可以生成一份 JSON，说明 kill
switch 只是 dry-run，人工批准记录只是证据，不代表实盘交易已开放。artifact 和脚本
都明确写出不联网、不下单、不撤单、不改单、不启用 Dashboard 下单控件。

## Files

```text
crates/cli/src/opt.rs
crates/cli/src/live.rs
scripts/ai/verify_v13_kill_switch_approval_artifact.sh
docs/rust-cutover/tasks/V130-004.md
docs/rust-cutover/release/v0_13_0_kill_switch_approval_artifact.md
docs/rust-cutover/evidence/V130-004.md
docs/rust-cutover/release/README.md
```

## Validation

Completed locally:

```bash
cargo test -p nautilus-cli production_kill_switch_approval_artifact --lib = PASS
scripts/ai/verify_v13_kill_switch_approval_artifact.sh = PASS
scripts/ai/verify_fast.sh = PASS
bash -n scripts/ai/verify_v13_kill_switch_approval_artifact.sh = PASS
cargo fmt --check = PASS
cargo check -p nautilus-cli = PASS
cargo clippy -p nautilus-cli --lib --tests -- -D warnings = PASS
target/debug/nautilus live production-kill-switch-approval-artifact --help = PASS
git diff --check = PASS
```

## Boundary

```text
kill_switch_dry_run=true
manual_approval_required=true
approval_artifact_only=true
production_order_submission_allowed=false
production_order_mutation_allowed=false
production_order_state_reads_allowed=false
listen_key_lifecycle_allowed=false
dashboard_order_controls_enabled=false
network_attempted=false
real_orders_submitted=false
production_trading_enabled=false
```

## Status

Ready for PR review. Hosted smoke is required before merge.
