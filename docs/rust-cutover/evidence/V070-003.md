# V070-003 Evidence - HTTP Read-Only Binance Testnet Probe Artifacts

Date: 2026-06-14
Executor: Codex
Task ID: V070-003
Shrimp task ID: `ed3f9695-a673-4624-9690-a4ca46a2567f`
Branch: `codex/V070-003-add-real-http-read-only-binance-testnet-connectivity-probe-artifacts`
Risk: medium

## Plain Chinese Summary

这次真的加了一个 Binance testnet 的 HTTP 只读连通性 probe，但它不是实盘、
不是下单、也不是生产 Binance 连接。

大白话说：默认还是完全离线。只有同时传
`--allow-testnet-network` 并设置 `NTPRO_ALLOW_TESTNET_NETWORK=1`，而且配置仍然是
testnet + order submission disabled，才会访问 Binance testnet 的 public
`/api/v3/time`。这个 endpoint 不需要 API key/secret。artifact 会记录是否尝试联网、
HTTP 状态、耗时、错误码和是否连通，但不会记录任何密钥值，也不会提交订单。

## Goal

Implement a real HTTP read-only Binance testnet connectivity probe artifact for
the explicitly opted-in `connectivity-probe` path.

## Files Changed

- `crates/cli/src/workflow.rs`
  - Runs the HTTP read-only probe only when `--allow-testnet-network`,
    `NTPRO_ALLOW_TESTNET_NETWORK=1`, read-only testnet config, and
    `--mode connectivity-probe` all pass.
  - Uses a public Binance testnet `/api/v3/time` GET with a timeout.
  - Records success or classified failure in `testnet/connectivity_probe.json`.
  - Runs blocking HTTP work on an OS thread so the Tokio CLI runtime is not
    dropped from an async context.
- `crates/cli/src/workflow_contract.rs`
  - Adds additive connectivity artifact fields:
    `endpoint_class`, `latency_ms`, `http_status`, and `error_code`.
- `crates/cli/src/opt.rs`
  - Updates help text from future networking to read-only networking.
- `crates/cli/Cargo.toml` and `Cargo.lock`
  - Adds direct `reqwest` usage for the CLI HTTP probe.
- `README.md`
  - Documents the V070 HTTP public read-only probe boundary.
- `examples/rust/binance/testnet_dry_run.toml`
  - Documents fail-closed HTTP probe opt-in requirements.
- `docs/rust-cutover/tasks/V070-003.md`
  - Adds the task record.
- `docs/rust-cutover/evidence/V070-003.md`
  - Adds this evidence record.

## Validation

| Command | Result | Summary |
| --- | --- | --- |
| `cargo fmt --check` | PASS | Rust formatting passed after applying rustfmt. |
| `cargo check -p nautilus-cli` | PASS | CLI crate compiles with direct `reqwest` dependency. |
| `cargo test -p nautilus-cli workflow::tests::binance_testnet --lib` | PASS | 16 Binance testnet workflow tests passed, including HTTP success/failure artifact fixtures. |
| `scripts/ai/verify_v06_binance_testnet_dry_run.sh` | PASS | Existing dry-run smoke still keeps `network_attempted=false`. |
| `scripts/ai/verify_fast.sh` | PASS | Toolchain and rustfmt fast smoke passed. |
| `cargo clippy -p nautilus-cli --all-targets -- -D warnings` | PASS | Targeted CLI clippy passed with warnings denied. |
| `NTPRO_ALLOW_TESTNET_NETWORK=1 cargo run -p nautilus-cli --bin nautilus -- workflow run --workflow binance-testnet --mode connectivity-probe --allow-testnet-network --config examples/rust/binance/testnet_dry_run.toml --run-id v070-http-probe --output target/ntpro-v070-http-probe` | PASS | Real public HTTP probe succeeded with `network_attempted=true`, `testnet_connection=true`, `http_status=200`, and `real_orders_submitted=false`. |
| secret scan | PASS | No inline credential assignment or credential value marker was found in changed docs/code/artifacts. |
| `git diff --check` | PASS | No whitespace errors. |

## Real HTTP Probe Artifact Summary

From `target/ntpro-v070-http-probe/testnet/connectivity_probe.json`:

```text
schema_version=ntpro.v07_binance_testnet_connectivity_probe.v1
requested_mode=connectivity-probe
network_permission_requested=true
env_network_permission=true
network_gate_status=allowed
network_attempted=true
testnet_connection=true
endpoint_class=binance-testnet-public-http-time
http_status=200
error_code=none
status=http_read_only_probe_ok
```

From `target/ntpro-v070-http-probe/testnet/credential_policy.json`:

```text
values_recorded=false
api_key_value_recorded=false
api_secret_value_recorded=false
secrets_redacted=true
```

From `target/ntpro-v070-http-probe/boundary.json`:

```text
real_orders_submitted=false
order_submission_mode=disabled
production_trading=false
real_funds=false
```

## Behavior Impact

Default behavior remains offline. `dry-run` mode does not open sockets even when
the network gate is allowed.

The only new network attempt is:

- workflow: `binance-testnet`;
- mode: `connectivity-probe`;
- CLI flag: `--allow-testnet-network`;
- env: `NTPRO_ALLOW_TESTNET_NETWORK=1`;
- config: Binance Spot testnet, `order_submission=disabled`,
  `real_orders_submitted=false`.

## Public API Impact

No Rust public crate API changes and no CLI command shape changes.

The local workflow artifact contract changes additively. Consumers of
`testnet/connectivity_probe.json` can read the new fields:

- `endpoint_class`;
- `latency_ms`;
- `http_status`;
- `error_code`.

## Migration Note

Dashboard and artifact consumers should treat missing V070-003 HTTP fields as
legacy/default values because the serde contract keeps these fields additive.

## Review / Merge Status

Medium-risk V070 batch task. The owner approved V070-003 through V070-007 for
automated execution and auto-merge on 2026-06-14.

## Rollback Plan

Revert this PR to remove the HTTP probe execution, `reqwest` CLI dependency,
additive connectivity artifact fields, tests, README/example updates, and this
evidence file. The workflow will return to V070-002 behavior where the allowed
network gate records intent but opens no socket.
