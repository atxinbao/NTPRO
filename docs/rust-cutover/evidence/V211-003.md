# V211-003 Evidence

Date: 2026-07-01
Executor: Codex
GitHub issue: `#679`
Task: `V211-003 Executable read-model projection replay`
Status: READY FOR REVIEW

## Summary

V211-003 adds an executable Rust read-model projection replay harness for the
critical v0.21.1 read_model golden traces. The promoted coverage is deliberately
narrow and leaves unimplemented cases as explicit remaining schema-only
follow-up.

Plain Chinese summary: 本任务把关键 read_model golden traces 从纯 schema 校验推进到
Rust 可执行 replay。已提升的 case 会由 Rust 从 input snapshot 派生 output event 并与
expected event 对比；未实现的 case 继续明确标注为 remaining schema-only，不伪装成
executable replay。

## Promoted Cases

```text
read_model.account_snapshot.fresh.001
read_model.account_snapshot.stale.001
read_model.order_lifecycle.matched.001
read_model.order_lifecycle.missing_ledger.001
read_model.risk_state.healthy.001
read_model.risk_state.mismatch.001
read_model.dashboard.readonly_complete.001
read_model.dashboard.missing_evidence_degraded.001
```

Replay harness:

```text
cargo test -p nautilus-cli --test golden_trace_read_model_projection
```

## Files Changed

```text
crates/cli/tests/golden_trace_read_model_projection.rs
docs/rust-cutover/golden_trace/RELEASE_REPLAY_SCOPE.json
docs/rust-cutover/release/v0_21_1_release_notes.md
docs/rust-cutover/evidence/V211-003.md
scripts/ai/verify_v21_1_read_model_projection_replay.sh
scripts/ai/verify_release.sh
verification.md
```

## Validation

```text
cargo test -p nautilus-cli --test golden_trace_read_model_projection = PASS
cargo clippy -p nautilus-cli --test golden_trace_read_model_projection --features defi -- -D warnings = PASS
scripts/ai/verify_v21_1_read_model_projection_replay.sh = PASS
scripts/ai/verify_release.sh v21.1-read-model-projection-replay = PASS
python3 scripts/ai/validate_golden_trace_release_scope.py --manifest docs/rust-cutover/golden_trace/RELEASE_REPLAY_SCOPE.json --trace-glob 'tests/golden/*.jsonl' = PASS
git diff --check = PASS
scripts/ai/verify_fast.sh = PASS
```

## Remaining Schema-Only

The release scope keeps 24 read_model cases as remaining schema-only follow-up
for V211-004 through V211-006. Those entries do not contain `evidence_id`,
`harness`, or `rust_entrypoint`, so they cannot be mistaken for executable
replay.

## Behavior Impact

This adds replay validation only. It does not add trading controls, order
mutation, automatic remediation, exchange connectivity, or a product-grade
trading terminal claim.

## Public API Impact

No public runtime API change.

## Rollback

Revert this PR to remove the Rust read-model projection replay test and restore
the read_model release scope entries to schema-only.
