# V061-001 Evidence - release wording and versioning alignment

Date: 2026-06-14
Executor: Codex
Task ID: V061-001

## Plain Chinese Summary

本任务只清理公开口径和文案，不改交易逻辑。

大白话说：现在 README、ROADMAP 和 Dashboard 文案都明确了当前正式发布点是
`ntpro-rust-only-v0.6.0`，当前工作队列是 `v0.6.1` hardening，下一阶段
`v0.7.0` 才允许进入真实 Binance testnet 只读连通性证明。`v0.6.1` 仍然是
offline-only，不连接 Binance，不下单，不读取真实凭证，不声明生产交易。

## Files Changed

- `README.md`
- `ROADMAP.md`
- `docs/versioning.md`
- `crates/cli/src/dashboard.rs`
- `docs/rust-cutover/design_readiness_gate.md`
- `docs/rust-cutover/evidence/V02-001.md`
- `docs/rust-cutover/tasks/V061-001.md`

Local-only state:

- `.agentflow/leases/V061-001.json` was written locally but is ignored by git
  and is not part of the PR.

## Commands Run

```bash
rg -n "<stale v0.2/v0.3 wording patterns>" ROADMAP.md README.md crates/cli/src/dashboard.rs docs
git diff --check
cargo test -p nautilus-cli dashboard --lib
scripts/ai/verify_fast.sh
rg -n ".agentflow/input|agentflow/input|input/issues|.agentflow/spec|spec/issues" .agentflow docs README.md ROADMAP.md scripts crates
```

## Validation Summary

Result: PASS.

- Stale wording scan: PASS, no matches.
- `git diff --check`: PASS.
- `cargo test -p nautilus-cli dashboard --lib`: PASS, 28 tests passed.
- `scripts/ai/verify_fast.sh`: PASS.
- `.agentflow/spec/issues/<issue-id>.json` is the expected issue-spec path.
- Legacy `.agentflow/input/issues/<issue-id>.json` scan: no existing
  `.agentflow/input` directory or `input/issues` references were found, so
  there was no file move required for this task.

## Behavior Impact

Documentation and user-visible Dashboard error/copy text only.

No workflow behavior changed. No artifact schema changed. No trading semantic
changed. No network behavior changed.

## Public API Impact

No public Rust API change.

The Dashboard HTTP API behavior is unchanged; only owner-visible text changed.

## Migration Note Status

No migration note required. This task does not change runtime behavior or a
public API contract.

## Rollback Plan

Revert this PR to restore the previous README, roadmap, Dashboard copy, and
versioning docs state. No runtime rollback is needed.
