# CRF-001 Cache Market-Data Persistence Fail-Closed Evidence

Date: 2026-07-29
Executor: Codex
GitHub issue: #1164
Status: LOCAL VALIDATION PASSED; HOSTED REVIEW PENDING

## Summary

The cache no longer reports success when a configured backing database cannot
persist a single mark price, index price, funding rate, or instrument status.

中文摘要：需要持久化且配置了数据库时，四条单条市场数据写入路径现在必须成功落盘，
否则在修改内存缓存前返回明确错误。未配置数据库的纯内存模式保持不变。

## Change Boundary

```text
database schema changed = false
public trait implementors broken = false
market-data values changed = false
trading semantics changed = false
frozen v0.32.0 release files changed = false
production capability inherited = false
```

## Validation

```text
targeted persistence regressions = PASS, 2 tests
nautilus-common non-logger unit suite = PASS, 940 tests
nautilus-common Clippy = PASS, warnings denied
backend runtime risk inventory = PASS, 28,983 signals / 1,215 files
current governance = PASS
backend freeze baseline = PASS, 27 boundaries
git diff check = PASS
```

Commands:

```bash
source scripts/ai/toolchain_env.sh
cargo test -p nautilus-common --lib test_single_
cargo test -p nautilus-common --lib -- \
  --skip logging::logger::tests::serial_tests \
  --skip logging::macros::tests::test_colored_logging_macros \
  --skip logging::macros::tests::test_default_macro_captures_module_path
cargo clippy -p nautilus-common --lib --tests -- -D warnings
scripts/ai/check_backend_runtime_risk_inventory.sh
scripts/ai/verify_release.sh current-governance backend-freeze-baseline
git diff --check
```

The complete `nautilus-common` process initially ran 953 tests. Its 940
non-logger tests passed; 13 existing logger tests failed because the same test
process had already initialized the process-global logger. The two CRF-001
regressions pass independently and in the 940-test run. No logger source is
changed by this task.

## Rollback

Revert this PR. No data migration or schema rollback is required.
