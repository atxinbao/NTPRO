# BPO-001 Backend Performance Baseline Evidence

Date: 2026-07-20
Executor: Codex
GitHub issue: #1120
Milestone: v0.33.0-backend-maintenance
Status: PRE-MERGE VALIDATION PASSED

## Summary

BPO-001 defines the pre-optimization measurement contract for the separately
scoped v0.33.0 backend maintenance track.

Plain Chinese summary: 当前变更只建立测量和防漂移规则。所有性能数据来自同一台本地
机器、同一工具链和同一外部 Cargo target；它们是后续同机比较的参考，不是 hosted
权威，也不改变任何运行能力。

## Change Boundary

```text
runtime or adapter behavior changed = false
public API or schema changed = false
trading semantics changed = false
docs/rust-cutover/release/v0_32_0_* changed = false
backend production capability inherited = false
hosted benchmark gate materialized = false
```

## Captured Environment

```text
hardware = Apple M3 Pro, 12 logical CPUs, 19,327,352,832 bytes memory
os = macOS 26.5.1, Darwin 25.5.0, aarch64
toolchain = rustc 1.95.0, cargo 1.95.0, LLVM 22.1.2
profile = release for CLI resources; bench-lto for Criterion
Criterion = 1 s warmup, 2 s measurement, 20 samples, 3 sessions
comparison authority = same_host_same_toolchain_back_to_back
```

macOS dynamic frequency and ASLR remained at platform defaults. The baseline
therefore records session CV and uses the greater of 10% or three times CV for
latency failure decisions. BPO-002 must establish the fixed hosted runner.

## Headline Baseline

| Surface | Median / value | Session CV |
| --- | ---: | ---: |
| core StackStr construction | 13.087 ns | 0.41% |
| model Price construction | 3.609 ns | 0.49% |
| data engine trade ingest | 93.091 ns | 0.75% |
| execution matching lookup | 8.687 ns | 5.63% |
| live runner dispatch, 100 events | 3,739.876 ns | 0.59% |
| network rate limiter | 41.797 ns | 0.71% |
| clean release CLI build | 370.00 s, 3,284,844,544 bytes peak RSS | n/a |
| incremental no-change build | 0.90 s, 216,481,792 bytes peak RSS | n/a |
| `nautilus` release binary | 20,637,152 bytes | n/a |
| `ntpro-node` release binary | 6,282,512 bytes | n/a |

The execution lookup capture is the noisiest workload. Its effective failure
threshold is `3 * 5.63% = 16.89%`, not the configured 10% floor.

## Commands

```bash
/usr/bin/time -lp env CARGO_TARGET_DIR=/tmp/ntpro-bpo001-measure-target \
  cargo build --locked -p nautilus-cli --release \
  --bin nautilus --bin ntpro-node --features defi

CARGO_TARGET_DIR=/tmp/ntpro-bpo001-measure-target \
  cargo bench --locked --profile bench-lto -p <package> --bench <bench> -- \
  '<filter>' --warm-up-time 1 --measurement-time 2 --sample-size 20
```

The exact six package, bench, and filter combinations are source controlled in
`backend_performance_baseline.json`.

## Validation

```text
backend performance baseline guard = PASS
workloads = 6
session observations = 18
build measurements = 2
binary measurements = 2
negative mutations = 8/8 PASS
ntpro-governance unit tests = 31/31 PASS
targeted ntpro-governance Clippy = PASS, -D warnings
current governance = PASS
backend freeze baseline = PASS, boundaries=27, negative_cases=20
docs/examples governance = PASS, markdown_files=117, local_links=293
zero-Python closeout = PASS
backend hygiene = PASS, fixture_entries=17
scripts/ai/verify_fast.sh = PASS
cargo fmt --check = PASS
git diff --check = PASS
frozen v0.32.0 release surface = unchanged
repository target after validation = absent
external measurement target = cleaned, 11,098 files / 8.9 GiB removed
external governance target = cleaned, 6,111 files / 1.0 GiB removed
```

Commands:

```bash
CARGO_TARGET_DIR=/tmp/ntpro-bpo001-governance-target \
  cargo test --locked -p ntpro-governance
CARGO_TARGET_DIR=/tmp/ntpro-bpo001-governance-target \
  cargo clippy --locked -p ntpro-governance --all-targets -- -D warnings
CARGO_TARGET_DIR=/tmp/ntpro-bpo001-governance-target \
  scripts/ai/verify_release.sh current-governance \
  backend-freeze-baseline backend-performance-baseline
CARGO_TARGET_DIR=/tmp/ntpro-bpo001-fast-target scripts/ai/verify_fast.sh
cargo fmt --all -- --check
git diff --check
cargo clean --target-dir /tmp/ntpro-bpo001-measure-target
cargo clean --target-dir /tmp/ntpro-bpo001-governance-target
```
