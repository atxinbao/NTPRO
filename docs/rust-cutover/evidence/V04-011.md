# V04-011 Evidence - ignored tests closure batch 2

Date: 2026-06-13
Executor: Codex
Task ID: V04-011

## Plain Chinese Summary

本任务没有强行修完整个仓库的所有 ignored tests，也没有删除测试。

本轮做的是 v0.4 Binance Sandbox Product Foundation 的 ignored-test 收口：
确认 v0.4 直接产品路径不依赖 `#[ignore]` 测试；把仍存在的 matching
engine、risk、PostgreSQL cache、live stress 等 ignored tests 明确标成
不属于 v0.4 Binance sandbox release evidence。换句话说，v0.4 可以用
默认 Rust 测试证明 Binance fixture replay、mock order lifecycle、risk
rejection、EMA/RSI strategy smoke 和 Dashboard panels，但不能把那些
被 scope out 的 ignored tests 当作已经修好。

## Files Changed

- `docs/rust-cutover/quality/ignored_tests_register.md`
  - Added the V04-011 Binance sandbox closure result.
  - Classified the adjacent ignored-test groups as scoped out for v0.4.
- `docs/rust-cutover/verification/ignored_tests_risk_register.md`
  - Added the V04-011 closure rule for the v0.4 readiness report.
  - Recorded that scoped-out ignored tests remain blockers or future work, not
    DONE evidence.
- `docs/rust-cutover/evidence/V04-011.md`
  - Captures this task evidence.

## Ignored-Test Disposition Summary

Scoped v0.4 files scanned:

```bash
rg -n "#\[ignore" crates/adapters/binance crates/trading/tests \
  crates/risk/tests/v04_binance_risk_rejection.rs crates/cli/src/dashboard.rs --glob '*.rs'
```

Result: no ignored tests were found in the scoped v0.4 Binance sandbox evidence
files.

Default-test evidence used by v0.4:

| Product path | Evidence path | Ignored-test dependency |
| --- | --- | --- |
| Binance fixture replay | `crates/adapters/binance/tests/v04_replay.rs` | None |
| Mock order lifecycle | `crates/adapters/binance/tests/v04_mock_lifecycle.rs` | None |
| Risk rejection smoke | `crates/risk/tests/v04_binance_risk_rejection.rs` | None |
| EMA strategy smoke | `crates/trading/tests/v04_ema_smoke.rs` | None |
| RSI strategy smoke | `crates/trading/tests/v04_rsi_smoke.rs` | None |
| Dashboard panels | `crates/cli/src/dashboard.rs` tests | None |

Scoped out for v0.4, not fixed:

| Group | Register IDs | V04-011 decision |
| --- | --- | --- |
| Matching-engine ignored tests | `IGN-EXEC-001` through `IGN-EXEC-003`; `IGN-HIGH-003` through `IGN-HIGH-005` | `SCOPED_OUT_FOR_V04`; still open for future runtime hardening. |
| Broad risk-engine ignored tests | `IGN-RISK-001` through `IGN-RISK-006`; `IGN-HIGH-006` through `IGN-HIGH-011` | `SCOPED_OUT_FOR_V04`; v0.4 only claims the deterministic halted-state Binance sandbox rejection from V04-009. |
| PostgreSQL cache ignored tests | `IGN-INFRA-001`, `IGN-INFRA-002`; `IGN-MED-004`, `IGN-MED-005` | `SCOPED_OUT_FOR_V04`; v0.4 does not claim durable PostgreSQL cache persistence. |
| Live stress/performance ignored tests | `IGN-LIVE-001`, `IGN-LIVE-002`; `IGN-MED-001`, `IGN-MED-002` | `RELEASE/PERF_ONLY_FOR_V04`; still manual/performance scoped. |

Remaining non-v0.4 ignored-test risk:

- `cargo test -- --ignored` still fails on
  `nautilus-dydx --test websocket test_subscription_restoration_tracking`
  with a 5s timeout.
- This dYdX reconnect test is not part of the V04 Binance sandbox evidence path.
- It must remain tracked as future adapter hardening, not as v0.4 release proof.

## Commands Run

```bash
source scripts/ai/toolchain_env.sh && cargo test -- --ignored
```

Result: failed with exit code 101 at the existing dYdX ignored reconnect test:

```text
test test_subscription_restoration_tracking ... FAILED
Timeout waiting for condition after 5.0s (limit 5.0s)
error: test failed, to rerun pass `-p nautilus-dydx --test websocket`
```

Interpretation: this proves there are still ignored-test risks in the wider
repo. V04-011 therefore scopes them out instead of treating them as complete.

```bash
scripts/ai/verify_full.sh
```

Result: passed with exit code 0.

Relevant covered stages:

```text
verify_full: fast checks
verify_full: clippy
verify_full: rust tests
verify_full: golden trace validation
verify_full: rust docs
verify_full complete
```

V04 evidence observed inside `verify_full`:

- `tests/v04_mock_lifecycle.rs`
- `tests/v04_replay.rs`
- `tests/v04_binance_risk_rejection.rs`
- `tests/v04_ema_smoke.rs`
- `tests/v04_rsi_smoke.rs`
- `dashboard::tests::*`

```bash
git diff --check
```

Result: passed.

## Behavior Impact

No runtime behavior changed. This PR only updates ignored-test risk registers
and task evidence.

## Public API Impact

None. No Rust public API, CLI command, adapter behavior, or dashboard runtime
surface changed.

## Migration Note Status

No migration note is required because this task does not change user-facing API
or behavior.

## Rollback Plan

Revert:

- `docs/rust-cutover/quality/ignored_tests_register.md`
- `docs/rust-cutover/verification/ignored_tests_risk_register.md`
- `docs/rust-cutover/evidence/V04-011.md`

Rollback restores the previous ignored-test register text and removes this
V04-011 evidence entry.
