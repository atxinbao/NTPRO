# V150-010 Evidence

Date: 2026-06-22
Executor: Codex
Task: V150-010 v0.15 readiness and release notes

## Summary

Prepared v0.15 readiness and release-note material for the Guarded Live Alpha
Mutation Scope + Execution Dry-Run Harness release line. This is release
accounting only and does not add runtime trading capability.

Plain Chinese summary: 这次只做 v0.15 发布材料收口。文档明确 v0.15 只能做生产
mutation 请求预览、人工审批、kill switch、dry-run 执行隔离、事故证据和 Dashboard
只读查看；不能真实下单、撤单、改单、重试、纠错，也不能用真实资金。

## Files Changed

- `docs/rust-cutover/tasks/V150-010.md`
- `docs/rust-cutover/release/v0_15_0_readiness_report.md`
- `docs/rust-cutover/release/v0_15_0_release_notes.md`
- `docs/rust-cutover/release/README.md`
- `docs/rust-cutover/evidence/V150-010.md`

## Behavior Impact

No runtime behavior changed. No production order submission, production order
mutation, production request sending, network-by-default behavior, execution
adapter call, real funds, production trading, or Dashboard order control was
added.

## Public API Impact

No Rust library API, CLI command contract, Dashboard API, adapter API, or
trading semantic changed.

## Migration Note

No migration note is required because this task only prepares release-facing
documentation.

## Boundary Confirmed

```text
release_accounting_only=true
production_request_sent=false
production_orders_submitted=0
production_order_mutations_attempted=0
production_adapter_called=false
dashboard_order_controls_enabled=false
real_funds=false
production_trading_enabled=false
tag_created=false
github_release_published=false
```

## Validation

PASS:

```text
grep -R "Guarded Live Alpha Mutation Scope + Execution Dry-Run Harness" docs/rust-cutover/release/v0_15_0_readiness_report.md docs/rust-cutover/release/v0_15_0_release_notes.md
grep -R "production order submission" docs/rust-cutover/release/v0_15_0_readiness_report.md docs/rust-cutover/release/v0_15_0_release_notes.md
```

Result: release-facing documents contain the v0.15 product claim and
production-order-submission non-goal markers.

PASS:

```text
scripts/ai/verify_release.sh release-surface-current-guard
```

Result:

```text
release_surface_current_guard=pass
== verify_release complete ==
```

PASS:

```text
scripts/ai/verify_release.sh v15-release-gates
```

Result:

```text
v15_release_gates status=ok request_sent=false network_attempted=false production_orders_submitted=0 production_order_mutations_attempted=0 dashboard_order_controls_enabled=false production_adapter_called=false
== verify_release complete ==
```

PASS:

```text
cargo fmt --all --check
scripts/ai/verify_fast.sh
git diff --check
```

Result: formatting, fast smoke, and whitespace checks passed.

## Rollback Plan

Revert this PR to remove the v0.15 readiness report, release notes, release
index updates, task file, and evidence file. No exchange state, credentials,
production orders, or runtime trading behavior require rollback.
