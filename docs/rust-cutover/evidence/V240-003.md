# V240-003 Evidence - rate-limit and throttle gate preview

Date: 2026-07-04
Executor: Codex
Task: `V240-003` / GitHub issue `#746`
Milestone: `v0.24.0`
Status: LOCAL VALIDATION PASSED

## Summary

V240-003 adds the v0.24.0 rate-limit and throttle gate preview contract,
golden trace coverage, release replay scope entries, and fail-closed verifier.
The gate records deterministic read-only decisions for allowed preview,
throttled, missing policy, and scope mismatch cases.

Plain Chinese summary: 本任务只新增 rate-limit / throttle preview gate 证据。
它证明 burst、window、venue cap 超限会 throttled，缺 policy 会
blocked_missing_limit，跨 scope 复用会 blocked_scope_mismatch；不调用 exchange
endpoint，也不调用 execution adapter。

## Files Changed

```text
docs/rust-cutover/tasks/V240-003.md
docs/rust-cutover/evidence/V240-003.md
docs/rust-cutover/release/v0_24_0_rate_limit_throttle_gate.md
docs/rust-cutover/golden_trace/RELEASE_REPLAY_SCOPE.json
tests/golden/v240_rate_limit_throttle_gate.jsonl
scripts/ai/verify_release.sh
scripts/ai/verify_v24_rate_limit_throttle_gate.sh
verification.md
```

## Boundary

```text
contract_status = preview_evidence_only_no_runtime_throttle_execution
over_limit = throttled
missing_limit_policy = blocked_missing_limit
cross_scope_reuse = blocked_scope_mismatch
dashboard_readonly_evidence = true
network_attempted = false
execution_adapter_call_allowed = false
production_order_mutation_allowed = false
new_submit_capability = false
dashboard_operation_controls_enabled = false
```

## Validation

```text
bash -n scripts/ai/verify_release.sh scripts/ai/verify_v24_rate_limit_throttle_gate.sh = PASS
python3 scripts/ai/golden_trace_runner.py tests/golden/v240_rate_limit_throttle_gate.jsonl --mode validate-only = PASS, rows=6
python3 scripts/ai/validate_golden_trace_release_scope.py --manifest docs/rust-cutover/golden_trace/RELEASE_REPLAY_SCOPE.json --trace-glob 'tests/golden/*.jsonl' = PASS, cases=110, executable=95, schema_only=15
scripts/ai/verify_release.sh v24-rate-limit-throttle-gate = PASS, golden_trace_cases=6, negative_selftest=1
scripts/ai/verify_fast.sh = PASS, fast smoke only
git diff --check = PASS
```

## Result

V240-003 local release validation passed. The preview gate is referenced by
`scripts/ai/verify_release.sh v24-rate-limit-throttle-gate`, and the six golden
trace rows prove allowed preview, burst/window/venue throttling, missing policy,
and scope mismatch outcomes. No runtime trading behavior, public API, submit
path, production order mutation, Dashboard operation control, or product-grade
live terminal claim is added.
