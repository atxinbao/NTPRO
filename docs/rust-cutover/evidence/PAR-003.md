# PAR-003 Risk Reducing Order-List Fixture Repair Evidence

Date: 2026-07-29
Executor: Codex
GitHub issue: #1171
Status: LOCAL VALIDATION AND INDEPENDENT REVIEW PASSED / HOSTED CHECKS PENDING

## Baseline Reproduction

```text
cargo test -p nautilus-risk --test risk_engine \
  test_submit_order_list_buys_when_trading_reducing_then_denies_orders \
  -- --ignored --nocapture
result = FAIL: forwarded command count left 2, right 1

cargo test -p nautilus-risk --test risk_engine \
  test_submit_order_list_sells_when_trading_reducing_then_denies_orders \
  -- --ignored --nocapture
result = FAIL: forwarded command count left 0, right 1
```

The tests sent opening commands but never applied fills, created Positions, or
initialized Portfolio state. Their LONG/SHORT preconditions were therefore
false.

## Repair Boundary

```text
production RiskEngine changed = false
real filled Position fixtures = true
Portfolio initialized from Cache = true
BUY + LONG list denial = per member
SELL + SHORT list denial = per member
rejected list forwarded to execution = false
exact decimal fixture strings = true
public API changed = false
production trading capability changed = false
v0.32.0 frozen files changed = false
```

## Executable Golden Trace

```text
case_id = risk.rejection.reducing_buy_order_list_long.001
case_id = risk.rejection.reducing_sell_order_list_short.001
trace = tests/golden/risk_rejection_schema.jsonl
status = executable_replay
evidence_id = PAR-003
harness = cargo test -p nautilus-risk --test golden_trace_risk_rejection
```

The harness constructs a real LONG or SHORT Position, initializes Portfolio,
executes a two-member `SubmitOrderList`, and compares every normalized
`OrderDenied` event with the fixture. Both cases assert zero forwarded execution
commands. Schema validation is not used as a substitute for runtime replay.

## Validation

```text
cargo test -p nautilus-risk
result = PASS (80 passed, 4 existing ignored placeholders)

cargo test -p nautilus-risk --features nautilus-model/high-precision
result = PASS (80 passed, 4 existing ignored placeholders)

cargo clippy -p nautilus-risk --all-targets --all-features \
  --features nautilus-model/high-precision -- -D warnings
result = PASS

REQUIRE_GOLDEN_REPLAY=1 scripts/ai/run_golden_traces.sh
result = PASS (289 cases: 98 executable, 186 validator executable,
5 schema-only scoped)

scripts/ai/check_backend_runtime_risk_inventory.sh
result = PASS (29,122 signals in 1,215 files)

scripts/ai/verify_release.sh current-governance backend-freeze-baseline
result = PASS

scripts/ai/verify_fast.sh
result = PASS

direct #[ignore] attribute scan
result = 23 (down from 25 before PAR-003)

git diff --check
result = PASS
```

The risk inventory removes three stale test TODO signals and eight obsolete
fixture unwraps, while adding three explicit test expectations and two panic
branches in the generic golden parser. Test-owned signals decrease by six.
Production-owned signals remain unchanged at 14,243.

The release replay scope increases from 287 to 289 cases and from 96 to 98
executable Rust replays. Validator-executable and schema-only classifications
are unchanged.

## Review State

Independent gatekeeper review session:

```text
session = 019fabfa-1230-7583-8aac-d94d934e39da
result = no actionable regression or blocking defect identified
reviewer reruns = standard/high-precision targets, complete risk_engine,
                 golden replay, format, Clippy, risk inventory
```

This remains high-risk trading-semantic evidence. Hosted checks must pass
before manual merge; auto-merge is prohibited.

## Rollback

Revert the PAR-003 PR and restore both ignored-test blocker entries. This would
remove default and executable evidence for order-list REDUCING rejection but
must not alter the frozen v0.32.0 release facts.
