# BFR-001 Block Legacy Production Mutation Network Paths Evidence

Date: 2026-07-29
Executor: Codex
GitHub issue: #1166
Status: LOCAL VALIDATION PASSED / HOSTED REVIEW PENDING

## Summary

The current CLI runtime rejects both legacy `manual_online` production mutation
commands before artifact loading, environment credential access, signing, or
HTTP execution.

中文摘要：后端冻结后的当前发行 CLI 不再允许历史工件链触发真实订单 POST 或实际
撤单 DELETE。离线工件评估继续可用，生产只读探针不受影响。

## Change Boundary

```text
production read-only probes changed = false
offline artifact evaluation changed = false
manual-online production submit reachable = false
manual-online actual cancel reachable = false
credential environment read before rejection = false
frozen v0.32.0 release files changed = false
```

## Validation

```text
cargo test -p nautilus-cli --lib
result = PASS (502 passed, 0 failed)

cargo test -p nautilus-cli --lib opt::tests
result = PASS (76 passed, 0 failed)

cargo clippy -p nautilus-cli --lib --tests -- -D warnings
result = PASS

scripts/ai/check_backend_runtime_risk_inventory.sh
result = PASS

scripts/ai/verify_release.sh current-governance backend-freeze-baseline
result = PASS

git diff --check
result = PASS
```

Focused regression coverage also passed for both blocked `manual_online` paths
and both retained offline artifact-evaluation paths. The rejection tests use
missing artifact paths and observable environment readers to prove that the
runtime exits before file or credential access and produces no output artifact.
CLI help text now describes both commands as offline legacy artifact evaluation
and identifies `--manual-online` as a retired fail-closed compatibility flag.

The canonical risk inventory decreased by eight production-owned `unwrap`
matches because the replaced inline CLI tests no longer deserialize blocked-path
artifacts. No production implementation signal was added.

## Rollback

Reverting this PR would restore production mutation network reachability and
would conflict with the backend freeze policy. A rollback requires a separately
scoped and explicitly authorized capability decision.
