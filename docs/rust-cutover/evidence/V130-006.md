# V130-006 Evidence - Decimal-only risk/execution amount boundary

Date: 2026-06-21
Executor: Codex

## Summary

V130-006 adds the v0.13 Decimal/string amount boundary contract and targeted
tests.

Plain Chinese summary: 已把 v0.13 金额字段边界固定为普通十进制字符串加 `Decimal`
处理。测试证明 `0.100000000000000001 + 0.200000000000000002` 不走 `f64`，并且
空值、负数、科学计数法、NaN、inf 都不会被当成有效金额。这个仍然只是 preflight
证据，不是实盘 money math 完成。

## Files

```text
crates/cli/src/live.rs
scripts/ai/verify_v13_decimal_amount_boundary.sh
docs/rust-cutover/tasks/V130-006.md
docs/rust-cutover/release/v0_13_0_decimal_amount_boundary.md
docs/rust-cutover/evidence/V130-006.md
docs/rust-cutover/release/README.md
```

## Validation

Completed locally:

```bash
cargo test -p nautilus-cli v13_live_alpha_amount_boundary_uses_decimal_strings_without_f64 --lib = PASS
cargo test -p nautilus-cli production_shadow_portfolio_runtime_preserves_decimal_string_notional_preflight --lib = PASS
scripts/ai/verify_v13_decimal_amount_boundary.sh = PASS
scripts/ai/verify_fast.sh = PASS
bash -n scripts/ai/verify_v13_decimal_amount_boundary.sh = PASS
cargo fmt --check = PASS
cargo check -p nautilus-cli = PASS
cargo clippy -p nautilus-cli --lib --tests -- -D warnings = PASS
git diff --check = PASS
```

## Boundary

```text
amount_boundary=decimal_string_only
parser=rust_decimal
aggregation=rust_decimal_string_sum
f64_aggregation_used=false
live_alpha_money_math_ready=false
risk_or_execution_grade=false
scientific_notation_allowed=false
production_order_submission_allowed=false
production_order_mutation_allowed=false
real_orders_submitted=false
production_trading_enabled=false
```

## Status

Ready for PR review. Hosted smoke is required before merge.
