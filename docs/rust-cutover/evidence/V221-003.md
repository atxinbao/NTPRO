# V221-003 Evidence - executable read-model replay expansion

Date: 2026-07-02
Executor: Codex
Task: `V221-003` / GitHub issue `#707`
Milestone: `v0.22.1`
Status: LOCAL VALIDATION PASSED

## Summary

V221-003 expands executable Rust projection replay for the Trader Terminal
Workbench read-model scope. The release scope now records 28 executable
read-model replay rows and 4 remaining read-model schema-only rows.

Plain Chinese summary: 本任务把 positions、fills、order duplicate/readback
mismatch、risk 状态、dashboard forbidden controls 等高风险 read-model 路径从
schema-only 提升到 Rust executable replay。它没有改变交易语义，也没有启用任何真实
操作入口。v0.22.1 仍是 Workbench/runtime bridge，不是完整 executable read-model
runtime。

## Files Changed

```text
crates/cli/tests/golden_trace_read_model_projection.rs
docs/rust-cutover/golden_trace/RELEASE_REPLAY_SCOPE.json
docs/rust-cutover/tasks/V221-003.md
docs/rust-cutover/release/v0_22_1_read_model_executable_replay.md
docs/rust-cutover/evidence/V221-003.md
docs/rust-cutover/release/README.md
verification.md
```

## Promoted Rows

```text
positions = 6 executable replay rows
orders = 3 newly promoted executable replay rows
fills = 6 executable replay rows
risk = 4 newly promoted executable replay rows
dashboard = 1 newly promoted forbidden-controls executable replay row
```

The promoted rows carry:

```text
status = executable_replay
evidence_id = V221-003
harness = cargo test -p nautilus-cli --test golden_trace_read_model_projection
```

Remaining read-model schema-only rows:

```text
read_model.contract.healthy_minimal.001
read_model.contract.fail_closed_missing_lineage_source_freshness.001
read_model.account_snapshot.missing_provenance.001
read_model.account_snapshot.redaction_breach.001
```

## Validation

```text
cargo fmt --all -- --check = PASS
python3 scripts/ai/validate_golden_trace_release_scope.py --manifest docs/rust-cutover/golden_trace/RELEASE_REPLAY_SCOPE.json --trace-glob 'tests/golden/*.jsonl' = PASS, 83 cases, 78 executable replay, 5 schema-only scoped
cargo test -p nautilus-cli --test golden_trace_read_model_projection -- --nocapture = PASS, 1 test passed
scripts/ai/verify_fast.sh = PASS, fast smoke only
```

## Behavior Impact

This is a verification-scope expansion. Runtime behavior and trading semantics
do not change. The harness now proves more read-model fixture projections are
executable Rust replay instead of schema-only evidence.

## Public API Impact

No public API changed.

## Rollback Plan

Revert this PR to restore the prior release scope manifest and narrower
read-model projection replay set. Do not roll back by deleting golden fixtures.
