# V221-005 Evidence - Workbench render smoke and read-only regression

Date: 2026-07-02
Executor: Codex
Task: `V221-005` / GitHub issue `#709`
Milestone: `v0.22.1`
Status: LOCAL VALIDATION PASSED

## Summary

V221-005 adds a deterministic render smoke for the Trader Terminal Workbench.
The smoke uses a representative `read_model_runtime` snapshot fixture and
executes the real Dashboard JavaScript renderer with a minimal DOM stub.

Plain Chinese summary: 本任务补上 v0.22.1 的 Workbench 渲染回归。输入是固定
`read_model_runtime` snapshot，输出是实际 Dashboard renderer 生成的 HTML。验证
覆盖账户、持仓、订单、成交、风险、告警、审计/provenance 和操作入口，并确认
submit/cancel/replace/amend/flatten 等真实操作入口没有出现在渲染结果或 renderer
代码中。

## Files Changed

```text
crates/cli/src/dashboard.rs
scripts/ai/verify_v22_workbench_render_smoke.sh
tests/golden/v221/workbench_render_snapshot.json
docs/rust-cutover/tasks/V221-005.md
docs/rust-cutover/evidence/V221-005.md
verification.md
```

## Fixture

```text
tests/golden/v221/workbench_render_snapshot.json
```

The fixture includes one `read_model_runtime` item with account, positions,
orders, fills, risk, alerts, audit/provenance, and gated operation entry fields.
All v0.22 required-false operation boundary fields are explicitly
`availability=available` and `value=false`.

## Assertions

```text
8 Workbench panels render from read_model_runtime
locked_readonly boundary marker is present
all_operation_controls_disabled marker is present
account, position, order, fill, risk, audit, and operation-entry values render
required false fields are explicitly false
button/form/input/action/fetch/control route surfaces are absent
Rust artifact -> snapshot regression exposes the same fields under read_model_runtime
```

## Validation

```text
bash -n scripts/ai/verify_v22_workbench_render_smoke.sh = PASS
python3 -m json.tool tests/golden/v221/workbench_render_snapshot.json >/dev/null = PASS
scripts/ai/verify_v22_workbench_render_smoke.sh = PASS, panels=8 readonly_boundary=locked false_fields=21
cargo test -p nautilus-cli trader_terminal_v221_workbench_snapshot_populates_render_smoke_fields --lib -- --nocapture = PASS, 1 test passed
cargo test -p nautilus-cli trader_terminal_ --lib -- --nocapture = PASS, 29 tests passed
cargo fmt --all -- --check = PASS
git diff --check = PASS
scripts/ai/verify_fast.sh = PASS, fast smoke only
```

## Behavior Impact

No runtime trading behavior changes. This is Dashboard render regression
coverage only and does not enable any submit, cancel, replace, amend, flatten,
fund transfer, account mutation, or automatic operation path.

## Public API Impact

No public API changed.

## Migration Note

No migration note is required.

## Rollback Plan

Revert this PR to remove the Workbench render smoke fixture, script, and Rust
regression test. Runtime behavior remains unchanged either way.
